# Parity table — PAYMENT (Stage 5 · §22 · §22.1 · §23) — FINAL

Repo `0cd399f`; paths relative to `frontend/`. `PAY-nn` = row `nn` of `payment.md`; `PAY-A..E` = the map's "requirements not covered by any row" addenda. Suffix `a/b` = one map row split because its parts land in different phases. Corrected statuses from the adversarial pass are applied. MD = `AUTOLENIS-COMPLETE-TRANSACTION-FLOW.md` (line numbers re-verified); HTML = `AutoLenis-Transaction-Flow.html` arrays (`S[4]` = Stage 5; `EXC[6..9]` payment rows; `EXC[38..42]` plan rows).

| Ref | Document & section | Requirement | Current route / service / model / component / email / cron / queue / admin action | Status | Stronger safeguard to preserve | Exact required change | Phase | Test level | Acceptance evidence | Owner-gated dependency | Legacy path affected | Final disposition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| PAY-01 | MD §5a L444; HTML S[4].entry | Active and verified account checked before payment is offered | `getRequestBuyer`→`isBuyerBlockedFromApi` denies suspended/disabled/purged with generic 401 (`lib/auth/api.ts:91-94,117`); no email-verified read on API path (`lib/auth/api.ts:41-66`); `app/api/buyer/deposit/create-intent/route.ts:17-18` | PARTIAL | Central buyer access gate `lib/auth/api.ts:91-94,117` runs before every `/api/buyer/*` handler — keep | Eligibility predicate returns named `ACCOUNT_INACTIVE` / `EMAIL_UNVERIFIED`; read `email_confirmed_at` on the API path | 3 | unit, integration | New `lib/services/payment/__tests__/deposit-eligibility.test.ts`; extend `app/api/buyer/deposit/__tests__/create-intent-enrollment.test.ts` (unverified email → 400 named code) | none | none | TO EXTEND |
| PAY-02 | MD §5a L444; HTML S[4].entry | Completed onboarding checked server-side | Route never reads `onboardingComplete` (`create-intent/route.ts:63-86`); `/buyer` layout gating is UX-only (`lib/services/buyer/journey.ts:98-113`) | MISSING | — | Add `onboardingComplete` check to the eligibility predicate; named `ONBOARDING_REQUIRED` | 3 | unit, integration | `deposit-eligibility.test.ts`: incomplete onboarding → named code, no PI created | none | none | TO IMPLEMENT |
| PAY-03 | MD §5a L444; HTML S[1], S[4].entry; EXC[1] | Usable geocoded location before payment | Not checked pre-payment (`create-intent/route.ts` reads no zip/city/state); matcher fails closed post-payment → paid buyer, zero invitations (`docs/plans/BUYER-LOCATION-GAP.md` §2) | MISSING | Matcher fail-closed `lib/services/auction/dealer-invitation.service.ts:199-209` — keep | Consume Phase 2 hard-location predicate (`geocoding.service`, lat/long on `buyers`) in the gate; named `LOCATION_REQUIRED` | 3 | unit, integration, playwright | `deposit-eligibility.test.ts`; defect #1 regression (workflow §7.1); Playwright: checkout blocked until ZIP geocodes | `GOOGLE_GEOCODING_API_KEY` production value (undeclared per gap doc) | webhook `inviteDealersToAuction` tail | TO IMPLEMENT |
| PAY-04 | MD §5a L444 | Approved, unexpired prequalification | `isPrequalValid(buyer.preQualification)` → `PREQUAL_REQUIRED` 400 (`create-intent/route.ts:63-71`) | ALREADY CORRECT | Single source of truth `isPrequalValid` | None (fold into the predicate unchanged) | 3 | unit | `create-intent-enrollment.test.ts` keeps `PREQUAL_REQUIRED` assertion | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| PAY-05 | MD §5a L444; HTML S[4].entry | Complete vehicle criteria | Proxy only: `shortlistItem.count > 0` → `SHORTLIST_REQUIRED` (`create-intent/route.ts:73-85`); VR completeness never checked | PARTIAL | — | Validate the VR's required criteria (Phase 1 columns: make/model/year/budget/location) and return the missing field by name | 3 | unit | `deposit-eligibility.test.ts`: each missing criterion → its own code | none | `SHORTLIST_REQUIRED` gate (kept as secondary) | TO EXTEND |
| PAY-06a | MD §5a L444; HTML S[3], S[4].entry | Co-buyer and trade elections exist on the request | Trade: `VehicleRequestFinancing.tradeIn Boolean?` (`prisma/schema.prisma:1075,1111`); co-buyer: no field under any spelling | PARTIAL | — | Explicit `co_buyer_election` / `trade_election` captured in Stage 4 flow (Phase 1 `co_buyers` table + `vehicle_requests.co_buyer_id`) | 4 | unit, integration | Stage 4 intake tests assert both elections persisted (non-null) | none | none | TO IMPLEMENT |
| PAY-06b | MD §5a L444 | Gate verifies both elections are recorded | Neither election read at the gate (`create-intent/route.ts:63-95`) | MISSING | — | Predicate requires non-null elections; named `ELECTIONS_REQUIRED` | 3 | unit | `deposit-eligibility.test.ts` | none | none | TO IMPLEMENT |
| PAY-07 | MD §5a L444; HTML S[4].entry | No conflicting open request | Gate checks an existing PAID deposit only (`create-intent/route.ts:88-95`); `hasActiveRequest` has zero callers (`lib/services/vehicle-request/vehicle-request.service.ts:21-26`); `app/api/buyer/requests/route.ts:128` only rate-limits | MISSING | Phase 1 partial unique index `vehicle_requests_one_open_per_buyer_key` (DB-enforced) | Gate reads the open-status set for this buyer; named `REQUEST_CONFLICT`; `ALREADY_PAID` scoped to the VR | 3 | unit, integration, concurrency | `deposit-eligibility.test.ts`; DB-backed index-violation test (Phase 1 CI migration job); `test:concurrency` duplicate submit | Owner-run cleanup of the 3 buyers holding multiple open VRs (workflow §5.6) before the index deploys | `ALREADY_PAID` buyer-wide rule | TO IMPLEMENT |
| PAY-08 | MD §5a L444, §5b L454-461; HTML S[4].buyer[0] | Disclosure acceptance captured and required at the gate | Only `reviewToken` parsed (`create-intent/route.ts:36-61`); no checkbox on `app/buyer/deposit/page.tsx` | MISSING | — | Checkout captures acceptance (timestamp + disclosure version) into the PAY-D columns; predicate blocks with `DISCLOSURE_REQUIRED` | 3 | unit, integration, playwright | `deposit-eligibility.test.ts`; Playwright: PI not created until acceptance; DB assertion on `disclosures_accepted_at` | none | none | TO IMPLEMENT |
| PAY-09 | MD §5a L446 | Any failure returns the buyer to the exact missing requirement, named | Server names `PREQUAL_REQUIRED`/`SHORTLIST_REQUIRED` (`create-intent/route.ts:70,80-84`); client maps only `PREQUAL_REQUIRED`, `REVIEW_*`, `CHARGE_UNSETTLED` (`app/buyer/deposit/page.tsx:149-158`) — `SHORTLIST_REQUIRED` collapses to "Unable to initialize payment" | PARTIAL | Named error-code pattern | Exhaustive code→step map in the checkout client; every predicate failure routes to its step | 3 | unit, playwright | Client mapping unit test; Playwright: each failure lands on the named step | none | none | TO EXTEND |
| PAY-10a | MD §5b L450 | `VehicleRequestStatus.PAYMENT_REQUIRED` exists | Enum lacks it (`prisma/schema.prisma:1654-1666`); only `PrequalFormClient.tsx:61` matches the token | MISSING | — | Append `PAYMENT_REQUIRED` (with `DRAFT`, `RADIUS_AUTHORIZATION_REQUIRED`) in the Phase 1 wave | 1 | integration | `pnpm test:migrations`; `prisma/__tests__` enum assertion; CI from-zero + re-apply no-op | Production deploy of Phase 1 wave (§13-D1 ledger resolve first) | none | TO IMPLEMENT |
| PAY-10b | MD §5b L450; HTML S[4].system[0] | VR enters `PAYMENT_REQUIRED` on eligibility pass; create returns the payment step | `POST /api/buyer/requests` creates no PI and returns no payment step (`app/api/buyer/requests/route.ts`); default `SUBMITTED` | MISSING | — | Transition on predicate pass inside `vehicle-request.service`; response carries the payment step | 3 | state-machine, integration | New `lib/services/vehicle-request/__tests__/payment-required-transition.test.ts` | none | `SUBMITTED` default | TO IMPLEMENT |
| PAY-11a | MD §5b L452; HTML S[4].tables | `deposits.vehicle_request_id` column | `Deposit` has no VR link (`prisma/schema.prisma:401-415`); only `Auction.vehicleRequestId` (`:443-451`, migration `20261003000000`) | BROKEN | — | Nullable indexed FK (SetNull) in the Phase 1 wave | 1 | integration | `pnpm test:migrations`; drift check; RLS review per `autolenis-supabase-postgres` | Production deploy of Phase 1 wave | none | TO IMPLEMENT |
| PAY-11b | MD §5b L452; HTML S[4].system[1] | Create or reuse ONE PaymentIntent tied to THAT VR | PI keyed per buyer per UTC day `deposit-buyer-${id}-${dayKey}` (`create-intent/route.ts:207-224`) | BROKEN | Reuse of existing PENDING PI + `client_secret` retrieval (`create-intent/route.ts:124-192`) | Key `deposit-vr-${vehicleRequestId}`; stamp `vehicleRequestId` in PI metadata; write `Deposit.vehicleRequestId` | 3 | integration, concurrency | Extend `create-intent-duplicate-charge.test.ts`; `test:concurrency`: parallel create-intent for one VR yields one PI | none | admin `send-link` / `create-intent` (buyer-keyed) | TO EXTEND |
| PAY-12 | MD §5b L455; HTML S[4].sees; MONEY[0] | Disclose $99 = the Standard plan paid in full, not a deposit | Copy: "$99 Limited-Time Auction Access Deposit — refund available on request" (`app/buyer/deposit/page.tsx:223-231,263-276`) | PARTIAL | Amount from `DEPOSIT_AMOUNT_CENTS` only (`lib/constants.ts:6-9`) | Rewrite checkout, success page, receipt and `PlanUpgradeCard` copy to Standard-plan framing | 3 | visual, playwright | `test:visual` baseline for `/buyer/deposit`; Playwright text assertion | Owner decision Q1: accept "$99 = Standard plan paid in full" legal copy + refund-policy wording | success page, `deposit-confirmation.tsx`, `PlanUpgradeCard.tsx:111` | OWNER-GATED |
| PAY-13 | MD §5b L456; HTML S[4].buyer[2] | Disclose Premium $400 balance available until funding clears | Shown only when buyer already PREMIUM (`app/buyer/deposit/page.tsx:252-256`) | PARTIAL | — | Single Premium line for every buyer (touchpoint 1 wording, Markdown "until funding clears" per §13-D6) | 3 | visual | `test:visual`; unit snapshot of checkout copy | none | none | TO EXTEND |
| PAY-14 | MD §5b L457; HTML S[4].sees | Disclose sourcing may extend 100→150→250 miles | Absent (`app/buyer/deposit/page.tsx`, no "miles") | MISSING | — | Add disclosure block (text sourced from one constants module shared with Phase 5 radius logic) | 3 | visual | `test:visual`; unit copy test | none | none | TO IMPLEMENT |
| PAY-15 | MD §5b L458; HTML S[4].sees | Disclose dealerships may be in another state | Absent | MISSING | — | Add to disclosure block | 3 | visual | as PAY-14 | none | none | TO IMPLEMENT |
| PAY-16 | MD §5b L459; HTML S[4].sees | Disclose expansion beyond 250 miles requires explicit authorization | Absent | MISSING | — | Add to disclosure block | 3 | visual | as PAY-14 | none | none | TO IMPLEMENT |
| PAY-17 | MD §5b L460; HTML S[4].sees | Disclose payment does not obligate purchase | Absent | MISSING | — | Add to disclosure block | 3 | visual | as PAY-14 | none | none | TO IMPLEMENT |
| PAY-18 | MD §5b L461; HTML S[4].sees; MONEY_PANELS[0] | Disclose published refund policy incl. manual, non-automatic review | "refund available on request … our team reviews every request" (`app/buyer/deposit/page.tsx:228,255,274`); no policy link | PARTIAL | Copy already states manual review | Link the published policy; state "not automatic" explicitly | 3 | visual | `test:visual`; unit copy test | Owner: published refund-policy text/URL | none | TO EXTEND |
| PAY-19 | MD §5c L465; HTML S[4].system[4], S[4].note | Six touches: immediate, 1h, 6h, 24h, 72h, day 7 | `deposit_reminder_1..6`, deltas 1h/+5h/+18h/+48h/+96h (`lib/services/crm/lifecycle-touch-drain.service.ts:60-63,155-247`); `lifecycle-scheduler.ts:85-108`; cron `*/15` (`vercel.json:272-273`); enrolled at create-intent (checkout open) | ALREADY CORRECT | `UNIQUE(base_key, sequence)`; single enroller (commit 79437f0); TCPA/DNC gating in `notifyContact` | Re-home the same cadence onto `comms_outbox` keyed to the VR (every-minute drain); direct producer retired behind the `LEGACY_PATH_WRITE` adapter | 3 | unit, integration | Extend `lib/services/crm/__tests__/deposit-reminder-cadence.test.ts` (same deltas) + new `lib/services/comms/__tests__/deposit-series-outbox.test.ts` | Production `lifecycle_touch_sequence_allowed` already admits 19 sequences (§13-D24) | deposit-reminder direct producer (§8.4) | TO EXTEND |
| PAY-20 | MD §5c L465 | Every message carries VR reference, request summary, checkout link, support contact | Generic copy; CTA `/buyer/deposit`; footer Unsubscribe only (`lifecycle-touch-drain.service.ts:155-247`; `lib/qstash/notify.ts:195`) | PARTIAL | — | Template renders VR reference + summary + support line; CTA to VR-scoped checkout (depends PAY-11b) | 3 | unit | Template render test in `deposit-series-outbox.test.ts` asserting all four elements | none | none | TO EXTEND |
| PAY-21 | MD §5c L465; HTML S[4].system[4] | Each send rechecks live payment and request status | `depositConversionResolved` re-read at drain: PAID / no PENDING / buyer halted → cancel; ignores VR status (`lib/qstash/state.ts:40-63`; drain `:705-709`) | PARTIAL | Send-time guard is authoritative (not the proactive cancel) | Dispatcher `state_recheck` callback for the deposit template reads VR status (cancelled/closed/expired) + deposit + dispute hold | 3 | unit | Port `lib/qstash/__tests__/deposit-conversion-guard.test.ts` to the outbox recheck; VR-cancelled case skips | none | none | TO EXTEND |
| PAY-22 | MD §5c L467, §5d L473 | Cancel remaining touches on payment | Proactive `cancelDepositReminderTouches` post-commit (`app/api/webhooks/stripe/route.ts:248-255`) + guard (drain `:599-620`) | ALREADY CORRECT | Guard catches races | None beyond re-home to `cancelByKey` | 3 | integration | `app/api/webhooks/__tests__/stripe-idempotency.test.ts` cancel assertion retained | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| PAY-23 | MD §5c L467; HTML S[4].note | Cancel on cancellation / closure / refund / dispute hold / suspension / do-not-contact | Refund/FAILED and suspension stop via guard (`lib/qstash/state.ts:52-62`); DNC suppresses per send but chain marks `done` and enqueues next (`lifecycle-touch-drain.service.ts:723-764`; `notify.ts:109,143-144`); VR cancel route never cancels (`app/api/buyer/requests/[requestId]/cancel/route.ts:27-32`); no dispute state | PARTIAL | Consent gating at send layer | `cancelByKey` from VR cancel/close/expire and PAY-38b hold; DNC cancels the series instead of chaining through | 3 | unit, integration | `deposit-series-outbox.test.ts`: each trigger cancels; DNC cancels remainder | none | none | TO EXTEND |
| PAY-24a | MD §5c L465 (one series) | ONE six-touch series | Four implementations: internal chain (active); legacy QStash `app/api/jobs/deposit-reminder/route.ts:26-70`; `lib/services/workflow.prebuilt.ts:114-127,458` on `deposit_pending` (engine off unless `CRM_INAPP_ENGINE_ENABLED==='true'`); `NudgeStage.DEPOSIT_IDLE` (`lib/services/nudge/nudge.service.ts:34,62`); `create-intent/route.ts:288-303` still emits `deposit_pending` | DUPLICATED | Single enroller | Consolidate onto the dispatcher series; stop `deposit_pending` driving a second series; wrap remaining producers in `LEGACY_PATH_WRITE` adapter | 3 | unit, integration | `app/api/buyer/deposit/__tests__/deposit-reminder-single-owner.test.ts` extended: exactly one series per VR | Owner decision Q5: which legacies may be deleted vs kept for Make.com parity (Make.com consumption UNVERIFIED) | QStash job route; prebuilt workflow; `DEPOSIT_IDLE` nudge; `deposit_pending` event | TO CONSOLIDATE |
| PAY-24b | MD §5c L465 | Legacy reminder implementations removed | As PAY-24a | DUPLICATED | — | Delete QStash route/consumer, prebuilt `depositReminder`, and (if owner agrees) `DEPOSIT_IDLE` once counter shows zero writes | 10 | unit | `LEGACY_PATH_WRITE` counter zero for 30 days; `test:coverage-check` after deletion | §13-D23 QStash removal; owner decision Q5 | `lib/qstash/*`, `app/api/jobs/deposit-reminder` | OWNER-GATED |
| PAY-25 | MD §5d L471; HTML S[4].system[2] | Query provider for existing succeeded / in-flight obligation before a new intent (buyer path) | Stripe lookup only when latest PENDING/PAID row has a PI id (`create-intent/route.ts:89-92,130`); misses FAILED-on-decline rows (`stripe/route.ts:621-624`), admin send-link rows with null PI (`send-link/route.ts:40-48,93-96`), older PENDING rows; no metadata search (contrast `service-fee.service.ts:77-97`) | PARTIAL | One shared rule `classifyPaymentConfirmation` (`lib/services/payment/payment-confirmation.ts:66-85`); `CHARGE_UNSETTLED` 409 never renders a card form | Add Stripe search by `metadata.vehicleRequestId`/`buyerId` (mirror `findSettledFeeIntent`); consider FAILED-with-live-PI and null-PI rows | 3 | unit, integration, concurrency | Extend `create-intent-duplicate-charge.test.ts` with the three escaped cases; `test:concurrency` | none | none | TO EXTEND |
| PAY-26 | MD §5d L471 | Same guard for admin-initiated intents | Admin `create-intent` has no PAID check, no provider query, silent `pi_admin_` fallback, same-day replay → P2002 500 (`app/api/admin/payments/deposit/create-intent/route.ts:34-61`); `send-link` reuses PENDING only, `Date.now()` key (`send-link/route.ts:36-48,88`) | BROKEN | `requirePermissionStrict("finance.payment_link.send")`, mandatory reason, `AdminAuditLog` | Route both admin paths through the shared provider-side guard; refuse when a settled/in-flight $99 exists for the VR; deterministic key; no synthetic PI on Stripe error | 3 | integration | New `app/api/admin/payments/__tests__/deposit-create-intent-guard.test.ts` (paid buyer → 409; replay → same PI, no P2002) | none | admin `send-link` Checkout Session path | TO EXTEND |
| PAY-27 | MD §5d L471 (fee) | $400 duplicate check provider-side | `findSettledFeeIntent` metadata search + post-create replay check (`lib/services/deal/service-fee.service.ts:77-157`); route returns `CHARGE_UNSETTLED` (`app/api/buyer/deals/[dealId]/fee/create-intent/route.ts:25-44`) | ALREADY CORRECT | Fail-open on search outage logged loudly (`service-fee.service.ts:125-131`) | None | 3 | unit | `lib/services/deal/__tests__/service-fee-duplicate-charge.test.ts` green | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| PAY-28 | MD §5d L473; HTML S[4].system[3] | On settlement atomically record payment + Stripe object | PAID flip + `PaymentProviderEvent` claim in one `$transaction` (`stripe/route.ts:162-229`); PI id unique on `Deposit.stripePaymentIntentId` (`schema.prisma:406`) | ALREADY CORRECT | DB-enforced matrix `lib/payments/deposit-state.ts:16-21`; claim inside tx with bounded `maxWait/timeout` | None | 3 | integration | `stripe-idempotency.test.ts:193-246` (replay ack, crash rollback) | none | none | PRESERVED STRONGER SAFEGUARD |
| PAY-29 | MD §5d L473; HTML S[4].rec | …attach it to the Vehicle Request | No VR link written; auction created without `vehicleRequestId` (`stripe/route.ts:202-214`; `deposit-activation.service.ts:207`) | MISSING | — | Set `Deposit.vehicleRequestId` from PI metadata inside the settlement tx (webhook + reconciler) | 3 | integration | Webhook test asserts `vehicleRequestId` populated; DB assertion in Playwright journey | none | none | TO IMPLEMENT |
| PAY-30 | MD §5d L473 | …mark request fulfillment-unlocked | `isFulfillmentUnlocked(buyerId)` = any PAID deposit for the buyer (`lib/services/payment/fulfillment-gate.ts:30-39`); duplicate `hasPaidDeposit` (`lib/qstash/state.ts:8`) | PARTIAL | Single predicate consumed by outreach/AI guards | Scope predicate to the VR (`vehicleRequestId`, PAID, unrefunded, undisputed); retire `hasPaidDeposit` onto it | 3 | unit | `lib/services/payment/__tests__/fulfillment-gate.test.ts` extended (second VR unpaid → false) | none | `post-intake-outreach.service.ts:159`, `dealer-opportunity-notification.service.ts:49`, `ai/action-intent/policy.ts:53` | TO EXTEND |
| PAY-31 | MD §5d L473 | …cancel reminders | As PAY-22 | ALREADY CORRECT | Guard + proactive cancel | None | 3 | integration | as PAY-22 | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| PAY-32 | MD §5d L473 | …queue the receipt | Webhook: idempotent `deposit-confirmed-${depositId}` (`stripe/route.ts:285-290`; `resend.service.ts:764-778`); reconciler settle sends nothing (`deposit-settlement.service.ts:205-222`) | PARTIAL | Idempotent send rail | Both settle paths enqueue the receipt through the dispatcher with the same dedup key | 3 | integration | `lib/services/payment/__tests__/deposit-settlement.test.ts`: settle enqueues receipt once | Owner decision Q3: reconciler runs the full post-settlement tail | none | TO EXTEND |
| PAY-33 | MD §5d L473; HTML S[4].system[3], S[5].entry | …open the sourcing case (no auction at settlement) | Creates PENDING auction in tx, launches + invites post-commit best-effort (`stripe/route.ts:202-214,259-263`); zero-invite result silent (`.catch`) | MISSING | Reconciler convergence + per-deposit idempotency guard (`lib/jobs/idempotency.ts`) | Settlement writes VR `ACTIVE_SOURCING` + seeds due-diligence checkpoints; neutralise reconciler `create_auction`/`invite` behind monitored adapter; retire `close`-on-zero branch | 3 | integration, state-machine, playwright | Extend `lib/services/auction/__tests__/deposit-activation.test.ts`; Playwright: request → checkout → webhook → sourcing case opened, no auction row | Legacy removal after zero `LEGACY_PATH_WRITE` for 30 days (§8.4) | Deposit settlement → immediate auction create + invite (webhook + activation reconciler) | TO IMPLEMENT |
| PAY-34 | MD §5d L475; HTML EXC[7] | Reconciliation job recovers missed webhooks; must be validated, enabled, monitored | Sweep `status:"PENDING"` AND PI not null (`lib/services/payment/deposit-settlement.service.ts:164-173`) misses send-link null-PI and FAILED-with-live-PI rows; strict `"true"` flag (`:74-76`); hardcoded production id `77934f10-…` (`:69-71`) | PARTIAL | Off-by-default; exclusion applied in query; never fabricates a `PaymentProviderEvent` | Widen sweep to the two escaped classes; fold `checkDepositProviderEvidence` into it (§13-D12); remove hardcoded id; enable | 3 | unit, integration | `deposit-settlement.test.ts:131-301` extended; `lib/services/monitoring/__tests__/deposit-provider-evidence.test.ts` folded; `app/api/cron/__tests__/deposit-activation-reconcile-route.test.ts` | `DEPOSIT_SETTLEMENT_RECONCILE_ENABLED=true` in production; owner decision Q8 (clear `DEFAULT_EXCLUDED_DEPOSIT_IDS`) | none | TO EXTEND |
| PAY-35 | MD §5d L475; HTML EXC[7], S[4].staff | Monitored and alerted on every recovered gap (Finance) | `raiseWebhookGapAlert` deduped `SYSTEM_ALERT` (`deposit-settlement.service.ts:108-135,222`); `withCronRun` (`cron/deposit-activation-reconcile/route.ts:35`); admin queues (`admin-queue.service.ts:12,114`) | ALREADY CORRECT | Dedup per PI; `withCronRun` monitor | Raise through `raiseException(PAYMENT_EXCEPTION, owner_role=FINANCE)`; keep `SYSTEM_ALERT` as read-only mirror | 3 | unit | `deposit-settlement.test.ts`: one `queue_items` row per recovered PI | none | `SYSTEM_ALERT` as exception store (§8.4 mirror) | TO CONSOLIDATE |
| PAY-36 | MD §5d L479; HTML EXC[8] | Confirmed payment matching no obligation → immediate Finance exception | `routed=false` → `raiseUnroutablePaymentException` deduped + `logger.error`, still acks 200 (`stripe/route.ts:31-56,601-609`) | ALREADY CORRECT | Never a bare 200 | Re-home to `raiseException`; behaviour unchanged | 3 | integration | `app/api/webhooks/__tests__/stripe-fee-commission.test.ts:162` retained | none | `SYSTEM_ALERT` mirror | ALREADY PRESENT (VERIFY IN PHASE) |
| PAY-37 | MD §5d L479; HTML S[4].fail, EXC[6], SUPSTATES[0] | Payment failure preserves the request and permits safe retry | Card decline → `PENDING→FAILED` (`stripe/route.ts:616-624`); `FAILED` terminal (`lib/payments/deposit-state.ts:12,16-21,37-41`); Elements retries same PI (`app/buyer/deposit/page.tsx:60-66`); on success webhook cannot flip yet unfiltered `findFirst` still creates/launches auction, invites, emails (`stripe/route.ts:186-198,202-214,258-265,285-290`); reconciler sweeps PENDING only | BROKEN | Late failure never downgrades PAID (`stripe-idempotency.test.ts:223`) | Reserve `FAILED` for `payment_intent.canceled`/expired; allow `FAILED→PAID` for a live PI; create-intent + reconciler consider FAILED rows; post-flip tail runs only when the flip succeeded | 3 | unit, integration, playwright | Failing-first: `lib/payments/__tests__/deposit-state.test.ts` + new webhook case FAILED→succeeded; Playwright: declined card then retry settles once, opens case once | none | none | TO EXTEND |
| PAY-38a | MD §5d L479 | Dispute/refund hold state exists on the deposit | No `Deposit.disputedAt`/hold field (`prisma/schema.prisma:401-415`; grep `disputedAt\|dispute_hold\|fulfillmentHold` → none) | MISSING | — | Add `deposits.disputed_at`, `hold_reason`, `hold_released_at` to the Phase 1 wave | 1 | integration | `pnpm test:migrations` | Production deploy of Phase 1 wave | none | TO IMPLEMENT |
| PAY-38b | MD §5d L479; HTML EXC[9], S[4].fail | Dispute or refund places fulfillment on hold and stops all unsent outreach | `charge.dispute.created` → `AdminAuditLog` only (`stripe/route.ts:769-798`); `charge.refunded` → REFUNDED + email (`:643-697`); nothing pauses sourcing/invitations/outreach | MISSING | Refund flip is matrix-guarded | On dispute/refund: set hold, pause sourcing case/auction (`pauseBuyerWorkflow`/cancel invitations), `cancelByKey` all outbox rows for the VR, `isFulfillmentUnlocked` false, `raiseException(PAYMENT_EXCEPTION)` | 3 | integration, state-machine | New `app/api/webhooks/__tests__/stripe-dispute-hold.test.ts`; `fulfillment-gate.test.ts` disputed → false | none | none | TO IMPLEMENT |
| PAY-39 | MD §5d L479; HTML EXC[7] | Reconciliation gaps alert Finance with the Stripe reference | Alert body carries PI id (`deposit-settlement.service.ts:124-129`) | ALREADY CORRECT | — | None (carried into PAY-35 row) | 3 | unit | `deposit-settlement.test.ts` asserts PI id in exception payload | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| PAY-40 | MD §5d L481; HTML S[4].gate, S[4].note | Nothing costly before this point | Auction/invitations only after PAID; outreach/dealer fan-out/AI gated on `isFulfillmentUnlocked` (`fulfillment-gate.ts:5-23`); but `inviteDealersToAuction` has no own deposit check (`dealer-invitation.service.ts` comments only `:161,167`) and PAY-37 lets a FAILED-status deposit invite dealers | PARTIAL | Fulfillment gate predicate | Sourcing-case opener and invitation service assert a settled, undisputed deposit for the VR (defence in depth); Apollo/enrichment gating verified in sourcing area | 3 | unit | `fulfillment-gate.test.ts`; invitation-service negative test (no settled deposit → refuses) | none | none | TO EXTEND |
| PAY-41 | MD §5d (concierge track, stronger than spec) | Concierge-track deposit never launches a competitive auction | Separate webhook branch mints CLOSED auction (`stripe/route.ts:345-470`); reconciler refuses non-standard tracks (`deposit-activation.service.ts:184-206`) | ALREADY CORRECT | Fail-closed on unknown track / provider outage | Preserve when the settlement side effect changes | 3 | unit, integration | `stripe-concierge-deposit.test.ts:168-221`; `lib/services/auction/__tests__/deposit-activation-concierge-isolation.test.ts`; `lib/services/payment/__tests__/deposit-fulfillment-track.test.ts` | none | none | PRESERVED STRONGER SAFEGUARD |
| PAY-42 | MD §22 L1026-1031; HTML MONEY[0..1] | Three movements; only $99 and $400 touch AutoLenis; $400 any time before funding clears | Constants correct (`lib/constants.ts:6-9`); $400 collected only at deal `FEE_PENDING` after financing (`service-fee.service.ts:104,135-142`; `app/buyer/deal/payment/page.tsx:89-132`) | PARTIAL | Amounts never from client | Allow $400 collection from $99 settlement until funding clears (PAY-58/59a); ladder no longer gates (PAY-64a) | 3 | unit, integration | `service-fee.test.ts` extended: intent allowed at any pre-funding stage | none | `DealStatus FEE_PENDING/FEE_PAID` ladder | TO EXTEND |
| PAY-43 | MD §22 L1032-1034; HTML MONEY[2], MONEY_PANELS[1] | Everything else paid to the dealership; AutoLenis never collects down payment / vehicle funds / holding deposit | Only `deposit`, `concierge_deposit`, `concierge_fee`, `service_fee` PI types (`stripe/route.ts:31`); `DealerPayment` is money-out only | ALREADY CORRECT | `ROUTABLE_PI_TYPES` allow-list | None | 3 | unit | Unit test pins the allow-list (extend `stripe-fee-commission.test.ts`) | none | none | PRESERVED STRONGER SAFEGUARD |
| PAY-44 | MD §22 L1034 | Stripe is the authority; local state never authoritative over the provider | `classifyPaymentConfirmation` used by create-intent, fee service, success pages, reconciler (`payment-confirmation.ts:66-93`); but `deposit/override` creates PAID with no PI (`app/api/admin/buyers/[buyerId]/deposit/override/route.ts:25-31,73`) and `mark-paid` flips without `retrievePaymentIntent` (`app/api/admin/payments/deposit/[depositId]/mark-paid/route.ts:63-64,92,111`) | PARTIAL | `providerConfirmed:false` audit metadata; matrix-guarded flip in `mark-paid` | `mark-paid` must retrieve the PI and require `succeeded` when one is attached; `override` stays a documented comp exception that raises a Finance exception | 3 | integration | `app/api/admin/payments/__tests__/deposit-mark-paid-fulfillment.test.ts` extended; `app/api/admin/buyers/__tests__/deposit-override-*.test.ts` retained | none | admin override / mark-paid | TO EXTEND |
| PAY-45 | MD §22.1 L1038; HTML MONEY_PANELS[0][0] | Refunds reviewed manually; no automatic refund | No cron/webhook refund; auction close and no-dealer close retain the $99 (`auction.service.ts:169-179`; `deposit-activation-policy.ts:22-30`); admin-only route (`admin/payments/deposit/[depositId]/refund/route.ts:19`); AI `admin.trigger_deposit_refund` `requiresHumanApproval:true`, `approverPermission:"finance.refunds"` (`lib/services/ai/action-intent/catalog.ts:231-236`; `engine.ts:154-160`; `policy.ts:99-106`) | ALREADY CORRECT | Human-approval + permission-scoped AI command; PAID-only policy | None | 3 | unit | `lib/services/ai/action-intent` tests assert `APPROVAL_REQUIRED` halt; `test:action-intent` | none | none | PRESERVED STRONGER SAFEGUARD |
| PAY-46a | MD §22.1 L1038; HTML MONEY_PANELS[0][1] | `RefundReason` stored on the refunded record | Enum exists (`prisma/schema.prisma:1914-1919`; migration `20260423180146_complete_schema/migration.sql:95`) with no model field | PARTIAL | — | Add `deposits.refund_reason RefundReason?` and `deals.fee_refund_reason RefundReason?` to the Phase 1 wave | 1 | integration | `pnpm test:migrations` | Production deploy of Phase 1 wave | none | TO IMPLEMENT |
| PAY-46b | MD §22.1 L1038 | Refund routes require a `RefundReason` | Free-text `reason` only (`admin/payments/deposit/[depositId]/refund/route.ts`; `concierge-fee/[dealId]/refund/route.ts`) | PARTIAL | Mandatory reason + `AdminAuditLog` | `z.nativeEnum(RefundReason)` required; persisted on the record and Stripe metadata | 3 | integration | `app/api/admin/payments/__tests__/deposit-refund-route.test.ts` extended (missing/invalid reason → 400) | none | admin refund routes | TO EXTEND |
| PAY-47 | MD §22.1 L1038; HTML MONEY_PANELS[0][2] | Refund execution idempotency-keyed | `refund-deposit-${depositId}` on all three deposit paths; `refund-concierge-fee-${dealId}` (`refund.service.ts:31,60`; deposit refund route `:65-68`; fee refund route `:52-55`) | ALREADY CORRECT | `charge_already_refunded` → sync only | None (key survives PAY-49 consolidation) | 3 | unit | `lib/services/payment/__tests__/refund-deposit-charge.test.ts` | none | none | PRESERVED STRONGER SAFEGUARD |
| PAY-48 | MD §22.1 L1038; HTML MONEY_PANELS[0][2] | Never labels a no-charge record as money refunded | Deposit: `NO_CHARGE`/`NO_STRIPE_CHARGE` (`refund.service.ts:26-28`; route `:41-49`). Fee route sets `feeRefundedAt` and tells buyer "refunded" with no Stripe call for null/`pi_fee_admin_` (`concierge-fee/[dealId]/refund/route.ts:42,68-89`); `REFUND_TRIGGERED` moves deal to `REFUNDED` even on `NO_CHARGE` (`admin/deals/[dealId]/action/route.ts:222-250`) | BROKEN | FS-K tests `refund-deposit-charge.test.ts:53-92` | Fee route returns `NO_CHARGE` (or "reconciled out of band") and never notifies "refunded"; deal status changes only when money moved | 3 | integration | New `app/api/admin/payments/__tests__/concierge-fee-refund-no-charge.test.ts`; `test:admin-deals` case for `REFUND_TRIGGERED` + `NO_CHARGE` | none | admin fee refund; `REFUND_TRIGGERED` | TO EXTEND |
| PAY-49 | MD §22.1 (implementation) | One refund primitive | `refund.service.processRefund` (`:23-45`, lacks `charge_already_refunded` sync + `succeeded` precheck), `refundDepositCharge` (`:47-74`), inline Stripe call in admin route (`admin/payments/deposit/[depositId]/refund/route.ts:55-91`); `lib/services/deposit/deposit.service.ts` named by the payments skill does not exist | DUPLICATED | Deposit-scoped key; PI `succeeded` precheck (route `:57-64`) | Collapse admin route and AI command onto `refundDepositCharge(+reason)`; delete `processRefund`; correct the stale skill reference | 3 | unit, integration | `refund-deposit-charge.test.ts` + `deposit-refund-route.test.ts` both exercise the one primitive | none | AI command `commands.ts:120-124` | TO CONSOLIDATE |
| PAY-50 | MD §22.1 L1040; HTML MONEY_PANELS[0][5], PLAN_DOWN[1] | Downgrade after $400 settled = refund request against the $400 only | No buyer downgrade; admin plan route flips plan only (`app/api/admin/buyers/[buyerId]/plan/route.ts:42-49`); fee refund route standalone | MISSING | — | Downgrade orchestration (PAY-79/80) opens a manual $400 refund review; never touches the deposit | 3 | unit, integration | New `lib/services/plan/__tests__/downgrade.test.ts`: after-settle path creates review, deposit untouched | none | none | TO IMPLEMENT |
| PAY-51 | MD §22.1 L1042; HTML MONEY_PANELS[0][3], MONEY_PANELS[2] | Cancellation and refund are separate decisions | `DEAL_CANCELLED` refunds latest PAID deposit before cancelling; `REFUND_TRIGGERED` refunds + flips REFUNDED (`admin/deals/[dealId]/action/route.ts:167-218,222-250`); `AUCTION_REFUND_TRIGGERED` refunds + cancels (`admin/auctions/[auctionId]/action/route.ts:158-200`); UI `AdminDealTabs.tsx:527,533`, `AdminAuctionDetail.tsx:302` | BROKEN | `cancelDeal` seam never refunds, CAS-guarded `expectedFrom` (`lib/services/deal/deal.service.ts:378-401`) | Split: cancel actions call `cancelDeal` only and record "payment treatment determined"; refund is its own audited action with `RefundReason`; Phase 10 orchestration reuses both | 3 | integration, state-machine | `test:admin-deals` / `test:admin-auctions`: cancel leaves deposit PAID; refund action separate | Owner decision Q4: split immediately vs gate behind `RefundReason` review | admin cancel actions; `AdminBuyerCommandCenter` | TO EXTEND |
| PAY-52 | MD §22.1 L1044, §23.2 L1146; HTML MONEY_PANELS[0][4], PLAN_UP[3] | Refunded / charged-back $99 cannot be the Premium credit → $499 gross | `netFee = PREMIUM_FEE_CENTS − DEPOSIT_AMOUNT_CENTS` unconditionally; `writeServiceFeePayment` always records credit 9900 (`service-fee.service.ts:19-32,104`); page displays credit conditionally but charges $400 (`app/buyer/deal/payment/page.tsx:31-36,90`) | MISSING | Display honesty on credit | Compute credit from the ledger (PAID, unrefunded, undisputed deposit for this VR); charge $499 otherwise; record actual credit | 3 | unit | `lib/services/deal/__tests__/service-fee.test.ts`: refunded/disputed deposit → 49900 gross, credit 0 | none | fee intent; `ServiceFeePayment` | TO EXTEND |
| PAY-53 | MD §22.1 L1044; HTML MONEY_PANELS[0][4] | Refunded / charged-back $99 reverses attributed affiliate commission | Deposit `charge.refunded` and dispute branches touch no commissions (`stripe/route.ts:651-697,769-798`); reversal exists for fee PI only (`:715-745`; `commission.service.ts:254-271`) | MISSING | PAID commissions never auto-reversed (human clawback alert) | On deposit refund/dispute, reverse PENDING/APPROVED commissions for that transaction; alert on PAID | 3 | integration | `stripe-fee-commission.test.ts` new case: deposit refund reverses; `lib/services/affiliate/__tests__/settle-commission.test.ts` | none | none | TO EXTEND |
| PAY-54 | MD §23.1 L1128-1131; HTML PLAN_MODEL[0..1] | Standard $99 / Premium $499 = $99 + $400 | Constants correct (`lib/constants.ts:6-9,33,40`, `STANDARD_FEE_CENTS=0`); UI "Free to start"/"no concierge fee" (`PlanUpgradeCard.tsx:111`; `deal/payment/page.tsx:55-73`; `admin/payments/fee-check/route.ts:41-43`) | PARTIAL | Constants | Reframe copy and `fee-check` display to "Standard $99 paid in full"; constants unchanged | 3 | visual, unit | `test:visual`; `fee-check` route test | Owner decision Q1 | `fee-check` | OWNER-GATED |
| PAY-55 | MD §23.1 L1133; HTML PLAN_MODEL[0] | Standard is not a free tier; every buyer pays $99 | Mechanics correct (deposit gate); copy "Free to start" (`PlanUpgradeCard.tsx:111`) | PARTIAL | Deposit gate | Copy fix | 3 | visual | `test:visual` | Owner decision Q1 | none | OWNER-GATED |
| PAY-56a | MD §23.1 L1135, §23.5 L1207; HTML PLAN_EDGE[3], PLAN_DOWN[4] | `plan_snapshots` table and binding FKs exist | Only `Buyer.plan`/`planUpgradedAt` (`prisma/schema.prisma:47-48`); no plan on VR/Deal | BROKEN | — | Phase 1 wave: `plan_snapshots` (buyer_id, vehicle_request_id, deal_id, plan, effective_at, actor, touchpoint, settled cents, reason) + `deals.plan_snapshot_id` + `vehicle_requests.plan_snapshot_id` | 1 | integration | `pnpm test:migrations`; CI from-zero | Production deploy of Phase 1 wave; owner decision Q2 (is the $400 per VR?) | none | TO IMPLEMENT |
| PAY-56b | MD §23.1 L1135; HTML PLAN_EDGE[3] | Plan elected per VR; buyer carries default; VR and Deal carry binding snapshot | Readers use `buyer.plan` (`app/buyer/deal/page.tsx:44`; `admin-payments.service.ts:55`; `admin-analytics.service.ts:332`; `fee-check`) | BROKEN | — | Election writes a snapshot bound to the VR; Deal creation copies `plan_snapshot_id`; every reader resolves plan via snapshot | 3 | unit, integration | New `lib/services/plan/__tests__/plan-snapshot.test.ts`; reader tests | none | every `buyer.plan` reader | TO EXTEND |
| PAY-57 | MD §23.1 L1137; HTML PLAN_EDGE[1] | Premium entitlements begin at settlement, not election | Self-serve upgrade flips `plan=PREMIUM` free ("INTENTIONALLY FREE AT THIS STAGE", `app/api/buyer/plan/upgrade/route.ts:12-16,31-37`); features keyed on `buyer.plan` (`deal/page.tsx:44`) | BROKEN | Race-safe `updateMany` flip | Split election (snapshot) from entitlement (settled `ServiceFeePayment.paidAt` / `settled_premium_cents`); upgrade route mints the $400 PI instead of flipping | 3 | unit, integration | `app/api/buyer/plan/__tests__/upgrade.test.ts` rewritten failing-first; `test:buyer-plan` | Owner decision Q6: confirm spec supersedes the 2026-07 "free upgrade" note in code | upgrade route; dashboard card | TO EXTEND |
| PAY-58 | MD §23.2 L1143; HTML PLAN_UP[0], S[4].system[5] | Window opens when the $99 settles | Upgrade route has no deposit check (`plan/upgrade/route.ts:17-44`); card shown pre-deposit (`PlanUpgradeCard.tsx:107-129`) | MISSING | — | `isUpgradeWindowOpen(vr)` = settled, unrefunded, undisputed $99 for this VR AND no `funding_cleared_at`; route + card consume it | 3 | unit | `lib/services/plan/__tests__/upgrade-window.test.ts` | none | none | TO IMPLEMENT |
| PAY-59a | MD §23.2 L1144; HTML PLAN_UP[0] | Window-close predicate (funding cleared) | No funding-clear awareness (`plan/upgrade/route.ts`; `deal/payment/page.tsx`) | MISSING | — | Predicate reads Phase 1 `deals.funding_cleared_at`; fee intent refused after it (admin exception PAY-76) | 3 | unit | `upgrade-window.test.ts`: cleared deal → closed | none | none | TO IMPLEMENT |
| PAY-59b | MD §23.2 L1144, L1149; HTML EXC[38] | Window closes at the funding-clearance checkpoint | No `FUNDING_PENDING` checkpoint exists today | MISSING | — | Phase 8 clearance transition stamps `funding_cleared_at`, closes window, triggers PAY-64b revert | 8 | state-machine, playwright | Deal state-machine suite; Playwright funding-clearance journey asserts upgrade refused after clearance | none | none | TO IMPLEMENT |
| PAY-60 | MD §23.2 L1145; HTML PLAN_UP[2] | Cost always $400 shown as $499 less $99; never re-quoted/prorated | Inconsistent: fee page "$499 − $99 = $400" (`deal/payment/page.tsx:98-123`); dashboard "Upgrade to Premium — $499" (`PlanUpgradeCard.tsx:122`); billing no price (`billing/page.tsx:90-92`); signup "$400 after deposit credit" (`SignUpClient.tsx:296-298`); admin `AdminDealTabs.tsx:190-194` | PARTIAL | `netFee` constant math | One shared price-line component fed by constants on every surface | 3 | visual, unit | `test:visual` on all five surfaces; unit for the shared component | none | none | TO EXTEND |
| PAY-61 | MD §23.2 L1146; HTML PLAN_UP[3] | $99 must be valid, paid, unrefunded, not charged back; else $499 gross | As PAY-52 | MISSING | — | Same change as PAY-52 (window predicate + credit rule share one ledger query) | 3 | unit | `service-fee.test.ts` (PAY-52) | none | none | TO EXTEND |
| PAY-62 | MD §23.2 L1147, §23.5 L1209; HTML PLAN_UP[4] | On settlement: assign named concierge same day; move ownership to `assigned_admin_id`; snapshot with touchpoint; notify buyer + concierge | Fee webhook: fee fields, ladder advance, `ServiceFeePayment`, email, commissions (`stripe/route.ts:475-592`); `assignedAdminId` written only by `admin/requests/[requestId]/route.ts:87,113` and `concierge-conversion.service.ts:179`; no roster | MISSING | Fee advance forced + audited, never regresses | Post-settlement service: concierge pick → `assignedAdminId`, snapshot append (touchpoint), dispatcher notices to buyer + concierge | 3 | integration | New `lib/services/plan/__tests__/premium-settlement.test.ts`; webhook test asserts assignment + snapshot | Owner: define the concierge roster/pool (no roster exists — UNVERIFIED) | fee webhook tail | TO IMPLEMENT |
| PAY-63 | MD §23.2 L1148; HTML PLAN_UP[5] | What does not change on upgrade | Fee branch touches only fee fields and ladder (`stripe/route.ts:496-510`; `deal.service.ts` `settleFeeLadderIfPaid`, dfb6b48) | ALREADY CORRECT | — | None | 3 | unit | `service-fee.test.ts` / `stripe-fee-commission.test.ts` negative assertions | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| PAY-64a | MD §23.2 L1149, §23.1 L1137; HTML EXC[38], EXC[39] | Unpaid $400 never gates the transaction | Premium deal blocks at `FEE_PENDING` until paid (`app/buyer/deal/page.tsx:44`; ladder `FINANCING_PENDING→FEE_PENDING→FEE_PAID`, `deal.service.ts:18-20`) | BROKEN | — | `FEE_PENDING/FEE_PAID` become optional side states; `advanceDealStatus` never waits on fee | 3 | state-machine | `lib/services/deal/__tests__` state-machine suite: unpaid Premium proceeds to `CONTRACT_PENDING` | none | `DealStatus FEE_PENDING/FEE_PAID` gating ladder | TO EXTEND |
| PAY-64b | MD §23.2 L1149; HTML EXC[38] | Election reverts to Standard at clearance if unpaid; transaction continues | No revert exists | BROKEN | — | Phase 8 clearance appends a `STANDARD` snapshot when `settled_premium_cents` is 0; buyer notified via dispatcher | 8 | state-machine, integration | Deal suite: clearance with unpaid balance → snapshot STANDARD, deal continues | none | none | TO IMPLEMENT |
| PAY-65 | MD §23.2a L1157; HTML PLAN_SEQ[0], S[4].system[5] | Touchpoint 1: single line on receipt + sourcing-started screen | Success page has no Premium line (`app/buyer/deposit/success/page.tsx:123-158`); template has none and says credit applies "when your deal closes" (`lib/services/email/templates/deposit-confirmation.tsx:19-117,80-82`) | MISSING | — | One named, non-pushy line on both; fix credit-at-close wording; record impression | 3 | unit, visual | `test:email` template render test; `test:visual` success page | none | none | TO IMPLEMENT |
| PAY-66 | MD §23.2a L1158; HTML PLAN_SEQ[1] | Touchpoint 2: offered alongside Best Price Report | No Premium mention (`components/buyer/OfferComparisonPanel.tsx`) | MISSING | — | In-app mention with impression tracking, gated by window + guardrails | 6 | unit, visual | Offer-report component test; `test:visual` | none | none | TO IMPLEMENT |
| PAY-67 | MD §23.2a L1159, L1165; HTML PLAN_SEQ[2] | Touchpoint 3: full-screen invitation once after acceptance, before dealer-confirmation view | `select-offer` has no interstitial (`app/api/buyer/auctions/[auctionId]/select-offer/route.ts`) | MISSING | — | Interstitial with shown-once state, stage-by-stage framing, $499 less $99, concierge role, one-action decline; never delays reaffirmation | 6 | integration, playwright | Playwright: selection → interstitial declinable, non-blocking → Deal at `DEALER_CONFIRMATION` | none | select-offer flow | TO IMPLEMENT |
| PAY-68 | MD §23.2a L1160; HTML PLAN_SEQ[3] | Touchpoint 4: email 1h after acceptance only if declined/dismissed | No sequence (`lifecycle-touch-drain.service.ts:62-70`) | MISSING | — | Dispatcher row `premium_upgrade_1` with state recheck (declined AND window open AND no suppression) | 6 | unit, integration | `lib/services/comms/__tests__/premium-touchpoints.test.ts` | none | none | TO IMPLEMENT |
| PAY-69 | MD §23.2a L1161; HTML PLAN_SEQ[4] | Touchpoint 5: email at reaffirmation or recap; final ask | None | MISSING | — | Dispatcher row `premium_upgrade_2` fired from reaffirmation/recap transition; then silence | 7 | unit, integration | `premium-touchpoints.test.ts` extended | none | none | TO IMPLEMENT |
| PAY-70 | MD §23.2a L1161 ("quietly available") | Existing always-on surfaces become the quiet dashboard option | `PlanUpgradeCard` unconditional (`app/buyer/dashboard/page.tsx:614-620`); billing link (`billing/page.tsx:90-92`); `deal/payment/page.tsx:77-79`; "Welcome to Premium" notification (`plan/upgrade/route.ts:82-92`) | PARTIAL | — | Gate every surface on `isUpgradeWindowOpen` + suppression predicate; no pushing | 3 | unit, visual | Dashboard component test; `test:visual` | none | dashboard card | TO EXTEND |
| PAY-71 | MD §23.2b L1171; HTML PLAN_GUARD[0] | Two emails then silence; declined twice → never asked again | No decline tracking | MISSING | — | Decline/dismiss counter on the plan snapshot (or `buyers`); suppression predicate consumed by touchpoints 4/5 | 3 | unit | `lib/services/plan/__tests__/upgrade-suppression.test.ts` | none | none | TO IMPLEMENT |
| PAY-72 | MD §23.2b L1172; HTML PLAN_GUARD[1] | Never sold on fear | Billing copy "priority dealer access, and expert negotiation" (`app/buyer/billing/page.tsx:92`) implies better dealer access | PARTIAL | — | Copy review of every Premium surface against "never a better deal, never a faster gate" | 3 | visual | `test:visual`; copy checklist in phase report | Owner: approve Premium copy | none | TO EXTEND |
| PAY-73 | MD §23.2b L1173; HTML PLAN_GUARD[2], EXC[40] | Suppress every prompt while transaction is in an exception state | Card unconditional (`PlanUpgradeCard.tsx:107-129`; `dashboard/page.tsx:165-166`) | MISSING | — | Suppression predicate reads open `queue_items` for the VR/deal; wired into every touchpoint | 10 | unit | `upgrade-suppression.test.ts`: open exception → suppressed | none | none | TO IMPLEMENT |
| PAY-74 | MD §23.2b L1174; HTML PLAN_GUARD[3] | Suppressed on DNC, dispute, chargeback, cancellation in progress, existing Premium | Only existing Premium (`plan/upgrade/route.ts:21-23`) | PARTIAL | Idempotent route | Predicate reads `do_not_contact`, PAY-38a hold, cancellation state, settled Premium | 3 | unit | `upgrade-suppression.test.ts` | none | none | TO EXTEND |
| PAY-75 | MD §23.2b L1175; HTML PLAN_GUARD[4] | Invitation never blocks; reaffirmation fires regardless | No interstitial | MISSING | — | Build PAY-67 as non-blocking; reaffirmation request dispatched in the selection tx | 6 | integration, playwright | Playwright PAY-67 journey asserts reaffirmation row created before interstitial dismissed | none | none | TO IMPLEMENT |
| PAY-76 | MD §23.2b L1176; HTML PLAN_GUARD[5] | Admin may open upgrade after funding clears with audited approval; never automatic | Admin plan route (SUPER/FINANCE_ADMIN, reason ≥10, `AdminAuditLog BUYER_PLAN_CHANGED`) flips entitlement, no charge (`app/api/admin/buyers/[buyerId]/plan/route.ts:15-66`) | PARTIAL | Role allow-list + reason + audit | Route opens the $400 collection (window override flag on snapshot), never flips entitlement | 3 | integration | `test:admin-authz` + new plan-route test: late exception mints PI, entitlement unchanged until paid | none | `AdminBuyerCommandCenter` plan buttons | TO EXTEND |
| PAY-77 | MD §23.2b L1178; HTML PLAN_UP[7] | Measure impressions, dismissals, conversions per touchpoint; stamp converting touchpoint | Conversion only: `BuyerActivityEvent PLAN_UPGRADED` + audit `source` (`plan/upgrade/route.ts:46-80`) | PARTIAL | Existing activity events | Impression/dismiss events per touchpoint; `touchpoint` written on the settling snapshot (touchpoint 1 in 3; 2–5 in 6/7) | 3 | unit | `plan-snapshot.test.ts`: touchpoint stamped; activity-event tests | none | none | TO EXTEND |
| PAY-78 | MD §23.2b L1180, §23.5 L1209; HTML PLAN_UP[6] | Standard buyer never disadvantaged; Operations pool owns every Standard transaction with a named owner | `assignedAdminId` nullable, manual (`admin/requests/[requestId]/route.ts:87,113`); no pool | PARTIAL | — | Ownership model: `owner_role=OPERATIONS` pool default on paid VR, named assignee on pickup from queue | 3 | unit | Ownership predicate test; queue-item owner assertions | Owner: Operations-pool semantics (who is "named" for Standard) | none | TO EXTEND |
| PAY-79 | MD §23.3 L1184; HTML PLAN_DOWN[0] | Before $400 settles: election change only; concierge released; ownership to pool; no refund | Admin plan route sets STANDARD, nulls `planUpgradedAt` (`admin/buyers/[buyerId]/plan/route.ts:42-49`); no release/ownership move; no buyer endpoint | PARTIAL | Reason + audit | Downgrade service: snapshot append, release `assignedAdminId`, pool ownership, notify; buyer endpoint `POST /api/buyer/plan/downgrade` | 3 | unit, integration | `downgrade.test.ts` | none | `AdminBuyerCommandCenter` "Downgrade to Standard" (`:1049-1055`) | TO EXTEND |
| PAY-80 | MD §23.3 L1186; HTML PLAN_DOWN[1], EXC[41] | After $400 settles: refund request → manual review on service delivered; full/partial/declined | Fee refund route refunds full amount on click (`concierge-fee/[dealId]/refund/route.ts:68-78`); no partial (`refunds.create` without `amount`); no service record | MISSING | Idempotency key; PI status check | Refund-request record (`queue_items PLAN_EXCEPTION`, owner FINANCE) with decision + partial `amount`; concierge activity log as evidence | 3 | unit, integration | `downgrade.test.ts` after-settle path; fee refund route partial-amount test | Owner decision Q7: Finance UI and partial refunds in scope; refund money-path activation | admin fee refund | TO IMPLEMENT |
| PAY-81 | MD §23.3 L1188; HTML PLAN_DOWN[2] | $99 never refunded on downgrade; never cancels transaction; never releases hold | Admin plan flip touches nothing else (`admin/buyers/[buyerId]/plan/route.ts:42-49`) | ALREADY CORRECT | By absence — keep the downgrade service deposit-blind | Negative assertions in PAY-79/80 tests | 3 | unit | `downgrade.test.ts`: deposit and deal status unchanged | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| PAY-82 | MD §23.3 L1190; HTML PLAN_DOWN[3] | Buyer told which services stop / continue | No notification on admin downgrade (`admin/buyers/[buyerId]/plan/route.ts`, no `notification.create`) | MISSING | — | Dispatcher template `plan_downgraded` listing stopped vs continuing services | 3 | unit | `test:email` template test; `downgrade.test.ts` enqueue assertion | none | none | TO IMPLEMENT |
| PAY-83 | MD §23.3 L1192; HTML PLAN_DOWN[5] | Re-upgrade while window open; refunded $400 paid again in full | Free flag flip (`plan/upgrade/route.ts:21-23`); after fee refund `feePaidAt` stays → `ALREADY_PAID` (`fee/create-intent/route.ts:25-27`); `findSettledFeeIntent` returns refunded PI as `charge_unsettled` (`service-fee.service.ts:85-95`); key `concierge-fee-${dealId}` replays 24h (`:141,150-157`) | BROKEN | `settleFeeLadderIfPaid` treats refunded fee as unpaid (`deal.service.ts:263-266`) — adopt this rule | Fee route treats `feeRefundedAt` as unpaid; refund-aware settled-intent search; idempotency key suffixed by attempt | 3 | unit, integration | `service-fee-duplicate-charge.test.ts` new case: refunded fee → new PI minted once | none | fee route | TO EXTEND |
| PAY-84 | MD §23.4 L1198; HTML PLAN_EDGE[0], EXC[42] | $99 charged back after Premium settled → Finance exception; entitlement holds; never silent downgrade | Dispute → `AdminAuditLog STRIPE_DISPUTE_CREATED` only (`stripe/route.ts:769-798`); no auto-downgrade (correct by absence) | PARTIAL | Audit row with due_by | `raiseException(PLAN_EXCEPTION, owner FINANCE)` naming deal + plan; no plan change | 3 | integration | `stripe-dispute-hold.test.ts` (PAY-38b) asserts exception + plan unchanged | none | none | TO EXTEND |
| PAY-85 | MD §23.4 L1199; HTML PLAN_EDGE[1] | Elect Premium at registration, never pay → Standard until settled; revert at clearance | Signup offers Premium; callback/session persist `plan=PREMIUM` free (`app/auth/signup/SignUpClient.tsx:283-303,310`; `app/auth/callback/route.ts:31-33,75-77`; `lib/auth/session.ts:86-90`; `app/api/admin/users/create/route.ts:128`) then gates at `FEE_PENDING` | BROKEN | — | Registration election → snapshot only (Phase 2 Stage 1); entitlement via PAY-57; revert via PAY-64b | 3 | unit, integration | `upgrade.test.ts`; auth callback test: Premium metadata → snapshot, no entitlement | none | `auth/callback`, `session.ts`, admin user-create | TO EXTEND |
| PAY-86 | MD §23.4 L1200; HTML PLAN_EDGE[2] | $499 in one transaction not offered | Only $99 and $400 intents (`service-fee.service.ts:104,135-142`; `create-intent/route.ts:227`) | ALREADY CORRECT | — | None | 3 | unit | `service-fee.test.ts` pins `netFee` | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| PAY-87 | MD §23.4 L1201; HTML PLAN_EDGE[4] | Standard asks at financing/contract → take upgrade, assign concierge that day | Upgrade possible at any time incl. after funding; no assignment (`plan/upgrade/route.ts`) | PARTIAL | — | Window predicate (PAY-58/59a) + settlement orchestration (PAY-62) | 3 | integration | `premium-settlement.test.ts` at `FINANCING_PENDING`/`CONTRACT_PENDING` | none | none | TO EXTEND |
| PAY-88 | MD §23.4 L1202, §23.1 L1135; HTML PLAN_EDGE[3] | Second Vehicle Request → new $99, fresh election | `ALREADY_PAID` buyer-wide (`create-intent/route.ts:88-95`); `isFulfillmentUnlocked` buyer-wide (`fulfillment-gate.ts:34-38`); plan buyer-wide | BROKEN | — | Per-VR deposit (PAY-11b), per-VR gate (PAY-30), per-VR snapshot (PAY-56b); allowed only after prior VR reaches `DEAL_CREATED`/terminal (Phase 1 index) | 3 | integration, concurrency | `create-intent-duplicate-charge.test.ts`: second VR after completed Deal mints a new PI | Owner decision Q2 | `ALREADY_PAID`, `hasPaidDeposit`, journey `depositPaid` fact | TO EXTEND |
| PAY-89 | MD §23.4 L1203; HTML PLAN_EDGE[5] | Deal cancelled after Premium → cancellation and refund separate; $400 manual review | `DEAL_CANCELLED` auto-refunds the $99, not the $400 (`admin/deals/[dealId]/action/route.ts:172-186`) | BROKEN | `cancelDeal` seam | PAY-51 split; cancel opens a `PLAN_EXCEPTION` review for the settled $400; Phase 10 orchestration consumes | 3 | integration, state-machine | `test:admin-deals`: cancel Premium deal → deposit PAID, review row, no refund | Owner decision Q4 | admin cancel actions | TO EXTEND |
| PAY-90 | MD §23.5 L1207; HTML PLAN_DOWN[4] | Every plan change appends a snapshot; settled history never rewritten | `AuditLog`/`AdminAuditLog` from/to; `Buyer.plan` overwritten; `planUpgradedAt` nulled (`plan/upgrade/route.ts:47-66`; `admin/buyers/[buyerId]/plan/route.ts:46,53-66`) | PARTIAL | Audit rows | All plan writers append to `plan_snapshots`; `Buyer.plan` becomes derived default; stop nulling history | 3 | unit | `plan-snapshot.test.ts`: append-only, no update/delete path | none | none | TO EXTEND |
| PAY-91 | MD §23.5 L1207; HTML PLAN_DOWN[4] | Fee reconciliation from the ledger, never the plan flag | `ServiceFeePayment` written on fee success (`service-fee.service.ts:19-44`; `stripe/route.ts:517`); readers filter by `buyer.plan`: `lib/services/analytics/admin-analytics.service.ts:330-333` (drops Premium buyers' $99), `lib/services/admin/admin-payments.service.ts:54-56` | BROKEN | Idempotent single writer on `dealId` | Readers compute from `ServiceFeePayment` + `Deposit` ledgers; add `ServiceFeePayment→Deal` relation; resolve `recordFeePayment` callers (dead per comment, `service-fee.service.ts:167-185`) | 3 | unit | `test:analytics` + `test:admin-payments` ledger-derived totals; `lib/services/affiliate/__tests__/commission-ledger-totals.test.ts` pattern | none | `recordFeePayment` | TO EXTEND |
| PAY-92 | MD §23.5 L1209 | Every paid request has a named owner | `assignedAdminId` optional, manual | PARTIAL | — | PAY-62 + PAY-78 | 3 | unit | Ownership predicate test | Owner: pool semantics (PAY-78) | none | TO EXTEND |
| PAY-93 | MD §5d L473; HTML S[4].tables | `payment_provider_events`, `idempotency_keys` used | `PaymentProviderEvent` unique `eventId` + `processed` (`prisma/schema.prisma:856-866`); `lib/jobs/idempotency.ts:34-121`; `WebhookEvent` model (`:2777`) unused by Stripe path (UNVERIFIED) | ALREADY CORRECT | Claim inside tx (deposit); claim-at-end (fee/refund) | Phase 1 brings `idempotency_keys` under the Prisma chain at the production definition (§13-D24); no behaviour change | 1 | integration | `pnpm test:migrations`; `stripe-idempotency.test.ts` | Production deploy of Phase 1 wave | none | ALREADY PRESENT (VERIFY IN PHASE) |
| PAY-94 | MD §6.5 L307, §22.1 L1044 | Commission settles at Deal completion; reverses on cancel / $99 refund / chargeback | Created at fee `payment_intent.succeeded` (`stripe/route.ts:561-591`); auto-APPROVED ≥7d by cron; reversed on fee refund only (`commission.service.ts:18-100,110-161,254-271,290-418`) | PARTIAL | ≤3 levels; idempotent on `qualifyingEventId`; PAID never auto-reversed; approval cron fails closed; DLQ replay | Creation/settlement trigger moves to Deal `COMPLETED`; reversal on deal cancel and deposit refund/dispute | 3 | unit, integration | `lib/services/affiliate/__tests__/settle-commission.test.ts`, `commission-approval-safety.test.ts` extended; webhook case removed | Owner of §6.5 area confirms timing change | fee webhook; `affiliates` cron; DLQ replay `operations.service.ts:98-99` | TO EXTEND |
| PAY-A | MD §23.1 L1131; HTML PLAN_MODEL[1] | Premium entitlements (named concierge, priority coordination, financing/insurance/contract/pickup support) delivered | `buyer.plan === "PREMIUM"` only changes copy/badges and gates the fee stage (`AdminDealTabs.tsx:190`; `billing/page.tsx:82-92`; `PlanUpgradeCard.tsx`; `deal/payment/page.tsx:38`); no service-delivery code path found | UNVERIFIED | — | Entitlement predicates read settled `plan_snapshots`/`ServiceFeePayment.paidAt`; concierge assignment (PAY-62) is the delivery hook; service delivery itself is an Ops process | 3 | unit | `plan-snapshot.test.ts` entitlement predicate; Ops runbook entry | Owner: concierge roster + delivery expectations | none | UNVERIFIED — no code delivers Premium services; only the entitlement predicate is buildable |
| PAY-B | MD §22.1 L1042; HTML MONEY_PANELS[0][3] | Issuing a refund does not erase the transaction record | No refund path deletes Deal/Deposit/Auction; `REFUND_TRIGGERED` sets `REFUNDED` + audit (`admin/deals/[dealId]/action/route.ts:238`; `deal.service.ts:66-68`) | ALREADY CORRECT | Append-only records | None; keep after PAY-48/51 changes | 3 | integration | `test:admin-deals`: refund leaves rows intact | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| PAY-C | MD §5c L465 | Reminder carries a secure checkout link | CTA is the plain authenticated `/buyer/deposit` (`lifecycle-touch-drain.service.ts:50-53,155-247`); `issueResumeToken` rail used only by `form_submitted` chain | PARTIAL | Existing resume-token rail | Deposit series CTA uses a signed, expiring, VR-scoped resume link from the same rail | 3 | unit | `deposit-series-outbox.test.ts`: link carries token, expires | none | none | TO EXTEND |
| PAY-D | MD §5b L454-461; HTML S[4].buyer[0] | Disclosure acceptance persisted (timestamp + version) | No `disclosureAccepted`/`radiusAuthoriz`/`distanceDisclos` field on Buyer, VR or Deposit (`prisma/schema.prisma` grep); body parses only `reviewToken` (`create-intent/route.ts:36-61`) | MISSING | — | Add `deposits.disclosures_accepted_at` + `disclosures_version` (and `vehicle_requests` mirror) to the Phase 1 wave; written by PAY-08 | 1 | integration | `pnpm test:migrations`; DB assertion in PAY-08 Playwright | Production deploy of Phase 1 wave | none | TO IMPLEMENT |
| PAY-E | MD §23 (all); HTML PLAN_MODEL, PLAN_UP[2] | One buyer-facing fee surface | Two pages derive fee state from `buyer.plan` + `deal.feePaidAt` with independent copy: `app/buyer/deal/payment/page.tsx:25-135` (charges) and `app/buyer/fee/page.tsx:67-90` (status/receipt) | DUPLICATED | — | Retire `app/buyer/fee/page.tsx` onto the deal payment page (redirect) or make both read the shared price-line component + snapshot | 3 | visual, playwright | `test:visual`; Playwright: `/buyer/fee` resolves to one canonical surface | none | `app/buyer/fee/page.tsx` | TO CONSOLIDATE |

## Owner-gated items

- **Production deploy of the Phase 1 migration wave** (§13-D1 ledger resolve first, then `prisma migrate deploy`) — gates PAY-10a, 11a, 38a, 46a, 56a, 93, D.
- **Owner-run cleanup** of the 3 buyers holding multiple open Vehicle Requests (workflow §5.6) before the one-open-request index — PAY-07.
- **`GOOGLE_GEOCODING_API_KEY`** production value (undeclared per `docs/plans/BUYER-LOCATION-GAP.md`) — PAY-03.
- **`DEPOSIT_SETTLEMENT_RECONCILE_ENABLED=true`** in production, plus **Q8** (clear `DEFAULT_EXCLUDED_DEPOSIT_IDS` / remove hardcoded id) — PAY-34.
- **Q1 — legal copy**: "$99 = Standard plan paid in full" reframing and refund-policy wording; published refund-policy text/URL — PAY-12, 18, 54, 55.
- **Q2 — plan per VR**: whether the $400 Premium balance is also per Vehicle Request — PAY-56a, 88.
- **Q3 — reconciler tail parity** (receipt / reminder-cancel / attribution on reconciler settle) — PAY-32.
- **Q4 — cancel-with-refund admin actions**: split immediately vs gate behind `RefundReason` review — PAY-51, 89.
- **Q5 — reminder legacies** (QStash job, prebuilt workflow, `DEPOSIT_IDLE` nudge) delete vs keep for Make.com; Make.com consumption UNVERIFIED — PAY-24a; **§13-D23** QStash dependency/route/env deletion — PAY-24b.
- **Q6 — supersede the 2026-07 "free upgrade" decision** recorded in `app/api/buyer/plan/upgrade/route.ts:12-16` — PAY-57.
- **Q7 — Finance UI + partial $400 refunds** (`refunds.create({amount})`) in scope; refund money-path activation — PAY-80.
- **Concierge roster / Operations-pool semantics** (no roster exists) — PAY-62, 78, 92, A.
- **Premium copy approval** ("never sold on fear") — PAY-72.
- **§6.5 area owner** confirms commission timing moves to Deal completion — PAY-94.
- **Legacy removal** of settlement→auction path after zero `LEGACY_PATH_WRITE` for 30 days — PAY-33.
- **§13-D12** fold of `checkDepositProviderEvidence` into the reconciler (one rail) — PAY-34/35; **§13-D24** production definition of `lifecycle_touch_schedule`/`idempotency_keys` — PAY-19, 93.

## Legacy paths

- Deposit settlement → immediate auction create + launch + invite (webhook tail `app/api/webhooks/stripe/route.ts:202-214,259-263`; activation reconciler `create_auction`/`invite`/`close` branches) — neutralised Phase 3, removed owner-gated (PAY-33, 40).
- Deposit-reminder direct producer + legacy series: `app/api/jobs/deposit-reminder/route.ts`, `lib/services/workflow.prebuilt.ts:114-127` on `deposit_pending`, `NudgeStage.DEPOSIT_IDLE`, `create-intent/route.ts:288-303` event emit, `lib/qstash/*` (PAY-19, 24a, 24b).
- Buyer-keyed PaymentIntent idempotency and admin `send-link` / admin `create-intent` (incl. `pi_admin_`/`pi_fee_admin_` synthetic ids, `metadata.depositId` link `stripe/route.ts:173-179`) (PAY-11b, 26).
- `ALREADY_PAID` buyer-wide rule, `hasPaidDeposit` (`lib/qstash/state.ts:8`), journey `depositPaid` fact, buyer-wide `isFulfillmentUnlocked` callers (`post-intake-outreach.service.ts:159`, `dealer-opportunity-notification.service.ts:49`, `ai/action-intent/policy.ts:53`) (PAY-07, 30, 88).
- `SHORTLIST_REQUIRED` proxy gate (PAY-05).
- `SYSTEM_ALERT` Notification as exception store — kept as read-only mirror (PAY-35, 36).
- Admin `deposit/override` and `mark-paid` local-authoritative flips (PAY-44).
- Admin cancel-with-refund actions `DEAL_CANCELLED`, `REFUND_TRIGGERED`, `AUCTION_REFUND_TRIGGERED`; `AdminDealTabs.tsx:527,533`, `AdminAuctionDetail.tsx:302`, `AdminBuyerCommandCenter` plan/downgrade buttons (PAY-48, 51, 76, 79, 89).
- Three refund implementations incl. AI command `commands.ts:120-124` (PAY-49); admin fee refund route (PAY-48, 80).
- `DealStatus FEE_PENDING/FEE_PAID` as a gating ladder (PAY-42, 64a).
- Every `Buyer.plan` reader: `app/buyer/deal/page.tsx:44`, `lib/services/admin/admin-payments.service.ts:54-56`, `lib/services/analytics/admin-analytics.service.ts:330-333`, `admin/payments/fee-check`; `recordFeePayment` (PAY-54, 56b, 91).
- Free Premium election writers: `app/auth/callback/route.ts:31-33,75-77`, `lib/auth/session.ts:86-90`, `app/api/admin/users/create/route.ts:128`; self-serve upgrade route; dashboard `PlanUpgradeCard` (PAY-57, 70, 85).
- Fee webhook tail (`stripe/route.ts:475-592`) incl. `metadata.type === "service_fee"` fallback; fee create-intent `ALREADY_PAID` on refunded fee (PAY-62, 83).
- Commission creation at fee payment, `affiliates` approval cron, DLQ replay `operations.service.ts:98-99` (PAY-94).
- Duplicate buyer fee surface `app/buyer/fee/page.tsx` (PAY-E); select-offer flow gains the interstitial (PAY-67).
