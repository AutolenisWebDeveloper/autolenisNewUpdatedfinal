# Parity table — area `contract` (Stages 13–15: contract request, Contract Shield, e-sign, dealer execution, financing completion / funding clearance, insurance)

Sources: `parity/contract.md` (rows C-01…C-59, D-1…D-5, incl. "Verification corrections (adversarial pass)" — corrected status wins), `docs/transaction-flow/IMPLEMENTATION-WORKFLOW.md` §5.2, §6.2, §8.1, §8.2, §8.4, §12.1, spec `AUTOLENIS-COMPLETE-TRANSACTION-FLOW.md` L776–876, HTML S[12..14]. Paths relative to `frontend/`. Rows are split (schema / behaviour) only where the parts land in different phases. Workflow §5.2 (production read, VERIFIED) supersedes the map on one point: `contract_scans.contract_version_id` and the full e-sign evidence schema are physically present in production; the "LOCAL/STAGING ONLY" migration headers are stale (§13-D33) and the remaining gate is `ESIGN_EXECUTED_ARTIFACT_ENABLED` (§13-D4). No test, build, DB, or MCP was run for this table (static read only).

| Ref | Document & section | Requirement | Current route / service / model / component / email / cron / queue / admin action | Status | Stronger safeguard to preserve | Exact required change | Phase | Test level | Acceptance evidence | Owner-gated dependency | Legacy path affected | Final disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C-01 | MD §Stage 13 Entry L778; HTML S[12].entry | Entry = financing terms locked or cash confirmed; plan level never blocks | `lib/services/deal/deal.service.ts:20-22` (CONTRACT_PENDING only from INSURANCE_PENDING), `:309-333` `advanceOnInsuranceSatisfied`; `prisma/schema.prisma:1711-1716` FinancingStatus has no lock/cash value; plan never checked (correct) | BROKEN | `expectedFrom` CAS guard `deal.service.ts:127,140-165` — keep | Gate CONTRACT_PENDING entry inside `advanceDealStatus` on `Financing.status` in {TERMS_LOCKED, NOT_REQUIRED_CASH} (Phase 7 checkpoint); drop insurance precondition (C-04) | 8 | state-machine, unit | `lib/services/deal/__tests__/deal-state-machine.test.ts` failing-first: entry refused without lock, allowed for cash; `advance-deal-status.test.ts` | owner decision (map Q1: migrate deals parked at INSURANCE_PENDING) | `INSURANCE_PENDING → CONTRACT_PENDING` edge; `advanceOnInsuranceSatisfied`; `prisma/backfill-insurance-gate.ts`; `manual_supabase_sql/backfill_insurance_gate.sql` | TO IMPLEMENT |
| C-02 | MD §14a L782; HTML S[12].system[0] | 24-hour secure upload request dispatched durably from the central transition, with reminder and escalation | Dealer email only from admin manual action `app/api/admin/deals/[dealId]/action/route.ts:88-98`; `deal.service.ts:194,225-241` `runArrivalHooks` has no CONTRACT_PENDING branch; `acquisition-comms.ts:47-50,153-164` buyer-only; template `dealer-contract-pending.tsx:44-45` no deadline | BROKEN | `emitDealStatusComms` idempotency key per (deal,status,buyer) `acquisition-comms.ts:189-194` — reuse for dealer dispatch | Add CONTRACT_PENDING branch in `runArrivalHooks`: create SALES_CONTRACT `DocumentRequest` (dueAt now+24h) + enqueue dealer email/in-app through `comms_outbox`; concierge track (no dealer) → Ops | 8 | integration, unit, playwright | New `lib/services/deal/__tests__/contract-request-dispatch.test.ts`: arrival creates one request + one outbox row (idempotent on retry); Playwright Phase 8 journey step 1 with DB assertion | none | `action/route.ts:87-99` manual send (becomes redundant); `dealer-contract-pending.tsx` gains deadline | TO EXTEND |
| C-03 | MD §14a L782; HTML S[12].tables `document_requests` | `document_requests` models deal-scoped request with due date and is used here | `prisma/schema.prisma:2191-2202` model exists; `lib/services/documents/document.service.ts:28-32` `requestDocument` never sets `dueAt`, zero callers; nothing reads the table | MISSING | none | Extend `requestDocument(dealId,type,requestedBy,{dueAt,reason})`; call from C-02 hook; SUBMITTED on ContractVersion create, VERIFIED on approval; reminders/escalation keyed on `dueAt` | 8 | unit, integration | New `lib/services/documents/__tests__/document-request.test.ts`: dueAt set, status transitions; Playwright DB assertion `document_requests.due_at` | none | none (greenfield use of existing model) | TO EXTEND |
| C-04 | MD §14a L788, §Stage 15 Entry L861; HTML S[12].system[1] | Insurance requested at the contract-request moment; never gates contract | `deal.service.ts:20-21,319-320` insurance gates CONTRACT_PENDING; `acquisition-comms.ts:143-151` INSURANCE_PENDING plan | BROKEN | none | Remove insurance from contract-entry path; fire insurance-request comms from the same CONTRACT_PENDING hook; insurance becomes release gate only (C-47/C-51) | 8 | state-machine, integration | `deal-state-machine.test.ts`: CONTRACT_PENDING reachable with insurance NOT_STARTED; outbox row for insurance request asserted | owner decision (map Q1) | `INSURANCE_PENDING` status + `advanceOnInsuranceSatisfied`; `admin-queue.service.ts:13-18` INSURANCE_EXCEPTION; `lib/services/buyer/journey.ts:83-87` | TO IMPLEMENT |
| C-05 | MD §14a L784-786; HTML S[12].dealer[1], note | Dealer prepares own complete package and confirms completeness; AutoLenis prescribes no forms | `app/api/dealer/contracts/upload-file/route.ts:37-64` single file; `dealer-contract.service.ts:165-167` one `documentUrl`; `schema.prisma:2624-2640` | PARTIAL | AutoLenis prescribes no forms (already correct) — keep | Allow multi-object package (or combined PDF) per version plus dealer "package complete" attestation (reuse `document_requests.status`/`fulfilledAt`); upload UI checklist | 8 | integration, playwright, visual | `app/api/dealer/contracts/__tests__/upload-completes-pipeline.test.ts` extended: attestation required; visual snapshot of upload page | none | `components/dealer/ContractUploadButton.tsx`; `app/dealer/contracts/upload/page.tsx` | TO EXTEND |
| C-06 | MD §14b L792; HTML S[12].system[2] | Shield compares contract vs winning offer, reaffirmation, recap: VIN, mileage, OTD components, doc fee, taxes, title/reg, trade, down payment, financing, each optional product, delivery | `contract-shield.service.ts:127-181` reads only `contractScanRule`, never Offer/recap; `contract-comparison.service.ts:4-15` 5-label text diff, zero callers | MISSING | factual, non-legal finding wording — keep | Structured extraction + per-field comparison in `contract-shield.service.ts` against accepted Offer (`otdPriceCents`, `feesCents`, `aprRate`, `termMonths`, VIN), `dealer_reaffirmations`, `deal_recaps` (Phase 7) feeding the same `fixList` | 8 | unit, integration | New `lib/services/contract-shield/__tests__/offer-recap-comparison.test.ts`: every field mismatch → named finding; Playwright hold step | none | `lib/services/contract/contract-comparison.service.ts` (retire) | TO IMPLEMENT |
| C-07 | MD §14b L794; HTML S[12].system[3] | Hold on any unexplained increase, addition, inconsistent total, changed VIN/financing/trade | `dealer-contract.service.ts:247-255` holds only on junk-fee score < 85; `lib/constants.ts:73-77` `getContractShieldResult` | MISSING | WARNING and FAIL both hold; non-PASS never auto-approves — keep | Comparison findings force non-PASS regardless of score; documented-review path in Ops queue | 8 | unit, state-machine | `contract-auto-approve.test.ts` extended: mismatch with score 100 → REJECTED, no auto-advance | none | none | TO IMPLEMENT |
| C-08 | MD §14b L794; HTML S[12].system[4] | Junk-fee, fee caps, APR validation, payment packing, disclosure checks from existing rule set | `contract-shield.service.ts:30-44,60-115` built-ins; `:145` JUNK_FEE_KEYWORD, `:162` FEE_CAP only; `app/admin/contract-shield/rules/page.tsx:43-46,215` lists APR_VALIDATION/PAYMENT_PACKING/DISCLOSURE_CHECK/FINANCE_MARKUP never evaluated | PARTIAL | built-ins always run; DB FEE_CAP skipped when built-in flagged (no double deduction) — keep | Implement branches for APR_VALIDATION (`maxApr` + locked APR), PAYMENT_PACKING, DISCLOSURE_CHECK (`requiredTerms`), FINANCE_MARKUP (`maxMarkupBps`) in `scanContract` | 8 | unit | New `lib/services/contract-shield/__tests__/rule-types.test.ts`: one case per rule type + skip-when-builtin | none | none | TO EXTEND |
| C-09 | MD §14b L796; HTML S[12].system[5] | Extraction failure retryable, never approval | `extract-text.ts:50-52` throws; `dealer-contract.service.ts:236-239` writes SCANNING, `:256-263` reset only in `catch`; cron `app/api/cron/contract-shield/route.ts:15-19` selects UPLOADED only — hard kill leaves SCANNING forever | PARTIAL (corrected from ALREADY CORRECT, map correction 1) | fail-closed throw; cron touches UPLOADED only; scan without version link un-approvable `contract-shield.service.ts:117-126` — keep | Cron (or `scanContractVersion` entry) reclaims SCANNING rows older than N minutes; bounded retry count + Ops exception after N failures | 8 | unit, integration | New `app/api/cron/__tests__/contract-shield-cron.test.ts`: stale SCANNING reclaimed; retry cap raises exception | none | none | TO EXTEND |
| C-10 | MD §14b L796; HTML S[12].system[6] | Uploads private, dealer-owned, versioned create-before-supersede | `upload-file/route.ts:24,58-64`; `dealer-contract.service.ts:163-178,21-27,189-192` correct; but `app/api/dealer/contracts/upload/route.ts:9,19` accepts any shape-valid key (no `dealer.id/dealId/` prefix check), zero UI callers (`ContractUploadButton.tsx:32`) | PARTIAL (corrected from ALREADY CORRECT, map correction 2; duplicated entry point) | SSRF-safe `contractDocumentPathSchema` `contract-document-ref.ts:38-55`; `SAFE_ID`; `assertDealerOwnsDeal`; `requirePermissionStrict("deals.esign.void")` on admin upload — keep | Delete JSON `upload` route (or enforce prefix ownership + object existence); `upload-file` becomes the sole dealer entry | 8 | integration, unit | `upload-completes-pipeline.test.ts` extended: foreign key refused / route gone; `contract-document-ref.test.ts` | none | `app/api/dealer/contracts/upload/route.ts` | TO CONSOLIDATE |
| C-11 | MD §14b L796; HTML S[12].system[7] | Approval binds to the exact reviewed version | `dealer-contract.service.ts:245-248,78-148`; `contract-shield.service.ts:187-189`; `[reviewId]/route.ts:88-91` (409 on NULL link); migration `20261016000000_contract_scan_version_link` header says LOCAL/STAGING ONLY but workflow §5.2 VERIFIED column + index + FK PRESENT in production; admin APPROVE uses `force:true` `[reviewId]/route.ts:101-107` | ALREADY CORRECT (map PARTIAL superseded by §5.2 production read) | NULL-link hard refusal; CAS claim `status != SUPERSEDED`; one-APPROVED-per-deal `:141-145` — keep | No code change; remove stale header annotation (§13-D33); record `reason` on forced hop | 8 | integration | `app/api/admin/contract-shield/__tests__/approve-binds-to-reviewed-version.test.ts` re-run; Playwright approve step DB assertion `contract_scans.contract_version_id` non-null | production ledger resolve `prisma migrate resolve --applied 20261016000000_contract_scan_version_link` (§6.2) | pre-migration `contract_scans` rows with NULL link (unapprovable by design) | ALREADY PRESENT (VERIFY IN PHASE) |
| C-12 | MD §14b L796; HTML S[12].system[7] | Upload arriving during review is rejected, not silently swapped | `dealer-contract.service.ts:169-178` supersedes every version incl. the one under review; `:100-117,128-139` stale approval refused; `upload-file/route.ts:43-49` no status gate | PARTIAL | approval-side CAS refusal `SUPERSEDED_BY_NEWER_UPLOAD` — keep as belt and braces | In `createContractVersionAndScan` refuse (409, reason) while current version's scan is in human review and deal at CONTRACT_REVIEW, unless REVISION_REQUESTED | 8 | integration | `upload-completes-pipeline.test.ts` extended: 409 during review; allowed after REVISION_REQUESTED | owner decision (map Q7: reject vs accept-and-refuse-stale) | none | TO IMPLEMENT |
| C-13 (schema) | MD §14b L798; HTML S[12].tables `contract_versions` | `contract_versions.document_hash` exists | `schema.prisma:2624-2640` no hash column; only `e_sign_envelopes.document_hash` | MISSING | none | Phase 1 wave: nullable `contract_versions.document_hash` (with `is_dealer_executed`, `executed_at`, `executed_document_hash`) | 1 | integration | `prisma/__tests__/migration-chain.test.ts` + new `prisma/__tests__/transaction-spine-schema.test.ts` column assertions; CI from-zero replay | production migration deploy (§13-D1 ledger resolve first) | none | TO IMPLEMENT |
| C-13 (behaviour) | MD §14b L798; HTML S[12].tables `contract_versions` | Approved bytes identifiable by hash | `buyer-signing.service.ts:92-95,212` hash computed only at envelope prepare | MISSING | shared `loadContractPdfBytes` `extract-text.ts:15-34` — reuse | Compute SHA-256 in `createContractVersionAndScan`; verify at approval and at envelope prepare (envelope hash must equal version hash) | 8 | unit, integration | `contract-auto-approve.test.ts` + `lib/services/esign/__tests__/buyer-signing.test.ts` extended: hash mismatch refuses prepare | none | existing ContractVersion rows (backfill by re-hashing stored objects) | TO IMPLEMENT |
| C-14 | MD §14c L802; HTML S[12].system[8] | Approved bytes bound to envelope by hash | `buyer-signing.service.ts:209-221` bind, `:398-412` re-hash + void; migration `20261013` columns in production projection (`esign-schema-gate.ts:14-18,69-98`; §5.2 PRESENT); test `buyer-signing.test.ts:240,295` | ALREADY CORRECT (ceremony dormant behind env flag, not schema — map correction 8) | hash recomputed before completion; consent bound to that hash — keep | None; add version-hash cross-check once C-13 lands | 8 | unit | `buyer-signing.test.ts` re-run with flag on in preview | `ESIGN_EXECUTED_ARTIFACT_ENABLED` activation (§13-D4) | none | ALREADY PRESENT (VERIFY IN PHASE) |
| C-15 | MD §14c L803; HTML S[12].note | A page view is not a signature | `app/api/buyer/esign/[dealId]/route.ts:49-52` GET writes `viewedAt` only; `sign/route.ts:26-31` needs all acks + typed name; `buyer-signing.service.ts:373-375`; `SigningCeremony.tsx:189,204-227` | ALREADY CORRECT | server-side acknowledgment validation — keep | none | 8 | unit, integration | `app/api/buyer/esign/__tests__/buyer-esign-routes.test.ts` re-run | none | none | PRESERVED STRONGER SAFEGUARD |
| C-16 | MD §14c L804; HTML S[12].buyer[1] | Affirmative e-records consent + adopted name; policy version + snapshot stored | `consent-policy.ts:20-29` DRAFT_V1, four acks; `buyer-signing.service.ts:82-89,374,418-452,460-481`; `schema.prisma:733-744`; migration `20261015` columns PRESENT (§5.2) | ALREADY CORRECT | append-only policy registry; snapshot never rewritten; consent cleared per attempt `:239-246` — keep | None in code; consent copy is DRAFT pending attorney review | 8 | unit | `lib/services/esign/__tests__/consent-policy.test.ts`, `buyer-signing.test.ts` | owner decision: attorney sign-off of consent copy; flag activation (§13-D4); ledger resolve `20261015` | none | ALREADY PRESENT (VERIFY IN PHASE) |
| C-17 | MD §14c L805; HTML S[12].system[9] | Identity, IP, device, timestamps recorded server-side | `lib/security/request-attribution.ts:16-26`; `sign/route.ts:51,56-65`; `buyer-signing.service.ts:436-452`; `buyer-contract-certificate.service.ts:26-38` | ALREADY CORRECT | DTO allow-list `esign-dto.ts:1-13`, `esign-schema-gate.ts:111-122` never returns IP/UA; audited evidence export `evidence/route.ts:40-45` — keep | none | 8 | unit | `esign-dto.test.ts`; `app/api/admin/deals/__tests__/esign-authz-route.test.ts` re-run | none | none | PRESERVED STRONGER SAFEGUARD |
| C-18 (schema) | MD §14c L806; HTML S[12].exit | Co-buyer signs when named as required signer | `schema.prisma:686` `dealId @unique` (one envelope per deal); `buyer-signing.service.ts:42` `SIGNER_ROLE = "BUYER"`; `request-vehicle/route.ts:106` boolean only | MISSING | none | Phase 1: `co_buyers` table (`is_required_signer`), `e_sign_envelopes.signer_kind` (BUYER/CO_BUYER) + `co_buyer_id`, partial unique `(deal_id, signer_kind)` | 1 | integration | `transaction-spine-schema.test.ts`: table/columns/index present; migration chain replay | production migration deploy; owner decision (map Q3: co-buyer identity model) | `ESignEnvelope` single-signer unique | TO IMPLEMENT |
| C-18 (signing) | MD §14c L806; HTML S[12].exit | Co-buyer signs when named as required signer | No co-buyer signer path anywhere (`rg coBuyer\|cosigner\|signerRole` → request form, admin display, constant only) | MISSING | per-signer consent snapshot + hash binding pattern in `buyer-signing.service.ts` — reuse | Signer-aware prepare/sign (envelope row per `signer_kind`), separate consent/hash per signer, COMPLETED only when all required signers signed; invited-signer auth; co-buyer record captured in Phase 4 | 8 | unit, integration, concurrency, playwright | `buyer-signing.test.ts` extended; new `lib/services/esign/__tests__/co-buyer-signing.test.ts`; concurrency: both signers complete once; Playwright buyer + co-buyer sign | owner decision (map Q3); flag activation | `ensureDealSigned` (must wait for all signers) | TO IMPLEMENT |
| C-19 | MD §14c L807; HTML S[12].system[10] | Changed document voids envelope; fresh consent + fresh signatures | `buyer-signing.service.ts:398-412` void only inside `recordBuyerSignature`; `:278-305` live envelope re-bound in place (no VOIDED, no history, attempt not incremented); `[dealId]/route.ts:46-54` shows superseded doc as `signable`; `dealer-contract.service.ts:159-187` no envelope write | PARTIAL (corrected from ALREADY CORRECT, map correction 3) | consent captured at sign time against current hash; terminal records immutable — keep | `createContractVersionAndScan` → `voidEnvelopeInternal(dealId,"Contract re-uploaded")` when live envelope; prepare archives + new attempt (or refuses) when bound version differs; clear `viewedAt`; notify buyer | 8 | unit, integration | `buyer-signing.test.ts` extended: upload voids live envelope; prepare on changed version increments attempt | flag activation | none | TO EXTEND |
| C-20 | MD §14c L808; HTML S[12].system[11], fail L823 | 14-day expiry; reissue against still-approved version; buyer reminded | `buyer-signing.service.ts:41,392-395,571-590,257-276`; `app/api/cron/esign-envelope-expiry/route.ts` + `vercel.json:144-145`; `resend/route.ts:22-24`; `nudge.service.ts:117-124`; `admin-queue.service.ts:19-24` | ALREADY CORRECT | CAS on every expiry write; COMPLETED never expired; immutable terminals — keep | None; extend reminder/expiry to co-buyer with C-18; reminder send moves onto dispatcher | 8 | unit, integration | `buyer-signing.test.ts` expiry cases; `app/api/cron/__tests__` esign expiry re-run | flag activation | nudge direct send (allowlist → dispatcher, Phase 10) | ALREADY PRESENT (VERIFY IN PHASE) |
| C-21 | MD §14c L809; HTML S[12].system | Fails closed when evidence storage unavailable; schema applied + verified before activation | `esign-schema-gate.ts:37-47`; `buyer-signing.service.ts:56-73,196,373` → 503 `ESIGN_UNAVAILABLE`; §5.2 VERIFIED schema PRESENT (headers `20261014/15` stale); bucket failure after DB commit still COMPLETES, artifact deferred to reconcile cron `:501-506,748-773,613-665` | PARTIAL | gate applied to reads (28-column projection); narrowed RETURNING — keep | Decide whether "evidence storage" includes artifact bucket; if so verify bucket writability (or persist artifact in the completion step) before COMPLETED; remove stale migration annotations | 8 | unit, integration | `esign-schema-gate.test.ts`; `buyer-signing.test.ts`: bucket write failure → not COMPLETED | `ESIGN_EXECUTED_ARTIFACT_ENABLED` activation (§13-D4); ledger resolve `20261014`, `20261015` (§6.2) | none | TO EXTEND |
| C-22 (schema) | MD §14d L813-815; HTML S[12].dealer[2] | Dealer returns fully executed copy; verified, stored, hashed, evidence, access granted | No dealer-executed fact on Deal/ContractVersion; `DealStatus` lacks DEALER_EXECUTED | MISSING | none | Phase 1: `DealStatus.DEALER_EXECUTED`; `contract_versions.is_dealer_executed/executed_at/executed_document_hash`; `deals.dealer_executed_contract_id` | 1 | integration | `transaction-spine-schema.test.ts`; enum append verified in CI replay | production migration deploy; owner decision (map Q2: countersign vs executed-package upload) | none | TO IMPLEMENT |
| C-22 (execution) | MD §14d L813-815; HTML S[12].dealer[2] | Dealer returns fully executed copy; verified, stored, hashed, evidence, access granted | `executed-contract.service.ts:1-16`, `buyer-signing.service.ts:782-826` buyer-side artifact only; `app/dealer/deals/[dealId]/page.tsx:53-55,189-190` "dealer does not sign"; no countersign/upload route | MISSING | private bucket + hash + null-only guarded artifact write pattern — reuse | Dealer execution step (executed-package upload or countersign on hashed version), correspondence check vs approved version + buyer envelope, store + hash, evidence record, access grants, `DEALER_EXECUTED` transition | 8 | integration, state-machine, playwright | New `lib/services/contract/__tests__/dealer-execution.test.ts`; `deal-state-machine.test.ts` DEALER_EXECUTED edge; Playwright dealer execution step | owner decision (map Q2) | `executed-contract.service.ts` (relabel as buyer-side artifact); `buyer-signing.service.ts:903-906` dealer copy | TO IMPLEMENT |
| C-23 | MD §14d L817; HTML S[12].note, exit | Not contract-executed merely because buyer signed; release blocked until executed copy stored | `buyer-signing.service.ts:504,514-526` buyer signature → SIGNED; `deal.service.ts:26` `SIGNED: ["PICKUP_SCHEDULED"]` open; `pickup.service.ts:49` `force: true`; `:903-906` "Purchase contract executed" | BROKEN | `SIGNING_PENDING` reachable only from CONTRACT_APPROVED — keep | `DEALER_EXECUTED` required predecessor of PICKUP_READINESS/PICKUP_SCHEDULED in `canTransition`; remove `force:true` from `schedulePickup`; correct dealer notification | 8 | state-machine, integration | `deal-state-machine.test.ts` failing-first: SIGNED → PICKUP refused; `app/api/admin/deals/__tests__/pickup-schedule-route.test.ts` | none | `ensureDealSigned`; `pickup.service.ts:49`; `journey/complete/route.ts:218-222` | TO IMPLEMENT |
| C-24 | MD §14d L819; HTML S[12].rec | Recorded: versions with hashes, scans + decisions, envelopes + consent + certificate, executed artifact + hash | `contract-shield.service.ts:184-189` append-only scans; `[reviewId]/route.ts:94-97,155-162,207-210,254-264` changeLog + audit; `schema.prisma:733-757`; `buyer-signing.service.ts:813-820` buyer-side artifact | PARTIAL | append-only scans (except D-4); null-only guarded artifact write — keep | C-13 + C-22 | 8 | integration | Playwright journey DB assertion: version hash, executed hash, changeLog entries present | none | none | TO EXTEND |
| C-25 | MD §14d L821; HTML S[12].sees | "Contract under review" → "Ready to sign" → "Signed — waiting on the dealership's countersignature" | `contract-shield.service.ts:228-235` non-PASS leaves deal at CONTRACT_PENDING (CONTRACT_REVIEW transient only); `acquisition-comms.ts:153-164` "being prepared"; `nudge.service.ts:108-114`; `status-labels.ts:77-81` mislabel; `app/buyer/contracts/[contractId]/page.tsx:56-61` | BROKEN (corrected from PARTIAL, map correction 4) | none | Non-PASS scan parks deal at CONTRACT_REVIEW as resting state; labels/comms per state; add countersignature-wait copy + timeline step after C-22 | 8 | state-machine, unit, visual | `deal-state-machine.test.ts`; `lib/services/notifications/__tests__` comms plan; `app/buyer/contracts/__tests__`; visual snapshot buyer contract page | none | `status-labels.ts:77-78` | TO IMPLEMENT |
| C-26 | MD §14d L823; HTML S[12].fail, staff[2] | Mismatch → correction + rescan; specific discrepancies named to both parties | Admin FLAG/REQUEST_REVISION notify both `[reviewId]/route.ts:150-248`; automated non-PASS: buyer gets `issueCount` only (`contract-shield.service.ts:350-372`, `resend.service.ts:942-947`); dealer gets nothing (`sendDealerContractIssuesEmail` callers `[reviewId]/route.ts:182,221`, `action/route.ts:153` only) | PARTIAL | admin actions already name issues to both — keep | On automated non-PASS enqueue dealer fixList + buyer named items through dispatcher; comparison mismatches name field/expected/found | 8 | unit, integration | `contract-auto-approve.test.ts` extended: outbox rows for both parties carry named items | none | direct `sendContractShieldAlertEmail` send (allowlist → dispatcher) | TO EXTEND |
| C-27 | MD §14d L823; HTML S[12].staff[1] | Overdue upload reminds dealership and escalates to Operations | `nudge.service.ts:100-116` buyer-only 24h nudge; `admin-queue.service.ts:5-40` no contract-overdue queue | MISSING | 24h nudge cooldown per buyer per stage — keep | Cron reminder at 12h/24h keyed on `document_requests.dueAt`; `raiseException(CONTRACT_OVERDUE)` at deadline with owner + deadline | 8 | unit, integration | New `app/api/cron/__tests__/contract-request-reminders.test.ts`; `queue_items` row asserted | none | none | TO IMPLEMENT |
| C-28 | MD §14d L823; HTML S[12].fail | Buyer or co-buyer non-signer reminded; envelope expires; reissuable | Buyer path correct (C-20); co-buyer absent (C-18) | PARTIAL | as C-20 | Extend reminder/expiry/reissue to co-buyer signer rows | 8 | unit | `buyer-signing.test.ts` co-buyer expiry/reissue cases | flag activation | none | TO EXTEND |
| C-29 | MD §14d L823; HTML S[12].fail | Dealership that does not execute is escalated; release stays blocked | No dealer-execution state (C-22); no queue | MISSING | none | `DEALER_EXECUTION_OVERDUE` via `raiseException` with deadline; release gate C-23 | 8 | unit, integration | cron test: overdue execution raises exception; state-machine gate asserted | none | none | TO IMPLEMENT |
| C-30 | MD §Stage 14 Entry L829; HTML S[13].entry | Entry = contract fully executed by both sides | `buyer-signing.service.ts:514-526` SIGNED = buyer only | MISSING | none | `FUNDING_PENDING` entered only from `DEALER_EXECUTED` in `canTransition` | 8 | state-machine | `deal-state-machine.test.ts`: FUNDING_PENDING refused from SIGNED | none | none | TO IMPLEMENT |
| C-31 (schema) | MD §Stage 14 Who L832; HTML S[13].staff[0..1], system[0] | Finance/Ops admin records completion vs evidence and separately confirms clearance; buyer never | `schema.prisma:1711-1716` FinancingStatus {PENDING, SELECTED, APPROVED, DECLINED}; no completion columns | MISSING | none | Phase 1: `FinancingStatus` + COMPLETED, NOT_REQUIRED_CASH (with TERMS_LOCKED etc.); `financing.completed_at/verified_by/verified_at/evidence_document_id/external_reference/expires_at` | 1 | integration | `transaction-spine-schema.test.ts` | production migration deploy | legacy `FinancingStatus` values retained in enum (§13-D18) | TO IMPLEMENT |
| C-31 (routes) | MD §Stage 14 Who L832; HTML S[13].staff[0..1], system[0] | Finance/Ops admin records completion vs evidence and separately confirms clearance; buyer never | No route; `app/api/buyer/financing/route.ts:64-92` buyer edits at any stage; `financing-reviews/[taskId]/resolve/route.ts:13-21` decisions limited, `OPERATIONAL_ROLES` gate | MISSING | `FinancingAuditEvent` hash chain `schema.prisma:5697-5720`; `OPERATIONAL_ROLES` gate — reuse | Admin `POST /api/admin/deals/[dealId]/financing/complete` and `/funding/clear` (Finance/Ops role) writing timestamps, verifier, evidence + audit event; refuse buyer/dealer Financing writes after CONTRACT_APPROVED | 8 | integration, unit | New `app/api/admin/deals/__tests__/financing-complete-route.test.ts`: role refusal, buyer refusal, audit appended | owner decision (map Q5: Finance role) | `app/api/buyer/financing/route.ts:85-92` (neutralised Phase 7, §8.4) | TO IMPLEMENT |
| C-32a–e (schema) | MD §Stage 14 L834-840; HTML S[13].note | Clearance requires unexpired approval, stipulations satisfied, down-payment method by dealer, dealer funding confirmation, trade payoff within good-through | `schema.prisma:2024` `expiryDate` unread; `:5760,5782-5786` stipulations exist; `:2059-2073` no down-payment method; `:2040-2058` no payoff date (`:3772` string only) | MISSING (b PARTIAL) | none | Phase 1: `financing.expires_at`, `trade_in_submissions.payoff_good_through_date/verified_payoff_cents`; gap: wave lacks clearance-checklist store, down-payment method, dealer funding confirmation — add additively to the Phase 1 wave | 1 | integration | `transaction-spine-schema.test.ts` | production migration deploy; owner approval of wave additions | none | TO IMPLEMENT |
| C-32a–e (service) | MD §Stage 14 L834-840; HTML S[13].note | Clearance requires all five items | No clearance logic; `review-queue.service.ts` STIP_REVIEW not consulted by any gate | MISSING | `CreditApplication.stipulations` + STIP_REVIEW human clearance — reuse for (b) | Funding-clearance service evaluates items (evidence, actor, timestamps) → `funding_cleared_at` only when all true; dealer route records down-payment method + funding confirmation | 8 | unit, integration, playwright | New `lib/services/financing/__tests__/funding-clearance.test.ts`: each missing item blocks; Playwright clearance step | owner decision (map Q5) | none | TO IMPLEMENT |
| C-32f (hold write) | MD §Stage 14 L841; HTML S[13].note | No funding hold, dispute, or chargeback on $99 or Premium fee | `app/api/webhooks/stripe/route.ts:769-796` `charge.dispute.created` writes `AdminAuditLog STRIPE_DISPUTE_CREATED` only; `DepositStatus` `schema.prisma:1566-1571` no DISPUTED/HELD | MISSING (evidence corrected, map correction 5) | verified, idempotent Stripe webhook — keep; extend, never duplicate | Existing dispute handler sets hold flag on Deposit/Deal (money-path owner, Phase 3 "dispute/refund → hold") | 3 | integration | `app/api/webhooks/__tests__` dispute case → hold flag persisted, idempotent replay | refund/money-path activation (owner) | none | TO EXTEND |
| C-32f (clearance read) | MD §Stage 14 L841; HTML S[13].note | No funding hold, dispute, or chargeback on $99 or Premium fee | No gate reads any hold | MISSING | none | Clearance item (f) reads the hold flag; hold blocks `funding_cleared_at` | 8 | unit | `funding-clearance.test.ts`: hold present → blocked | none | none | TO IMPLEMENT |
| C-33 | MD §Stage 14 L843; HTML S[13].system[3] | Premium window closes at clearance; unpaid election reverts to Standard | `schema.prisma:47-48,1461` `Buyer.plan`/`planUpgradedAt`; `app/api/buyer/plan/upgrade/route.ts:31-37` no stage guard; `service-fee.service.ts:26,104,182` | MISSING (evidence corrected, map correction 6: election exists, window does not) | race-safe `updateMany` guard + audit on upgrade — keep | Gate upgrade route on deal not funding-cleared; at `funding_cleared_at` revert unpaid PREMIUM→STANDARD with `plan_snapshots` row (Phase 3 model) | 8 | unit, integration | `app/api/buyer/plan/__tests__/upgrade.test.ts` extended: refused after clearance; clearance test asserts revert | none | none | TO EXTEND |
| C-34 (schema) | MD §Stage 14 L845; HTML S[13].rec, tables | `financing.status = COMPLETED/NOT_REQUIRED_CASH`, `financing_completed_at`, `funding_cleared_at`, verifier, evidence on Deal | `schema.prisma:574-620` Deal lacks fields; `:2059-2073` Financing lacks fields | MISSING | none | Phase 1: `deals.financing_terms_locked_at/financing_completed_at/funding_cleared_at`; financing verifier/evidence columns (C-31) | 1 | integration | `transaction-spine-schema.test.ts` | production migration deploy | none | TO IMPLEMENT |
| C-34 (audit) | MD §Stage 14 L845; HTML S[13].rec | Audit entries with source, external ref, amounts, APR, term, payment, expiry, VIN, verifier, time | `FinancingAuditEvent` only lender-decision events `financing-orchestrator.service.ts:73-83` | PARTIAL | hash-chained audit — reuse | New `FinancingAuditEventType` completion/clearance values carrying the listed fields | 8 | unit | `lib/services/financing/__tests__/financing-audit.test.ts` extended | none | none | TO EXTEND |
| C-35 | MD §Stage 14 L847; HTML S[13].sees | "Financing complete — preparing your vehicle for delivery" or outstanding condition + owner | `acquisition-comms.ts:140-215` no FUNDING_PENDING/cleared plan | MISSING | none | Comms plan + buyer status surface for FUNDING_PENDING/cleared via dispatcher; outstanding item + owner read from `queue_items` | 8 | unit, visual | comms plan test in `lib/services/notifications/__tests__`; visual snapshot buyer deal page | none | none | TO IMPLEMENT |
| C-36 | MD §Stage 14 Exit L849 | Exit = financing completed and funding cleared | see C-31/C-32 | MISSING | none | Exit from FUNDING_PENDING only when both timestamps set | 8 | state-machine | `deal-state-machine.test.ts` | none | none | TO IMPLEMENT |
| C-37 | MD §Stage 14 L851; HTML S[13].system[4] | Financing change sends transaction back through recap, contract, Shield, signatures; never stale contract | `app/api/buyer/financing/route.ts:85-92`, `app/api/buyer/deal/financing/route.ts:22-29` upsert at any stage, no envelope void/rescan; no Financing listener | MISSING | `apply/route.ts:51` stage-gate pattern — mirror | On terms write after CONTRACT_APPROVED: void envelope, supersede approved version, new recap version, deal → RECAP_PENDING/CONTRACT_PENDING with reason; block buyer edits after contract request | 8 | unit, integration, state-machine | New `lib/services/financing/__tests__/financing-change-sendback.test.ts`; `deal-state-machine.test.ts` return point | none | buyer financing routes (neutralised Phase 7, §8.4) | TO IMPLEMENT |
| C-38 | MD §Stage 14 L851; HTML S[13].fail | Stale trade payoff requires refreshed quote before clearance | No payoff date (C-32e) | MISSING | none | Clearance item (e) compares `payoff_good_through_date` to now; stale → exception + refreshed-quote request | 8 | unit | `funding-clearance.test.ts` stale payoff case | none | none | TO IMPLEMENT |
| C-39 (schema) | MD §Stage 14 L851; HTML S[13].tables `queue_items` | Unresolved clearance holds Deal; Ops exception with owner and deadline | `schema.prisma:1890-1907` `QueueItemType`/`QueueItemStatus` enums exist with no model; `admin-queue.service.ts:5-40` derived from status + age | MISSING (evidence corrected, map correction 7) | reuse the existing enums — keep | Phase 1: `queue_items` table on existing enums plus `exception_code`, `owner_role`, `deadline_at`, `return_point`, `idempotency_key` | 1 | integration | `transaction-spine-schema.test.ts` | production migration deploy | none | TO IMPLEMENT |
| C-39 (raise) | MD §Stage 14 L851; HTML S[13].tables `queue_items` | Unresolved clearance holds Deal; Ops exception with owner and deadline | No writer (Phase 2 adds `raiseException`) | MISSING | none | Clearance failure → `raiseException(FINANCING_EXCEPTION, refs, owner, deadline, return_point)`; Deal held | 8 | unit, integration | `funding-clearance.test.ts` exception row; `lib/services/operations/__tests__` | none | `admin-queue.service.ts` derived queue (kept as read-only mirror) | TO IMPLEMENT |
| C-40 | MD §Stage 14 L853; HTML S[13].gate | No conditional/spot delivery: release requires cleared; clearance requires completed — structural | `deal.service.ts:26-30,134-138` gates only SIGNED + insurance; `pickup.service.ts:49,100` forces | MISSING | `advanceDealStatus` CAS — keep | `funding_cleared_at` + `DEALER_EXECUTED` preconditions inside `advanceDealStatus` for PICKUP_READINESS/PICKUP_SCHEDULED/COMPLETED (not at call sites); remove `force:true` | 8 | state-machine, concurrency | `deal-state-machine.test.ts` failing-first; concurrency suite: concurrent schedule vs clearance | none | `pickup.service.ts:49` | TO IMPLEMENT |
| C-41 | MD §Stage 15 Entry L861; HTML S[14].entry | Entry = contract requested; insurance requested at the same moment | see C-04 | BROKEN | none | C-04 | 8 | integration | as C-04 | owner decision (map Q1) | as C-04 | TO IMPLEMENT |
| C-42 | MD §Stage 15 Who L864; HTML S[14].buyer[0], note | Buyer uploads proof or binds externally; AutoLenis assists with quotes only, never binds | `upload-proof/route.ts`; `request-quote/route.ts:99-129` QUOTE_REQUESTED + audit; `insurance-requests/respond/route.ts:57-60`; `InsuranceQuote.isMock` `schema.prisma:2104`; no POLICY_SELECTED/POLICY_BOUND writer | ALREADY CORRECT | AutoLenis never binds — keep | None for the boundary; clarify dead POLICY_SELECTED/POLICY_BOUND semantics in C-43 | 8 | unit | `app/api/buyer/insurance/__tests__/request-quote.test.ts` re-run | none | none | PRESERVED STRONGER SAFEGUARD |
| C-43 (schema) | MD §Stage 15 L866; HTML S[14] | Status model EXTERNAL_UPLOADED → UNDER_REVIEW → VERIFIED / POLICY_BOUND / REJECTED / EXPIRED | `schema.prisma:1493-1502` InsuranceStatus lacks UNDER_REVIEW/REJECTED/EXPIRED; `:1706-1710` InsurancePolicyStatus never updated; `:2111-2126` | PARTIAL | none | Phase 1: `InsuranceStatus` + UNDER_REVIEW, REJECTED, EXPIRED (FAILED retained, §13-D7); `insurance_policies.rejection_reason/reviewed_by/reviewed_at` | 1 | integration | `transaction-spine-schema.test.ts` enum values present | production migration deploy; §13-D7 | none | TO IMPLEMENT |
| C-43 (writes) | MD §Stage 15 L866; HTML S[14] | Status model transitions written by the workflow | Upload writes EXTERNAL_UPLOADED only; no UNDER_REVIEW/REJECTED/EXPIRED writers | PARTIAL | none | Upload → UNDER_REVIEW; decision → VERIFIED/REJECTED; cron → EXPIRED; FAILED→REJECTED semantics; update UI maps | 8 | unit, state-machine | New `lib/services/insurance/__tests__/insurance-status.test.ts` transitions | owner decision (map Q4: retire vs repurpose EXTERNAL_UPLOADED) | `app/buyer/insurance/page.tsx:83-86`; `AdminBuyerCommandCenter.tsx:857`; `backfill_insurance_gate.sql:44,64,87` | TO IMPLEMENT |
| C-44 | MD §Stage 15 L868; HTML S[14].system[0] | An upload is not approval (today it satisfies, advances the Deal, passes release) | `upload-proof/route.ts:136-139` EXTERNAL_UPLOADED, `:155` `advanceOnInsuranceSatisfied`; `deal.service.ts:41-45` satisfied set includes EXTERNAL_UPLOADED, `:134-138` COMPLETED gate, `:320-330`; `AdminBuyerCommandCenter.tsx:857`; `app/buyer/insurance/page.tsx:74,180`; `backfill_insurance_gate.sql:44`; `upload-proof-gate.test.ts` pins defect | BROKEN | proof pointer persisted transactionally `upload-proof/route.ts:112-140`; re-upload clears verifier `:119-124`; private bucket — keep | Remove EXTERNAL_UPLOADED from `INSURANCE_SATISFIED`; upload → UNDER_REVIEW + `raiseException` queue item; no deal advance on upload; update D-5 sites; rewrite gate test failing-first | 8 | integration, state-machine | `app/api/buyer/insurance/__tests__/upload-proof-gate.test.ts` rewritten failing-first; `deal-state-machine.test.ts` COMPLETED refused with UNDER_REVIEW | owner decision (map Q4: backfill of EXTERNAL_UPLOADED deals already released) | `advanceOnInsuranceSatisfied`; `prisma/backfill-insurance-gate.ts`; `backfill_insurance_gate.sql` | TO IMPLEMENT |
| C-45 | MD §Stage 15 L868; HTML S[14].staff[0], tables `queue_items` | Review workflow with Operations queue and decision trail | `journey/complete/route.ts:195-202`, `complete-all/route.ts:126-130` write VERIFIED without policy record; no verify/reject route; no `insurance-proofs` signed-URL route (`upload-proof/route.ts:91-92`; `admin/documents/[documentId]/signed-url/route.ts:19` other buckets only); `admin-queue.service.ts:13-18` | MISSING | private `insurance-proofs` bucket — keep | `POST /api/admin/insurance/[policyId]/verify` and `/reject` (Ops role, audited; writes `reviewed_by/at`, provider, policy number, dates, VIN match); signed-URL viewer route; `INSURANCE_REVIEW` queue via `raiseException`; buyer notification | 8 | integration, unit, playwright | New `app/api/admin/insurance/__tests__/verify-reject-route.test.ts`; Playwright insurance review step with DB assertion | none | journey shortcut routes (stop writing VERIFIED) | TO IMPLEMENT |
| C-46 (schema) | MD §Stage 15 L870; HTML S[14].staff[1] | Verification confirms policy active, names buyer (+co-buyer), matches VIN | `schema.prisma:2111-2126` InsurancePolicy has no VIN / named-insured fields | MISSING | none | Phase 1: `insurance_policies.vin`, `covers_co_buyer` (+ named insured) | 1 | integration | `transaction-spine-schema.test.ts` | production migration deploy | none | TO IMPLEMENT |
| C-46 (verify) | MD §Stage 15 L870; HTML S[14].staff[1] | Verification confirms policy active, names buyer (+co-buyer), matches VIN | No verification logic | MISSING | none | Verify checklist inside the C-45 decision: active dates, named insured incl. co-buyer, VIN equals deal VIN | 8 | unit | `verify-reject-route.test.ts`: VIN mismatch → reject required | none | none | TO IMPLEMENT |
| C-47 | MD §Stage 15 L870; HTML S[14].system[1] | Only VERIFIED or POLICY_BOUND permits release | `deal.service.ts:41-45,134-138` EXTERNAL_UPLOADED also permits | BROKEN | none | Satisfied set = {VERIFIED, POLICY_BOUND} with expiry check (C-51), applied in C-44 | 8 | state-machine | `deal-state-machine.test.ts` | none | D-5 sites | TO IMPLEMENT |
| C-48 | MD §Stage 15 L872; HTML S[14].rec | `insurance_policies` with provider, policy number, dates, proof, verifier, verification time | `schema.prisma:2111-2126` fields exist; `upload-proof/route.ts:121-134` writes `proofUrl`, `isExternal`, `status: ACTIVE` only, nulls verifier | PARTIAL | transactional proof pointer — keep | Buyer form captures provider/policy number/effective/expiry at upload; verifier/time written at decision | 8 | integration, visual | `upload-proof-gate.test.ts`: fields persisted; visual snapshot insurance page | none | none | TO EXTEND |
| C-49 | MD §Stage 15 Exit L874; HTML S[14].exit | Exit = verified or policy bound | Exit happens at upload (C-44) | BROKEN | none | C-44 | 8 | state-machine | as C-44 | none | as C-44 | TO IMPLEMENT |
| C-50 | MD §Stage 15 L876; HTML S[14].fail, buyer[1] | Rejection names the specific defect and requests corrected document | No rejection path; FAILED has no writer; re-upload `upload-proof/route.ts:119-124` | MISSING | re-upload supersedes in place — keep | Reject action with `rejection_reason` → REJECTED + buyer notification naming defect; re-upload → UNDER_REVIEW | 8 | integration | `verify-reject-route.test.ts`: reason required, outbox row with defect | none | none | TO IMPLEMENT |
| C-51 | MD §Stage 15 L876; HTML S[14].system[2] | Expiry before pickup blocks release until corrected | `InsurancePolicy.expiryDate` never written or read; `deal.service.ts:41-45,134-138` static set | MISSING | none | Release gate checks `expiryDate > pickup date`; cron flips EXPIRED + notifies; re-verification path | 8 | unit, state-machine | `deal-state-machine.test.ts` expired policy refused; new `app/api/cron/__tests__/insurance-expiry.test.ts` | none | none | TO IMPLEMENT |
| C-52 | MD §Stage 15 L876; HTML S[14].note | Insurance never blocks contract preparation; blocks the vehicle leaving the lot | C-04 gate on CONTRACT_PENDING | BROKEN | none | C-04 + C-40 | 8 | state-machine | as C-04 / C-40 | owner decision (map Q1) | as C-04 | TO IMPLEMENT |
| C-53 | HTML S[12].tables `contract_scans`, `e_sign_envelopes` | Tables present | `schema.prisma:631-655,684-773`; §5.2 VERIFIED present in production | ALREADY CORRECT | none | none (`document_requests` usage → C-03) | 8 | n/a | schema presence (§5.2 production read) | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| C-54 | HTML S[13].tables `financing`, `external_pre_approvals`, `financing_audit_events`, `deals` | Models carry Stage-14 fields | `schema.prisma:2059,2015,5697,574` models exist without Stage-14 fields | PARTIAL | none | C-31/C-34 Phase 1 columns; `external_pre_approvals.deal_id` FK | 1 | integration | `transaction-spine-schema.test.ts` | production migration deploy | none | TO EXTEND |
| C-55 | HTML S[14].tables `insurance_policies`, `queue_items` | Both tables present | `insurance_policies` present; `queue_items` missing (C-39) | MISSING | none | C-39 Phase 1 table | 1 | integration | `transaction-spine-schema.test.ts` | production migration deploy | none | TO IMPLEMENT |
| C-56 | HTML S[14].sees | Requirements + secure submission link, then under review, then cleared | `app/buyer/insurance/page.tsx:180` "under review" while gate already open; no VERIFIED/cleared comms | PARTIAL | none | Buyer copy per state (requested / under review / verified / rejected / expired) via dispatcher; page states | 8 | unit, visual | comms plan test; visual snapshot insurance page | none | none | TO EXTEND |
| C-57 | MD §14b L796; HTML S[12].staff[0] "Review held contracts" | Admin can open the held contract under review | `app/api/admin/contracts/[versionId]/signed-url/route.ts:18` signs bucket `contracts` while uploads live in `dealer-contracts` (`upload-file/route.ts:24`, `admin/deals/[dealId]/contract/upload-file/route.ts:33`, `extract-text.ts:13`, `buyer-signing.service.ts:40`); gated only by `getAdminFromRequest` | BROKEN (new row, map correction 10) | `requirePermissionStrict("deals.esign.void")` on admin upload — mirror | Sign against `dealer-contracts` via shared bucket constant / `getContractViewUrl`; add permission check; route test | 8 | integration | New `app/api/admin/contracts/__tests__/signed-url-route.test.ts`: correct bucket, permission refusal | none | none | TO IMPLEMENT |
| C-58 | HTML S[12].staff[0] "Review held contracts" | Held-contract queue complete and current | `app/admin/contract-shield/page.tsx:72-86` last 50 scans, any non-PASS incl. superseded, no version link; `admin-queue.service.ts:10` CONTRACT_FAIL lists FAIL only (WARNING hold in no queue) | PARTIAL (new row, map correction 11) | none | Queue keyed on deal's current version with undecided non-PASS scan (incl. WARNING) via `raiseException`; admin page reads `queue_items` | 8 | unit, integration | `lib/services/admin/__tests__` queue test: WARNING included, superseded excluded | none | `admin-queue.service.ts` derived list (mirror) | TO IMPLEMENT |
| C-59 | MD §14b L796, §Stage 14 L851 | Never proceeds on a stale contract (signing side) | `buyer-signing.service.ts:377-412,514-526` ignores `Deal.status`; `[reviewId]/route.ts:203-218` REQUEST_REVISION neither supersedes nor voids, no scan-status check; `journey/reopen/route.ts:98-104` forces CONTRACT_PENDING | PARTIAL (new row, map correction 12) | hash/version check at sign — keep | Rollback paths supersede approved version + `voidEnvelopeInternal`; `recordBuyerSignature` requires `Deal.status` in {CONTRACT_APPROVED, SIGNING_PENDING}; REQUEST_REVISION checks scan status | 8 | unit, integration | `buyer-signing.test.ts` extended: sign refused at CONTRACT_PENDING; `approve-binds-to-reviewed-version.test.ts`: revision voids envelope | flag activation | `journey/reopen` shortcut | TO IMPLEMENT |
| D-1 | MD §14b L796 | Single ContractVersion writer | `dealer-contract.service.ts:159-187` canonical vs `lib/services/contract/contract-upload.service.ts:5-13` `uploadContract` (no supersede, no scan, zero callers) | DUPLICATED | canonical create-before-supersede + fail-closed scan — keep | Delete `uploadContract`; fold `validateUpload` (mime/size) into upload routes | 8 | unit | grep-guard test (pattern of `no-docusign-runtime.test.ts`): no `contractVersion.create` outside canonical service | legacy removal (owner-gated, §8.4 pattern) | `contract-upload.service.ts` | TO CONSOLIDATE |
| D-2 | MD §14b L796 | One sanctioned Contract Shield override path | (i) `[reviewId]/route.ts:70-148` version-bound; (ii) `action/route.ts:137-165` CONTRACT_SHIELD_OVERRIDDEN unlinked v999 PASS scan, no version approval, from `AdminDealTabs.tsx:520-521`; (iii) journey `complete/route.ts:205-212,218-222`, `complete-all/route.ts:132-136` force stages; `contract-shield.service.ts:374-379` "only override" claim false | DUPLICATED | (i) version-bound, audited approval — keep | Retire (ii) action + UI button; (iii) require APPROVED version / COMPLETED envelope / DEALER_EXECUTED or remove; instrument `LEGACY_PATH_WRITE` | 8 | integration, unit | `app/api/admin/deals/__tests__` action test: CONTRACT_SHIELD_OVERRIDDEN rejected; `lib/services/admin/__tests__/workflow-stage-seam.test.ts` extended | legacy removal (owner-gated) | `action/route.ts:137-165`; journey complete / complete-all / reopen | TO CONSOLIDATE |
| D-3 | MD §14a (documents) | One `verifyDocument` | `lib/services/documents/document.service.ts:21-26` and `document-verification.service.ts:36-38` byte-identical | DUPLICATED | none | Keep `document.service` body; re-point callers; delete duplicate | 8 | unit | `pnpm typecheck`; `lib/services/documents/__tests__` | none | `document-verification.service.ts` | TO CONSOLIDATE |
| D-4 | MD §14d L819 | Scan decisions append-only | `admin-queue.service.ts:57-59` CONTRACT_FAIL resolve mutates prior scan FAIL→WARNING | DUPLICATED (append-only violated) | `[reviewId]` route appends `changeLog` / new versions — keep | Route queue resolve through the `[reviewId]` decision path; never `contractScan.update(status)` | 8 | unit | new queue-resolve test in `lib/services/admin/__tests__`: resolve leaves scan status untouched | none | `admin-queue.service.ts:57-59` | TO CONSOLIDATE |
| D-5 | MD §Stage 15 L870 | Single insurance-satisfied definition | `deal.service.ts:41-45` (authoritative); `AdminBuyerCommandCenter.tsx:857`; `app/buyer/insurance/page.tsx:74`; `backfill_insurance_gate.sql:44,51,64,87,93`; `prisma/backfill-insurance-gate.ts` | DUPLICATED | authoritative set in `deal.service` — keep | Export one `INSURANCE_SATISFIED` from `lib/domain`; UI imports it; retire backfill scripts | 8 | unit | `lib/domain/__tests__` set test; grep guard for literal restatement | legacy removal (backfill scripts) | backfill scripts | TO CONSOLIDATE |
| U-1 | MD §14c L809 | Buckets `contracts` / `legal-documents` exist and are private | `wave1_private_buckets.sql:25,28` lists `dealer-contracts`, `insurance-proofs` only; `contracts` referenced by `20260919000005…:5` comment; no code creates it | UNVERIFIED | none | Owner verifies buckets in Supabase before flag activation; add to Phase 8 preflight | 8 | n/a | `storage.buckets` read (preview + production) recorded in phase report | owner verification (Supabase storage) | none | UNVERIFIED — no storage access in session |
| U-2 | MD §14c L809 | `ESIGN_EXECUTED_ARTIFACT_ENABLED` state per environment | `.env.example:142` blank; `esign-schema-gate.ts:37-47` strict `"true"` | UNVERIFIED | strict-string gate, default off — keep | Owner sets flag in preview for Phase 8 verification; production activation is an owner decision | 11 | n/a | env inventory line in `ACCEPTANCE-REPORT.md` | `ESIGN_EXECUTED_ARTIFACT_ENABLED` activation (§13-D4) | none | UNVERIFIED — env not readable in session |
| U-3 | MD §14b L794 | Production `ContractScanRule` rows of unevaluated types | Scanner silently ignores APR_VALIDATION / PAYMENT_PACKING / DISCLOSURE_CHECK / FINANCE_MARKUP rows (`contract-shield.service.ts:145,162`) | UNVERIFIED | none | Read-only query before C-08 lands; existing rows go live once branches exist — review thresholds first | 8 | n/a | `contract_scan_rules` count by `rule_type` recorded in phase report | owner read-only DB query | none | UNVERIFIED — the evidence is a run-time fact (cron rows in the last 24 h, live rule rows), not a schema read; read-only production SQL WAS available all session (WORKFLOW 4/5), so the reason is timing, not access. Owner-run at Phase 11 |
| U-4 | MD §14b L796, §14c L808 | Hourly contract-shield and esign crons actually run | `vercel.json:64-65,144-145` schedules; `CronJobLog` not inspected | UNVERIFIED | `withCronRun` monitoring — keep | Inspect `cron_job_logs` for both crons during acceptance | 11 | n/a | `cron_job_logs` rows in last 24h cited in `ACCEPTANCE-REPORT.md` | owner read-only DB query | none | UNVERIFIED — the evidence is a run-time fact (cron rows in the last 24 h, live rule rows), not a schema read; read-only production SQL WAS available all session (WORKFLOW 4/5), so the reason is timing, not access. Owner-run at Phase 11 |

## Owner-gated items

1. **Production deploy of the Phase 1 schema wave** (`20261106000000_transaction_spine_enums` then `20261106000100_transaction_spine_foundation`) — §13-D1 ledger resolve first, then `prisma migrate deploy`; verify schema + ledger after. Gates every "(schema)" row: C-13, C-18, C-22, C-31, C-32a–e, C-34, C-39, C-43, C-46, C-54, C-55. Includes owner approval of the wave additions this table flags (clearance-checklist store, down-payment method, dealer funding confirmation — C-32a–e).
2. **Ledger resolve** `prisma migrate resolve --applied` for `20261014000000_esign_envelope_history`, `20261015000000_esign_consent_and_executed_artifact`, `20261016000000_contract_scan_version_link` (§6.2) and removal of the stale "LOCAL/STAGING ONLY" headers (§13-D33) — C-11, C-16, C-21.
3. **`ESIGN_EXECUTED_ARTIFACT_ENABLED=true`** activation (§13-D4): preview for Phase 8 verification; production only on owner decision, after attorney sign-off of the DRAFT_V1 consent copy — C-14, C-16, C-18, C-19, C-20, C-21, C-28, C-59, U-2.
4. **Owner decisions (map open questions):** Q1 INSURANCE_PENDING retirement + migration of parked deals (C-01, C-04, C-41, C-52); Q2 dealer execution mode — countersign vs executed-package upload (C-22); Q3 co-buyer identity model (C-18); Q4 EXTERNAL_UPLOADED retire/repurpose + backfill of already-released deals, §13-D7 FAILED retained (C-43, C-44); Q5 Finance role for Stage 14 (C-31, C-32a–e); Q7 reject-upload-during-review vs accept-and-refuse-stale (C-12).
5. **Refund / money-path activation** for the Stripe dispute → hold flag (C-32f hold write, Phase 3).
6. **Legacy removal (owner-gated after zero `LEGACY_PATH_WRITE`):** `contract-upload.service.ts` (D-1); admin `CONTRACT_SHIELD_OVERRIDDEN` action + journey complete / complete-all / reopen shortcuts (D-2); insurance backfill scripts (D-5).
7. **Owner verification / read-only queries:** Supabase buckets `contracts`, `legal-documents` (U-1); `contract_scan_rules` by type (U-3); `cron_job_logs` for contract-shield and esign crons (U-4).

## Legacy paths

- `INSURANCE_PENDING → CONTRACT_PENDING` edge + `advanceOnInsuranceSatisfied` (`lib/services/deal/deal.service.ts:20-21,309-333`) + `prisma/backfill-insurance-gate.ts` + `prisma/manual_supabase_sql/backfill_insurance_gate.sql` (C-01, C-04, C-44, D-5).
- `INSURANCE_EXCEPTION` derived queue keyed on INSURANCE_PENDING (`lib/services/admin/admin-queue.service.ts:13-18`); journey stage derivation `lib/services/buyer/journey.ts:83-87` (C-04).
- `app/api/admin/deals/[dealId]/action/route.ts:87-99` manual dealer contract-pending email (C-02); `:137-165` `CONTRACT_SHIELD_OVERRIDDEN` + `components/admin/AdminDealTabs.tsx:520-521` (D-2).
- Admin journey shortcut routes `app/api/admin/buyers/[buyerId]/journey/complete`, `complete-all`, `reopen` — force transitions, direct `insuranceStatus` / `contractShieldStatus` writes (C-23, C-45, C-59, D-2).
- `app/api/dealer/contracts/upload/route.ts` JSON second upload entry point (C-10).
- `lib/services/contract/contract-upload.service.ts` (D-1); `lib/services/contract/contract-comparison.service.ts` (C-06); `lib/services/documents/document-verification.service.ts` duplicate (D-3).
- `admin-queue.service.ts:57-59` CONTRACT_FAIL resolve mutating a prior scan (D-4); `:10` FAIL-only queue (C-58); derived queues kept as read-only mirror of `queue_items` (C-39).
- `ensureDealSigned` buyer-only → SIGNED (`buyer-signing.service.ts:504,514-526`); `lib/services/pickup/pickup.service.ts:49` `force: true`; dealer copy `buyer-signing.service.ts:903-906`; `executed-contract.service.ts` relabelled as buyer-side artifact (C-22, C-23, C-40).
- `lib/domain/status-labels.ts:77-78` CONTRACT_PENDING labelled "Contract Review" (C-25).
- Buyer financing routes `app/api/buyer/financing/route.ts:85-92`, `app/api/buyer/deal/financing/route.ts:22-29` (neutralised Phase 7 per §8.4; C-31, C-37).
- Direct sends `sendContractShieldAlertEmail`, `sendDealerContractIssuesEmail`, nudge sends → dispatcher allowlist, zero in Phase 10 (C-20, C-26).
- Insurance satisfied-set restatements `AdminBuyerCommandCenter.tsx:857`, `app/buyer/insurance/page.tsx:74,83-86` (C-43, D-5).
- Pre-migration `contract_scans` rows with NULL `contract_version_id` (unapprovable by design, C-11); `ESignEnvelope.docusignEnvelopeId` / `documentKey` columns retained for pre-cutover rows; existing `ContractVersion` rows backfilled by re-hash (C-13).
