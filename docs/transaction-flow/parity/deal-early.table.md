# Deal-early (Stages 10–12: reaffirmation · recap · financing checkpoints · `credit_applications` freeze) — FINAL parity table

Source map: `parity/deal-early.md` (HEAD `0cd399f`) including its "Verification corrections (adversarial pass)" section; no `deal-early-gaps-r*.md` files existed. Where a correction changed a row's status or evidence, the corrected version is used (A5/B9/D16 ceiling operand is client-supplied; D-1 extra defects; B3 baseline note; B5 → N1 `IdentityFirewallEntry`; B11 prospect claim token unhashed → new B11b; B14 admin reopen exists but does not return to offers; B19 ranking connection absent; C5 gap widened; C6 Stripe Connect absent confirmed; D8 three gaps; E16/UNVERIFIED #2/#3/#4/#6/#7/#8 resolved; B2 fourth `force` caller; D2/D3 test gap). Paths relative to `frontend/` unless prefixed. Rows: A–E = map sections (split a/b/c where parts land in different phases), D-n = map Duplicates, N = requirements added by the corrections pass, O = out-of-scope findings carried so nothing is dropped. HTML stage index: Stage n → `S[n-1]` (`AutoLenis-Transaction-Flow.html`: S[9] L600-614, S[10] L616-626, S[11] L628-637, FIN_CP L854-857, FIN_PANELS L859-862). Phases follow IMPLEMENTATION-WORKFLOW §8.1/§8.2 (Stages 10–12 → Phase 7; schema → Phase 1; Deal creation at `DEALER_CONFIRMATION` → Phase 6; Contract Shield recap comparison, financing-completion/funding gate and send-back → Phase 8; retention/legacy drops → Phase 10); test levels follow §12.1.

Schema note — the §8.2 Phase 1 wave already lists `DealStatus + DEALER_CONFIRMATION, RECAP_PENDING`, `deals.{vehicle_hold_until, condition_disclosure_acknowledged_at, recap_confirmed_by_*_at, financing_terms_locked_at, financing_completed_at, otd_cents_confirmed, down_payment_cents, vin, dealer_id, rooftop_id, co_buyer_id}`, `dealer_reaffirmations`, `deal_recaps`, `FinancingStatus + 7 values`, `financing.{down_payment_cents … failure_reason}`, `external_pre_approvals.deal_id`, `offers` expansion, `co_buyers`, `trade_in_submissions.deal_id`, `queue_items`, and enforcement object 3 (`prisma/__tests__/credit-applications-frozen.test.ts` + 410 apply handler). **Not enumerated in that list and needed by this area** (add to the wave): `dealer_scorecard_snapshots.reaffirmation_failure_count` (B19a), `FinancingAuditEventType + TERMS_LOCKED, FINANCING_COMPLETED, FINANCING_FAILED, FINANCING_EXPIRED, CASH_CONFIRMED, EVIDENCE_ATTACHED` and `financing_audit_events.{deal_id, financing_id}` (D12a), `financing_audit_events` mirror to `SlaViolation` reaffirmation type (B20 uses existing model), and the `IdentityFirewallEntry` decision (N1a). These are flagged per row below.

| Ref | Document & section | Requirement | Current route / service / model / component / email / cron / queue / admin action | Status | Stronger safeguard to preserve | Exact required change | Phase | Test level | Acceptance evidence | Owner-gated dependency | Legacy path affected | Final disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A1a | MD §2 L83; §12 L737 | Never collect an SSN in the buy transaction — kill the live write path | `POST /api/buyer/financing/apply` validates `ssn` (`app/api/buyer/financing/apply/route.ts:17`), calls `createCreditApplication` (`:81-90`) → `ssnEncrypted: encryptOptionalField(input.ssn)` (`lib/services/financing/credit-application.service.ts:108`); orchestrator decrypts (`financing-orchestrator.service.ts:53`) | BROKEN | Fail-closed without `FINANCING_ENCRYPTION_KEY` (`apply/route.ts:28-30`); AES-256-GCM at rest; PII never in audit payload (`lender-service.ts:108-118`); buyer-safe select (`credit-application.service.ts:64-81`) | Replace the route with a bodyless 410 handler (no `request.json()`, no `ssn` field anywhere); delete `app/api/buyer/financing/__tests__/apply-route.test.ts`; freeze test (A4a) refuses to allowlist the file | 1 | integration | `prisma/__tests__/credit-applications-frozen.test.ts` fails if `apply/route.ts` references `creditApplication`/`ssn`; route test asserts 410 with empty body for an authenticated buyer | none | `app/api/buyer/financing/apply/route.ts` + test; `package.json` `test:financing-routes` glob | TO IMPLEMENT |
| A1b | MD §2 L83; §12 L737 | Same — retire SSN/DOB/income inputs from the service layer and turn 410 into a redirect | `CreateCreditApplicationInput` carries `ssn/dob/employment/income` (`credit-application.service.ts`); `lib/security/field-encryption.ts` consumers are exactly `credit-application.service.ts`, `financing-orchestrator.service.ts`, `apply/route.ts` | BROKEN | Keep the encrypt-at-rest pattern documented in `autolenis-auth-security-privacy` for any future PII field | 410 handler becomes a redirect to `/buyer/deal/financing`; delete `CreateCreditApplicationInput` PII fields with the service (A2); remove `field-encryption.ts` once no consumer remains | 7 | unit, integration | Freeze test allowlist reaches zero; `rg -l field-encryption app lib` → 0 or a documented surviving consumer | none | `lib/security/field-encryption.ts` | TO IMPLEMENT |
| A2 | MD §2 L84; §12 L737 | Never accept, submit or underwrite a lender credit application | Dormant Phase-5 engine: `financing-orchestrator.service.ts:33-180` (`requestLenderDecision`, zero production callers), `lender/lender-service.ts:126-130`, `credit-application.service.ts:17-27`, `apply/route.ts:97` `submitApplication`; admin review queue `app/api/admin/financing-reviews/**` | BROKEN | Mock adapter never silently approves (`mock-lender-adapter.ts:198-202`); fail-closed on missing credentials; `no-inhouse-financing-on-auction-spine.test.ts` | Remove orchestrator, lender adapters, review-queue service/routes/pages from `app/ lib/` (or move to `archive/` outside both); re-point Ops follow-up to `queue_items` `FINANCING_EXCEPTION` | 7 | unit, integration | Freeze test scans `lib/services/financing/**` with zero allowlist entries; `pnpm test:coverage-check` green after `test:financing*` scripts are pruned | none | `lib/services/financing/{financing-orchestrator,review-queue,credit-application}.service.ts`, `lender/*`, `app/api/admin/financing-reviews/**`, `app/admin/financing-reviews/page.tsx`, `app/admin/manual-reviews/page.tsx:6,95-98,201-249`, `components/admin/financing/ResolveReviewControl.tsx`, `lib/admin/nav.ts:236` | TO IMPLEMENT |
| A3 | MD §2 L85; §12 L737 | Never approve, fund, disburse, pull lender credit or issue lender disclosures | No disbursement/credit-pull code. Disclosure slots `ADVERSE_ACTION_NOTICE`, `TILA_DISCLOSURE`, `FCRA_*` in `ComplianceRuleType` (migration `20261004000000…:31`; `compliance-rule.service.ts:62-85`); orchestrator would render ECOA notice (`financing-orchestrator.service.ts:159-173`) | PARTIAL | Fail-closed rule resolution + audited `RULE_ABSENT_FAIL_CLOSED` (pattern to keep); reject route refuses FCRA template (`external-preapprovals/[id]/reject/route.ts:70-76`) | Retire `ADVERSE_ACTION_*`/`TILA_DISCLOSURE` rule evaluation and `NOTICE_GENERATED/NOTICE_SENT` writers from the transaction path; enum values stay (append-only) | 7 | unit | `lib/services/financing/__tests__/compliance-rule.test.ts` trimmed to surviving rule types; `rg "ADVERSE_ACTION" app lib` → only enum/migration | none | `compliance-rule.service.ts`; `FinancingAuditEventType.NOTICE_*` (`schema.prisma:5687-5688`) | TO IMPLEMENT |
| A4a | MD §2 L91; L1509; L1542; FIN_PANELS | `credit_applications` frozen — build-failing guard | Reads/writes reachable today: `apply/route.ts:71-97`, `app/buyer/financing/page.tsx:39-45`, `review-queue.service.ts:92-151` via `admin/financing-reviews/[taskId]/resolve/route.ts:49-55`. Existing guard scans only the auction spine (`lib/services/auction/__tests__/no-inhouse-financing-on-auction-spine.test.ts:37-46`) | BROKEN | RLS enabled, no policies (`20261005…:57`); partial-unique one-active-per-deal (`20261007…:16-18`); existing spine guard | Enforcement object 3: `prisma/__tests__/credit-applications-frozen.test.ts` scanning the §8.2 route/service tree with a phase-tagged allowlist (stale entries fail); share the `FORBIDDEN` token list with E15 rather than duplicating it | 1 | integration | Allowlist-scan test green with today's read-only references listed; fails when `apply/route.ts` is added | none | `lib/services/auction/__tests__/no-inhouse-financing-on-auction-spine.test.ts` (extend, never delete) | TO EXTEND |
| A4b | MD §2 L91; L1509 | Same — remove every transaction-tree reference | E1–E11 below | BROKEN | Prisma model + migrations untouched until retention sign-off | Delete/re-point every allowlisted reference; allowlist shrinks to zero; model relations stay | 7 | integration | Freeze test allowlist empty; `rg creditApplication app lib/services` → 0 outside `prisma/` | retention sign-off before any DROP (§13-D9) | files in E1–E11 | TO IMPLEMENT |
| A5 | MD §12c L764; FIN_PANELS L859-862 | Buyer can never mark financing completed | Buyer alone leaves the stage: `app/api/buyer/financing/route.ts:64-92` (`Financing.status=SELECTED` `:73,:81`, `advanceDealStatus(…"FEE_PENDING",{actorRole:"BUYER"})` `:88`); `app/api/buyer/deal/financing/route.ts:23`. **Corrected:** the "ceiling" compares client `otdAmountCents` (`financing/route.ts:13,37`) and stores it as `approvedAmountCents` (`:71,79`) — never `deal.offer.otdPriceCents` | PARTIAL | Cap read only from `buyer.preQualification.maxOtdAmountCents`, never from client (pattern); 422 `BUDGET_EXCEEDED` | Buyer routes write path/intent only (`NOT_STARTED → IN_PROGRESS`); ceiling operand becomes the Deal/Offer OTD; `TERMS_LOCKED`/`COMPLETED` writes only via admin checkpoint service (D11); `canTransition` refuses to leave `FINANCING_PENDING` without `TERMS_LOCKED`/`NOT_REQUIRED_CASH` | 7 | unit, integration, state-machine | New `lib/services/deal/__tests__/financing-checkpoint.test.ts`: buyer actor cannot set `TERMS_LOCKED`/`COMPLETED`; route tests assert no `advanceDealStatus` call from buyer routes; `deal-state-machine.test.ts` gains financing guard cases | none | `app/api/buyer/financing/route.ts`, `app/api/buyer/deal/financing/route.ts`, `app/buyer/deal/financing/page.tsx`, `app/api/admin/buyers/[buyerId]/journey/complete/route.ts:161-172`, `complete-all/route.ts:108-111` | TO IMPLEMENT |
| B1 | MD §10 L640; HTML S[9] L600-614 | Finding: today selection mints a Deal at financing-pending with no dealer response | `lib/services/deal/select-offer.service.ts:53-55`, `app/api/buyer/requests/[requestId]/offer/respond/route.ts:81-86`, `app/api/admin/deals/route.ts:44-52` all `status: "FINANCING_PENDING"`; seed `scripts/seed-sandbox-deal.ts:222` at `SIGNING_PENDING` | ALREADY CORRECT | Auction-row `FOR UPDATE` + one-ACCEPTED-offer invariant (`select-offer.service.ts:41-62`) | None as a finding; superseded by B2b (creation at `DEALER_CONFIRMATION`) | 6 | state-machine | `deal-state-machine.test.ts` asserts the new entry status after Phase 6 | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| B2a | MD §10 L642; HTML S[9] | `DealStatus` gains `DEALER_CONFIRMATION` (+ `RECAP_PENDING`) | `prisma/schema.prisma:1513-1530` lacks both | MISSING | `TRANSITIONS: Record<DealStatus, DealStatus[]>` (`deal.service.ts:15-34`) forces compile-time table update | Additive `ALTER TYPE "DealStatus" ADD VALUE` in the Phase 1 wave (already listed §8.2) | 1 | integration | CI migration job: enum values present; `pnpm typecheck` forces `TRANSITIONS` + `dealStatusCommsPlan` cases | production migration deploy (§13-D1) | none | TO IMPLEMENT |
| B2b | MD §10 L642; §9 | Selection creates the Deal at `DEALER_CONFIRMATION`; `TRANSITIONS` gains `… → DEALER_CONFIRMATION → RECAP_PENDING → FINANCING_PENDING` | Creation sites `select-offer.service.ts:54`, `offer/respond/route.ts:84`, `admin/deals/route.ts:51`; consumers `lib/services/buyer/journey.ts:88-91`, `lib/domain/status-labels.ts`, `acquisition-comms.ts:106-121` (total switch), `nudge.service.ts:71-79`, `admin-buyer-command-center.service.ts:34-45,86-95`, `components/admin/AdminDealTabs.tsx:31`, `workflow/move/route.ts:18`, `ai/action-intent/catalog.ts:170-175` | MISSING | `advanceDealStatus` CAS seam + `expectedFrom` (`deal.service.ts:104-216`) | Mint at `DEALER_CONFIRMATION` in all three sites; insert the two nodes in `TRANSITIONS`; extend every listed consumer (labels, comms plan, nudges, active-status lists, admin tabs) | 6 | unit, state-machine, concurrency | `deal-state-machine.test.ts` failing-first cases for the new edges; `select-offer-concurrency.test.ts` asserts lineage + entry status; `status-labels.test.ts` exhaustive | none | `moveBuyerWorkflowStage` (`admin-buyer-command-center.service.ts`, `force` pass-through; `journey/complete/route.ts:68-76`) must learn the new nodes | TO EXTEND |
| B2c | MD §10 L642-685 | `DEALER_CONFIRMATION → RECAP_PENDING` guarded by reaffirmation + acknowledgment + no open material change | No guard; `canTransition` has no facts beyond insurance (`deal.service.ts:104-241`) | MISSING | `advanceOnInsuranceSatisfied` narrow-driver pattern (`deal.service.ts:309-338`) | Guard inside `advanceDealStatus` (typed `ReaffirmationRequiredError`) reading `dealer_reaffirmations.status=CONFIRMED`, `deals.condition_disclosure_acknowledged_at`, no `MATERIAL_CHANGE_PENDING`; admin `force` stays audited | 7 | unit, state-machine | `deal-state-machine.test.ts`: transition refused until all three facts; `force` path writes `DealStatusHistory` with reason | none | `app/api/admin/deals/[dealId]/action/route.ts:59-85` `force`; `workflow/move` | TO IMPLEMENT |
| B3a | MD §10 L645-655; HTML S[9] `tables: dealer_reaffirmations` | Persist the dealer's confirmed values + artefacts | No `dealer_reaffirmations` model; `Offer` (`schema.prisma:533-572`) lacks VIN/mileage/photos; baseline VIN/mileage exist on `AuctionVehicle`/`InventoryItem` (`schema.prisma:500-516`) | MISSING | none | `dealer_reaffirmations` table per §8.2 Phase 1 (status enum, confirmed values jsonb, `hold_until`, artefact URLs, `buyer_acknowledged_at`, material-change jsonb, `reminded_12h_at`, `due_at`) | 1 | integration | CI migration job: table + RLS on/no policies; rollback drops only it | production migration deploy (§13-D1) | none | TO IMPLEMENT |
| B3b | MD §10 L645-655; HTML S[9] | Within 24h the winning dealership confirms availability, VIN+mileage, OTD, every fee/incentive/add-on, delivery terms, out-of-state handling, ability, trade subject to inspection, hold-until, condition/history/photos | No dealer confirm route (`rg availab\|confirm.*vehicle app/api/dealer` → unrelated); dealer deal page next-action switch `app/dealer/deals/[dealId]/page.tsx:44-…` | MISSING | `getRequestDealer` + ownership via `offer.dealerId` (`dealer-deals.service.ts`) | New `lib/services/deal/dealer-reaffirmation.service.ts` + `POST /api/dealer/deals/[dealId]/reaffirm` (dealer-scoped, `due_at = createdAt+24h`), comparing confirmed VIN/odometer against `AuctionVehicle` baseline; dealer CTA for `DEALER_CONFIRMATION` | 7 | unit, integration, playwright | New `lib/services/deal/__tests__/dealer-reaffirmation.test.ts` + `app/api/dealer/deals/__tests__/reaffirm-route.test.ts` (wrong dealer → 403, missing field → 422); Phase 7 Playwright: dealer reaffirms | none | `app/dealer/deals/[dealId]/page.tsx` next-action switch | TO IMPLEMENT |
| B4 | MD §10 L657; HTML S[9] | Remind the dealership at 12 hours | No cron (`ls app/api/cron`: `dealer-award-dispatch`, `dealer-followup`, `dealer-inactive`, `dealer-invitation-reminder`, `sla-check`…) | MISSING | `dealer-invitation-reminder` idempotency pattern; `emitDealerAwardOutcomes` dedupe keys (`dealer-award.ts:148`) | Reaffirmation pass in `sla-check` (or new `dealer-reaffirmation-reminder` cron): `DEALER_CONFIRMATION` deals with `due_at-12h < now` and `reminded_12h_at IS NULL` → outbox reminder; `due_at < now` → timeout (B18) | 7 | unit, integration | New `app/api/cron/__tests__/dealer-reaffirmation-reminder.test.ts`: reminder enqueued once (dedup_key), timeout path invoked once | none | `app/api/cron/sla-check/route.ts`; `health.service.ts:522-536` | TO IMPLEMENT |
| B5 | MD §10 L659; HTML S[9]/S[10] | Identity firewall lifts at reaffirmation, not before; dealership then receives buyer + co-buyer contact, trade packet, secure handoff | Lifts at award: `dealer-award.ts:132-148` first name + last initial + `/dealer/deals/{dealId}`; `dealer-deals.service.ts:111-121,154-164` full name/email/phone/city/state/zip; `app/dealer/deals/[dealId]/page.tsx:40` `contactVisible = status !== "PENDING"`; `app/dealer/financing/page.tsx:35,76-77` buyer name/email at any stage; no co-buyer, `TradeInSubmission` has no `dealId` (`schema.prisma:2036-2056`) | BROKEN | Pre-award anonymization (`app/api/dealer/auctions/[auctionId]/route.ts:10-11,78,113`); award payload limited to first name + last initial (`dealer-award.ts:27-29`) | Server-side PII projection in `getDealerDealById` gated on `dealer_reaffirmations.status=CONFIRMED` (or status ≥ `RECAP_PENDING`); release (contact, co-buyer, trade packet) written by the reaffirmation service + ledger (N1b); fix comment `dealer-deals.service.ts:39-43` | 7 | unit, integration, playwright | New `lib/services/dealer/__tests__/dealer-deals-firewall.test.ts`: `DEALER_CONFIRMATION` deal → no PII fields in projection; after confirm → present; Playwright asserts dealer page hides contact pre-reaffirmation | owner decision: does the lift also govern `app/dealer/financing/page.tsx` (open Q6) | `lib/services/notifications/dealer-award.ts`, `dealer-deals.service.ts:111-121`, `app/dealer/deals/[dealId]/page.tsx:40`, `app/dealer/financing/page.tsx` (§8.4 "removed in phase") | TO IMPLEMENT |
| B6a | MD §10 L661; L1500 | `deals.condition_disclosure_acknowledged_at` | `Deal` (`schema.prisma:574-616`) has no such column | MISSING | none | Column in the Phase 1 wave (already listed) | 1 | integration | CI migration job: nullable column present | production migration deploy (§13-D1) | none | TO IMPLEMENT |
| B6b | MD §10 L661 | Buyer acknowledges the condition disclosure once, before recap | No route/component (`rg acknowledg app/buyer` → only `SigningCeremony.tsx`) | MISSING | `getDealForBuyer` ownership scope | `POST /api/buyer/deals/[dealId]/acknowledge-disclosure` (idempotent: second call no-op) writing the timestamp; guard consumed by B2c | 7 | integration, playwright | Route test: non-owner 404, second call idempotent; Phase 7 Playwright: buyer acknowledges | none | `app/buyer/deal/page.tsx:41-48` steps list | TO IMPLEMENT |
| B7 | MD §10a L665 | Buyer approval required for OTD increase, different VIN, new fee/add-on, APR/term/payment change, odometer >500 mi over confirmed, delivery >7 days later, loss of required attribute | No material-change model/classifier; offer revisions are pre-selection only (`schema.prisma:551-552`, `lib/services/offer/offer-revision*`) | MISSING | Offer revision lineage (`originalOfferId @unique`) | Server-side classifier in `dealer-reaffirmation.service.ts` producing a material-change proposal (jsonb on the reaffirmation row per §8.2) with the seven triggers; status `MATERIAL_CHANGE_PENDING` | 7 | unit | `dealer-reaffirmation.test.ts`: table-driven cases — each trigger → pending decision; unchanged → auto-confirm | none | none | TO IMPLEMENT |
| B8 | MD §10a L667 | Lower OTD with everything else unchanged applies automatically in the buyer's favour | none | MISSING | none | Auto-accept branch of the classifier writing `otd_cents_confirmed` + timeline event, audited | 7 | unit | `dealer-reaffirmation.test.ts`: lower OTD only → CONFIRMED without buyer decision; audit row present | none | none | TO IMPLEMENT |
| B9 | MD §10a L667 | Change above the approved ceiling cannot be accepted at all | Ceiling only at buyer financing routes and against a **client-supplied** amount (`financing/route.ts:13,37`; `apply/route.ts:64`); none at selection or dealer-side change | PARTIAL | Cap sourced from `preQualification.maxOtdAmountCents` server-side | Classifier refuses (422, no pending decision) any proposed OTD > `maxOtdAmountCents`, comparing the dealer's confirmed OTD — not a client number | 7 | unit | `dealer-reaffirmation.test.ts`: proposed OTD > cap → refused, no `MATERIAL_CHANGE_PENDING` row | none | none | TO EXTEND |
| B10 | MD §10a L669; HTML S[9] | Material changes shown side by side with a single accept/reject | none | MISSING | none | Buyer component (confirmed vs proposed table) + `POST /api/buyer/deals/[dealId]/material-change/decide` (`ACCEPT`/`REJECT`, idempotent); reject → B18 | 7 | integration, playwright, visual | Route test (owner scope, one decision); Playwright: change → decide; visual baseline for the side-by-side screen | none | none | TO IMPLEMENT |
| B11a | MD §10b L673 (BUILT — sequence) | Outside winner completes rooftop claim, account verification, dealer agreement, business verification before the Deal advances | Parts exist: `account-claim.service.ts:50-107` (hashed single-use 7-day token), `prospect-claim.service.ts:82-140`, `dealer-verification.service.ts:37-113,126-137`; not sequenced — placeholder dealer on Offer (`schema.prisma:554-559`), award email links `/dealer/deals/{dealId}` (`dealer-award.ts:137,242-249`), no `canTransition` check, `DEALER_VERIFICATION_GATE` flag default OFF (`dealer-auction-eligibility.service.ts:20-21,53`) | PARTIAL | `verifyDealerLicense` only path to `verified=true` (audit-logged); `DealerAccountClaimToken` SHA-256 single-use CAS; honest provenance note (`prospect-claim.service.ts:121-128`) | Award dispatch issues a claim token for placeholder winners instead of the dead link; reaffirmation guard requires a claimed `Dealer` bound to the Deal (`deals.dealer_id`) with `getDealerVerificationState` all-true before `→ RECAP_PENDING` | 7 | unit, integration, state-machine | `dealer-reaffirmation.test.ts`: placeholder dealer unverified → transition refused with named reason; award test asserts claim link for placeholder | owner decision: re-point `Offer.dealerId` vs `deals.dealer_id` lineage (open Q4); `DEALER_VERIFICATION_GATE` remains optional at invitation | `dealer-award.ts:125-149` WON branch; `dealer-deals.service.ts` ownership query; `dealer-auction-eligibility.service.ts` | TO EXTEND |
| B11b | MD §10b L673; dealer-marketplace rule 6 | Prospect claim token must be hashed and single-use (outside winner path) | `prospect-claim.service.ts:39` `claimToken: rawToken`; `:60,:89` plaintext `findUnique`; `schema.prisma:4364` `claimToken String? @unique` — raw, reusable until expiry | BROKEN | `account-claim.service.ts` hashing/CAS consume pattern | Store `token_hash`, compare by hash, consume once (reuse `account-claim` helpers); out of area but on the Stage 10b path | 5 | unit | `lib/services/dealer-recruitment/__tests__/prospect-claim.test.ts`: raw token not persisted; second use rejected | none | `prospect-claim.service.ts` | TO EXTEND |
| B12a | MD §10c L677; L1500 | Hold-until timestamp on the Deal | `Deal` (`schema.prisma:574-616`) has no `vehicle_hold_until` | MISSING | none | Column in the Phase 1 wave (already listed) | 1 | integration | CI migration job: column present | production migration deploy (§13-D1) | none | TO IMPLEMENT |
| B12b | MD §10c L677 | Hold-until written from the reaffirmation | none | MISSING | none | Reaffirmation service copies `hold_until` to `deals.vehicle_hold_until` in the same transaction | 7 | unit | `dealer-reaffirmation.test.ts`: confirm → both columns equal | none | none | TO IMPLEMENT |
| B13 | MD §10c L677; §8.4 (`holds` cron) | If the contract has not been requested before hold expiry, ask the dealership to extend or release | No hold logic; contract request driven by insurance gate (`deal.service.ts:309-338`); `app/api/cron/holds` is a no-op | MISSING | none | Repurpose `app/api/cron/holds` (§13-D26): `vehicle_hold_until < now()` and status < `CONTRACT_PENDING` → outbox extend/release prompt + `queue_items` `DEAL_EXCEPTION` (idempotent key per deal+hold) | 7 | unit, integration | New `app/api/cron/__tests__/holds.test.ts`: expired hold → one prompt + one queue item; second run no-op | none | `app/api/cron/holds` (no-op reconciler, repurposed) | TO EXTEND |
| B14 | MD §10c L677 | A released hold returns the buyer to the remaining valid offers | Selection closes the auction, only chosen offer `ACCEPTED` (`select-offer.service.ts:56-60`); `cancelDeal` (`deal.service.ts:377-402`); select-offer route 409 `ALREADY_SELECTED` while any offer ACCEPTED (`select-offer/route.ts:59-65`). **Corrected:** admin `AUCTION_REOPENED` (`admin/auctions/[auctionId]/action/route.ts:208-225`) re-runs the auction, does not cancel the Deal or reset the offer | MISSING | `cancelDeal` single terminal writer with `expectedFrom` pin (commit `4e52391`); one-Deal-per-auction invariant | New `lib/services/deal/return-to-offers.service.ts`: `cancelDeal(reason)` + accepted offer → `WITHDRAWN`/`SUBMITTED` + reopen buyer selection on the closed auction (not a fourth path; may call the reopen action); respects `Deal.offerId @unique` | 7 | unit, integration, concurrency | New `lib/services/deal/__tests__/return-to-offers.test.ts`: after return, select-offer accepts a different offer; concurrent return+select serialized | none | `AUCTION_REOPENED` admin action; `select-offer.service.ts` `ACCEPTED` invariant check | TO IMPLEMENT |
| B15 | MD §10 L681 | Record reaffirmation values, hold-until, disclosure artefacts, acknowledgment, every material change + decision | none | MISSING | `DealStatusHistory` (`schema.prisma:2810-2822`) + `buyerActivityEvent` per transition (`deal.service.ts:167-186`) | Rows on `dealer_reaffirmations` + timeline events through the existing seam (`deal-timeline.service.ts:4` `recordTimelineEvent`) | 7 | unit | `dealer-reaffirmation.test.ts`: each write produces a timeline event with actor | none | none | TO IMPLEMENT |
| B16 | MD §10 L683; HTML S[9] "Buyer sees" | "Dealership confirmed — review your vehicle and condition report" / specific change awaiting decision | Static 7-step list (`app/buyer/deal/page.tsx:41-49`); `acquisition-comms.ts:113-121` sends "Your deal is confirmed" at `FINANCING_PENDING` (premature) | MISSING | `dealStatusCommsPlan` total switch (compile-time) | `DEALER_CONFIRMATION` + `RECAP_PENDING` comms-plan entries enqueued via `comms_outbox`; buyer deal page state per stage; journey derivation (`journey.ts`) | 7 | unit, visual | `acquisition-comms` test for new cases (dedup_key per deal+status); visual baseline of buyer deal page states | none | `acquisition-comms.ts`, `app/buyer/deal/page.tsx`, `lib/services/buyer/journey.ts` | TO EXTEND |
| B17 | MD §10 L685 Exit | Exit = dealership reaffirmed, buyer acknowledged, no unresolved material change | none | MISSING | none | Same guard as B2c (single implementation) | 7 | state-machine | `deal-state-machine.test.ts` covers the exit facts | none | none | TO IMPLEMENT |
| B18 | MD §10 L687 Fail | Rejection/timeout/failed verification/unavailable inventory/unaccepted change → back to remaining offers with reason | none (see B14) | MISSING | none | `return-to-offers.service.ts` with a reason enum recorded on timeline + buyer outbox notice; invoked by reaffirm route (reject), cron (timeout), verification guard, decide route (reject) | 7 | unit, integration | `return-to-offers.test.ts`: each reason → same terminal effect + notice enqueued once | none | none | TO IMPLEMENT |
| B19a | MD §10 L687 (BUILT — connect) | Reaffirmation failure count on the scorecard snapshot | `DealerScorecardSnapshot` (`schema.prisma:1255-1269`) has no reaffirmation metric | MISSING | none | Add `reaffirmation_failure_count`/`reaffirmation_rate` to the Phase 1 wave (not in §8.2 list) | 1 | integration | CI migration job: column present | production migration deploy (§13-D1) | none | TO IMPLEMENT |
| B19b | MD §10 L687 (BUILT — connect) | Failure recorded on the scorecard and affects future invitation ranking | `computeDealerScorecard` (`dealer-scorecard.service.ts:21-45`) computes `dealCompletionRate`; **corrected:** `scoreDealerForAuction` reads only `offerWinRate` + `junkFeeRatio` + `tier` (`dealer-invitation.service.ts:56-66`), so today a failed reaffirmation changes ranking by nothing | PARTIAL | Metrics computed from real rows, no hardcoding | Compute the metric in `computeDealerScorecard`, snapshot it (`dealer-scorecard-snapshot/route.ts:56`), subtract a weight in `scoreDealerForAuction`; surface in `dealer-dashboard.service.ts:137` | 7 | unit | `lib/services/dealer/__tests__/dealer-scorecard.test.ts` + `lib/services/auction/__tests__/dealer-invitation.test.ts`: failure lowers score deterministically | none | none | TO EXTEND |
| B20 | MD §10 L687 | Repeated failures trigger an SLA violation and review | `SlaViolation` model (`schema.prisma:3346-3357`) never written (`rg slaViolation.(create\|upsert)` → none); `checkSLAs` only writes `Notification` (`health.service.ts:519-536`) | MISSING | SLA alert dedupe/throttle (`health.service.ts:303-325`) | Write `SlaViolation{entityType:"DEALER", slaType:"REAFFIRMATION"}` at N failures in window + `queue_items` `DEALER_EXCEPTION`; dealer command-center surface | 7 | unit | `return-to-offers.test.ts` / `sla` test: Nth failure creates one violation; N+1 does not duplicate | owner decision: N and window (policy value) | `lib/services/monitoring/health.service.ts` | TO IMPLEMENT |
| C1 | MD §11 L696 Entry; HTML S[10] L616-626 | Entry = dealership reaffirmed (`RECAP_PENDING`) | Depends on B2/B17 | MISSING | none | `RECAP_PENDING` reached only through the B2c guard | 7 | state-machine | `deal-state-machine.test.ts` | none | none | TO IMPLEMENT |
| C2a | MD §11 L699-717; HTML S[10] `tables: deal_recaps` | Versioned recap store | No `deal_recaps` model | MISSING | none | `deal_recaps` table per §8.2 Phase 1 (version, itemised jsonb, optional products accept/decline, trade/equity/down payment/path/amount financed/est. payment/delivery/plan, both confirmations, `superseded_by`, `dispute_reason`) | 1 | integration | CI migration job: table present; RLS on/no policies | production migration deploy (§13-D1) | none | TO IMPLEMENT |
| C2b | MD §11 L699-717; HTML S[10] | One consolidated recap: parties, rooftop, vehicle, itemised OTD, optional products, trade + payoff good-through, equity, down payment, path, amount financed, est. payment beside OTD, delivery, plan obligation | Scattered inputs only: `Offer{otdPriceCents,vehiclePriceCents,taxCents,feesCents,junkFeeItems,aprRate,termMonths}`, `TradeInSubmission` (buyer-level), `Financing` (no down payment), `Buyer.plan`, `Pickup{location,scheduledAt}`; `app/api/buyer/deal/financing/route.ts:38-52` projects otd/apr/term only | MISSING | `buyerFacingDealerName(deal.offer)` (`app/buyer/deal/page.tsx:39`) | New `lib/services/deal/deal-recap.service.ts` building version 1 from reaffirmation + expanded Offer + `co_buyers` + deal-linked trade-in + `Financing` + `plan_snapshots`; recap is the input Contract Shield reads in Phase 8 | 7 | unit, integration | New `lib/services/deal/__tests__/deal-recap.test.ts`: itemised sum equals `otd_cents_confirmed`; missing line → build refused with named field | none (Offer expansion, co-buyer, trade `deal_id` land in Phases 1/4/6) | none | TO IMPLEMENT |
| C3 | MD §11 L719 | Buyer confirms; dealership confirms | none | MISSING | none | `POST /api/buyer/deals/[dealId]/recap/confirm` + `POST /api/dealer/deals/[dealId]/recap/confirm` writing `buyer_confirmed_at`/`dealer_confirmed_at` on the current version (+ `deals.recap_confirmed_by_*_at`); `RECAP_PENDING → FINANCING_PENDING` only when both set | 7 | integration, state-machine, playwright | Route tests (scope, idempotent, stale version → 409); `deal-state-machine.test.ts` guard; Phase 7 Playwright recap step | none | none | TO IMPLEMENT |
| C4 | MD §11a L723 | Every optional product separately named, priced, accepted/declined | none | MISSING | none | Line items `{name, price_cents, accepted}` on `deal_recaps`; confirm refused while any product undecided | 7 | unit | `deal-recap.test.ts`: undecided product → confirm refused | none | none | TO IMPLEMENT |
| C5 | MD §11a L723 | Optional product may never first appear in the contract; if it does, Contract Shield holds it | Shield keyword patterns only (`contract-shield.service.ts:31-110`); **corrected:** `ADDON_PATTERNS` (`:35-44`) lack GAP/service contract/maintenance/extended warranty/protection package; Shield never reads Offer/Deal amounts (`:288` dealer id only) | PARTIAL | Contract Shield gate `SIGNING_PENDING` only from `CONTRACT_APPROVED` (`deal.service.ts:22-24`); existing packing/disclosure rules | Recap-comparison rule: any priced product in the contract not present-and-accepted on the latest confirmed `deal_recaps` → FAIL (hold); widen `ADDON_PATTERNS` with the spec's product names | 8 | unit | `lib/services/contract-shield/__tests__/contract-shield.test.ts`: GAP present in contract, absent from recap → FAIL; accepted → PASS | none | `contract-shield.service.ts` rule runner; `ContractScanRule` config | TO EXTEND |
| C6 | MD §11b L727 | No funds buyer → dealership before execution; no holding deposits; no card on file | Only rails are $99 deposit + concierge fee to AutoLenis (`schema.prisma:582-586`, `service-fee.service.ts`); **confirmed:** `rg transfer_data\|on_behalf_of\|application_fee_amount\|stripeAccount app lib` → 0 | ALREADY CORRECT | Fee state from verified Stripe PI only; refund pair fields | Executable guard test: no `app/api/buyer/**` or `lib/payments/**` file creates a PaymentIntent with `transfer_data`/`on_behalf_of`/`stripeAccount`; document rule in `autolenis-payments-and-ledger` | 7 | unit | New `lib/payments/__tests__/no-buyer-to-dealer-rail.test.ts` (token scan, non-empty file list) | none | none | PRESERVED STRONGER SAFEGUARD |
| C7 | MD §11 L729 Recorded | Recap version with both confirmations + timestamps; later change → revised version; settled figures never silently rewritten | none | MISSING | `DealStatusHistory` + `financing_audit_events` append-only patterns | `deal_recaps` append-only: new version + `superseded_by`; service never `UPDATE`s amounts; DB trigger blocking amount updates (reuse `20261004…` trigger pattern) | 7 | unit, integration | `deal-recap.test.ts`: amendment → v2, v1 unchanged; DB-backed trigger test in CI migration job | none | none | TO IMPLEMENT |
| C8 | MD §11 L731 Buyer sees | Complete recap, single confirm, plain-language next steps | none | MISSING | none | Buyer recap page (`app/buyer/deal/recap/page.tsx`) applying the HTML S[10] design; dealer recap view | 7 | playwright, visual | Phase 7 Playwright recap confirm; visual baselines (buyer + dealer) | none | none | TO IMPLEMENT |
| C9 | MD §11 L733 Exit | Exit = both parties confirmed | none | MISSING | none | Guard on `RECAP_PENDING → FINANCING_PENDING` (C3) | 7 | state-machine | `deal-state-machine.test.ts` | none | none | TO IMPLEMENT |
| C10 | MD §11 L735 Fail; L1511 | Disputed figure → dealership correction → new version; repeated failure → Ops, Deal frozen at recap | Only `cancelDeal` exits (`deal.service.ts:377-402`) | MISSING | none | `POST …/recap/dispute` (buyer) storing `dispute_reason`, dealer correction creates v(n+1); after N versions raise `queue_items` `DEAL_EXCEPTION` and remain at `RECAP_PENDING` (`frozen_at`/`frozen_reason`) | 7 | unit, integration | `deal-recap.test.ts`: dispute → new version; Nth dispute → one queue item, status unchanged | owner decision: N (policy value) | none | TO IMPLEMENT |
| D1 | MD §12 L739; HTML S[11] L628-637 | All financing happens outside AutoLenis — refer, coordinate, follow up, verify only | In-house engine (A2, dormant); buyer path picker (`app/buyer/deal/financing/page.tsx:14-18`); 24h buyer nudge (`nudge.service.ts:71-79`); external evidence review (D8) | BROKEN | 24h buyer nudge; external evidence review | Retire engine (A2/A4b); keep path selection; Ops follow-up via `queue_items` `FINANCING_EXCEPTION` | 7 | integration | Freeze test allowlist zero; queue writer test for the financing follow-up item | none | see A2 | TO IMPLEMENT |
| D2a | MD §12a L743-747; FIN_CP L854-857 | Checkpoint 1: terms locked before contract request | One stage exited on path selection; `deal.service.ts:15-34` ladder | MISSING | `advanceOnInsuranceSatisfied` narrow-driver pattern (`deal.service.ts:309-338`) | Guard: leaving `FINANCING_PENDING` requires `Financing.status ∈ {TERMS_LOCKED, NOT_REQUIRED_CASH}` (typed `FinancingRequiredError`); `force` audited; **corrected:** `deal-state-machine.test.ts` has zero financing assertions today | 7 | unit, state-machine | `deal-state-machine.test.ts` failing-first: `FINANCING_PENDING → FEE_PENDING` refused at `IN_PROGRESS`, allowed at `TERMS_LOCKED`/`NOT_REQUIRED_CASH` | none | `financing/route.ts:88`, `deal/financing/route.ts:23`, admin `journey/complete*`, `admin/deals/[dealId]/action/route.ts:59-85` | TO IMPLEMENT |
| D2b | MD §12a L743-747; FIN_CP | Checkpoint 2: financing completed after signing, before release | `COMPLETED` gate = insurance only (`deal.service.ts:134-138`) | MISSING | Insurance hard-gate pattern | Guard on `FUNDING_PENDING → PICKUP_READINESS` (and `→ COMPLETED`) requiring `Financing.status ∈ {COMPLETED, NOT_REQUIRED_CASH}` + `funding_cleared_at`; admin `complete` route records against evidence | 8 | state-machine | `deal-state-machine.test.ts`: release refused at `TERMS_LOCKED`; Phase 9 readiness item re-reads the same fact | none | `app/api/dealer/pickup/scan/route.ts:131`; admin complete action | TO IMPLEMENT |
| D3 | MD §12a L749 | Owner's rule: never `COMPLETED` unless financing `COMPLETED`/`NOT_REQUIRED_CASH` and funding cleared | `deal.service.ts:29` `PICKUP_SCHEDULED → COMPLETED` direct; `:134-138` insurance only; dealer scan completes (`pickup/scan/route.ts:131`) | MISSING | `InsuranceRequiredError` pattern | `FinancingRequiredError` + `FundingRequiredError` checked inside the seam for every edge into `COMPLETED`, including `force` (audited) | 8 | state-machine, concurrency | `deal-state-machine.test.ts`; Phase 9 atomic-completion concurrency test asserts the guard under a race | none | dealer scan + admin complete routes now surface a typed error | TO IMPLEMENT |
| D4a | MD §12b L753 (BUILT — EXTEND) | `FinancingStatus` gains `NOT_STARTED, IN_PROGRESS, TERMS_LOCKED, COMPLETED, FAILED, EXPIRED, NOT_REQUIRED_CASH` | Enum `PENDING, SELECTED, APPROVED, DECLINED` (`schema.prisma:1711-1716`; default `:2070`) | MISSING | none | Additive `ALTER TYPE … ADD VALUE` ×7 in the Phase 1 wave; default → `NOT_STARTED`; legacy values kept (cannot drop) | 1 | integration | CI migration job: 11 values present; `pnpm db:check-drift` count does not rise | production migration deploy (§13-D1); legacy values stay (§13-D18) | none | TO IMPLEMENT |
| D4b | MD §12b L753 | Code writes only the new vocabulary; legacy rows mapped or left | Writers `financing/route.ts:73,81`, `financing-orchestrator.service.ts:112,120`, `scripts/seed-sandbox-deal.ts:234`; readers `FinancingPreApprovalClient.tsx:64-73`, `app/dealer/financing/page.tsx:12-18`, `admin-buyer-command-center.service.ts:528` | MISSING | none | Single writer (`financing-checkpoint.service.ts`) refusing `PENDING/SELECTED/APPROVED/DECLINED`; seed updated; backfill only if owner confirms `SELECTED→IN_PROGRESS`, `APPROVED→TERMS_LOCKED` | 7 | unit | `financing-checkpoint.test.ts`: legacy value write → refused; seed script typechecks | owner decision: backfill semantics (open Q1/Q2, §13-D18) | `scripts/seed-sandbox-deal.ts:222-236` | TO IMPLEMENT |
| D5 | MD §12b L753 | Path remains `DEALER`, `EXTERNAL`, `CASH` | `FinancingPath` enum (`schema.prisma:1844-1848`); `Deal.financingPath String?` (`:580`); both buyer routes validate the three strings | ALREADY CORRECT | Server-side path validation on both buyer routes | None required; optional typing of `Deal.financingPath` as the enum is O-2 | 7 | unit | `financing-checkpoint.test.ts` rejects a fourth path | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| D6 | MD §12b L753 | `financing` gains `down_payment_cents, external_reference, evidence_document_id, terms_locked_at, completed_at, verified_by, verified_at, expires_at, failure_reason` | `Financing` (`schema.prisma:2059-2073`) has none; down payment captured but not persisted (`financing/route.ts:14,55`) | MISSING | none | Nullable columns in the Phase 1 wave (already listed); `evidence_document_id` FK → `external_pre_approvals.id` | 1 | integration | CI migration job: columns + FK present | production migration deploy (§13-D1) | none | TO IMPLEMENT |
| D7 | MD §12b (usage map) | Inventory of `FinancingStatus` writers/readers; no exhaustive switch anywhere | Writers/readers as D4b; string-keyed maps; journey gating on `financingPath` truthiness (`journey.ts:24,88-91`, `app/buyer/layout.tsx:244`, `journey-status/route.ts:56`) | ALREADY CORRECT | none | One labeler in `lib/domain/status-labels.ts` for `FinancingStatus`; exhaustiveness test; journey derives stage from `Financing.status` | 7 | unit | `lib/domain/__tests__/status-labels.test.ts` gains `FinancingStatus` exhaustiveness (currently none) | none | `journey.ts:88-91`, `app/buyer/layout.tsx:244`, `journey-status/route.ts:56` | TO CONSOLIDATE |
| D8a | MD §12c L757 (BUILT — EXTEND) | `external_pre_approvals.deal_id` | Model (`schema.prisma:2015-2034`) lacks `deal_id` | PARTIAL | none | Nullable FK + index in the Phase 1 wave (already listed) | 1 | integration | CI migration job: column + index present | production migration deploy (§13-D1) | none | TO EXTEND |
| D8b | MD §12c L757 | Evidence record attached to the financing checkpoint; approval locks terms on the Deal | Submit `app/api/buyer/prequal/external/route.ts:8-45`; status `external-status/route.ts:22-25`; approve upserts `PreQualification` only (`approve/route.ts:69-108`). **Corrected gaps:** no open-submission guard (`external/route.ts:35-45`), `documentUrl` client-supplied path signed verbatim (`external/route.ts:17`; `document/route.ts:19`), account deletion hard-deletes rows (`account/route.ts:65`) | PARTIAL | `finance.preapproval.decide` MONEY tier (`permissions.ts:83`); OFAC attestation + `ComplianceEvent` (`approve:20-28,53-67`); ≥10-char reason; `SUBMITTED`-only guard; private bucket + signed URL; `upload-letter` allowlist/10 MB/private bucket (`upload-letter/route.ts:11-38`) | Submit accepts optional `dealId` (owner-scoped) and refuses a second open submission; `documentUrl` must be an upload-letter path under the buyer's prefix; approve with `dealId` → lock-terms service (D11); soft-delete evidence when a Deal exists | 7 | unit, integration | `app/api/buyer/prequal/__tests__/external-route.test.ts`: duplicate open → 409, foreign path → 422; approve test asserts `Financing` write when `dealId` set | none | `approve/route.ts` (branch on `dealId`), `FinancingPreApprovalClient.tsx`, `external-status/route.ts`, `app/api/buyer/account/route.ts:65` | TO EXTEND |
| D9 | MD §12c L757 | `external_pre_approval_documents` holds the artefacts | `ExternalPreApprovalDocument` (`schema.prisma:3322-3331`) zero references (confirmed); artefacts live in `ExternalPreApproval.documentUrl` (`:2026`) + `VehicleRequestFinancing.preApprovalLetterUrl` (`:1090`) | DUPLICATED | Private bucket `prequal-letters` + signed URL (`document/route.ts:19`) | One store: wire `ExternalPreApprovalDocument` (multi-doc, `verifiedAt`) and copy `documentUrl` into it, or drop the table — owner picks; spec favours the documents table | 7 | unit, integration | Document route test reads via the chosen store; drift check shows no dead table left unreferenced | owner decision: artefact store | `document/route.ts` | OWNER-GATED |
| D10 | MD §12c L757 | `vehicle_request_financing` feeds the checkpoint rather than duplicating it | Written at request creation (`car-request-financing.service.ts:145-178`; `app/api/buyer/requests/route.ts:147-154,220-223`); nothing at Deal stage reads it | PARTIAL | Lead score/badge server-side only; "NEVER shown to dealers" fields | At Stage 12 entry seed `Financing.path` + `down_payment_cents` from `VehicleRequestFinancing` (`FINANCE_OUTSIDE→EXTERNAL`, `CASH→CASH`, else `DEALER`) and pre-create a deal-linked `ExternalPreApproval` from its APPROVED branch | 7 | unit | `financing-checkpoint.test.ts`: request with APPROVED branch → `IN_PROGRESS` + evidence row, buyer not re-asked | owner decision: `LEASE` → `FinancingPath` mapping (UNVERIFIED #9) | `app/buyer/deal/financing/page.tsx` (re-asks path), `prequal/external/route.ts` (re-asks lender) | TO EXTEND |
| D11 | MD §12c L759 | Only an authorised Finance/Ops admin records completion, only against external evidence | Nearest: `resolveReviewTask` (`review-queue.service.ts:116-131`, `OPERATIONAL_ROLES` `resolve/route.ts:34-35`) advances `CreditApplication` and never touches `Financing` (confirmed); `ResolveReviewControl.tsx:10,24` | MISSING | `requirePermissionStrict` + `PERMISSION_ROLES` tiers; reason-required actions; `AdminAuditLog` | `POST /api/admin/deals/[dealId]/financing/{lock-terms,complete,fail}` behind a MONEY-tier permission requiring `evidenceId`; `lib/services/deal/financing-checkpoint.service.ts` writes `Financing` + audit (D12b) | 7 | unit, integration | `app/api/admin/deals/__tests__/financing-route.test.ts`: OPERATIONS role → 403, no evidence → 422, success writes status + audit | owner decision: reuse `finance.preapproval.decide` or new `finance.financing.record` (open Q5) | `app/api/admin/financing-reviews/**` resolve path (retired) | TO IMPLEMENT |
| D12a | MD §12c L759 (BUILT) | Audit trail vocabulary for financing recordings | `FinancingAuditEventType` (`schema.prisma:5683-5722`) is lender-decisioning; rows key on `creditApplicationId` | PARTIAL | Append-only DB triggers, hash chain (`financing-audit.service.ts:92-178`; migration `20261004000000…:58-104`) | Add enum values `TERMS_LOCKED, FINANCING_COMPLETED, FINANCING_FAILED, FINANCING_EXPIRED, CASH_CONFIRMED, EVIDENCE_ATTACHED` + nullable `deal_id`/`financing_id` columns to the Phase 1 wave (not in §8.2 list) | 1 | integration | CI migration job: enum values + columns; trigger still blocks UPDATE/DELETE | production migration deploy (§13-D1) | none | TO EXTEND |
| D12b | MD §12c L759 (BUILT) | Every recording writes source, external reference, amount, down payment, APR, term, payment, expiry, VIN, verifier, time | External approvals audited only in `AdminAuditLog` without APR/term/VIN (`approve/route.ts:121-126`); nothing writes the chain for `Financing` | PARTIAL | Serializable append, `verifyFinancingAuditChain`, non-PII `ComplianceEvent` mirror | `financing-checkpoint.service.ts` appends one chain event per recording with the spec's field list (VIN from `deals.vin`) keyed on `deal_id`/`financing_id` | 7 | unit, integration | `lib/services/financing/__tests__/financing-audit.test.ts` extended: event payload contains all eleven fields; chain verifies | owner decision: chain vs `AdminAuditLog` (open Q3; recommendation: chain) | `financing-audit.service.ts` (extend) | TO EXTEND |
| D13 | MD §12d L763 | Cash sets `NOT_REQUIRED_CASH` here; confirmed received at funding clearance | `CASH` requires `termMonths ≥ 12` + `aprDecimal` and stores `SELECTED` with an amortised payment (`financing/route.ts:12-18,54-83`); PATCH advances immediately (`deal/financing/route.ts:12-28`) | MISSING | none | `path=CASH` → `Financing{status: NOT_REQUIRED_CASH, down_payment_cents}` with no APR/term/monthly; recap shows amount due at handover; receipt confirmed in Phase 8 funding clearance | 7 | unit, playwright | `financing-checkpoint.test.ts`: CASH → no loan fields; Phase 7 Playwright cash branch | none | both buyer financing routes | TO IMPLEMENT |
| D14 | MD §12 L765 Exit | Exit = `TERMS_LOCKED` or `NOT_REQUIRED_CASH`; recap reflects locked terms | none | MISSING | none | Recap revision hook (C7) fired by `financing-checkpoint.service.ts` on lock | 7 | unit | `deal-recap.test.ts`: lock → new version with locked APR/term/payment | none | none | TO IMPLEMENT |
| D15 | MD §12 L767 Fail | Failed/expired → another external path; no auto-cancel; hold re-evaluated; Ops owns follow-up; buyer told plan + by when | No `FAILED/EXPIRED`; `expiryDate` used only at approval (`approve:76`); orchestrator decline never cancels (`financing-orchestrator.service.ts:159-173`); follow-up = credit-app queue + 24h nudge | PARTIAL | Never auto-cancel on decline; review-queue partial-unique idempotency pattern | Cron pass (reuse B13 `holds`/`sla-check`): `expires_at < now` at `TERMS_LOCKED` → `EXPIRED` + `queue_items` `FINANCING_EXCEPTION` + outbox notice with plan/date; admin `fail` with `failure_reason`; hold re-evaluation hook | 7 | unit, integration | Cron test: expiry → one queue item + one notice; `deal-state-machine.test.ts`: Deal status unchanged on FAILED | none | `financing-reviews` queue → `queue_items` | TO EXTEND |
| D16 | HTML S[11] system L633 | Recheck the buyer's approval is current and sufficient | Buyer writes check `prequal.expiresAt` + ceiling against a client amount (`financing/route.ts:31-46`; `apply/route.ts:55-66`); `isPrequalValid` (`lib/services/prequal/prequal.service.ts`); no recheck at lock | PARTIAL | `isPrequalValid`; cap from prequal only | `financing-checkpoint.service.ts` reuses `isPrequalValid` + ceiling against `otd_cents_confirmed` at lock-terms and at `→ FEE_PENDING` | 7 | unit | `financing-checkpoint.test.ts`: expired prequal → lock refused; OTD > cap → refused | none | none | TO EXTEND |
| D17 | HTML S[11] staff L635 | Assist with the external handoff on Premium | Not inspected; `Buyer.plan`; `app/buyer/deal/page.tsx:43` treats fee done when `plan !== "PREMIUM"` | UNVERIFIED | none | Expose a Stage 12 hook (`queue_items` task for Premium deals at `IN_PROGRESS`) the plan-sequence area keys on | 7 | unit | Queue-writer test: Premium deal entering Stage 12 → one concierge task | none | none | UNVERIFIED — plan/upgrade sequence area owns the behaviour (build order #15) |
| E1 | E.1 (`apply/route.ts:71-77`) | `prisma.creditApplication.findFirst` pre-check on a buyer transaction route | READ, reachable via `POST /api/buyer/financing/apply` | BROKEN | none | Removed with the 410 handler (A1a) | 1 | integration | Freeze test: file has zero references | none | `app/api/buyer/financing/apply/route.ts` | TO IMPLEMENT |
| E2 | E.1 (`apply/route.ts:81-90,97`) | `createCreditApplication` + `submitApplication` write (encrypted SSN/income/employment/DOB) | WRITE, reachable | BROKEN | Encryption at rest until removal | Removed with the 410 handler (A1a) | 1 | integration | Freeze test refuses allowlisting the file | none | same | TO IMPLEMENT |
| E3 | E.1 (`app/buyer/financing/page.tsx:39-45`) | Buyer page reads `creditApplication` status | READ (no PII) on a transaction page | BROKEN | none | Drop the read; page redirects to `/buyer/deal/financing` or is deleted | 7 | integration | Freeze test allowlist entry removed; page test asserts redirect | none | `app/buyer/financing/page.tsx` | TO IMPLEMENT |
| E4 | E.1 (`credit-application.service.ts:100,133,147,170,183`) | Service-layer CRUD + CAS | via E2/E7 | BROKEN | CAS `updateMany` pattern | Delete or archive with A2 | 7 | unit | Freeze test; `pnpm test:coverage-check` after test deletion | none | `lib/services/financing/credit-application.service.ts` | TO IMPLEMENT |
| E5 | E.1 (`financing-orchestrator.service.ts:37-176`) | Orchestrator: only writer of `Financing.status=APPROVED` (`:107`) and advances to `FEE_PENDING` (`:142`); zero callers | dormant | BROKEN | Never silent approve/decline | Delete with A2; `APPROVED` never written again | 7 | unit | Freeze test; `rg "APPROVED" lib/services/deal` → only enum label | none | `lib/services/financing/financing-orchestrator.service.ts` | TO IMPLEMENT |
| E6 | E.1 (`review-queue.service.ts:37-117`) | Review-task CRUD keyed on `creditApplicationId`; `advanceApplication` | admin-reachable via `/api/admin/financing-reviews/**` | BROKEN | Partial-unique one-open-per-app-type | Retire; financing follow-ups become `queue_items` `FINANCING_EXCEPTION` | 7 | unit, integration | Freeze test; queue-writer test | none | `lib/services/financing/review-queue.service.ts` | TO IMPLEMENT |
| E7 | E.1 (`admin/financing-reviews/route.ts:10`; `[taskId]/resolve/route.ts:50`) | Admin list/resolve routes | admin | BROKEN | `OPERATIONAL_ROLES` check pattern | Delete routes + `resolve-route.test.ts`; prune `test:financing-routes` | 7 | integration | Freeze test; coverage-check green | none | `app/api/admin/financing-reviews/**` | TO IMPLEMENT |
| E8 | E.1 (`app/admin/financing-reviews/page.tsx:5,29,73`; `app/admin/manual-reviews/page.tsx:6,98,229`) | Admin pages listing review tasks | admin pages | BROKEN | none | Delete page; remove `manual-reviews` financing section (`:201-249`) + `StatCard` (`:178`); drop `lib/admin/nav.ts:236` entry | 7 | visual | Visual baseline of `/admin/manual-reviews` without the section | none | `app/admin/financing-reviews/page.tsx`, `app/admin/manual-reviews/page.tsx`, `lib/admin/nav.ts:236` | TO IMPLEMENT |
| E9 | E.1 (`financing-audit.service.ts:45,74,110,120`) | Chain writes `creditApplicationId` column | keep module | PARTIAL | Hash chain, triggers | Column stays nullable/unused; key on `deal_id`/`financing_id` (D12) | 7 | unit | `financing-audit.test.ts` appends without `creditApplicationId` | none | none | TO EXTEND |
| E10 | E.1 (`lender/lender-service.ts:15,49-106`; `lender/types.ts:153-162`; `mock-lender-adapter.ts`) | Adapters receive decrypted SSN in memory | via E5 only | BROKEN | none | Delete with E5 | 7 | unit | Freeze test; `lender-adapter.test.ts` deleted | none | `lib/services/financing/lender/*` | TO IMPLEMENT |
| E11 | E.1 (`compliance-rule.service.ts:55-57`) | `creditApplicationId` in guard ctx | via E5 | PARTIAL | Fail-closed pattern (A3) | Remove the ctx field; keep the service if any rule type survives | 7 | unit | `compliance-rule.test.ts` compiles without the field | none | `compliance-rule.service.ts` | TO EXTEND |
| E12 | E.1 (`schema.prisma:78,611,5727-5795,5797-5822`) | Prisma relations, enum and model for `credit_applications` / `FinancingReviewTask` | schema | PARTIAL | RLS deny-all; FK CASCADE on review task | Keep frozen (0 rows); relations may stay; physical drop only after retention sign-off | 10 | n/a | Production read-only row count = 0 recorded in `ACCEPTANCE-REPORT.md`; drift check lists the table as retained | retention sign-off + owner-run DROP (§13-D9) | none | OWNER-GATED |
| E13 | E.1 (migrations `20261005000000`, `20261006000000`, `20261007000000`) | DDL, RLS enable, partial-unique | migrations | ALREADY CORRECT | RLS deny-all; partial-unique | No new migration until removal; `rollback.sql` never touches them | 10 | n/a | Migration-chain guard test unchanged | retention sign-off (§13-D9) | none | OWNER-GATED |
| E14a | E.1 (`app/api/buyer/financing/__tests__/apply-route.test.ts`) | Apply-route test | test | BROKEN | none | Delete with the 410 handler; replace with a 410 assertion | 1 | integration | Coverage-check green; 410 test present | none | `package.json:50-51` `test:financing-routes` glob | TO IMPLEMENT |
| E14b | E.1 (`lib/services/financing/__tests__/*.test.ts`; `admin/financing-reviews/__tests__/resolve-route.test.ts`) | Engine/queue tests + `test:financing*` scripts | tests | BROKEN | `test:coverage-check` guard | Delete with the code; remove `test:financing-routes` (glob would match nothing — `tsx --test` unmatched-glob behaviour UNVERIFIED #5) and adjust `test:all` chain (`package.json:57`) | 7 | n/a | `pnpm test:coverage-check` + `test:all` chain parse (§12.2 correction 2) green | none | `package.json` scripts | TO IMPLEMENT |
| E15 | E.1 (`lib/services/auction/__tests__/no-inhouse-financing-on-auction-spine.test.ts:37-46`) | Existing forbidden-token scan over the auction spine | test | PARTIAL | Existing non-empty file-list sanity test (`:59-63`) | Widen `spineFiles()` + `FORBIDDEN` per E.3 (adds `creditApplication`, `credit_applications`, `CreditApplication`, `credit-application.service`, `review-queue.service`, `financing-reviews`, `ssnEncrypted`, `ssn:`, `"ssn"`), or export the list to the Phase 1 freeze test so there is one source of tokens | 1 | integration | Guard fails on a planted `creditApplication` reference under `lib/services/deal/`; reachable from `pnpm test` (`package.json:12`) | none | none | TO EXTEND |
| E16 | E.1 (`scripts/expected_schema.json`) | Whether the schema snapshot pins tables/enums | **Resolved:** it is the output of `scripts/schema_drift_audit.py:97`, not a CI assertion; predates Phase 5 | ALREADY CORRECT | none | None; regenerate after the Phase 1 wave | 1 | n/a | `pnpm db:check-drift` run in the Phase 1 gate | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| E17 | E.1 (`app/api/buyer/account/route.ts:65-67`) | Account deletion leaves encrypted-SSN `credit_applications` rows (FK `ON DELETE RESTRICT`, `20261005…:52-55`) | buyer deletion route | BROKEN | Soft-delete branch when a legal Deal exists (`account/route.ts:71-72`) | Deletion must delete/anonymise `credit_applications` rows for the buyer (0 rows expected) before the retention drop; same fix as O-1 | 10 | integration | `app/api/buyer/account/__tests__/route.test.ts`: deletion with a legacy row succeeds and leaves no `ssnEncrypted` | retention sign-off (§13-D9) | `app/api/buyer/account/route.ts` | OWNER-GATED |
| E-FT | E.3; §8.2 Phase 1 enforcement object 3 | Build-failing freeze test covering the whole transaction route tree | Only the auction-spine guard exists (E15) | MISSING | E15 | `prisma/__tests__/credit-applications-frozen.test.ts` with phase-tagged allowlist; shares tokens with E15; reachable from `test:migrations`/`test:all` | 1 | integration | Planted reference in `app/api/buyer/**` fails the build; stale allowlist entry fails the build | none | none | TO EXTEND |
| D-1 | Duplicates D-1 | Two buyer routes advance the same financing stage | `POST /api/buyer/financing` (writes `Financing` + path + advance; **zero client callers**) and `PATCH /api/buyer/deal/financing` (path + advance; UI caller `app/buyer/deal/financing/page.tsx:45`). **Extra defects:** unguarded `request.json()` (`deal/financing/route.ts:12` → 500 on bad JSON); deal lookup has no status filter (`:16`; `financing/route.ts:48-52`) and the fallback update writes onto a terminal deal (`:25-28`) | DUPLICATED | OTD ceiling pattern; path validation | Collapse into `lib/services/deal/financing-checkpoint.service.ts`; one buyer route writes path/intent only, resolves the **active** deal, validates JSON with zod; delete the POST scenario route | 7 | unit, integration | Route test: invalid JSON → 400; terminal-only deals → 404; no `advanceDealStatus` call; `rg "/api/buyer/financing\"" app components` → 0 | none | both routes; `app/buyer/deal/financing/page.tsx:45` | TO CONSOLIDATE |
| D-2 | Duplicates D-2 | Three stores for the buyer's external pre-approval | `ExternalPreApproval` (`schema.prisma:2015-2034`), `VehicleRequestFinancing` APPROVED branch (`:1088-1094`), `PreQualification{isExternal,rawResponse}` written by approve (`approve/route.ts:77-108`) | DUPLICATED | OFAC attestation on approval | `external_pre_approvals` is the evidence record; `VehicleRequestFinancing` feeds it (D10); `PreQualification` no longer overwritten with lender data once `deal_id` path exists | 7 | unit | Approve test with `dealId`: `PreQualification` untouched, `Financing` written | none | `approve/route.ts:77-108` | TO CONSOLIDATE |
| D-3 | Duplicates D-3 | Two financing-outcome writers | `Financing` vs `CreditApplication` decision snapshot (`schema.prisma:5751-5758`) | DUPLICATED | none | Resolved by retiring `CreditApplication` (A2/E4) | 7 | n/a | Freeze test allowlist zero | none | see A2 | TO CONSOLIDATE |
| D-4 | Duplicates D-4 | Two admin paths advance past financing without evidence | `POST /api/admin/deals/[dealId]/action` (`DEAL_STAGE_ADVANCED`, `force`); `journey/complete` (`case "financing"` sets `EXTERNAL` + advance) / `complete-all`; both via `advanceDealStatus` | DUPLICATED | Both use the seam; `force` audited | Both must call `financing-checkpoint.service.ts` (evidence required); `force` remains audited bypass only | 7 | integration | Admin route tests: advance without evidence → 422 unless `force` (audit row asserted) | none | `admin/deals/[dealId]/action/route.ts:59-85`; `journey/complete*` | TO CONSOLIDATE |
| D-5 | Duplicates D-5 | Three document artefact stores | `ExternalPreApproval.documentUrl`, `VehicleRequestFinancing.preApprovalLetterUrl`, unused `ExternalPreApprovalDocument` | DUPLICATED | Private bucket + signed URLs | Resolved by D9 owner decision | 7 | n/a | see D9 | owner decision: artefact store (D9) | `document/route.ts` | OWNER-GATED |
| D-6 | Duplicates D-6 | Two dealer/buyer status-label heuristics with non-existent values | `app/dealer/financing/page.tsx:12-18` (`FUNDED, ACTIVE, IN_REVIEW…`); `FinancingPreApprovalClient.tsx:64-73` merged key space | DUPLICATED | none | One labeler in `lib/domain/status-labels.ts` (D7) | 7 | unit, visual | `status-labels.test.ts` exhaustive; visual baselines of both pages | none | both components | TO CONSOLIDATE |
| N1a | HTML S[10] `tables: identity_firewall_entries` L610 | A persisted identity-firewall release record per Deal — schema | `IdentityFirewallEntry` (`schema.prisma:2755-2764`, `@@map("identity_firewall_entries")`) is an anti-circumvention flag table with zero code references (`rg identityFirewallEntry app lib` → none) | MISSING | Do not silently repurpose a table with different semantics | Owner picks: extend the model with `deal_id, released_at, released_by, scope` or create a `deal_identity_releases` table and correct the HTML name; add to the Phase 1 wave | 1 | integration | CI migration job: chosen columns/table present | owner decision: reuse vs new table | none | OWNER-GATED |
| N1b | HTML S[10] L610 | Release ledger written at reaffirmation | none | MISSING | none | Reaffirmation service writes one release row per scope (contact, co-buyer, trade packet, handoff) in the same transaction as B5's PII gate | 7 | unit | `dealer-reaffirmation.test.ts`: confirm → release rows; PII projection reads the ledger | depends on N1a decision | none | TO IMPLEMENT |
| N2 | HTML S[10] `tables: dealer_verifications` L610 | Reaffirmation references the dealer's verification record for outside winners | `DealerVerification` + `getDealerVerificationState` (`dealer-verification.service.ts:89-113,126-137`); nothing at Deal level reads it | PARTIAL | `verifyDealerLicense` only path to `verified=true` | Read `getDealerVerificationState` in the B11a guard; persist `dealer_verification_id` reference on the reaffirmation row | 7 | unit | `dealer-reaffirmation.test.ts`: unverified outside winner → refused | none | none | TO EXTEND |
| N3 | HTML S[9] system L607; §23.2a touchpoint 5 | Second and final Premium email fires at Stage 10 and asking stops after it | No premium-upgrade email in `lib/services/{notifications,deal,email}` (`rg -i "premium.*(email\|invite\|upgrade)"` → none) | UNVERIFIED | none | Stage 10 confirm emits a `DEALER_CONFIRMED` event the plan-sequence area's touchpoint-5 producer subscribes to (outbox `template_key`, `dedup_key` per deal) | 7 | unit | Outbox test: one touchpoint-5 row per deal; second confirm → no duplicate | none | none | UNVERIFIED — plan/upgrade sequence area owns the copy and suppression rule |
| N4 | HTML S[11] entry L629 | Stage 12 entered only after both recap confirmations | `FINANCING_PENDING` entered at Deal creation (`select-offer.service.ts:53-55`) | MISSING | none | Covered by the `RECAP_PENDING → FINANCING_PENDING` guard (C3/C9) — listed so the entry condition is explicit | 7 | state-machine | `deal-state-machine.test.ts` | none | none | TO IMPLEMENT |
| N5 | HTML S[11] sees L634 | "Your financing terms are confirmed — the dealership is preparing your contract" | `dealStatusCommsPlan` has only the `FINANCING_PENDING` message (`acquisition-comms.ts:113-121`); nothing fires on a `Financing` write | MISSING | `emitDealStatusComms` idempotent seam | `financing-checkpoint.service.ts` enqueues the outbox template on `TERMS_LOCKED`/`NOT_REQUIRED_CASH` (`dedup_key` deal+status) | 7 | unit | Outbox test: one row per deal per status | none | none | TO IMPLEMENT |
| N6 | FIN_PANELS L862 3rd bullet | A financing change affecting the contract sends the deal back through recap, contract, Shield and signatures — never a stale contract | `Financing` writes have no effect on Deal/`ContractVersion`/`ContractScan`/`ESignEnvelope`; only backward edge `CONTRACT_REVIEW → CONTRACT_PENDING` (`deal.service.ts:23`); `financing/route.ts:64-92` has no contract-state check | MISSING | Contract Shield approval gate | Guard in `financing-checkpoint.service.ts`: change after `CONTRACT_PENDING` requires a recap revision, voids the envelope and resets to `RECAP_PENDING` (or is refused); Phase 8 "send-back on financing change" | 8 | unit, state-machine | `deal-state-machine.test.ts`: financing change at `SIGNING_PENDING` → envelope voided + `RECAP_PENDING`; stale-contract path impossible | none | none | TO IMPLEMENT |
| N7 | HTML S[11] system L633 | Expiry tracked on the financing record | `Financing` has no `expiresAt` (`schema.prisma:2059-2073`); only `ExternalPreApproval.expiryDate` (`:2025`) | MISSING | none | `financing.expires_at` in the Phase 1 wave (D6); evaluated by D15 | 1 | integration | CI migration job: column present | production migration deploy (§13-D1) | none | TO IMPLEMENT |
| N8 | MD §12 L741; §2 L85 | No credit pull in the transaction | MicroBilt/iPredict only under `lib/services/prequal/**`; none under `lib/services/deal\|financing/**` except the dormant mock adapter | ALREADY CORRECT | `no-inhouse-financing-on-auction-spine` guard (extend, never delete) | Add `microbilt`, `ipredict` to the widened FORBIDDEN list (E15/E-FT) | 7 | unit | Guard fails on a planted `microbilt` import under `lib/services/deal/` | none | none | PRESERVED STRONGER SAFEGUARD |
| O-1 | Out-of-scope O-1 (= E17) | Account deletion leaves `credit_applications` rows | `account/route.ts:61-69` deletes `externalPreApproval`, `financingScenario`, never `creditApplication` | BROKEN | Soft-delete branch when a Deal exists | See E17 (privacy/retention; `autolenis-auth-security-privacy`) | 10 | integration | see E17 | retention sign-off (§13-D9) | `app/api/buyer/account/route.ts` | OWNER-GATED |
| O-2 | Out-of-scope O-2 | `Deal.financingPath` is `String?` while `FinancingPath` enum exists | `schema.prisma:580` vs `:1844-1848`; `CreditApplication.financingPath` uses the enum | PARTIAL | Route-level validation of the three strings | Optional: cast column to the enum in the Phase 1 wave (additive migration with `USING`) — not required by spec | 1 | integration | CI migration job if adopted | owner decision: type the column | none | OWNER-GATED |
| O-3 | Out-of-scope O-3; L1514 | `TradeInSubmission` has no `dealId`/`vehicleRequestId` (blocks recap trade lines) | `schema.prisma:2036-2056` | MISSING | none | `trade_in_submissions.{vehicle_request_id, deal_id}` in the Phase 1 wave (already listed); packet capture in Phase 4 | 1 | integration | CI migration job: columns present | production migration deploy (§13-D1) | none | TO IMPLEMENT |
| O-4 | Out-of-scope O-4; L1499 | No co-buyer model (blocks recap/handoff lines) | none | MISSING | none | `co_buyers` table in the Phase 1 wave (already listed); capture in Phase 4 | 1 | integration | CI migration job: table present | production migration deploy (§13-D1) | none | TO IMPLEMENT |
| O-5 | Out-of-scope O-5; L1503 | `Offer` lacks VIN/mileage/photos/expiry/itemisation (Stage 10/11 cannot be data-complete) | `schema.prisma:533-572` | MISSING | none | `offers` expansion in the Phase 1 wave (already listed); dealer-supplied values in Phase 6 | 1 | integration | CI migration job: columns present | production migration deploy (§13-D1) | none | TO IMPLEMENT |
| O-6 | Out-of-scope O-6 | Buyer route computes a monthly payment for `CASH` and requires loan terms | `app/api/buyer/financing/route.ts:12-18` | BROKEN | none | Resolved by D13 + D-1 consolidation | 7 | unit | `financing-checkpoint.test.ts` CASH case | none | `app/api/buyer/financing/route.ts` | TO CONSOLIDATE |
| O-7 | Out-of-scope O-7 (= E16) | `expected_schema.json` omits the Phase-5 tables | Generated output of `scripts/schema_drift_audit.py:97` (resolved) | ALREADY CORRECT | none | Regenerate after the Phase 1 wave | 1 | n/a | `pnpm db:check-drift` in the Phase 1 gate | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| O-8 | Out-of-scope O-8 | Comment asserts buyer identity is safe to expose on any owned deal | `dealer-deals.service.ts:39-43` | BROKEN | none | Correct the comment when B5 lands | 7 | n/a | Code review of the B5 diff | none | `dealer-deals.service.ts:39-43` | TO IMPLEMENT |
| O-9 | Out-of-scope O-9 | AI intent `admin.advance_deal_status` exposed to `ALL_ADMIN`; must not pass `force` past the new financing/reaffirmation gates | `ai/action-intent/catalog.ts:160-175` (human approval required); handler not inspected | UNVERIFIED | Human-approval requirement on the intent | Verify the handler never sets `force`; add a unit test pinning that | 7 | unit | `lib/ai/action-intent/__tests__/catalog.test.ts`: handler call omits `force` | none | none | UNVERIFIED — intent handler body not read |

## Owner-gated items

- **Production migration deploy** of the Phase 1 wave (§13-D1 ledger resolve first, then `prisma migrate deploy`): B2a, B3a, B6a, B12a, B19a, C2a, D4a, D6, D8a, D12a, N1a, N7, O-3, O-4, O-5.
- **Retention sign-off + owner-run DROP** of `credit_applications`, `financing_review_tasks` and their enums (§13-D9): A4b, E12, E13, E17, O-1 — the freeze test is the control until then.
- **Legacy `FinancingStatus` values** stay in the Postgres enum with code refusing to write them; backfill of `SELECTED`/`APPROVED` rows only on owner confirmation of semantics (§13-D18; open Q1/Q2): D4a, D4b.
- **Outside-winner lineage**: re-point `Offer.dealerId` from the placeholder vs carry the claimed dealer on `deals.dealer_id` (open Q4): B11a.
- **Permission tier** for recording `TERMS_LOCKED`/`COMPLETED`: reuse `finance.preapproval.decide` or add `finance.financing.record` (open Q5): D11.
- **Audit trail choice** for financing recordings: `financing_audit_events` chain (recommended) vs `AdminAuditLog` (open Q3): D12b.
- **Identity-firewall scope**: whether the lift also governs `app/dealer/financing/page.tsx` (open Q6): B5.
- **`IdentityFirewallEntry` reuse vs new release-ledger table** and the HTML table-name reconciliation: N1a, N1b.
- **Artefact store**: wire `ExternalPreApprovalDocument` or drop it (D9, D-5).
- **`LEASE` → `FinancingPath` mapping** (UNVERIFIED #9): D10.
- **Policy values**: reaffirmation-failure count/window for the SLA violation (B20); recap dispute count before freezing (C10).
- **Optional** typing of `Deal.financingPath` as the enum (O-2).
- **`DEALER_VERIFICATION_GATE`** feature flag stays optional at invitation; the Phase 7 gate at Deal advancement is mandatory regardless (B11a).
- Legacy-path removals in §8.4 ("removed in phase" for buyer self-advance and award-time firewall lift) need no separate gate; anything else legacy is retired only after zero `LEGACY_PATH_WRITE` for 30 days of production traffic (Phase 10).

## Legacy paths

- `app/api/buyer/financing/apply/route.ts` + `__tests__/apply-route.test.ts` — live SSN intake → 410 in Phase 1, redirect in Phase 7 (A1a/A1b, E1, E2, E14a).
- `app/buyer/financing/page.tsx:39-45` — `creditApplication` read → redirect/delete (E3).
- `lib/services/financing/{financing-orchestrator,review-queue,credit-application}.service.ts`, `lender/*`, `lib/security/field-encryption.ts` — dormant engine → retired (A1b, A2, E4, E5, E6, E10).
- `app/api/admin/financing-reviews/**`, `app/admin/financing-reviews/page.tsx`, `app/admin/manual-reviews/page.tsx:95-249`, `components/admin/financing/ResolveReviewControl.tsx`, `lib/admin/nav.ts:236` — credit-app review queue → `queue_items` financing follow-ups (E7, E8, D11, D15).
- `compliance-rule.service.ts`, `FinancingAuditEventType.NOTICE_*` — lender-disclosure rule types retired from the transaction path; pattern kept (A3, E11).
- `POST /api/buyer/financing` (zero client callers) and `PATCH /api/buyer/deal/financing` — buyer self-advance to `FEE_PENDING` → path/intent write only via `financing-checkpoint.service.ts`; POST route deleted (A5, D-1, D13, O-6; §8.4 "removed in phase").
- `app/api/admin/buyers/[buyerId]/journey/complete*`, `app/api/admin/deals/[dealId]/action` (`force`), `moveBuyerWorkflowStage` (`admin-buyer-command-center.service.ts`), `workflow/move` — admin advance without evidence → checkpoint service; `force` audited (B2b, B2c, D2a, D-4).
- `lib/services/notifications/dealer-award.ts`, `lib/services/deal/dealer-award-dispatch.service.ts` — award with first name + last initial and a dead `/dealer/deals` link for placeholder winners → keep; add claim-token issuance; full-PII release moves to reaffirmation (B5, B11a).
- `lib/services/dealer/dealer-deals.service.ts:39-43,111-121,154-164`, `app/dealer/deals/[dealId]/page.tsx:40`, `app/dealer/financing/page.tsx:35,76-77` — buyer PII at `FINANCING_PENDING` → gated on post-reaffirmation status (B5, O-8; §8.4 "removed in phase").
- `lib/services/dealer-recruitment/prospect-claim.service.ts:39,60,89` — raw, reusable prospect claim token → hashed single-use (B11b, Phase 5).
- `lib/services/dealer/dealer-auction-eligibility.service.ts` (`DEALER_VERIFICATION_GATE` default OFF) — optional invitation gate kept; mandatory Deal-advancement gate added (B11a).
- `app/api/cron/holds` — no-op reconciler → repurposed for vehicle-hold expiry and financing expiry passes (B13, D15; §13-D26).
- `app/api/admin/auctions/[auctionId]/action` `AUCTION_REOPENED` and `select-offer.service.ts` `ACCEPTED` invariant — re-runs the auction; coordinated by `return-to-offers.service.ts`, not a fourth path (B14, B18).
- `lib/services/notifications/acquisition-comms.ts:113-121` — premature "Your deal is confirmed" at `FINANCING_PENDING` → new stage entries through `comms_outbox` (B16, N5).
- `app/buyer/deal/page.tsx:41-49`, `lib/services/buyer/journey.ts:88-91`, `app/buyer/layout.tsx:244`, `app/api/buyer/journey-status/route.ts:56` — stage derived from `financingPath` truthiness → from `Financing.status` (B16, D7).
- `app/buyer/deal/financing/page.tsx`, `app/buyer/deal/financing/pre-approval/**`, `app/api/buyer/prequal/{external,external-status}/route.ts`, `app/api/admin/external-preapprovals/[id]/{approve,reject,document}` — prequal-level evidence → deal-linked evidence + lock-terms branch; status map updated (D8b, D9, D10, D-2, D-5, D-6).
- `app/api/buyer/account/route.ts:65-67` — hard-deletes `externalPreApproval`; never deletes `creditApplication` → soft-delete evidence when a Deal exists; retention fix for credit rows (D8b, E17, O-1).
- `scripts/seed-sandbox-deal.ts:222-236` — seeds `Financing SELECTED` → new vocabulary (D4b).
- `lib/services/auction/__tests__/no-inhouse-financing-on-auction-spine.test.ts` — extend (shared token list with the Phase 1 freeze test), never delete (E15, E-FT, N8).
- `lib/services/contract-shield/contract-shield.service.ts:31-110` — keyword-only add-on rules → recap comparison added in Phase 8 (C5).
- `lib/services/monitoring/health.service.ts:519-536`, `app/api/cron/sla-check` — notification-only SLA checks → reaffirmation reminder/timeout and `SlaViolation` writes (B4, B20).
