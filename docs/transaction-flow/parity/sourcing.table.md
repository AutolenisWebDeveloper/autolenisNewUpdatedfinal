# Parity table — area `sourcing` (Stage 6, Stage 7, §25)

Source map: `parity/sourcing.md` (HEAD 0cd399f) including its adversarial-pass corrections (corrected status wins; no `sourcing-gaps-r*.md` files exist). Phases from `IMPLEMENTATION-WORKFLOW.md` §8.1/§8.2; test levels from §12.1. Paths relative to `frontend/`. HTML arrays are 0-indexed: Stage 6 = `S[5]` (L550–561), Stage 7 = `S[6]` (L563–573), identity panel = `MONEY_PANELS[3]` (L806). Rows split with `a`/`b` land in different phases (schema half → Phase 1; settlement half → Phase 3; consequences → Phase 10).

| Ref | Document & section | Requirement | Current route / service / model / component / email / cron / queue / admin action | Status | Stronger safeguard to preserve | Exact required change | Phase | Test level | Acceptance evidence | Owner-gated dependency | Legacy path affected | Final disposition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| S6-01a | MD §Stage 6 L487; HTML S[5] | Schema: deposit bound to one Vehicle Request; deposit can carry a dispute state | `Deposit` is buyer-level; `DepositStatus` PENDING/PAID/REFUNDED/FAILED, no DISPUTED (`schema.prisma:1566-1571`); `Auction.vehicleRequestId String?` (`schema.prisma:443`) | MISSING | none | Phase 1 wave: `deposits.vehicle_request_id` FK (named in §8.2) plus `deposits.disputed_at` (add to wave; not yet named) | 1 | integration | `pnpm test:migrations` + CI migration job assert both columns; `prisma/__tests__` schema test | production migration deploy (§13-D1) | none | TO IMPLEMENT |
| S6-01b | MD §Stage 6 L487; HTML S[5] | Entry gate: settled, undisputed payment attached to the request; `ACTIVE_SOURCING` means paid sourcing | `lib/services/payment/fulfillment-gate.ts:31-39` any `Deposit{status:PAID}` for buyer; `request-progression.service.ts:120-126` reaches ACTIVE_SOURCING pre-payment; `stripe/route.ts:769-795` dispute → `adminAuditLog` only | PARTIAL | Gate reads server-side deposit status only, never client | `isFulfillmentUnlocked(requestId)` requires PAID ∧ `refundedAt` null ∧ `disputedAt` null ∧ `deposit.vehicleRequestId = request`; ACTIVE_SOURCING set only by settlement (S6-38) | 3 | unit, integration, state-machine | extend `lib/services/payment/__tests__/fulfillment-gate.test.ts` (disputed / refunded / other-request deposit → locked); `request-progression.test.ts` no pre-payment ACTIVE_SOURCING | none | `request-progression.service.ts:118-126` auto-progression; `fulfillment-gate.ts` callers | TO EXTEND |
| S6-02a | MD §Stage 6 L489; HTML S[5] system bullet 1 | Settlement opens a sourcing case; auction is no longer created at settlement | `app/api/webhooks/stripe/route.ts:258-265` "BUG1 FIX: Launch auction and invite dealers"; `deposit-activation.service.ts:184-227` create→launch→invite; no `SourcingCase` symbol anywhere | MISSING | Deposit-activation idempotency guard + bounded convergence (`deposit-activation.service.ts:168-176`); concierge-track fail-closed (`:199-206`) | Settlement txn opens `sourcing_cases` row (S6-28) + seeds checkpoints; webhook/reconciler `create`/`launch`/`invite` branches neutralised behind `LEGACY_PATH_WRITE` adapter | 3 | integration, playwright | new `app/api/webhooks/__tests__/stripe-settlement-sourcing-case.test.ts` (no `auction.create`, case row once, replay-safe); Playwright Phase 3: checkout → webhook → case opened, no auction row | legacy removal after 30 days zero `LEGACY_PATH_WRITE` (§8.4) | `stripe/route.ts:258-265`; `deposit-activation.service.ts` launch/invite branches; `deposit-activation-reconcile` cron | TO IMPLEMENT |
| S6-02b | MD §Stage 6 L489, §Stage 7 L532; HTML S[5]/S[6] | Auction launches only when the invitation field is ready (§7) | `launchAuction` unconditional (`auction.service.ts:46-52`); admin `launch-auction/route.ts:150-172` creates+launches+invites | MISSING | none | Sourcing-case service creates `Auction{PENDING}` only on readiness pass (S7-01b..08) and flips ACTIVE after invitations exist (S7-07) | 5 | unit, state-machine, playwright | new `lib/services/auction/__tests__/sourcing-case.test.ts`; state-machine suite for sourcing statuses; Playwright: paid request → case → readiness → launch with N invitations | none | admin `launch-auction` route | TO IMPLEMENT |
| S6-03 | MD §6a step 0 L493; HTML S[5] system "Resolve…" | Each candidate resolves to holding rooftop (mapped, in radius) + comparable-unit rooftops; union, dedupe by rooftop | Matcher reads registered `Dealer` rows (`dealer-invitation.service.ts:285-288`); `outside-invite.service.ts:223-244` no candidate join; `AuctionVehicle` carries make/model/year only (`dealer-invitation.service.ts:117-148`); `InventoryItem.rooftopId` written (`listing-rooftop-resolution.service.ts:70-75`) never read by sourcing | MISSING | Reuse `resolveRooftop` strong-key resolution (`lib/services/dealer/dealer-rooftop.service.ts:85-125`) | Resolve each `auction_vehicles` candidate → `InventoryItem.rooftopId` + comparable rooftops; union/dedupe; persist per-rooftop `candidate_ids[]` on the case and invitation | 5 | unit | `sourcing-case.test.ts`: two candidates on one rooftop → one rooftop carrying both ids; out-of-radius holding rooftop excluded | none | none (new) | TO IMPLEMENT |
| S6-04 | MD §6a L494; HTML S[5] | Band 1: registered dealerships within 100 miles | `coverage.service.ts:38` `RADIUS_TIERS = [25, 50, 100, 150]`; `dealer-invitation.service.ts:256-261` stop when ≥8 registered; `:313` `pickNearbyDealers` | PARTIAL | Coordless dealer excluded (`dealer-invitation.service.ts:91-109`) | Replace tiers with 100 / 100–150 / 150–250 / >250-authorised; band 1 = registered ≤100 | 5 | unit | extend `lib/services/auction/__tests__/coverage.test.ts` + `dealer-invitation.test.ts` with band assertions | none | `RADIUS_TIERS` consumers `selectCoverageRadius`, `selectCoverageRadiusForZip`, `request-coverage-gate.service.ts` | TO EXTEND |
| S6-05 | MD §6a L495; HTML S[5] | Band 2: outside dealerships within 100 miles | Outside rooftops never auto-invited; admin-only `autoMintOutsideInvitesFromSourcing` at flat `AUTO_MINT_RADIUS_MILES = 150` (`lib/services/auction/outside-invite.service.ts:24`), sole caller `launch-auction/route.ts:253-265`; reconciler counts only `AuctionInvitation` (`deposit-activation.service.ts:131,147,288`) | PARTIAL | Rooftop dedup + `@@unique([auctionId, rooftopId])` (`outside-invite.service.ts:120-132,143`; `schema.prisma:495`); send-safe filter (`:226-228`) | Automatic path mints outside rooftops at band 2 (≤100) after registered, writing `auction_invitations` (`is_registered_dealer=false`); `outside_auction_invites` writes stopped, reads kept | 5 | unit, integration | extend `outside-invite.test.ts`; `sourcing-case.test.ts` band-2 mint; DB assertion no new `outside_auction_invites` row | legacy removal: owner-gated drop of `outside_auction_invites` | `outside_auction_invites` writer; admin `launch-auction`/`invite-outside-dealers` (manual override) | TO EXTEND |
| S6-06 | MD §6a L496; HTML S[5] | Band 3: additional dealerships in the 100–150 band | Ladder reaches 150 registered-only, re-assessing the whole radius (`coverage.service.ts:250-254`, `:126-133`) | PARTIAL | none | Band-by-band expansion over both pools (annulus only, S6-09) | 5 | unit | `coverage.test.ts` annulus case (band-1 rooftop not re-counted at band 3) | none | same as S6-04 | TO EXTEND |
| S6-07 | MD §6a L497; HTML S[5] | Band 4: 150–250 band | Max automatic radius 150 (`coverage.service.ts:38`; `dealer-invitation.service.ts:21` `MAX_DISTANCE_MILES = 150`) | MISSING | none | Add 250 band, entered only after 150 exhausted | 5 | unit | `sourcing-case.test.ts` ladder order 100→150→250 | none | none | TO IMPLEMENT |
| S6-08a | MD §6a L498; HTML S[5] | Schema: buyer-recorded explicit maximum distance | `VehicleRequest` has no radius field (`schema.prisma:1022-1072`); `rg maxDistance` / `rg RADIUS_AUTHORIZ` → none | MISSING | none | Phase 1 wave: `vehicle_requests.authorized_max_radius_miles`, `radius_authorization_requested_at` (named in §8.2) | 1 | integration | `pnpm test:migrations` + CI migration job column assertions | production migration deploy | none | TO IMPLEMENT |
| S6-08b | MD §6a L498, L526; HTML S[5] | Beyond 250 only after the buyer records a maximum; buyer route to record it | No route; no enforcement | MISSING | none | Buyer route `POST /api/buyer/requests/[id]/radius-authorization` (validated, audited, event); ladder refuses >250 without it | 5 | unit, integration, playwright | `sourcing-case.test.ts` (>250 refused unauthorised); route test; Playwright zero-coverage → radius authorisation screen | none (plan resolves owner Q8: field lives on `vehicle_requests`) | none | TO IMPLEMENT |
| S6-09 | MD §6a L500; HTML S[5] | Each expansion searches only the new band and reuses valid candidates | Full rescan per tier (`coverage.service.ts:126-133,163-208` via `:250-253`); invite path re-geocodes all dealers per tick (`dealer-invitation.service.ts:302-312`); no persisted candidate set | BROKEN | Prospect resolution persisted as side effect (`coverage.service.ts:9-12`) | Persist candidate set per band on the sourcing case; expand by annulus; reuse validated candidates | 5 | unit | `sourcing-case.test.ts`: band-2 pass queries only 100–150 annulus and keeps band-1 candidates | none | `selectCoverageRadius` callers | TO IMPLEMENT |
| S6-10 | MD §6a L500; HTML S[5] | Buyer-authorised maximum never exceeded | No authorisation exists; cap is constant 150 (`coverage.service.ts:38`; `dealer-invitation.service.ts:21`) | MISSING | Constants are server-side | Ladder cap = `min(bandRadius, authorized_max_radius_miles)` enforced in service | 5 | unit | `sourcing-case.test.ts` cap case | none | none | TO IMPLEMENT |
| S6-11 | MD §6a L500; HTML S[5] | Radius is server-side policy on the request, never client-controlled | No auction/sourcing route accepts radius; only catalogue `app/api/buyer/search/route.ts:45-47`, `app/api/public/inventory/route.ts:32`; `shortlist-radius.ts:10-15` firewall; admin `launch-auction/route.ts:107-113` and `action/route.ts:95-125` have **no distance check** | PARTIAL | Shortlist↔sourcing import firewall (`lib/services/shortlist/shortlist-radius.ts:10-15`); no client radius param | Policy value on request/sourcing case; distance guard or audited override reason on admin `launch-auction` and `DEALER_INVITED` | 5 | unit, integration | extend `app/api/admin/auctions/__tests__` (out-of-radius `dealerId` refused without override; override audited); firewall test kept | none | admin `launch-auction`; `DEALER_INVITED`; untargeted `dealer-opportunity-notification.service.ts:55-60` broadcast (owner Q7) | TO EXTEND |
| S6-12 | MD §6b L504; HTML S[5] | Validation: real physical rooftop | Registered assumed real (status ACTIVE); rooftop resolution only for prospects (`dealer-rooftop.service.ts:85-125`); invite path needs no `rooftopId` (`dealer-invitation.service.ts:285-288`) | PARTIAL | Host-conflict veto, no auto-merge on weak keys (`dealer-rooftop.service.ts:96-112`) | Readiness requires `rooftopId` (or verified address/placeId) for both pools | 5 | unit | `dealer-rooftop.test.ts` preserved; `sourcing-case.test.ts` rooftop-less dealer not ready | none | none | TO EXTEND |
| S6-13 | MD §6b L504; HTML S[5] | Known location and calculated distance | Invite fail-closed (`dealer-invitation.service.ts:91-109,302-313`); coverage fails open (`coverage.service.ts:111-118,135-141`); `GOOGLE_GEOCODING_API_KEY` read at `geocoding.service.ts:101,124` but undeclared in `env.d.ts`/`.env.example` | PARTIAL | Invite-path fail-closed; tests `dealer-invitation.test.ts:224,266` | Coverage counting fails closed (coordless rooftop not invitation-ready); persist distance (S6-39) | 5 | unit | extend `coverage.test.ts` (coordless not counted); keep `dealer-invitation.test.ts:224` | env: `GOOGLE_GEOCODING_API_KEY` production presence (UNVERIFIED) | `request-coverage-gate.service.ts` | TO EXTEND |
| S6-14 | MD §6b L504; HTML S[5] | Make or inventory fit | Make match is ranking bonus only, "never a gate" (`dealer-invitation.service.ts:22-24,70-76,81-89`); no inventory-fit check | PARTIAL | none (deliberate Y4 bonus semantics superseded by spec) | Fit becomes a validation item (`DealerRooftop.makes`/`preferredMakes` or holds candidate/comparable) with audited admin override | 5 | unit | extend `dealer-invitation.test.ts` (non-fit rooftop not ready; override audited) | none | `makeMatchBonus` + tests | TO EXTEND |
| S6-15 | MD §6b L504; HTML S[5] | Active operating status | Registered: `status:"ACTIVE", isSystemPlaceholder:false` (`dealer-invitation.service.ts:285-296`; `dealer-auction-eligibility.service.ts:52-64,122-156`). Outside pool has **no** status check (`outside-invite.service.ts:223-244`; `DealerRooftop` no status column `schema.prisma:4007-4035`; `invite-outside-dealers/route.ts:53-63`); admin bulk lacks placeholder filter (`launch-auction/route.ts:107-113`) | PARTIAL | Concierge-converted exclusion (`:99-106`); placeholder exclusion; `DEALER_VERIFICATION_GATE` flag default OFF (`feature-flags.service.ts:10-13`) | Add rooftop operating-status signal (column in Phase 1 wave) validated for outside pool; placeholder filter on admin path; decide verification gate | 5 | unit, integration | `sourcing-case.test.ts` closed rooftop not ready; admin route test placeholder refused | env/feature flag decision: `DEALER_VERIFICATION_GATE` ON? | admin `launch-auction`, `invite-outside-dealers` | TO EXTEND |
| S6-16 | MD §6b L504; HTML S[5] | No duplicate or suppression | Registered unique `schema.prisma:529`; outside dedup `outside-invite.service.ts:120-144`; `contact-resolution.service.ts:92-100`. CONFIRMED: `sendIdempotent` (`resend.service.ts:129-200`) consults only `EmailSendLog`, no suppression — all invitation/reminder sends (`dealer-invitation.service.ts:390`, `launch-auction/route.ts:281,298`, `action/route.ts:140`, `invite-outside-dealers/route.ts:70`) bypass suppression; only `lib/qstash/notify.ts:143-148` checks it | PARTIAL | `SEND_SAFE_STATUSES` invariant "contactable == send-safe" (`contact-resolution.service.ts:31-34`) | Suppression/opt-out check as §6b validation item for both pools; invitation sends move to the Phase 2 dispatcher (suppression-aware) | 5 | unit, integration | new `dealer-invitation.test.ts` case (suppressed email → not ready); extend `lib/services/email/__tests__/suppression-tier.test.ts`; `no-direct-transactional-send` allowlist shrinks | none | direct `sendIdempotent` invitation sends (allowlist Phase 2 → zero Phase 10) | TO EXTEND |
| S6-17 | MD §6b L504; HTML S[5] | Deliverable contact | Outside: MX + not suppressed (`contact-resolution.service.ts:1-16`); registered `dealer.user.email` assumed; dealer with no email skipped but counted invited (`dealer-invitation.service.ts:389,414`) | PARTIAL | none | Readiness requires a deliverable channel on both pools; no-email dealer not counted | 5 | unit | `sourcing-case.test.ts` no-email dealer excluded from ready count | none | none | TO EXTEND |
| S6-18 | MD §6b L504; HTML S[5] | Relevant sales / Internet Sales / BDC / management role | Role-derived inboxes (`contact-resolution.service.ts:42-48`); `DealerContactProfile.title` stored not validated (`schema.prisma:4183-4195`); registered dealers have no role concept | PARTIAL | none | Role-fit check on chosen contact profile during validation (title allowlist or role-derived inbox) | 5 | unit | `contact-resolution.test.ts` extension (non-role title → not ready) | none | none | TO EXTEND |
| S6-19 | MD §6b L506; HTML S[5] | Stored contacts are used first | Tier 1 reuses VERIFIED/ROLE_DERIVED (`contact-resolution.service.ts:10-16,50-58,72-76`; rooftop twin tier 0 `:167-203`); backfill Phase 0 (`dealer-contact-backfill.service.ts:13-19`) | ALREADY CORRECT | `CONTACT_STALE_MONTHS = 6` re-verification | none — keep tier order when wiring S6-02b | 5 | unit | existing `contact-resolution.test.ts` tier-1 cases stay green | none | none | PRESERVED STRONGER SAFEGUARD |
| S6-20 | MD §6b L506; HTML S[5] | Paid enrichment only after payment and only when stored + public paths fail | Live: Apollo only if `allowPaid && rooftopId` after tiers 1–3 (`contact-resolution.service.ts:77-80,324-330`), no caller sets `allowPaid`; `apollo.service.ts:222-224`; ledger `apollo-credit-ledger.service.ts:54-69`. Backfill cron Phase 1 reveals with **no payment linkage** (`dealer-contact-backfill.service.ts:231,337`; `vercel.json:16-17`), gated only by `APOLLO_REVEAL_ENABLED` | PARTIAL | Ledger reserve/release; `ApolloReveal @@unique([rooftopId, cycleKey])`; `apolloPersonId @unique`; opt-in `allowPaid` | Pass `allowPaid:true` only from the post-settlement case for in-band rooftops after stored/public fail; gate backfill reveals on an open paid request (or retire Phase 1 of backfill); record spend on case | 5 | unit | extend `contact-resolution.test.ts` (no paid tier pre-settlement) and `dealer-contact-backfill.test.ts` (no reveal without paid request) | env flag `APOLLO_REVEAL_ENABLED` activation; owner Q1 (pre-payment LLM spend acceptable?) | `dealer-contact-backfill` cron | TO EXTEND |
| S6-21 | MD §6b L506; HTML S[5] | Rooftop contact profile with better addressability joined before spending | `getRooftopContacts` twin lookup (`contact-resolution.service.ts:27,72-76`); never-downgrade rule (`schema.prisma:4200-4203`); VERIFIED preferred (`outside-invite.service.ts:192,267-269`) | ALREADY CORRECT | No-downgrade merge rule | none | 5 | unit | existing `contact-resolution.test.ts` + `outside-invite.test.ts` stay green | none | none | PRESERVED STRONGER SAFEGUARD |
| S6-22 | MD §6c L512; HTML S[5] | 5–8 invitation-ready rooftops → launch automatically | Any count 1..8 launches (`dealer-invitation.service.ts:324-327,351-360`); `MIN_COVERAGE_DEALERS = 3` soft-hold never blocks (`coverage.service.ts:33`; `request-coverage-gate.service.ts:7-11`) | MISSING | none | §6c decision table on the sourcing case (`ready_count` → AUTO_LAUNCH / LIMITED_PENDING_APPROVAL / EXPAND_OR_MANUAL / ZERO_COVERAGE_REVIEW) | 5 | unit, state-machine | `sourcing-case.test.ts` table-driven 0 / 1–2 / 3–4 / 5–8 / >8 | owner Q3: keep `MIN_COVERAGE_DEALERS` as pre-payment thin-coverage signal? | `MIN_COVERAGE_DEALERS` consumers | TO IMPLEMENT |
| S6-23 | MD §6c L513; HTML S[5] | More than 8 → rank and invite the best eight | Scoring + slice 8 (`dealer-invitation.service.ts:16,42-79,323-327`); `findMany` no `orderBy` (`:285-288`) → non-deterministic ties; admin bulk no `.max(8)` (`launch-auction/route.ts:45,165-172`); `DEALER_INVITED` no cap, no load increment (`action/route.ts:113-117`); `releaseAuctionLoad` (`:432-444`) drives load negative → bonus at `:60`; `DEALER_REMOVED` no decrement (`:89-92`) | PARTIAL | Capacity cut at load ≥5 (`dealer-invitation.service.ts:61`; `coverage.service.ts:27,130`) | Deterministic tie-break (score, then id); unified ranking across both pools; cap 8 on admin bulk or audited override; symmetric load increment/decrement | 5 | unit | extend `dealer-invitation.test.ts` (tie determinism) and `auction-capacity.test.ts` (load never negative; DEALER_INVITED/REMOVED symmetric) | none | admin `launch-auction`; `DEALER_INVITED`/`DEALER_REMOVED` | TO EXTEND |
| S6-24 | MD §6c L514, L518; HTML S[5] staff bullet 1 | 3–4 → limited auction only with audited Operations approval | No limited-auction concept; 3–4 launches. Admin route has `reason` + audit `AUCTION_LAUNCHED_BY_ADMIN` (`launch-auction/route.ts:44-56,322-346`) | MISSING | Admin route requires `reason` + SUPER_ADMIN/OPERATIONS_ADMIN (`:64-66`) | Admin action `LIMITED_AUCTION_APPROVED` (reason, audit, event) gating launch when ready count 3–4 | 5 | integration, state-machine | `app/api/admin/auctions/__tests__` new test: 3–4 ready → launch refused without approval; approval writes audit + transitions case | none | none | TO IMPLEMENT |
| S6-25 | MD §6c L515; HTML S[5] | 1–2 → continue expansion, source manually, or close after review | 1–2 launches automatically (`dealer-invitation.service.ts:332,351`) | MISSING | none | Case holds; `raiseException(SOURCING_EXCEPTION)` to Ops queue with expand/manual/close actions | 5 | unit, state-machine | `sourcing-case.test.ts` 1–2 → hold + one queue item (idempotent) | none | none | TO IMPLEMENT |
| S6-26a | MD §6c L516; HTML S[5] staff bullet 3 | Retire the timed 120-min auto-close on zero invitations | `classifyActivation` `'invite'`→`'close'` at 120 min (`deposit-activation-policy.ts:35-40`; `deposit-activation.service.ts:56,150,234-243`); test pins 120 (`__tests__/deposit-activation.test.ts:23`); Ops alerted after close (`:91-97`) | BROKEN | Cause-keyed `AUCTION_ZERO_INVITATIONS` audit (`dealer-invitation.service.ts:150-212`); Ops alert; no auto-refund (`deposit-activation-policy.ts:22-25`) | Reconciler `close`-on-zero branch retired behind monitored adapter; keep audit + exception; no status write | 3 | unit | rewrite `deposit-activation.test.ts` (zero invitations → no CLOSED write, `LEGACY_PATH_WRITE` row, exception raised) | legacy removal after 30 days zero `LEGACY_PATH_WRITE` | `deposit-activation-reconcile` cron; `NO_DEALER_CLOSE_GRACE_MINUTES` | TO IMPLEMENT |
| S6-26b | MD §6c L516; HTML S[5] staff bullet 3 | 0 → close as no coverage only after Operations review | Auto-close without review (S6-26a); outside-only auctions counted as zero because reconciler counts only `AuctionInvitation` (`deposit-activation.service.ts:131,147,288`) while `live-status/route.ts:34` counts both pools | BROKEN | Same as S6-26a | Zero ready → `ZERO_COVERAGE_REVIEW` hold on case; close is an audited admin action; count both pools | 5 | unit, state-machine | state-machine test: `ZERO_COVERAGE_REVIEW → CLOSED_NO_MATCH` only via admin action; unit test counts outside invitations | none | `deposit-activation-reconcile` cron | TO IMPLEMENT |
| S6-27 | MD §6c L518; HTML S[5] | Limited auction requires fully searched radius, documented scarcity/urgency, field-size disclosure to buyer, audited approval | None; buyer sees only `dealersInvited` once live (`live-status/route.ts:33-46`) | MISSING | none | `LIMITED_AUCTION_APPROVED` requires ladder exhausted + reason; buyer notice "N dealerships" via dispatcher | 5 | unit, playwright | `sourcing-case.test.ts` preconditions; Playwright buyer sees field-size disclosure | none | none | TO IMPLEMENT |
| S6-28 | MD §Stage 6 Recorded L520; HTML S[5] tables | Sourcing case with band, candidates, validation outcomes, enrichment spend, readiness state | Closest: `VehicleRequest.coverageHoldAt/Reason` (`schema.prisma:1056-1057`); `AUCTION_ZERO_INVITATIONS` audit; `ApolloReveal` (`schema.prisma:4061-4087`); `VehicleRequestEvent` (`request-progression.service.ts:110,125`) | MISSING | none | Phase 1 wave (add — not yet named in §8.2): `sourcing_cases` (vehicle_request_id unique, band, status, readiness jsonb, enrichment_spend_cents, ops decision refs) + `sourcing_candidates` (rooftop_id, distance_miles, validation jsonb, candidate_ids[]) | 1 | integration | `pnpm test:migrations` + CI migration job; `rollback.sql` guarded drop | production migration deploy | none | TO IMPLEMENT |
| S6-29a | MD §Stage 6 Recorded L520 [BUILT]; HTML S[5] | Readiness checkpoints seeded on settlement with owner + timestamp | Model `schema.prisma:1139-1152`; admin CRUD `app/api/admin/requests/[requestId]/checkpoints/route.ts:25-66`, `.../complete/route.ts:29-49`; `initializeCheckpoints` has no callers (`vehicle-request-due-diligence.service.ts:4-27`) | PARTIAL | Audit + `VehicleRequestEvent` on every checkpoint mutation | Settlement calls `initializeCheckpoints` with §7 readiness items (owner system/ops), idempotent | 3 | unit, integration | extend `lib/services/payment/__tests__/deposit-settlement.test.ts` (checkpoints seeded once) | none | none | TO EXTEND |
| S6-29b | MD §Stage 6 Recorded L520 [BUILT]; HTML S[5] | Launch reads the checkpoints | `allCheckpointsComplete` gates only admin "send offer" (`app/admin/requests/[requestId]/page.tsx:50-51`); launch reads nothing | PARTIAL | Same as S6-29a | Readiness check = `allCheckpointsComplete` + system items; blockers surfaced per checkpoint owner | 5 | unit | `sourcing-case.test.ts` incomplete checkpoint → blocker named | none | admin request page `canSendOffer` | TO EXTEND |
| S6-30 | MD §Stage 6 Buyer sees L522; HTML S[5] buyer | "Finding dealerships" with band progress and honest expectations | `status-labels.ts:31-33` "Sourcing Vehicles"; `app/buyer/requests/[requestId]/offer/page.tsx:70-73`; webhook copy `stripe/route.ts:216-218`; `app/buyer/auction/[auctionId]/page.tsx:36-52` — no band/progress | PARTIAL | none | Buyer sourcing status view fed by the case (band, ready count, expectation copy) in HTML visual design | 5 | playwright, visual | Playwright journey step asserts band text; `test:visual` baseline for buyer sourcing page | none | none | TO EXTEND |
| S6-31 | MD §Stage 6 Exit L524; HTML S[5] | Exit when ready field meets threshold or Ops approves limited auction | Exit is immediate launch (S6-02 evidence) | MISSING | none | Case exit transitions bound to S6-22/S6-24 outcomes | 5 | state-machine | sourcing status state-machine suite (legal exits only) | none | none | TO IMPLEMENT |
| S6-32a | MD §Stage 6 If it fails L526; HTML S[5] | Schema: `RADIUS_AUTHORIZATION_REQUIRED` status | `VehicleRequestStatus` lacks it (`schema.prisma:1654-1666`) | MISSING | none | Phase 1 wave enum value (named in §8.2) | 1 | integration | migration test + `pg_enum` assertion | production migration deploy | `status-labels.ts`, admin filters | TO IMPLEMENT |
| S6-32b | MD §Stage 6 If it fails L526; HTML S[5] | At 250 without coverage → `RADIUS_AUTHORIZATION_REQUIRED`; ask buyer for max additional distance | No transition, no buyer prompt | MISSING | none | Case sets status + `radius_authorization_requested_at`; buyer notice via dispatcher; route S6-08b | 5 | state-machine, playwright | state-machine test; Playwright zero-coverage → radius authorisation screen | none | none | TO IMPLEMENT |
| S6-33 | MD §Stage 6 If it fails L526; HTML S[5] | Remind at 24 and 72 hours | None; pattern `lib/services/crm/lifecycle-scheduler.ts:127-145` | MISSING | none | Enqueue two `comms_outbox` rows (+24h/+72h) with `cancel_key`; cancelled on authorisation | 5 | integration | `comms-outbox` integration test: two rows, state recheck skips after authorisation | none | none | TO IMPLEMENT |
| S6-34a | MD §Stage 6 If it fails L526; HTML S[5] | Schema: abandonment timestamp | No field | MISSING | none | Phase 1 wave `vehicle_requests.abandoned_at` (named in §8.2) | 1 | integration | migration test column assertion | production migration deploy | none | TO IMPLEMENT |
| S6-34b | MD §Stage 6 If it fails L526; HTML S[5] | Close as abandoned after 14 days, preserving history | No timer; `EXPIRED`/`CANCELLED` exist, no cron writes them (`schema.prisma:1654-1666`; `ls app/api/cron`) | MISSING | none | Reconciler branch (reuse `coverage-hold-reconcile` tick) sets `abandoned_at` + terminal status with event; never deletes | 5 | integration, state-machine | cron route test in `app/api/cron/__tests__`; state-machine test history preserved | none | none | TO IMPLEMENT |
| S6-35 | MD §Stage 6 If it fails L526; HTML S[5] | Zero coverage creates an Ops case and buyer notice; closure and refund separate decisions | Ops `SYSTEM_ALERT` (`deposit-activation.service.ts:76-97`); buyer only post-close "no offers" (`auction.service.ts:168-182`); audit `dealer-invitation.service.ts:180-212` | PARTIAL | No auto-refund at close (`auction.service.ts:169-174`) | `raiseException(SOURCING_EXCEPTION, zero coverage)` + buyer "no coverage yet" via dispatcher from the case; close/refund stay manual | 5 | unit, integration | extend `deposit-activation-exception.test.ts`; outbox row assertion | none (refund remains manual by design) | `SYSTEM_ALERT` store (read-only mirror per §8.4) | TO EXTEND |
| S6-36 | HTML S[5] staff "Source manually where automated sourcing falls short" | Manual sourcing by Operations | Three admin routes: `launch-auction` bulk (refuses if PENDING/ACTIVE exists `:93-104`), `invite-outside-dealers`, `DEALER_INVITED`; none case-bound, none distance-checked, no reason for shortfall | PARTIAL | Admin role + reason on bulk launch | Bind manual sourcing to the case (reason, distance guard, audit); allow top-up of an open case | 5 | integration | `app/api/admin/auctions/__tests__` + `test:admin-authz` route tests | none | the three admin routes | TO EXTEND |
| S6-37 | HTML S[5] system "Search the rooftop pool rather than the catalogue" | Rooftop-pool search; catalogue radius not binding | Not bound: `shortlist-radius.ts:10-15` firewall (correct). Automatic path reads `dealers`, not `dealer_rooftops`; only admin auto-mint reads rooftops (`outside-invite.service.ts:223`); `Dealer.rooftopId` (`schema.prisma:161`) | PARTIAL | Import firewall | `dealer_rooftops` becomes the sourcing unit for both pools (registered dealer → rooftop via `Dealer.rooftopId`, minted via `resolveRooftop` if absent) | 5 | unit | `sourcing-case.test.ts` registered dealer without rooftop resolved before ready | none | none | TO EXTEND |
| S6-38 | MD §Stage 6 Entry L487; HTML S[5] | Settlement moves the request to `ACTIVE_SOURCING` and joins deposit ↔ request | Webhook never touches `VehicleRequest` (`stripe/route.ts:201-214`); ACTIVE_SOURCING set pre-payment (`request-progression.service.ts:118-126`) or by admin (`launch-auction/route.ts:205-211`) | BROKEN | none | Settlement handler sets `ACTIVE_SOURCING` (from `PAYMENT_REQUIRED`) and writes `deposits.vehicle_request_id` | 3 | integration, state-machine | extend `deposit-settlement.test.ts`; `request-progression.test.ts` only settlement enters ACTIVE_SOURCING | none | `request-progression.service.ts` auto-progression | TO IMPLEMENT |
| S6-39 | MD §6b L504 (Recorded); HTML S[5] | Distance is a recorded validation outcome | Distance transient (`dealer-invitation.service.ts:104-108`; `outside-invite.service.ts:252-262`); neither invitation stores it | PARTIAL | none | Persist `distance_miles` on `sourcing_candidates` and `auction_invitations` (add column to Phase 1 wave; only `auction_vehicles.distance_miles` is named) | 5 | unit, integration | unit test mint stores distance; DB assertion non-null on every invitation | none | none | TO EXTEND |
| S7-01a | MD §Stage 7 Entry L532; HTML S[6] | Payments side: no synthetic PAID deposit; dispute/refund after launch holds fulfilment | Admin mints `Deposit{status:"PAID"}` without Stripe intent (`launch-auction/route.ts:134-144`); disputes audit-only (`stripe/route.ts:769-795`); refunded deposit skipped only for stranded auctions (`deposit-activation-policy.ts:32`) so live auction + reminders continue | PARTIAL | Concierge-track fail-closed (`resolveDepositFulfillmentTrack`, `deposit-activation.service.ts:199-206`) | Remove synthetic-PAID minting (admin launch requires a real settled deposit); dispute/refund → hold + cancel unsent outreach (Phase 3 scope) | 3 | integration | `test:webhooks` new dispute/refund-after-launch test (auction held, outbox cancelled); admin launch route test refuses without settled deposit | none | admin `launch-auction` | TO IMPLEMENT |
| S7-01b | MD §Stage 7 Entry L532; HTML S[6] | Readiness item: $99 settled and undisputed | Webhook PAID (`stripe/route.ts:180-215`); reconciler PAID + not refunded (`deposit-activation.service.ts:199-206`); no undisputed check | PARTIAL | Same as S7-01a | Readiness reads deposit PAID ∧ `refundedAt` null ∧ `disputedAt` null ∧ bound to request | 5 | unit | `auction-launch-readiness.test.ts` (new) disputed → blocker | none | none | TO IMPLEMENT |
| S7-02 | MD §Stage 7 Entry L532; HTML S[6] | Prequalification and approved ceiling attached | Gated upstream at `create-intent/route.ts:63-70` (`PREQUAL_REQUIRED` via `isPrequalValid`); admin path bypasses (`launch-auction/route.ts:139-144`); ceiling read later (`app/api/dealer/auctions/[auctionId]/route.ts:74-80`) | PARTIAL | Prequal gate at payment | Readiness item via Phase 2 approval-recheck helper (APPROVED, unexpired, `maxOtdAmountCents` present) | 5 | unit | `auction-launch-readiness.test.ts` expired prequal → blocker | none | admin `launch-auction` | TO EXTEND |
| S7-03 | MD §Stage 7 Entry L532; HTML S[6] | Vehicle criteria complete | `ensureAuctionVehicleFromRequest` degrades make-agnostic (`dealer-invitation.service.ts:111-148`); `create-intent/route.ts:72-76` refuses empty shortlist but candidates dropped (`:128-143`); admin optional (`launch-auction/route.ts:214-230`) | PARTIAL | Empty-shortlist refusal at payment | Readiness requires make/model/year range + ≥1 `auction_vehicles` candidate (Phase 4) | 5 | unit | `auction-launch-readiness.test.ts` no candidate → blocker | none | none | TO EXTEND |
| S7-04 | MD §Stage 7 Entry L532; HTML S[6] | Required dealer count or approved exception exists | None (S6-22..26) | MISSING | none | Readiness item bound to §6c outcome (AUTO_LAUNCH or LIMITED approval) | 5 | unit, state-machine | `auction-launch-readiness.test.ts`; sourcing state-machine suite | none | none | TO IMPLEMENT |
| S7-05 | MD §Stage 7 Entry L532; HTML S[6] | Every contact send-safe against suppression and opt-out | Outside: at selection (`contact-resolution.service.ts:132-136`); registered: none; email rail has no suppression (S6-16 `resend.service.ts:129-200`) | PARTIAL | `SEND_SAFE_STATUSES` invariant | Readiness item re-checks suppression for every invitation contact at launch | 5 | unit | `auction-launch-readiness.test.ts` suppressed contact → blocker | none | direct `sendIdempotent` sends | TO EXTEND |
| S7-06 | MD §Stage 7 Entry L532; HTML S[6] | Every rooftop within permitted distance | Automatic: haversine ≤ radius (`dealer-invitation.service.ts:313`); admin `launch-auction/route.ts:107-113` and `action/route.ts:95-125` no check | PARTIAL | Automatic-path geo filter | Readiness item using persisted `distance_miles` ≤ permitted; admin guard (S6-11) | 5 | unit, integration | readiness unit test; admin route test | none | admin `launch-auction`; `DEALER_INVITED` | TO EXTEND |
| S7-07 | MD §Stage 7 Entry L532; HTML S[6] | Auction and per-dealer invitation references exist before launch | `launchAuction` (ACTIVE, endsAt) before invitations (`stripe/route.ts:259-265`; `deposit-activation.service.ts:210-227`); ACTIVE with 0 invitations possible (defect #1) | BROKEN | none | Create `auction_invitations` (QUEUED) while PENDING; flip ACTIVE + `endsAt` in one txn only when ≥1 invitation row exists | 5 | integration, concurrency, state-machine | new `auction-launch-readiness.test.ts`; DB assertion: no ACTIVE auction with zero invitations (defect #1 regression); concurrent launch attempts activate once | none | webhook/reconciler launch path | TO IMPLEMENT |
| S7-08 | MD §Stage 7 L534; HTML S[6] | Failure keeps auction pending and shows the exact missing prerequisite; never half-ready | No pending-with-blocker state; half-ready goes ACTIVE then auto-closes (S6-26) | MISSING | none | Readiness result persisted on case/checkpoints with owner; PENDING auction never activates on failure | 5 | unit, playwright | readiness unit test blocker list; Playwright readiness screen shows named blocker | none | none | TO IMPLEMENT |
| S7-09 | MD §Stage 7 L537; HTML S[6] | System launches a 48-hour sealed auction | 48h (`lib/constants.ts:46`); admin `hours` 1–168 (`launch-auction/route.ts:47,153-162`); dealers see count + own offer only (`app/api/dealer/auctions/[auctionId]/route.ts:11-13,111-114`); competitiveness-check anonymised median; anti-snipe +30 min (`anti-snipe.service.ts:14-16`) | PARTIAL | No competitor prices to dealers; anti-snipe atomic extension cap | Confirm sealed semantics; restrict or audit `hours` override; disable median/anti-snipe if owner rules them incompatible | 5 | unit | `anti-snipe.test.ts` preserved or retired per decision; admin route test override audited | owner decision Q2: median feedback + anti-snipe vs "sealed" | admin `hours` override; competitiveness-check route | OWNER-GATED |
| S7-10 | MD §Stage 7 L537; HTML S[6] dealer bullet 1 | Unique, expiring, auction-and-rooftop-bound invitation link per rooftop | Outside: plaintext uuid + `expiresAt` + `rooftopId` (`schema.prisma:460-498`; `outside-invite.service.ts:45-58,113-116`; `outside-dealer-offer/[token]/route.ts:90-96`); registered: no token/expiry (`schema.prisma:518-531`) | PARTIAL | `inviteRejection` enforces token expiry independent of auction status (replay-after-reopen guard) | Mint hashed token (`invitation-token.service.ts` `hashToken`) per `auction_invitations` row for both pools; public token route resolves both tables | 5 | unit, integration, playwright | new `auction-invitation-token.test.ts` (expired/replayed/foreign-rooftop refused); extend `invitation-token.test.ts`; Playwright dealer opens token link | owner Q5: token-only vs session access for registered dealers | `AuctionInvitation` readers: `app/api/dealer/auctions/[auctionId]/route.ts`, `cron/dealer-invitation-reminder`, `dealer-scorecard.service.ts`, `offer.service.ts:127` | TO IMPLEMENT |
| S7-11 | MD §Stage 7 L537; HTML S[6] | Registered and outside get the same secure invitation class; no generic dashboard link | `/dealer/opportunities` (`dealer-invitation.service.ts:401`); `/dealer/auctions/{id}` (`launch-auction/route.ts:290,307`); QStash `jobs/dealer-invited/route.ts:29,35` | BROKEN | none | One dispatcher template with tokenised link for both pools | 5 | unit | extend `lib/services/email/__tests__/dealer-invitation-template.test.ts` (link carries token, no generic path) | legacy removal: QStash routes deleted Phase 10 | `dealer-auction-invitation.tsx`; `jobs/dealer-invited` | TO IMPLEMENT |
| S7-12 | MD §Stage 7 L539; HTML S[6] | Invitation carries full criteria, required-vs-preferred, general location + distance, trade indication, pickup/delivery preference, deadline, link | Template props `dealer-auction-invitation.tsx:12-22`; `resend.service.ts:1602-1612`; `invite-outside-dealers/route.ts:70-82` literal "Vehicle Requested"; dealer view `app/api/dealer/auctions/[auctionId]/route.ts:34-63` | PARTIAL | none | Payload from request (Phase 1 criteria/`delivery_preference` columns, trade packet flag, `distance_miles`, real `endsAt`) | 5 | unit | `dealer-invitation-template.test.ts` fields present; `dealer-invitation-pii.test.ts` extended to new fields | none | `invite-outside-dealers` placeholders | TO EXTEND |
| S7-13 | MD §Stage 7 L539, §25.1 L1225; HTML S[6] | Invitation carries no buyer identity | Selects only zip/city/state (`dealer-invitation.service.ts:218-225`); `dealer-invitation-pii.test.ts:1-20`; dealer view strips `buyerId` (`app/api/dealer/auctions/[auctionId]/route.ts:72-80,111-114`). Gap: admin `notes` (`launch-auction/route.ts:38,226`) reaches dealers unscanned (`:52`) | ALREADY CORRECT | Structural PII exclusion + PII test | Keep; extend PII test to new payload fields; scan/redact `AuctionVehicle.notes` before dealer exposure | 5 | unit | `dealer-invitation-pii.test.ts` extended (new fields + notes redaction) | none | none | PRESERVED STRONGER SAFEGUARD |
| S7-14a | MD §Stage 7 L541; HTML S[6] | Schema: per-invitation delivery states | `AuctionInvitation` has `sentAt/viewedAt/respondedAt` only (`schema.prisma:518-531`); outside `:478-488` | PARTIAL | none | Phase 1 wave: `AuctionInvitationStatus` enum + `queued_at…offer_submitted_at`, `reminder_50/90_sent_at` (named in §8.2) | 1 | integration | migration test enum + columns | production migration deploy | none | TO IMPLEMENT |
| S7-14b | MD §Stage 7 L541; HTML S[6] | Track queued, sent, delivered, opened, bounced, declined, responded, offer submitted | Resend webhook updates `EmailSendLog`/suppression only (`app/api/webhooks/resend/route.ts:102-107,188-189`); OPENED from `dealer/auctions/[auctionId]/route.ts:105-108`; OFFER_SUBMITTED from `offer.service.ts:127-130`; "declined" synthesised in UI (`app/dealer/auctions/page.tsx:77-83,207-208`) | PARTIAL | none | Dispatcher → QUEUED/SENT; Resend events mapped by idempotency key → DELIVERED/OPENED/BOUNCED; decline route (S7-24); state machine on enum | 5 | integration, state-machine | Resend webhook test → invitation status; `AuctionInvitationStatus` state-machine suite | none | Resend webhook; `app/dealer/auctions/page.tsx` synthesis | TO EXTEND |
| S7-15 | MD §Stage 7 L543; HTML S[6] | Undeliverable contact/rooftop replaced early in the window | Bounce → suppression + prospect pause only (`webhooks/resend/route.ts:50-56,102-107`); walk-down only on next resolution (`contact-resolution.service.ts:35-41`) | MISSING | none | On BOUNCED: re-run contact resolution for the rooftop or mint next in-band rooftop; old row → REPLACED | 5 | integration | bounce → replacement invitation + REPLACED test | none | none | TO IMPLEMENT |
| S7-16 | MD §Stage 7 L543; HTML S[6] | Nonresponders reminded at 50% and 90% of the window | Three rails: QStash +24h/+42h (`jobs/dealer-invited/route.ts:39-43`; `dealer-bid-reminder/route.ts:25-27,47-51`); cron 5–7h (`cron/dealer-invitation-reminder/route.ts:19-20,32-36`); `cron/auction-close/route.ts:46-84` ≤2h reminder with empty vehicle fields sharing key `resend.service.ts:1614-1624`; outside invites no reminder | PARTIAL | Reminder idempotency key per auction+email | One dispatcher pair computed from `endsAt` (50%/90%) for both pools, cancelled on offer/decline, `reminder_50/90_sent_at`; retire cron + auction-close reminder blocks + QStash rail | 5 | integration | outbox integration test: two rows at `endsAt`-24h/-4.8h, cancelled on response; `auction-close` cron test sends no reminder | legacy removal: QStash deleted Phase 10 | `jobs/dealer-bid-reminder`; `cron/dealer-invitation-reminder`; `cron/auction-close:46-84` | TO CONSOLIDATE |
| S7-17 | MD §Stage 7 Recorded L545; HTML S[6] tables | `auctions` linked to the Vehicle Request and the deposit | `depositId` required+unique, `vehicleRequestId` nullable (`schema.prisma:420,443`); set only by admin (`auction.service.ts:15-28,34-44`); webhook leaves null (`stripe/route.ts:208-214`) | PARTIAL | `resolveOwnedVehicleRequestId` cross-buyer guard (`auction.service.ts:34-44`) | Auction created at launch from the case with `vehicleRequestId` + `depositId` always set | 5 | unit | extend `create-auction-vehicle-request.test.ts` (null request refused) | none | historical auctions with null `vehicleRequestId` (compat read) | TO EXTEND |
| S7-18 | MD §Stage 7 Recorded L545; HTML S[6] tables | `auction_invitations` per rooftop with token hash, expiry, delivery state | `AuctionInvitation` none of these (`schema.prisma:518-531`); outside plaintext token (`:467`, `:477`); hashing only in `invitation-token.service.ts:1-36` / migration `20260828000000_dealer_invitation_token_hash`; no RLS policy names these tables (14 RLS files, none match) | MISSING | Hashed-token pattern | Phase 1 wave: `rooftop_id`, `token_hash` unique, `expires_at`, `status`, `is_registered_dealer`, `candidate_ids[]`, `distance_miles` (add), partial unique `(auction_id, rooftop_id)`; `dealer_id` nullable | 1 | integration | migration test; `pnpm db:check-drift`; RLS status recorded in phase report | production migration deploy; owner decision on RLS coverage (UNVERIFIED) | `outside_auction_invites` (kept for reads) | TO IMPLEMENT |
| S7-19 | MD §Stage 7 Buyer sees L547; HTML S[6] buyer | "Auction is live", invited count, close time, offer count — never competing prices | `live-status/route.ts:15-50`; `app/buyer/auction/[auctionId]/page.tsx:36-52`; `:34` counts bounced/undelivered as invited | ALREADY CORRECT | Truthful engagement signal (`deriveAuctionEngagement`); no amounts | Keep; after S7-14 derive `dealersInvited` from non-BOUNCED/REPLACED invitations | 5 | unit | `auction-engagement.test.ts` stays green; live-status test excludes bounced | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| S7-20 | MD §Stage 7 Exit L549; HTML S[6] | Auction closes at deadline, or when all invited dealers have responded | Deadline close `closeExpiredAuctions` (`auction.service.ts:218-226`; `vercel.json` auction-close); no all-responded close | PARTIAL | Close idempotency (`auction-close-idempotency.test.ts`) | Early close when every invitation is DECLINED/OFFER_SUBMITTED, respecting revision window, under the existing close lock | 6 | unit, concurrency | extend `auction-close-idempotency.test.ts`; `test:concurrency` all-responded vs deadline race closes once | none | `cron/auction-close` | TO EXTEND |
| S7-21 | MD §Stage 7 If it fails L551; HTML S[6] | Bounced invitation creates an Ops contact-replacement exception | Only suppression on bounce (`webhooks/resend/route.ts:102-107`); rail `deposit-activation.service.ts:64-89` | MISSING | none | `raiseException(AUCTION_EXCEPTION, contact replacement)` keyed by invitation id on BOUNCED | 5 | integration | Resend bounce → one queue item (idempotent) test | none | Resend webhook | TO IMPLEMENT |
| S7-22 | MD §Stage 7 If it fails L551; HTML S[6] | Launch that cannot reach readiness holds and surfaces the blocker with an owner | Stranded deposits surface only after auto-close (`deposit-activation.service.ts:91-97`) | MISSING | none | Readiness stall → `raiseException(SOURCING_EXCEPTION)` with blocker + owner + return point | 5 | unit, integration | readiness test blocker persisted; queue item assertion | none | none | TO IMPLEMENT |
| S7-23 | HTML S[6] system L566 | One invitation per rooftop carrying every candidate it can serve | One per (auction, dealer) / (auction, rooftop) (`schema.prisma:529,495`); not candidate-aware | PARTIAL | Uniqueness constraints | `candidate_ids[]` filled by S6-03; partial unique `(auction_id, rooftop_id)` | 5 | unit, integration | DB assertion one row per rooftop; unit test candidate ids populated | none | none | TO EXTEND |
| S7-24 | HTML S[6] dealer "Open, decline or respond" | Dealer can decline an invitation | No route (`ls app/api/dealer/auctions/[auctionId]` → `route.ts`, `insights/`); UI-synthesised (`app/dealer/auctions/page.tsx:77-83,207-208`) | MISSING | none | `POST /api/dealer/auctions/[auctionId]/decline` (own invitation, idempotent) → DECLINED, reminders cancelled, feeds S7-20 | 5 | integration, playwright | route test (authz, idempotent); Playwright dealer declines | none | none | TO IMPLEMENT |
| S7-25 | MD §Stage 7 L539; HTML S[6] | Invitation states the real submission deadline | Hardcoded "48 hours" / `expiryHours: AUCTION_DURATION_HOURS` (`dealer-invitation.service.ts:383,402`); admin single computes from `endsAt` (`action/route.ts:137-139`) | PARTIAL | none | Compute deadline from `auction.endsAt` everywhere | 5 | unit | `dealer-invitation-template.test.ts` deadline equals `endsAt` | none | none | TO EXTEND |
| 25-01 | MD §25.1 L1225; HTML MONEY_PANELS[3] L806 | Invited dealerships receive criteria, general location, distance, trade indication — no identity | `post-intake-outreach.service.ts:9-13`; S7-12/S7-13 evidence | PARTIAL | PII test | Add distance + trade indication to invitation payload (with S7-12) | 5 | unit | `dealer-invitation-template.test.ts` + PII test | none | none | TO EXTEND |
| 25-02 | MD §25.1 L1225 [BUILT — sequence it]; HTML S[9] tables | Name, email, phone, exact address released only at Stage 10 after win **and reaffirmation** | Winner email releases first name + last initial at award (`lib/services/notifications/dealer-award.ts:28-29`; `cron/dealer-award-dispatch/route.ts:1-8`); no reaffirmation model (`rg -i reaffirm` → none); `identity_firewall_entries` unused (`schema.prisma:2755-2765`) | PARTIAL | Award email withholds email/phone/exact budget | Release only on `dealer_reaffirmations` CONFIRMED; write `IdentityFirewallEntry` on lift; award email carries no name | 7 | unit, state-machine | rewrite `lib/services/notifications/__tests__/dealer-award.test.ts` (no name before reaffirmation); reaffirmation state-machine suite | none | identity lift at award (`dealer-award.ts`, `dealer-deals.service.ts`, dealer deal page) — §8.4 Phase 7 | TO IMPLEMENT |
| 25-03 | MD §25.1 L1225; HTML MONEY_PANELS[3] | Losing dealerships never receive buyer contact information | Lost email position/total only (`resend.service.ts:1662-1672`); dealer view no buyer relation (`app/api/dealer/auctions/[auctionId]/route.ts:33-34`) | ALREADY CORRECT | Structural exclusion | none; extend PII test to lost-email payload | 5 | unit | `dealer-invitation-pii.test.ts` extended to `sendDealerOfferLostEmail` params | none | none | PRESERVED STRONGER SAFEGUARD |
| 25-04 | MD §25.2 L1229 [BUILT]; HTML MONEY_PANELS[3] | Buyer↔dealership messages monitored for CONTACT_ATTEMPT, EXTERNAL_DEAL, IDENTITY_MISMATCH, PAYMENT_BYPASS | `checkAntiCircumvention` covers three patterns, no IDENTITY_MISMATCH (`messaging.service.ts:6-20,22-45`); buyer route scans only dealer threads (`app/api/buyer/messages/route.ts:62-77`); dealer sends unscanned on `app/api/dealer/messages/route.ts:78-84` and `threads/[threadId]/route.ts:62-68` (`isRedacted:false`) | BROKEN | Flagged content redacted + thread FLAGGED + alert; buyer↔support deliberately unscanned | Route both dealer sends through `sendMessage`; add IDENTITY_MISMATCH once defined; optional LLM classifier behind AI kill switch | 5 | unit, integration | new `lib/services/messaging/__tests__/anti-circumvention.test.ts`; dealer message route tests (pattern → redacted + flagged) | owner Q6: IDENTITY_MISMATCH definition | `app/api/dealer/messages/route.ts`; `threads/[threadId]/route.ts` | TO EXTEND |
| 25-05 | MD §25.2 L1229; HTML MONEY_PANELS[3] | Detection creates a record with matched pattern and routes to Operations | Live: `Message.antiCircumventionFlag` + FLAGGED + `SYSTEM_ALERT` (`messaging.service.ts:25-45`); dead: `recordCircumventionAttempt`/`checkCircumventionRisk` zero callers (`lib/services/trust/anti-circumvention.service.ts:5-16`); `trust-check` cron counts only (`app/api/cron/trust-check/route.ts:13-19`) | PARTIAL | Redaction in stored message | `sendMessage` → `recordCircumventionAttempt` (spec table) + `raiseException(DEALER_EXCEPTION)`; retire `PlatformAlert P1` + `SYSTEM_ALERT` duplicates | 5 | unit | `anti-circumvention.test.ts`: one detection → one `circumvention_attempts` row + one queue item, idempotent | none | `trust-check` cron; `PlatformAlert P1` rail | TO CONSOLIDATE |
| 25-06 | MD §25.2 L1231; HTML MONEY_PANELS[3] | Confirmed off-platform attempt after a paid auction registers on the dealership scorecard | Scorecard inputs win/completion/response/junk-fee only (`lib/services/dealer/dealer-scorecard.service.ts:2,18-36`); `CircumventionAttempt.userId` no `dealerId` (`schema.prisma:2741-2753`) | MISSING | none | Add `circumvention_attempts.dealer_id` (Phase 1 wave addition) and a scorecard component from resolved-confirmed attempts | 10 | unit | `lib/services/dealer/__tests__` scorecard test (confirmed attempt lowers score; dismissed does not) | none | none | TO IMPLEMENT |
| 25-07 | MD §25.2 L1231; HTML MONEY_PANELS[3] | May suspend the dealership from future invitations | Admin sets `SUSPENDED` (`app/api/admin/dealers/[dealerId]/status/route.ts:7,21-24`; `suspend/route.ts:19-28`); excluded by `status:"ACTIVE"` (`dealer-invitation.service.ts:286`); no link from circumvention | PARTIAL | SUSPENDED excluded from matching; role + reason required | Ops resolution action "confirm → suspend from invitations" recording reason + attempt id | 10 | integration | `test:admin-dealers` route test (suspend from confirmed attempt writes audit) | none | none | TO EXTEND |
| 25-08 | MD §25.2 L1231; HTML MONEY_PANELS[3] | Grounds for termination | `TERMINATED` settable by admin (`status/route.ts:7`); no linkage | PARTIAL | Role-gated status change | Policy wiring: termination action available from confirmed attempt with reason | 10 | integration | admin dealers route test | owner decision: termination criteria | none | TO EXTEND |
| 25-09a | MD §25.2 L1231; HTML MONEY_PANELS[3] | Attribution: detections attributed to the initiating party | Buyer is the only party scanned/redacted/flagged in dealer threads (`messaging.service.ts:25-45`; `app/api/buyer/messages/route.ts:76-77`; `app/api/dealer/messages/route.ts:78-84`) | BROKEN | Sender-keyed record (`senderId`/`userId`) | With 25-04 scan both directions; record `initiator_role` on the attempt | 5 | unit | `anti-circumvention.test.ts` dealer-initiated attributed to dealer; buyer message untouched | none | dealer message routes | TO IMPLEMENT |
| 25-09b | MD §25.2 L1231; HTML MONEY_PANELS[3] | Buyers protected, not penalised, when the dealership initiates | No policy distinguishes consequences | BROKEN | none | Scorecard/suspension consequences apply only to dealer-initiated confirmed attempts; buyer never penalised | 10 | unit | scorecard/suspension tests exclude buyer-initiated attempts | none | none | TO IMPLEMENT |
| 25-10 | HTML S[6] tables L570 | `identity_firewall_entries` participates in Stage 7 | Table exists, zero code refs (`schema.prisma:2755-2765`; only schema/migration/`scripts/expected_schema.json`) | MISSING | none | Write withheld-state entry per auction/rooftop at launch; lifted at Phase 7 reaffirmation (25-02) | 5 | unit, integration | launch test writes entry; DB assertion one per invitation | owner decision only if table is dropped instead (legacy removal) | none | TO IMPLEMENT |
| 25-11 | MD §25.2 L1231 "after a paid auction" | Consequence rule scoped to attempts after a paid auction | Detection keyed on thread/message/sender; `MessageThread.dealId/requestId` (`schema.prisma:2130-2133`) never tied to a PAID deposit | MISSING | none | Resolve thread → request/deal → auction → deposit PAID at detection; store `after_paid_auction` on the attempt | 5 | unit | `anti-circumvention.test.ts` paid vs unpaid thread flag | none | none | TO IMPLEMENT |
| 25-12 | MD §25.2 L1229 "review and resolution" | Ops can resolve a detection | Admin messages routes GET/POST only (`app/api/admin/messages/[threadId]/route.ts:15,46`); `CircumventionAttempt.resolved/resolvedBy` never written; FLAGGED has no transition back (`rg FLAGGED app/api/admin` → none) | MISSING | none | Audited admin action confirm / dismiss (suspend in Phase 10) writing `resolved/resolvedBy` and FLAGGED → ACTIVE | 5 | integration, state-machine | admin messages route test; `MessageThread` status state-machine test | none | none | TO IMPLEMENT |

## Owner-gated items

- Production deploy of the Phase 1 migration wave (§13-D1 ledger resolve first, then `prisma migrate deploy`) — S6-01a, S6-08a, S6-28, S6-32a, S6-34a, S7-14a, S7-18; the wave must also gain `deposits.disputed_at`, `sourcing_cases`/`sourcing_candidates`, `auction_invitations.distance_miles`, `dealer_rooftops` operating-status signal, `circumvention_attempts.dealer_id` (not yet named in §8.2).
- Env / feature flags: `GOOGLE_GEOCODING_API_KEY` production presence (S6-13, UNVERIFIED); `DEALER_VERIFICATION_GATE` ON/OFF decision (S6-15); `APOLLO_REVEAL_ENABLED` activation and owner Q1 on pre-payment LLM enrichment spend (S6-20).
- Owner decisions: Q2 sealed-auction semantics vs anonymised median + anti-snipe (S7-09, OWNER-GATED); Q3 fate of `MIN_COVERAGE_DEALERS` soft-hold (S6-22); Q5 token-only vs session access for registered dealers (S7-10); Q6 IDENTITY_MISMATCH definition (25-04); termination criteria (25-08); use-vs-drop of `identity_firewall_entries` (25-10); RLS coverage for `auction_invitations`, `outside_auction_invites`, `messages`, `circumvention_attempts`, `identity_firewall_entries` (S7-18, UNVERIFIED in repo).
- Legacy removals (§8.4): settlement launch+invite path and reconciler close branch after 30 days of zero `LEGACY_PATH_WRITE` (S6-02a, S6-26a); drop of `outside_auction_invites` after zero writes (S6-05, S7-18); QStash producers/consumers/env deleted in Phase 10 (S7-11, S7-16); retirement of the untargeted `notifyActiveDealersOfOpportunity` broadcast (owner Q7, S6-11).
- Money path: refunds remain manual by design — no activation requested (S6-35, S7-01a).

## Legacy paths

- `app/api/webhooks/stripe/route.ts:258-265` — launch + invite on settlement → becomes "open sourcing case" (Phase 3 neutralise; removal owner-gated).
- `lib/services/auction/deposit-activation.service.ts` `'launch'`/`'invite'`/`'close'` branches, `NO_DEALER_CLOSE_GRACE_MINUTES`, and the `deposit-activation-reconcile` cron (Phase 3).
- `lib/services/vehicle-request/request-progression.service.ts:118-126` — pre-payment auto-progression into `ACTIVE_SOURCING` (Phase 3).
- `app/api/admin/buyers/[buyerId]/launch-auction/route.ts` — synthetic PAID deposit, no distance check, no `.max(8)`, `hours` override, generic links, `notes` unscanned (Phases 3/5).
- `app/api/admin/auctions/[auctionId]/action/route.ts` `DEALER_INVITED`/`DEALER_REMOVED` — no distance/cap, load drift (Phase 5).
- `app/api/admin/buyers/[buyerId]/invite-outside-dealers/route.ts` and `outside_auction_invites` writes (Phase 5 stop; reads kept; owner-gated drop).
- `lib/services/auction/coverage.service.ts` `RADIUS_TIERS`, fail-open counting; `request-coverage-gate.service.ts` `MIN_COVERAGE_DEALERS` (Phase 5).
- `AuctionInvitation` readers: `app/api/dealer/auctions/[auctionId]/route.ts`, `app/api/cron/dealer-invitation-reminder`, `lib/services/dealer/dealer-scorecard.service.ts`, `lib/services/offer/offer.service.ts:127`, `app/dealer/auctions/page.tsx` (Phase 5).
- Direct sends: `lib/services/email/resend.service.ts` `sendDealerAuctionInvitationEmail`/`sendIdempotent` (no suppression), `lib/services/email/templates/dealer-auction-invitation.tsx` (allowlist Phase 2 → zero Phase 10).
- Reminder rails: QStash `app/api/jobs/dealer-invited`, `app/api/jobs/dealer-bid-reminder` (Phase 2 neutralise, Phase 10 delete); `app/api/cron/dealer-invitation-reminder`; `app/api/cron/auction-close/route.ts:46-84` reminder block (Phase 5 consolidate).
- `app/api/webhooks/resend/route.ts` — bounce handling extended to invitation state + Ops exception (Phase 5).
- `lib/services/dealer-recruitment/dealer-contact-backfill.service.ts` cron — paid reveals without payment linkage (Phase 5).
- `app/admin/requests/[requestId]/page.tsx:50-51` `canSendOffer` checkpoint gate (Phase 5 re-scope).
- `app/api/dealer/messages/route.ts`, `app/api/dealer/messages/threads/[threadId]/route.ts` — unscanned dealer sends (Phase 5).
- `lib/services/trust/anti-circumvention.service.ts` dead writers, `app/api/cron/trust-check`, `PlatformAlert P1` rail (Phase 5 consolidate).
- `lib/services/notifications/dealer-award.ts:28`, `dealer-deals.service.ts`, dealer deal page — identity lift at award (Phase 7 per §8.4).
- `lib/services/acquisition/dealer-opportunity-notification.service.ts:55-60` untargeted first-20-ACTIVE broadcast from `app/api/public/request-vehicle/route.ts:677` (owner Q7).
- `SYSTEM_ALERT` Notification as exception store — kept as read-only mirror (§8.4).
