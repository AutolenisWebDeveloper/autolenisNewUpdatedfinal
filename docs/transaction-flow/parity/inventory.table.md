# Parity table — area `inventory` (Stage 4, §22a, §32 rows, §33 steps 28/30/31, Appendix, HTML S[3]/INV/QUAL/BUDGET/FINDINGS)

Source: `parity/inventory.md` at HEAD 0cd399f including its adversarial corrections (corrected statuses win). Paths relative to `frontend/`. Phases per `IMPLEMENTATION-WORKFLOW.md` §8.1/§8.2; test levels per §12.1. Rows split (a/b/c/d) only where parts land in different phases. R62–R64 are the requirements the corrections pass found uncovered.

| Ref | Document & section | Requirement | Current route / service / model / component / email / cron / queue / admin action | Status | Stronger safeguard to preserve | Exact required change | Phase | Test level | Acceptance evidence | Owner-gated dependency | Legacy path affected | Final disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R1 | MD Stage 4 Entry L406; HTML S[3].entry | Entry = current, unexpired approval | `lib/services/buyer/journey.ts:101-109` gates `search` on onboardingComplete && prequalValid; `app/buyer/search/page.tsx:40-57` renders for any buyer; `/api/buyer/search` `route.ts:20-23` auth only; `requests/route.ts:122` no prequal gate | PARTIAL | none | Gate the new qualified-results service/surface on `isPrequalValid`; keep custom-request path open; catalogue stays "general" | 4 | unit, playwright | extend `lib/services/buyer/__tests__/journey.test.ts`; new `qualified-results.test.ts` asserts no results/no draw for unapproved buyer; Playwright approved-buyer journey | none | `app/buyer/search` reachable pre-approval as general catalogue | TO IMPLEMENT |
| R2 | MD §4a L410; HTML S[3].buyer[0], QUAL[0] | System presents qualified results automatically | none; `app/buyer/search` manual filter over `inventory_items` (`search/route.ts:127-137`); `inventory-match.service.ts:23-75` dashboard ranking, no ZIP/radius/provider | MISSING | none | Build qualified-results service (R32) and auto-render after APPROVED in buyer portal | 4 | unit, integration, playwright | new `lib/services/inventory/__tests__/qualified-results.test.ts`; Playwright approved buyer → results shown without manual search | MarketCheck terms §13-D8 (cache only) | `/buyer/search`; `findMatchedVehicles` widget | TO IMPLEMENT |
| R3 | MD §4a L410; HTML QUAL[1] | Buyer deliberately adds ≤5; system never saves on buyer's behalf | buyer route `app/api/buyer/shortlist/route.ts:99-101` (gated, cap `:91-93`); CORRECTION: admin upsert `app/api/admin/buyers/[buyerId]/shortlist/route.ts:89-93` writes AUCTION_READY on buyer's behalf, audited `:96-106` | PARTIAL | cap counts AVAILABLE only (`shortlist.service.ts:14-28`) | Route admin writer through `shortlistGate` or require explicit override reason + audit; no cron/service writer | 4 | unit, integration | new `app/api/admin/__tests__/admin-shortlist-gate.test.ts`; extend `shortlist-radius-gate.test.ts` | none | admin shortlist upsert route | TO EXTEND |
| R4a | MD §4a L410; HTML S[3].system[0] | Both entry methods write the SAME Vehicle Request; only `entry_type` differs (schema) | `VehicleRequest` `schema.prisma:1022-1070` has no entry type; rg `entry_type\|entryType` negative | MISSING | none | Phase 1 wave: `entry_type` enum `VehicleRequestEntryType` {INVENTORY_SELECTION, CUSTOM_REQUEST}, `inventory_item_id` FK | 1 | integration | `prisma/__tests__` chain test + CI from-zero replay; `pnpm db:check-drift` | production migration deploy (§13-D1 ledger first) | none | TO IMPLEMENT |
| R4b | MD §4a L410; HTML S[3].system[0] | Shortlist path creates/attaches the VehicleRequest (INVENTORY_SELECTION) | shortlist writes only `shortlist_items`; custom → `requests/route.ts:169-199` `intakeBuyerRequest`; `deposit/create-intent/route.ts:76-84` gates on shortlist rows only | MISSING | none | On first shortlisted candidate (owner Q4: or at deposit) create/attach the open VehicleRequest with `entry_type`; both paths converge | 4 | integration, playwright | extend `app/api/buyer/shortlist/__tests__`: shortlist → VehicleRequest row asserted; Playwright shortlist 5 → one request | owner decision (Q4 attach timing) | shortlist-only journey with no VehicleRequest | TO IMPLEMENT |
| R5 | MD §4a L410; HTML INV[8] | Out-of-radius listing cannot be shortlisted; seeds custom request | `shortlistGate` `shortlist-radius.ts:87,186-188`; server refuses `shortlist/route.ts:75-79`; CTA `buildSimilarRequestHref`; CORRECTION: admin route bypasses gate (`:1-11`, `:89`) | PARTIAL | fail-CLOSED on unplaceable buyer/listing (`shortlist-radius.ts:183-185`; `route.ts:52-60`) | Apply gate to admin writer (R63); buyer-portal search cards render gate action | 4 | unit, integration | `shortlist-radius-gate.test.ts:102` kept; new admin-shortlist-gate test | none | `/buyer/search` cards show Shortlist for out-of-radius rows (server refuses) | TO EXTEND |
| R6 | MD §4a L412; HTML S[3].system | Candidate revalidated for price/availability/VIN/location/freshness before continuing; carries distance | `create-intent:76-84` counts rows; only `app/api/admin/buyers/[buyerId]/auction-vehicles/route.ts:76-97` checks availability; CORRECTION: `launch-auction/route.ts:216-228` (`LaunchAuctionPanel.tsx:249`) attaches with no check; no distance on ShortlistItem/AuctionVehicle (`schema.prisma:389-399,500-515`) | MISSING | admin attach refuses unavailable (`auction-vehicles/route.ts:76-97`) | `revalidateCandidate()` (re-run `shortlistGate` + price/VIN vs `listing_snapshot`) at Stage 4 exit, deposit create-intent and launch; stamp `distance_miles`/`revalidated_at` (schema R42) | 4 | unit, integration, concurrency | new `lib/services/shortlist/__tests__/candidate-revalidation.test.ts`; extend `auction-vehicle-availability.test.ts` to launch route | owner decision (Q7 revalidation timing) | create-intent row-count gate; launch-auction unguarded attach | TO IMPLEMENT |
| R7 | MD §4a L412 | Failed revalidation returns buyer to search with explanation; request stays open | `app/buyer/shortlist/page.tsx:41-88`, `components/buyer/ShortlistClient.tsx:238-300` "No longer available" only; availability = isActive && price>0 (`shortlist-availability.ts:23-28`) | PARTIAL | none | Extend with `dropped_reason` (price/VIN/location/freshness) + return-to-search UI; request remains open | 4 | unit, playwright, visual | candidate-revalidation.test.ts reasons; Playwright dropped candidate shows reason; shortlist visual baseline | none | none | TO EXTEND |
| R8a | MD §4a L414 | Custom request criteria fields (schema) | first-class on VehicleRequest: makePreference/modelPreference/yearMin/yearMax/maxBudgetCents/notes (`schema.prisma:1022-1032`); rest in notes META blob | PARTIAL | none | Phase 1: `body_type`, `drivetrain`, `exterior_colors[]`, `interior_colors[]`, `max_mileage`, `condition_preference`, `required_features[]`, `preferred_features[]`, `purchase_timeframe`, `down_payment_cents`, `delivery_preference` | 1 | integration | migration chain test; from-zero replay | production migration deploy | notes META parser | TO IMPLEMENT |
| R8b | MD §4a L414 | Form/service capture all criteria structurally | `app/buyer/requests/new/page.tsx:55-100` form; META blob `:309-331`; `requests/route.ts:44-53,188` folds condition into notes; drivetrain/colours/required-vs-preferred/pickup-delivery not captured | PARTIAL | none | Write structured columns; split required/preferred; stop notes round-trip; keep parser read-only for legacy rows | 4 | unit, integration, visual | new `app/api/buyer/requests/__tests__/criteria-capture.test.ts`; visual request form | none | `lib/services/vehicle-request/notes-parser.ts`; admin detail `meta.*` reads | TO EXTEND |
| R9a | MD §4a L416 | Criteria travel into dealer invitations | `ensureAuctionVehicleFromRequest` carries make/model/yearMin only (`dealer-invitation.service.ts:117-147`) | PARTIAL | none | Invitations carry `candidate_ids[]` + structured criteria (Phase 5 scope) | 5 | unit, integration | extend `lib/services/auction/__tests__` invitation payload test | none | `ensureAuctionVehicleFromRequest` fallback | TO EXTEND |
| R9b | MD §4a L416 | Criteria into offer validation and buyer comparison | none; features/mileage/colour never leave notes blob | MISSING | none | Offer validation fills `required_feature_matches`/`_mismatches` (Phase 1 columns); comparison renders them | 6 | unit, state-machine | offer validation suite `lib/services/offer/__tests__` | none | none | TO IMPLEMENT |
| R10a | MD §4a L416; HTML QUAL[0] | Vehicle missing a REQUIRED criterion flagged mismatch, never ranked equivalent (qualified results/match) | `inventory-match-score.ts:35` weights, `:70-77` priceScore, `:87-96` computeMatchScore — blend, no flag; price exclude in WHERE `inventory-match.service.ts:19`, `request-inventory-match.service.ts:71`; `criteriaWhere` duplicated with different semantics (`:16-21` vs `:64-73`) | MISSING | none | Required-vs-preferred evaluation with `mismatch_reasons` in qualified-results ranking; consolidate `criteriaWhere` | 4 | unit | extend `inventory-match-score.test.ts` + `request-inventory-match.test.ts`: required miss → flagged, ranked below | none | `inventory-match-refresh` cron → `VehicleRequestMatchResult` | TO IMPLEMENT |
| R10b | MD §4a L416 | Offer with required-criterion miss flagged in ranking | no offer→criteria check | MISSING | none | Persist `required_feature_mismatches` on offers; ranking never treats as equivalent | 6 | unit | best-price ranking suite | none | none | TO IMPLEMENT |
| R11a | MD §4b L418-426; HTML S[3].tables co_buyers | Co-buyer record table | no model/table (rg negative, correction 22) | MISSING | none | Phase 1 `co_buyers` (buyer_id, vehicle_request_id, name, email, phone, address, role, requested_by_primary_at, share_consent_at+version, is_required_signer; RLS on) | 1 | integration | migration chain test; RLS-enabled assertion | production migration deploy | none | TO IMPLEMENT |
| R11b | MD §4b L418-426; HTML S[3].note | Stage 4 co-buyer capture: no SSN/credit/lender app; consent verbatim; required-signer flag | public form boolean only (`app/api/public/request-vehicle/route.ts:106` → `Notification.metadata` `:603-609`; `components/public/RequestVehicleFormClient.tsx:309,465`) | MISSING | none | Buyer-portal capture route/service under `lib/services/vehicle-request`; migrate public boolean to `requested_by_primary_at`; no SSN field anywhere Lifecycle: §8.2a is canonical. | 4 | unit, integration, playwright, visual | new co-buyer capture test; `credit-applications-frozen.test.ts` scan covers co-buyer tree; Playwright election recorded | none | public `coBuyer` boolean in `Notification.metadata` | TO IMPLEMENT  |
| R11c | MD §4b L424 | Co-buyer reaches winning dealer at reaffirmation via secure handoff | none | MISSING | none | Handoff in `dealer_reaffirmations` confirmation (Phase 7) | 7 | integration, state-machine | reaffirmation suite asserts co-buyer payload only after CONFIRMED | none | none | TO IMPLEMENT |
| R11d | MD §4b L426 | Required co-buyer signer blocks signed status | none | MISSING | none | `e_sign_envelopes.signer_kind` CO_BUYER; Deal cannot reach signed without co-buyer envelope | 8 | state-machine, playwright | esign suite; Playwright buyer + co-buyer sign | `ESIGN_EXECUTED_ARTIFACT_ENABLED` activation (§13-D4) | none | TO IMPLEMENT |
| R12a | MD §4c L428-433; HTML S[3].tables trade_in_submissions | Trade packet columns | `TradeInSubmission` `schema.prisma:2036-2056` lacks vehicle_request_id, deal_id, lienholder_name, payoff_good_through_date, title_in_hand, title_state, has_second_key, photo_urls, bringing_to_pickup | PARTIAL | none | Phase 1 additive columns + `share_consent_at`, `preliminary_allowance_cents`, `verified_payoff_cents` | 1 | integration | migration chain test | production migration deploy | none | TO EXTEND |
| R12b | MD §4c L428-433 | Trade packet captured once and attached to the Vehicle Request | `submitTradeIn` `lib/services/trade-in/trade-in.service.ts:7-26` via `POST /api/buyer/trade-in` (`route.ts:6-19`); form `app/buyer/trade-in/page.tsx:52-70`; DUPLICATE public capture `BuyerOpportunity.tradeInDetails` (`unified-buyer-intake.service.ts:234-235`, `request-vehicle/route.ts:112-125`); `VehicleRequestFinancing.tradeIn` boolean | DUPLICATED | none | Extend form/service with new fields + photo upload; link `vehicle_request_id`; migrate `tradeInDetails` JSON into canonical row (dedupe buyer+VIN); one capture path | 4 | unit, integration, visual | new `lib/services/trade-in/__tests__/trade-packet.test.ts`; DB assertion one row per request | none | `BuyerOpportunity.tradeInDetails`; `VehicleRequestFinancing.tradeIn` | TO CONSOLIDATE |
| R12c | MD §4c L430 | Trade packet carried onto the Deal | `Deal` `schema.prisma:574-654` no trade columns; `TradeInSubmission` buyerId only; no dealer/deal/esign read path (corrections 9-10) | MISSING | none | Set `trade_in_submissions.deal_id` at Deal creation; handoff to dealer at reaffirmation; deal-stage trade screen | 7 | integration, state-machine | deal-lineage test asserts `deal_id`; reaffirmation payload includes packet | none | none | TO IMPLEMENT |
| R13 | MD §4c L432; HTML S[3].note | Every buyer-facing trade screen states dealership performs appraisal; estimate not an offer | widget disclaimer `components/buyer/TradeInValuationWidget.tsx:109,121`; form promises "Dealers will see your trade-in details" (`app/buyer/trade-in/page.tsx:114,146`) though only reads are buyer GET + purge `app/api/buyer/account/route.ts:66` | BROKEN | none | Add mandated sentence to form + deal-stage screens; remove the "dealers will see" promise until R12c exists; label `valuationCents` "estimate, not an offer" | 4 | unit, visual | new copy-assertion test over trade screens; visual trade-in page | none | `TradeInValuation` admin valuation model | TO EXTEND |
| R14 | MD Stage 4 Exit L435; HTML S[3].exit | Exit = complete criteria, co-buyer election, trade election recorded | no completeness state; `hasTradeIn` boolean only (`requests/route.ts:198`) | MISSING | none | Elections derived from `co_buyers`/trade rows + explicit election fields; completeness check consumed by Phase 3 §5a eligibility recheck | 4 | unit, state-machine | vehicle-request completeness test; Phase 3 eligibility test consumes it | none | none | TO IMPLEMENT |
| R15a | MD Stage 4 Fail L437 | DRAFT status exists | `VehicleRequestStatus` `schema.prisma:1654-1666` has no DRAFT | MISSING | none | Phase 1: enum + DRAFT, PAYMENT_REQUIRED, RADIUS_AUTHORIZATION_REQUIRED; partial unique open-request index includes DRAFT | 1 | integration | migration chain test; unique-index violation test in CI migration job | production migration deploy; §5.6 precondition cleanup (owner-run) | none | TO IMPLEMENT |
| R15b | MD Stage 4 Fail L437; HTML S[3].fail | Drafts do not spend on enrichment or contact dealerships | `POST /api/buyer/requests` creates SUBMITTED and intake pipeline runs enrichment/outreach off the row (`requests/route.ts:165-206`; `vehicle-request.service.ts:35-37`) | BROKEN | none | Lane 1 intake handler excludes DRAFT from enrichment/outreach/intake-reconcile; promotion only at Stage 4 exit → Phase 3 payment | 2 | unit, integration | intake handler test: DRAFT row → zero enrichment/outreach calls | none | intake-reconcile cron / `processBuyerOpportunityIntake` | TO IMPLEMENT |
| R15c | MD Stage 4 Fail L437 | Request persists as DRAFT until Stage 4 exit; no reminders/auctions for drafts | `createVehicleRequest` writes SUBMITTED (`vehicle-request.service.ts:35-37`) | BROKEN | none | Stage 4 writes DRAFT; exit sets PAYMENT_REQUIRED; reminder enrolment (Phase 3) and auction creation key off non-draft statuses | 4 | state-machine, integration | vehicle-request state-machine suite: DRAFT → PAYMENT_REQUIRED only on completeness | none | none | TO IMPLEMENT |
| R16 | MD §22a L1050-1057; HTML INV[1], INV[2] | Two surfaces: catalogue (sweep) + qualified results (live query) | catalogue `(public)/inventory/page.tsx` via `inventory-sync-full` cron (`vercel.json:96-97`, ≤10 calls; `app/api/cron/inventory-sync-full/route.ts:9-11`); qualified surface absent | PARTIAL | none | Build qualified-results surface (R32); keep catalogue separate and labelled | 4 | unit, playwright, visual | Playwright approved buyer → qualified results; visual inventory pages | MarketCheck terms §13-D8 (cache) | `/buyer/search` | TO IMPLEMENT |
| R17a | MD §22a L1059-1061; HTML INV[3] | Criteria-hash cache table | none; generic `SearchCache` (`schema.prisma:4447-4467`, used by compound-search + geocoding) exists (correction 21) | MISSING | none | Evaluate `SearchCache` `searchType="qualified_results"` before creating `inventory_query_cache`; create only if §13-D8 allows | 1 | integration | migration test (if created); reuse decision recorded in Phase 1 plan | MarketCheck terms §13-D8 | none | OWNER-GATED |
| R17b | MD §22a L1061 | Repeat queries served from cache; spend once per prequalified buyer | none | MISSING | none | qualified-results service hashes normalised criteria, TTL, draws ledger only on miss; uncached if D8 denies | 4 | unit, integration | `qualified-results.test.ts`: identical criteria → one provider call | MarketCheck terms §13-D8 | none | TO IMPLEMENT |
| R18 | MD §22a L1067; HTML INV[4] | 100-mile ceiling is AutoLenis policy in code | `SHORTLIST_RADIUS_MILES=100` (`shortlist-radius.ts:87`, isolated `:74-77`); `clampRadius` `inventory-source-config.service.ts:78-86` applied `:206`; adapter clamp `marketcheck.adapter.ts:353`; admin tool sends no radius (`search-tool/run/route.ts:90-97`) | ALREADY CORRECT | two independent clamps; `clampRadius(null)`→DEFAULT | Keep; qualified-results imports the shortlist constant; admin tool via adapter (R57) | 4 | unit | `shortlist-radius-gate.test.ts`; `marketcheck-market-config.test.ts` clamp case | none | admin search tool radius omission (provider default UNVERIFIED) | ALREADY PRESENT (VERIFY IN PHASE) |
| R19 | MD §22a L1068; HTML INV[5] | Qualified results filter by condition preference and around approved amount | sweep hardcodes `car_type: "used"` (`adapter:348`), `price_min: "1"` (`:375`), optional `price_max` (`:381`); no buyer condition reaches any query | PARTIAL | none | qualified-results query takes `condition_preference` (New/Used/Either) + generous budget band (R37) | 4 | unit | `qualified-results.test.ts`: params include car_type per preference | none | none | TO IMPLEMENT |
| R20 | MD §22a L1069; HTML INV[6] | Distance on every card; ZIP prompt before distances/shortlist actions when no stored location | cards show distance (`(public)/inventory/page.tsx:386-391`; `[vehicleId]/page.tsx:337-342`; `components/buyer/BuyerSearchClient.tsx:594-597`); zip-prompt `:262-275`; BUT public catalogue uses `?zip=` + static `lookupZip` (`lib/utils/zip-coords.ts:310-313`, 128 entries); `/api/buyer/search` `route.ts:43-46,114` resolves stored ZIP via static table → silent null; shortlist page no distance; buyer search cards no ZIP prompt/gate (`BuyerSearchClient.tsx:603-618`) | PARTIAL | shortlist API fails closed on unplaceable buyer | Prefer stored `buyer.zip` + `geocodeZip` everywhere; distance on shortlist candidates; ZIP prompt gates Shortlist button in buyer portal; retire lookupZip-only paths | 4 | unit, playwright, visual | new geocode-resolution test; Playwright no-ZIP buyer sees prompt before shortlist; visual shortlist page | `GOOGLE_GEOCODING_API_KEY` declared and set in production | `lib/utils/zip-coords.ts` static table | TO CONSOLIDATE |
| R21 | MD §22a L1070; HTML INV[7] | In radius → Add to shortlist, up to five | as R3/R5 (`shortlist/route.ts:62-79,91-93`); admin bypass | PARTIAL | cap counts AVAILABLE | see R3, R5, R63 | 4 | unit, integration | `shortlist-radius-gate.test.ts` | none | admin shortlist route | TO EXTEND |
| R22 | MD §22a L1071; HTML INV[8] | Out-of-radius card: plain distance, pre-filled request; never labelled qualified/local/confirmed/held/auction-eligible | public correct: `buildSimilarRequestHref` `shortlist-availability.ts:104-121`, `REQUEST_PREFILL_KEYS` `:90-93`, hydrated `requests/new/page.tsx:164-199`; CORRECTION: `components/buyer/VehicleDetailPanel.tsx:82-86` "Eligible for private 48-hour auction" + live Add to Shortlist `:100-108` for every LANE_1 row; `app/buyer/inventory/[vehicleId]/page.tsx:262` never runs `shortlistGate`; `ShortlistClient.tsx:181,205-209` AUCTION READY asserted at write | BROKEN | server refusal still holds | Run `shortlistGate` on buyer detail page; drop auction-eligible chip or derive from gate+revalidation; render gate action on buyer search cards | 4 | unit, playwright, visual | new buyer-detail-gate test; copy-prohibition test; visual buyer detail | none | `readinessState` "AUCTION_READY" default | TO EXTEND |
| R23a | MD §22a L1072; HTML INV[9] | Sourcing not limited by catalogue/query radius (isolation) | `shortlist-radius.ts:74-77` import isolation; `MAX_RADIUS_MILES` only in `lib/services/inventory` + admin tool | ALREADY CORRECT | import isolation enforced | Keep; add test guarding the import boundary | 4 | unit | import-boundary test in `lib/services/shortlist/__tests__` | none | none | PRESERVED STRONGER SAFEGUARD |
| R23b | MD §22a L1072, L1102-1108 | Sourcing ladder 100→150→250 then buyer authorisation | `lib/services/auction/coverage.service.ts:38` `RADIUS_TIERS = [25, 50, 100, 150]` | PARTIAL | none | Server-side ladder 100/150/250 + RADIUS_AUTHORIZATION_REQUIRED (Phase 5 scope) | 5 | unit, state-machine | sourcing ladder test in `lib/services/auction/__tests__` | none | `coverage.service.ts` tiers | TO EXTEND |
| R24a | MD §22a L1073; HTML INV[10] | Rooftop identity columns for ingest | `dealer_rooftops` `schema.prisma:4007-4040` no MC id; `mcRooftopId/mcDealerId` on items (`:986-987`) unusable as join key; `inventory_items.rooftop_id` + index + FK are PRESENT in production (WORKFLOW 6.1 class (b)); only the ledger row is missing, so the file header's "WRITTEN BUT NOT APPLIED" is stale (D1, D33) | PARTIAL | none | Phase 1: `dealer_rooftops.mc_rooftop_id` (unique where not null); `inventory_items` listing_id, provider_last_seen_at, days_on_lot, mc_website_id, mc_location_id, mc_category, external_dealer_website | 1 | integration | migration chain test; apply 20261104/20261105 first | production migration deploy (20261104, 20261105, Phase 1 wave) | env-tier config until applied | TO EXTEND |
| R24b | MD §22a L1073; HTML INV[10], FINDINGS[7] | Provider dealer object requested + captured on ingest; listing resolves to real rooftop; pool grows | adapter reads `listing.dealer.*` (`marketcheck.adapter.ts:74-88,394-424`); orchestrator persists (`orchestrator.ts:353-369,384-399,423-438,459`); `resolveListingRooftops` MATCH-never-MINT (`listing-rooftop-resolution.service.ts:14,59,96`); include flags absent (rg `include_dealer\|include_mc_dealership\|mc_category` → none); `inventory_items.dealer_id` NULL by design | PARTIAL | MATCH-never-MINT; ambiguous left unlinked; Null-Island coords rejected (`adapter:114-134`) | Request `dealer`+`mc_dealership`+`build`; read ids from `mc_dealership`; reject `dist > radius`; dedupe on `mc_rooftop_id`; unmatched rooftops minted only via dealer-database-ingestion (match-then-mint) | 4 | unit, integration | `marketcheck-dealer-provenance.test.ts` re-fixtured to real payload; `listing-rooftop-resolution.test.ts` mint-through-ingestion case | live payload shape (default include UNVERIFIED — resolved by sending flags) | `externalDealerName` LANE_2 name-match (`orchestrator.ts:92-105`) | TO EXTEND |
| R25 | MD §22a L1074; HTML INV[11] | Listing is a specification; availability confirmed only by offer and reaffirmation | copy `[vehicleId]/page.tsx:332`; buyer detail `app/buyer/inventory/[vehicleId]/page.tsx:32-34,252-253` | ALREADY CORRECT | none | Keep copy; reaffirmation confirmation is Phase 7 | 4 | unit, visual | copy-prohibition test; visual detail page | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| R26 | MD §22a L1075; HTML INV[12] | Listed price is the benchmark for cross-vehicle OTD ranking | `priceCents` + `priceHistory` persisted (`orchestrator.ts:332-334`); "% vs market average" from catalogue mean (`[vehicleId]/page.tsx:50-55,149-152`); no ranking | PARTIAL | none | Snapshot listed price/VIN in `auction_vehicles.listing_snapshot` at candidate creation; ranking consumes it (R41, Phase 6) | 4 | unit | candidate creation test asserts `listing_snapshot.priceCents` | none | catalogue-mean statistic on detail page | TO IMPLEMENT |
| R27 | MD §22a L1076; HTML INV[13] | 7-day freshness note; 30-day view-only, not shortlistable | `STALE_FLAG_WINDOW_MS` 7d / `SHORTLIST_FRESHNESS_WINDOW_MS` 30d (`shortlist-radius.ts:90-93,150-156`); server refuses STALE_LISTING (`route.ts:22-23,75-79`); CONFLICT: `FRESHNESS_WINDOW_MS` 48h (`inventory-eligibility.ts:23`) drives stale sweep (`stale-sweep.service.ts:70`) and `isActive` filters hide rows at 48-72h once `INVENTORY_STALE_SWEEP_MODE=enforce` (`.env.example:204`, default dry_run); buyer search cards no note | PARTIAL | never-seen = EXPIRED; dealer/admin rows exempt from freshness not radius | Owner decision Q2: align clocks (sweep deactivates at 30d; eligibility 7d) or document 48h = sourcing eligibility only; add note to buyer search cards | 4 | unit | extend `stale-sweep.test.ts` + `shortlist-radius-gate.test.ts:134,141` with aligned windows | owner decision (freshness clocks); production `INVENTORY_STALE_SWEEP_MODE` | 48h cadence comment `inventory-stale-sweep/route.ts:93-104` | OWNER-GATED |
| R28 | MD §22a L1077, L1091; HTML INV[14], QUAL[2] | Zero/thin results → pre-filled custom request offered alongside | `inRadiusCount === 0` panel `(public)/inventory/page.tsx:276-297` no pre-fill; buyer search empty state `BuyerSearchClient.tsx:855-936` no pre-fill; no thin threshold | PARTIAL | none | Define thin threshold; render request CTA alongside results; pre-fill from current criteria via `REQUEST_PREFILL_KEYS` | 4 | unit, playwright, visual | thin-results test; Playwright thin results show CTA + pre-filled form | none | none | TO EXTEND |
| R29 | MD §22a L1078; HTML INV[15] | Buyer-facing copy never says verified/confirmed/held | relabelled public/buyer search (`page.tsx:45-49`, `[vehicleId]/page.tsx:25-29`, `BuyerSearchClient.tsx:47-51`); VIOLATIONS: `lib/constants.ts:127` LANE_1 "Verified" rendered `ShortlistClient.tsx:179,204`; `(public)/inventory/page.tsx:312` "verified dealers"; `[vehicleId]/page.tsx:264` "Contract Shield verifies all listed features"; `VehicleDetailPanel.tsx:83-85` auction-eligible | PARTIAL | none | Rename lane label (check admin uses), fix three strings, remove chip; add prohibited-word test over buyer-facing inventory components | 4 | unit, visual | new copy-prohibition test in `lib/services/shortlist/__tests__`; visual shortlist/detail | none | `INVENTORY_LANES` constant + admin lane UI; per-page LANE_CONFIG copies | TO EXTEND |
| R30 | MD §22a L1079; HTML INV[16], FINDINGS[6] | Sweep + query on one call budget; Ops alert before ceiling | ledger `inventory_sources` (`schema.prisma:2435-2439`); `tryConsumeCall` atomic `updateMany` `inventory-call-budget.service.ts:94-124` fail-closed `:117-122`; `rollCycleForward` `:63`; alerts `inventory-budget-alert.service.ts:17,42-47,100-121` raised `orchestrator.ts:549-583`; admin tool draws (`search-tool/run/route.ts:65-86`) but unmetered on env-tier (`:80-85`); no query consumer | PARTIAL | draw-before-dispatch, no refund, fail-closed, roll-forward cycle, min()-ed ceilings (`inventory-source-config.service.ts:224-231`) | qualified-results draws via `makeCallBudget`; raise alert from `tryConsumeCall` callers; remove env-tier unmetered path after migration | 4 | unit, integration | extend `inventory-call-budget.test.ts`, `inventory-budget-alert.test.ts`, `inventory-search-tool-budget.test.ts:83-115` with query consumer | production migration deploy (ledger columns) | env-tier `INVENTORY_SWEEP_ZIP`/`MARKETCHECK_MONTHLY_CALL_BUDGET` | TO EXTEND |
| R31 | MD §22a L1079; HTML INV[16] | Provider failure shown as "search unavailable" + request path, never empty market | buyer search DB-only, `search-error` branch `BuyerSearchClient.tsx:546-556` no CTA; admin tool distinguishes (`run/route.ts:104-139`); sweep outcomes distinct (`IInventoryAdapter.ts:52-65`; `rollUpOutcome` `orchestrator.ts:190-213`) | PARTIAL | outcome taxonomy distinct from ZERO_RESULTS | Map provider failure/budget exhaustion to unavailable state + request CTA in qualified results and `search-error` branch | 4 | unit, playwright | `qualified-results.test.ts` provider 5xx → UNAVAILABLE not empty; Playwright failure state | none | none | TO EXTEND |
| R32 | MD §22a L1085; HTML QUAL[0], S[3].system[1] | After approval: ZIP + approved amount + criteria → live provider query; required ahead of preferred; distance/freshness/detail/status per result | none (R2/R10/R17); ZIP prerequisite RESOLVED: `lib/services/prequal/prequal.service.ts:300-309` `backfillBuyerLocation` | MISSING | none | Implement `lib/services/inventory/qualified-results.service.ts` reusing `MarketCheckAdapter.search` (zip/radius/price band/make/model/years/car_type), `makeCallBudget`, `gateCatalogue` rows, prequal gate | 4 | unit, integration, playwright | `qualified-results.test.ts`; Playwright approved buyer → results with distance + freshness | MarketCheck terms §13-D8 (cache) | `/buyer/search` DB query | TO IMPLEMENT |
| R33 | MD §22a L1086; HTML QUAL[1] | System never auto-saves to shortlist | as R3 — admin writer exception | PARTIAL | none | see R3/R63 | 4 | unit | shortlist writer inventory test: only buyer route + gated admin override | none | admin shortlist route | TO EXTEND |
| R34 | MD §22a L1087; HTML QUAL[2] | Thin/absent → pre-filled request confirmed alongside results | as R28 | PARTIAL | none | see R28 | 4 | playwright | as R28 | none | none | TO EXTEND |
| R35 | MD §22a L1088; HTML QUAL[3] | Sourcing only after $99 settles; ladder independent of catalogue | no sourcing from shortlist writes (rg negative); intake spends off SUBMITTED (R15b); settlement creates auction immediately (`app/api/webhooks/stripe/route.ts:206-214,262`) | PARTIAL | none | Settlement opens sourcing case (Phase 3); ladder Phase 5; DRAFT no-spend Phase 2 | 3 | integration, playwright | Phase 3 Playwright: settlement opens case, no auction row created | none | deposit-settlement immediate auction create + invite (§8.4) | TO IMPLEMENT |
| R36 | MD §22a L1090 | Before prequal: general catalogue only; nothing presented as qualified | nothing labelled qualified (rg → only "qualified dealers" `LiveAuctionView.tsx:144`); budget banners APPROVED-only (`app/buyer/search/page.tsx:40-57`; `BuyerSearchClient.tsx:320-400`) | ALREADY CORRECT | zero/undetermined ceiling never $0 (`search/route.ts:49-67`) | Keep; R64 gate preserves it once R32 exists | 4 | unit | qualified-results gate test | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| R37 | MD §22a L1094-1098; HTML BUDGET[0-2] | Filter generously; near/over-ceiling shown; enforce at offer/selection/contract only | hard cap `/api/buyer/search` `route.ts:64-82` `budgetGuarded`; disabled button `(public)/inventory/[vehicleId]/page.tsx:145,379-386`; `VehicleDetailPanel.tsx:106,112-114` disabled when over; hard exclude `inventory-match.service.ts:19`; `budget-gating.test.ts` pins strict | BROKEN | strict browse-time cap (STRICTER than spec); zero ceiling never $0 | Owner decision §13-D16; if the spec wins: headroom band (e.g. ≤1.10×) + "OTD checked at offer" flag. **Split by §11.6 ruling 9:** Phase 4 changes only the browse filter; the ceiling is enforced at offer validation and selection in Phase 6 (offers/B2, offers/B12) and at contract request in Phase 8 (contract/C-01). | 4 | unit, playwright | rewrite `app/api/buyer/search/__tests__/budget-gating.test.ts` to headroom; Playwright over-ceiling listing visible + flagged | owner decision (approved-amount headroom) | `budgetGuarded`/`maxBudgetCents` fields; `budget-fit-indicator` | OWNER-GATED |
| R38 | MD §22a L1102-1108 | Two ceilings: shortlist/qualified 100 (policy); sourcing ladder own records | shortlist 100 correct (R18); ladder stops at 150 (R23b) | ALREADY CORRECT | policy constant isolated | Keep; ladder in R23b | 4 | unit | `shortlist-radius-gate.test.ts` | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| R39 | MD §22a L1110-1112; HTML S[3].rec | Candidate model: ≤5 candidates per request; auction covers all; offers bind; selection collapses to one VIN | manual admin copy `components/admin/LaunchAuctionPanel.tsx:249` → `launch-auction/route.ts:216-228` (unrevalidated, no distance); webhook creates auction with no vehicles (`stripe/route.ts:206-214`) then `inviteDealersToAuction` `:262` synthesises one make-signal row (`dealer-invitation.service.ts:117-147`); `Offer` `schema.prisma:533-565` no candidate column | PARTIAL | admin attach availability check (`auction-vehicles/route.ts:76-97`) | Phase 4: create `auction_vehicles` from shortlist at Stage 4 exit with `vehicle_request_id`, `distance_miles`, `listing_snapshot`, `candidate_status`; offers bind + selection collapse in Phase 6 | 4 | unit, integration, concurrency | new `lib/services/auction/__tests__/candidate-binding.test.ts`; DB trigger cap test; Playwright shortlist 5 → 5 candidates | none | `ensureAuctionVehicleFromRequest` (keep as custom-request fallback); launch-auction manual attach | TO IMPLEMENT |
| R40 | MD §22a L1116 | Each candidate → holding rooftop + comparable rooftops; union/dedupe recording served candidates | nothing consumes `rooftop_id` in `lib/services/auction` (rg `rooftopId` negative) | MISSING | none | Sourcing case consumes `inventory_items.rooftop_id`; `auction_invitations.candidate_ids[]` | 5 | unit, integration | sourcing-case test: candidate rooftop first, dedupe per rooftop | production migration 20261105 (`rooftop_id`) | none | TO IMPLEMENT |
| R41 | MD §22a L1117-1121 | Per-candidate offer cap; per-/cross-candidate ranking vs listed price; zero-offer; selection collapse | none | MISSING | none | Phase 6 offer model/ranking with `auction_vehicle_id` | 6 | unit, concurrency | offer/best-price suites; `test:concurrency` selection | none | none | TO IMPLEMENT |
| R42 | MD §32 L1517 | `auction_vehicles` carries ≤5 candidates with distance; offers bind (schema, P0) | `AuctionVehicle` `schema.prisma:500-515` lacks distance/request link/snapshot; `Offer` lacks `auction_vehicle_id` (rg negative); manual admin copy exists (correction 5) | PARTIAL | none | Phase 1: `auction_vehicles` + vehicle_request_id, distance_miles, candidate_status, listing_snapshot, revalidated_at, dropped_reason; `offers.auction_vehicle_id`; per-request cap trigger | 1 | integration | migration chain test; trigger raise test in CI migration job | production migration deploy | none | TO IMPLEMENT |
| R43a | MD §32 L1518 | Five-cap + in-radius enforced at write time (DB) | `shortlist_items` DDL: PK, unique(shortlist_id, inventory_item_id), FK shortlists only (init migration:174-182,759,867); no FK on `inventory_item_id` (`shortlist-availability.ts:5`); no trigger | PARTIAL | app cap counts AVAILABLE | Phase 1: `shortlist_items_enforce_cap()` BEFORE INSERT trigger (FOR UPDATE parent, P0001 at five); FK `inventory_item_id` (SET NULL vs RESTRICT decision); `distance_miles` column | 1 | integration, concurrency | DB-backed trigger test (CI migration job); shortlist cap concurrency test (Phase 4 gate) | production migration deploy; owner decision (FK on-delete) | none | TO IMPLEMENT |
| R43b | MD §32 L1518 | Single gated shortlist writer/deleter | 3 writers: buyer `route.ts:99` (gated), admin `route.ts:89` (cap only), `shortlist.service.ts:41` `addToShortlist` (no callers); 3 deleters: `[itemId]/route.ts:13`, `route.ts:131`, `shortlist.service.ts:47`; two "has shortlist" definitions (`create-intent:76-78` rows vs `journey-status/route.ts:50` + `app/buyer/layout.tsx:215` `countAvailableItems`) | DUPLICATED | fail-closed gate | One service function wrapping `shortlistGate` used by buyer + admin routes; delete dead `addToShortlist`/`removeFromShortlist`/`getShortlistReadiness`; create-intent uses `countAvailableItems` | 4 | unit, integration | writer inventory test; extend `shortlist-radius-gate.test.ts` for admin path | none | `shortlist.service.ts` dead functions; create-intent row-count gate | TO CONSOLIDATE |
| R44 | MD §32 L1519 | `inventory_items.last_seen_at` gates shortlist at 7/30 days (P1) | implemented in `shortlistGate`; undermined by 48h sweep (R27) | PARTIAL | never-seen = EXPIRED (`freshnessOf` `:151`) | Resolve with R27; use `provider_last_seen_at` (Phase 1 column) as the clock | 4 | unit | `shortlist-radius-gate.test.ts` freshness cases with aligned windows | owner decision (freshness clocks) | 48h `FRESHNESS_WINDOW_MS` | OWNER-GATED |
| R45 | MD §32 L1520 | `VehicleRequestOffer` kept as staff intake, must write canonical `offers` row (P1) | `Offer` `:533-565` no link; `Deal.vehicleRequestOfferId` `:578` (not Offer); no `prisma.offer.create` in `lib/services/vehicle-request` (correction 6) — staff path lands on Deal directly | MISSING | none | `createAndSendOffer` writes canonical `Offer` in the same transaction (Phase 6 scope) | 6 | unit, integration | vehicle-request offer test asserts Offer row + lineage | none | `vehicle_offers`/`dealer_offer_submissions` parallel models (§8.4: keep as intake) | TO IMPLEMENT |
| R46 | MD §33 step 28 L1556 | Radius-filter the shortlist action; specification path; ≤5 candidates on request | first two landed (ecb1ada: R5, R22 public); candidates on request PARTIAL (R39) | PARTIAL | server gate | Complete via R4b/R39/R42/R43 | 4 | unit, integration, playwright | Phase 4 Playwright: shortlist 5 → sixth refused → out-of-radius card offers request | none | none | TO EXTEND |
| R47 | MD §33 step 29 L1557 | Sourcing resolves candidates against rooftop pool | depends on R24/R40 | MISSING | none | Phase 5 sourcing case | 5 | integration | as R40 | production migration 20261105 | none | TO IMPLEMENT |
| R48 | MD §33 step 30 L1558; HTML FINDINGS[0-2] | Sweep targeted at served markets, sized as shop window, run rate matched | market from `inventory_sources` center_zip/radius/filter_* → env → NOT_CONFIGURED (`inventory-source-config.service.ts:148-238`; `adapter:160-174`); daily ≤10 calls (`vercel.json:96-97`); DFW repoint in 20261104 NOT APPLIED; single row `@@unique([type, name])` (`schema.prisma:2447`); markets admin route manages `MarketCoverage` only (`app/api/admin/inventory/markets/route.ts:11,21`, correction 20) | PARTIAL | no silent default ZIP; `is_active` kill switch (`:190-193`) | Apply migrations; multi-market: one MARKETCHECK source row per served market or a markets table (owner Q6); run-size anomaly → Ops exception | 4 | unit, integration | `marketcheck-market-config.test.ts` multi-market; `sync-yield.test.ts` anomaly → `raiseException` | production migration deploy (20261104/20261105); owner decision (multi-market model) | `INVENTORY_SWEEP_ZIP` env tier; `inventory-sync-priority` unscheduled route | TO EXTEND |
| R49 | MD §33 step 31 L1559 | Live qualified query cached on criteria hash; rooftop captured from every listing | query/cache MISSING (R17/R32); rooftop capture sweep-only (R24) | MISSING | none | R32 + R17b + R24b | 4 | unit, integration, playwright | as R32/R17b/R24b | MarketCheck terms §13-D8 | none | TO IMPLEMENT |
| R50 | MD Appendix L1590-1607; HTML FINDINGS[1] | Market size informs sweep sizing | cap 500 rows/10 calls (`inventory-source-config.service.ts:32-36`) | ALREADY CORRECT | compiled ceilings min()-ed | Keep | 4 | unit | `marketcheck-pagination.test.ts` | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| R51 | MD Appendix; HTML FINDINGS[7] | Dealer data captured; null dealer reference fixed durably | dealer object persisted + `rooftop_id` (R24); `inventory_items.dealer_id` stays NULL by design; `dealer_rooftops` lacks MC id; migration 20261105 NOT APPLIED | PARTIAL | MATCH-never-MINT | R24a/R24b | 4 | integration | `listing-rooftop-resolution.test.ts`; DB assertion `rooftop_id` populated after sweep | production migration 20261105 | none | TO EXTEND |
| R52 | MD Appendix; HTML FINDINGS[0] | NY literal removed; no default geography | `adapter:24-27,350`; `marketcheck-market-config.test.ts` | ALREADY CORRECT | NOT_CONFIGURED makes zero calls (`adapter:160-174`) | Keep | 4 | unit | `marketcheck-market-config.test.ts` | none | none | PRESERVED STRONGER SAFEGUARD |
| R53 | MD Appendix; HTML FINDINGS[2] | Quota: one daily sweep + ledger | R30/R48 (`vercel.json:96-97`; `orchestrator.ts:240`) | ALREADY CORRECT | ledger draw-before-dispatch | Keep | 4 | unit | `inventory-call-budget.test.ts` | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| R54 | MD Appendix; HTML FINDINGS[3] | Silent 429s must alert | DEFERRED → healthScore 0 (`orchestrator.ts:487`; `computeHealthScore` `:128-138`) → <70 alert `:587-595`; budget alerts pre-cap; per-run DEFERRED itself non-alerting | ALREADY CORRECT | health alert | Phase 4: read `Retry-After`/`Quota-*`; classify 422 by message; run-size anomaly → Ops exception via `raiseException` | 4 | unit | orchestrator health test; `sync-yield.test.ts` | none | `withCronRun` alerting (observability area) | TO EXTEND |
| R55 | MD Appendix; HTML FINDINGS[4] | Stale rows reachable by sweep predicate | predicate with NULL branches, LANE_1-without-dealer sweepable, dry_run default, 150-row breaker (`stale-sweep.service.ts:43,52-55,69-111,206-220`) | ALREADY CORRECT | dry_run default; blast-radius breaker; deactivated ids recorded | Keep; window alignment per R27 | 4 | unit | `stale-sweep.test.ts` | `INVENTORY_STALE_SWEEP_MODE=enforce` (owner flips) | none | PRESERVED STRONGER SAFEGUARD |
| R56 | MD Appendix; HTML FINDINGS[5] | Run size is a range — no fixed threshold | `sync-yield.ts:25,33,41,44,68-69,79` `classifyYield` uses provider `num_found` | ALREADY CORRECT | only downgrades, never upgrades | Keep | 4 | unit | `sync-yield.test.ts` | none | none | PRESERVED STRONGER SAFEGUARD |
| R57 | MD Appendix; HTML FINDINGS[6] | Second consumer metered; one provider client | admin tool draws from ledger (`search-tool/run/route.ts:65-86`) BUT inline fetch to `marketcheck-prod.apigee.net` (`:99`) vs adapter `api.marketcheck.com` (`:383`); no radius clamp; no dealer object; `add/route.ts:252-278` writes LANE_3 without dealer; unmetered on env-tier | DUPLICATED | ledger draw | Route admin tool through `MarketCheckAdapter` (clamp, dealer capture, single host); retire apigee client (§8.4, Phase 4) | 4 | unit, integration | `inventory-search-tool-budget.test.ts` extended to adapter path; rg assertion no second host | none | admin inventory search tool inline provider client | TO CONSOLIDATE |
| R58 | HTML S[3].system (headroom flag) | Filter on listing price with headroom; flag results whose realistic OTD would breach ceiling | hard filter (R37); no flag | MISSING | strict cap (owner) | With R37 decision: headroom band + over-ceiling flag on card | 4 | unit, visual | `budget-gating.test.ts` headroom + flag; visual card state | owner decision (approved-amount headroom) | `budgetGuarded` | OWNER-GATED |
| R59 | HTML S[3].system (catalogue separate) | Every listing shown in general catalogue, separate from qualified results | `gateCatalogue` no-filter (`shortlist-radius.ts:257-297`); `lib/services/shortlist/__tests__/catalogue-no-filter.test.ts` | ALREADY CORRECT | `GateResult.visible` always true (test-pinned `:124-134`) | Keep; qualified surface labelled distinctly | 4 | unit, visual | `catalogue-no-filter.test.ts` | none | none | PRESERVED STRONGER SAFEGUARD |
| R60 | HTML S[3].system (attach candidates), S[3].rec | Each shortlisted vehicle attached to the one Vehicle Request as candidate, ≤5, with distance | R4b/R39/R43 | MISSING | none | R4b + R39 + R42/R43a | 4 | integration, playwright | candidate-binding test; Playwright 5 candidates with distance | none | none | TO IMPLEMENT |
| R61 | HTML S[3].tables | Tables `co_buyers`, `shortlists`, `auction_vehicles` present | `co_buyers` MISSING; `shortlists`/`auction_vehicles` present | MISSING | none | R11a | 1 | integration | migration test lists `co_buyers` | production migration deploy | none | TO IMPLEMENT |
| R62 | MD §22a L1070-1072; HTML INV[6-8] (uncovered, correction 13) | Distance is a label, never a filter — legacy public inventory API | `app/api/public/inventory/route.ts:75-78` bounding box + `:135-136` drops rows with null/over-radius distance; no consumer (rg negative) | BROKEN | none | Delete route or route through `gateCatalogue`; add no-consumer assertion | 4 | unit | `catalogue-no-filter.test.ts` extended to route, or route-deleted test | none | `app/api/public/inventory` (dead-but-live) | TO CONSOLIDATE |
| R63 | MD §4a L410, §22a L1086 (uncovered, corrections 1-3) | Admin shortlist writer honours radius/freshness/availability gate | `app/api/admin/buyers/[buyerId]/shortlist/route.ts:80-93` cap only, no gate, no test | BROKEN | audited `SHORTLIST_ITEM_ADDED` `:96-106` | Wrap in shared gated service (R43b) with explicit override reason recorded; test | 4 | unit, integration | new `app/api/admin/__tests__/admin-shortlist-gate.test.ts` | none | admin shortlist route | TO EXTEND |
| R64 | MD §22a L1061; HTML INV[3] (uncovered) | Live query spent only for a buyer who completed prequalification | none (query unbuilt); `/api/buyer/search` `route.ts:20-23` auth only | MISSING | none | qualified-results service requires `isPrequalValid` before any ledger draw | 4 | unit | `qualified-results.test.ts`: unapproved buyer → no provider call, no draw | none | none | TO IMPLEMENT |

## Owner-gated items

- Production deploy of the Phase 1 additive wave (`§13-D1` ledger reconciliation first, then `prisma migrate deploy`) ; migrations `20261104` / `20261105` are already **applied** in production — every object of both was probed PRESENT (WORKFLOW 6.1 class (b)); only their ledger rows are missing, which `D1` records, so no deploy of them is needed (R4a, R8a, R11a, R12a, R15a, R17a, R24a, R30, R40, R42, R43a, R47, R48, R51, R61); §5.6 open-request precondition cleanup is owner-run (R15a).
- `§13-D8` MarketCheck terms — whether qualified results may be cached on a criteria hash (`inventory_query_cache` / `SearchCache` reuse) (R2, R16, R17a, R17b, R32, R49).
- Owner decision Q1 — approved amount at browse time: keep the current strict hard cap or adopt the spec's generous headroom band + over-ceiling flag (R37, R58).
- Owner decision Q2 — freshness clocks: reconcile 48h `FRESHNESS_WINDOW_MS` (sweep/eligibility) with the 7/30-day shortlist windows; production value of `INVENTORY_STALE_SWEEP_MODE` (R27, R44, R55).
- Owner decision Q4 — when the shortlist creates/attaches the VehicleRequest (first candidate vs deposit) (R4b).
- Owner decision Q6 — multi-market sweep model (one `inventory_sources` row per market vs markets table) (R48).
- Owner decision Q7 — candidate revalidation timing (create-intent, settlement, launch, or all) (R6).
- Owner decision — `shortlist_items.inventory_item_id` FK on-delete behaviour (SET NULL vs RESTRICT) (R43a).
- `GOOGLE_GEOCODING_API_KEY` declared in `env.d.ts`/`.env.example` and set in production (R20).
- `ESIGN_EXECUTED_ARTIFACT_ENABLED` activation (`§13-D4`) for the co-buyer signing gate (R11d).
- Live MarketCheck payload verification (dealer object default inclusion, `price_min=1`, provider default radius) — resolved in code by sending the include flags explicitly and clamping radius, but not verifiable in this session (R18, R24b).

## Legacy paths

- `/buyer/search` + `/api/buyer/search` (catalogue search with 50-mile default filter, static `lookupZip`, budget hard cap) — becomes the general catalogue or is superseded by qualified results (R1, R2, R16, R20, R32, R37).
- `app/api/public/inventory/route.ts` — dead-but-live public endpoint that filters by radius and drops null-coordinate rows (R62).
- Admin inventory search tool inline provider client (`marketcheck-prod.apigee.net`, unclamped, no dealer capture) — retired in Phase 4 per §8.4 (R18, R57).
- `lib/services/shortlist/shortlist.service.ts` dead `addToShortlist`/`removeFromShortlist`/`getShortlistReadiness`; duplicate deleters; `deposit/create-intent` shortlist row-count gate vs `countAvailableItems` (R43b, R6).
- `app/api/admin/buyers/[buyerId]/shortlist` ungated admin writer; `app/api/admin/buyers/[buyerId]/launch-auction` unrevalidated shortlist → `auction_vehicles` copy (R3, R5, R6, R39, R63).
- `ensureAuctionVehicleFromRequest` make-signal AuctionVehicle (`dealer-invitation.service.ts:117-147`) — keep as fallback for custom requests (R9a, R39).
- Deposit settlement → immediate auction create + invite (`app/api/webhooks/stripe/route.ts:206-262`) — neutralised in Phase 3 per §8.4 (R35, R39).
- `findMatchedVehicles` / `VehicleMatchScore` dashboard widget; `inventory-match-refresh` cron → `VehicleRequestMatchResult`; duplicated `criteriaWhere` (R2, R10a).
- `lib/services/vehicle-request/notes-parser.ts` META blob + admin `meta.*` reads (R8a, R8b).
- Public `coBuyer` boolean in `Notification.metadata` (R11b).
- `BuyerOpportunity.tradeInDetails` JSON, `VehicleRequestFinancing.tradeIn` boolean, `TradeInValuation` admin valuation model (R12b, R13).
- Intake-reconcile cron / `processBuyerOpportunityIntake` running enrichment/outreach off SUBMITTED requests (R15b).
- `INVENTORY_LANES` constant ("Verified" label) + admin lane UI; per-page `LANE_CONFIG`/`LANE_INFO` copies; `readinessState` "AUCTION_READY" default (R22, R29).
- `lib/utils/zip-coords.ts` static ZIP table (R20).
- 48h `FRESHNESS_WINDOW_MS` sweep clock and `inventory-stale-sweep/route.ts:93-104` cadence comment (R27, R44).
- Env-tier config (`INVENTORY_SWEEP_ZIP`, `INVENTORY_SWEEP_RADIUS_MILES`, `MARKETCHECK_MONTHLY_CALL_BUDGET`) and `inventory-sync-priority` unscheduled route until migrations apply (R24a, R30, R48).
- `externalDealerName/Phone/City/State` LANE_2 name-match (`orchestrator.ts:92-105`) (R24b).
- `coverage.service.ts:38` `RADIUS_TIERS` ending at 150 (R23b).
- `vehicle_offers` / `dealer_offer_submissions` parallel offer models — keep as staff intake writing canonical `offers` (R45).
- `withCronRun` alerting for DEFERRED runs (observability area) (R54).
