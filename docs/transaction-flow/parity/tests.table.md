# Parity table — area: tests (§34 acceptance tests, §29 safeguards, quality gates)

Source map: `parity/tests.md` at HEAD 0cd399f, with its "Verification corrections (adversarial pass)" applied (corrected status wins). No `tests-gaps-r*.md` files existed. Ref key: `T<n>` = Part B row n (split rows carry a/b/c suffixes); `S<n>` = Part C §29 bullet n; `S-SC` = HTML SAFE "Marketplace integrity" scorecard bullet; `T55–T58` = the four requirements the corrections added. Paths are relative to `frontend/` unless prefixed `.github/`, `.claude/`, `docs/`. Spec = `docs/transaction-flow/AUTOLENIS-COMPLETE-TRANSACTION-FLOW.md` (MD); HTML = `docs/transaction-flow/AutoLenis-Transaction-Flow.html` (`ACCEPT` L975-979, `SAFE` L934). Phases per `IMPLEMENTATION-WORKFLOW.md` §8.1/§8.2; test levels per §12.1.

| Ref | Document & section | Requirement | Current route / service / model / component / email / cron / queue / admin action | Status | Stronger safeguard to preserve | Exact required change | Phase | Test level | Acceptance evidence | Owner-gated dependency | Legacy path affected | Final disposition |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| T1 | MD §34 L1563; HTML ACCEPT[0] | All four scenarios complete through the same spine (scenario-level proof) | No scenario harness; CI E2E runs only `tests/e2e/dealer-outreach.spec.ts` (`.github/workflows/ci.yml:299-300`); `playwright.config.ts:7-11` says deep journey needs live Supabase | MISSING | DSN guard pattern in `tests/integration/dealer-funnel.itest.ts:22` (refuses non-`autolenis_e2e`) | Add `tests/scenarios/*.itest.ts` (real Postgres, `/autolenis_e2e/` guard, Stripe/DocuSign adapters mocked) driving A–D through real services; script `test:scenarios`; CI job reusing the `migrations` postgres service | 11 | integration, playwright | `tests/scenarios/scenario-{a,b,c,d}.itest.ts` green in a CI postgres job; `ACCEPTANCE-REPORT.md` cites run ids | owner decision: node:test integration harness vs Playwright (map open question 1); Supabase preview project for Playwright | `tests/integration/*.itest.ts` (run by nobody in CI) is the template | TO IMPLEMENT |
| T2 | MD §34 L1567 (A); HTML ACCEPT[0][0] | $99 settles against the request | `app/api/webhooks/__tests__/stripe-idempotency.test.ts:193` marks PAID + creates auction; `lib/services/payment/__tests__/deposit-settlement.test.ts:205`; Deposit has no `vehicleRequestId` FK (`prisma/schema.prisma` model Deposit) | PARTIAL | Replay test `stripe-idempotency.test.ts:204` (duplicate ack, zero side effects) | After `deposits.vehicle_request_id` lands, assert the PAID deposit references the open request and settlement opens the sourcing case instead of creating an auction | 3 | integration, state-machine | Extended `stripe-idempotency.test.ts` + `deposit-settlement.test.ts`: PAID deposit → request `ACTIVE_SOURCING`, `auction.create` never called | production migration (Phase 1 wave `deposits.vehicle_request_id`) | Deposit settlement → immediate auction create + invite (webhook + activation reconciler), §8.4 row 1 | TO EXTEND |
| T3 | MD §34 L1567 (A); HTML ACCEPT[0][0] | Sourcing opens only after payment | `lib/services/vehicle-request/__tests__/request-progression.test.ts:66` advances SUBMITTED → ACTIVE_SOURCING with side effects and no deposit; `:84` thin coverage does not block; service header `request-progression.service.ts:9` claims deposit-first | BROKEN (test encodes opposite) | none | Rewrite `:66/:84/:91` so ACTIVE_SOURCING requires a settled $99; add failing-first "unpaid request never reaches ACTIVE_SOURCING" | 3 | state-machine, unit | `request-progression.test.ts` new case "unpaid request stays PAYMENT_REQUIRED"; runtime gate in `request-progression.service.ts` traced (currently UNVERIFIED) | none | `app/api/cron/intake-reconcile` progression path | TO IMPLEMENT |
| T4 | MD §34 L1567 (A); HTML ACCEPT[0][0] | $0 fee resolves for Standard | `lib/services/deal/__tests__/service-fee.test.ts:63` asserts a $499/$99/$400 row for every deal; `lib/services/deal/service-fee.service` | BROKEN (test encodes different fee model) | Idempotency cases `service-fee.test.ts:77,85`; duplicate-charge `service-fee-duplicate-charge.test.ts:97,115,145` | Add plan-aware cases: Standard → fee row $0 and `FEE_PENDING` auto-cleared; Premium → $400 net of $99 credit; keep idempotency cases | 3 | unit, state-machine | `service-fee.test.ts` cases "Standard resolves $0" and "Premium resolves 40000 cents after 9900 credit"; `deal-state-machine.test.ts` FEE_PENDING skip | none | `FEE_PENDING` gate driven by signup `user_metadata.plan` (Phase 3 defect 2) | TO IMPLEMENT |
| T5 | MD §34 L1567 (A); HTML ACCEPT[0][0] | Financing completed and verified by authorized staff before release | `financing-orchestrator.test.ts:82` (in-house adapter → FEE_PENDING); `app/api/admin/financing-reviews/__tests__/resolve-route.test.ts:51,62` (403 without operational role); `schema.prisma:1516-1517` no verified checkpoint; `:1711-1716` FinancingStatus lacks VERIFIED/TERMS_LOCKED | PARTIAL | `lib/services/auction/__tests__/no-inhouse-financing-on-auction-spine.test.ts:48`; `resolve-route.test.ts:51` staff-only authz | State-machine test `NOT_STARTED → IN_PROGRESS → TERMS_LOCKED` only via Finance/Ops action with actor recorded; authz test buyer/dealer cannot mark verified Lifecycle: §8.2a is canonical. | 7 | state-machine, unit | New `lib/services/financing/__tests__/financing-checkpoint.test.ts`; `deal-state-machine.test.ts` extended; `no-inhouse-financing-on-auction-spine.test.ts` still green | production migration (FinancingStatus values, Phase 1); §13-D9 `credit_applications` retention | In-house credit application route/UI + orchestrator/lender services (`lender-adapter.test.ts`, `credit-application.test.ts`), §8.4; buyer self-advance `POST /api/buyer/financing` | TO IMPLEMENT  |
| T6 | MD §34 L1567 (A); HTML ACCEPT[0][0] | Possession confirmed; completion commits once | `app/api/dealer/pickup/__tests__/scan-route.test.ts:140,205`; `lib/services/deal/__tests__/advance-deal-status.test.ts:106,112` | ALREADY CORRECT (unit) | Exactly-once completion event + CAS (`advance-deal-status.test.ts:106,112,128`) | None at unit level; Phase 9 adds `HANDOVER_PENDING` + buyer possession confirmation + real-DB concurrent completion (see T38) | 9 | unit, concurrency | Existing cases stay green after atomic-completion refactor; new possession-confirmation case in `scan-route.test.ts` | none | Non-crypto pickup QR nonce (replaced in Phase 9) | ALREADY PRESENT (VERIFY IN PHASE) |
| T7 | MD §34 L1568 (B); HTML ACCEPT[0][1] | Valid $99 credit; $400 settles | `app/api/buyer/plan/__tests__/upgrade.test.ts:92` "upgrade succeeds with NO charge"; `:117` idempotent no-op; `app/api/buyer/plan/upgrade`; `app/api/webhooks/stripe` | BROKEN (test encodes opposite) | Idempotency case `upgrade.test.ts:117` (second call reuses intent) | Replace `:92`: upgrade creates a 40000-cent PaymentIntent with 9900 credit when a PAID deposit exists; second call reuses intent; add `premium-fee` webhook settlement case to `test:webhooks` | 3 | unit, integration | Rewritten `upgrade.test.ts`; new `app/api/webhooks/__tests__/premium-fee-settlement.test.ts` (integer cents asserted) | refund/money-path activation (Stripe test mode `livemode=false`) | Signup-flag Premium (`app/auth/callback/route.ts:31-33`) | TO IMPLEMENT |
| T8 | MD §34 L1568 (B) (Markdown-only clause) | Concierge assigned at Premium settlement | No test; `lib/services/concierge/__tests__/concierge-conversion.test.ts` is the separate concierge product; webhook branches `concierge_deposit`/`concierge_fee` (`app/api/webhooks/stripe/route.ts:31,345`) unrelated | MISSING | none | Webhook → service test asserting an Operations assignment record (ownership move) is written when the $400 settles | 3 | integration | New case in `premium-fee-settlement.test.ts`: assignment row + `queue_items` ownership; `grep assignConcierge` non-empty | none | none | TO IMPLEMENT |
| T9 | MD §34 L1568 (B); HTML ACCEPT[0][1] | Every release checkpoint passes | No checkpoint concept on the deal spine; only `app/api/admin/requests/[requestId]/checkpoints/route.ts` (due-diligence, unrelated); 0 tests match `checkpoint` | MISSING | none | Table-driven state-machine test enumerating each release checkpoint (`FUNDING_PENDING` six-item clearance list) and its release condition, pattern `deal-state-machine.test.ts` | 8 | state-machine | New `lib/services/deal/__tests__/release-checkpoints.test.ts` covering all six clearance items and refusal on any unmet item | none | none | TO IMPLEMENT |
| T10 | MD §34 L1568 (B); HTML ACCEPT[0][1] | Co-buyer signs | `coBuyer` boolean only (`app/api/public/request-vehicle/route.ts:106`, `RequestVehicleFormClient.tsx:309`); 0 tests match `co.?buyer`; `lib/services/esign` | MISSING | `buyer-signing.test.ts:240,295` hash binding per envelope | Extend `lib/services/esign/__tests__/buyer-signing.test.ts` with two-signer envelope: not COMPLETED until both sign; hash binding per signer; required-signer flag from Phase 4 capture | 8 | unit, state-machine, playwright | `buyer-signing.test.ts` "two required signers" cases; Playwright sign (buyer + co-buyer) journey in Phase 8 gate | `ESIGN_EXECUTED_ARTIFACT_ENABLED` (§13-D4, preview only) | none | TO EXTEND |
| T11a | MD §34 L1569 (C); HTML ACCEPT[0][2] | Custom Vehicle Request reaches payment immediately | Dashboard Vehicle Request never reaches payment (HTML BUILD row 3); `lib/services/acquisition/__tests__/unified-intake-emit.test.ts:47` | MISSING | none | Intake test: custom-request submission → eligibility recheck → `PAYMENT_REQUIRED` with no intermediate sourcing state | 2 | unit, state-machine | `test:intake` case "custom request lands PAYMENT_REQUIRED"; `request-progression.test.ts` transition table row | none | none | TO IMPLEMENT |
| T11b | MD §34 L1569 (C); HTML ACCEPT[0][2] | No pre-payment sourcing spend | `request-progression.test.ts:66,84,91`; `lib/services/acquisition/__tests__/post-intake-outreach-status.test.ts:71` (outreach gated only on buyerId) | BROKEN | Existing Apollo paid-enrichment gate (Phase 5 note) | Add "no provider call, no dealer outreach, no paid enrichment before deposit PAID" assertions in `test:intake` and `test:vehicle-request` | 3 | unit | `post-intake-outreach-status.test.ts` new case: unpaid → 0 provider/outreach calls; `request-progression.test.ts` side-effect spy count 0 | none | acquisition intake pipeline pre-payment outreach | TO IMPLEMENT |
| T12a | MD §34 L1569 (C); HTML ACCEPT[0][2] | `NOT_REQUIRED_CASH` exists on `FinancingStatus` | `schema.prisma:1711-1716` `{PENDING SELECTED APPROVED DECLINED}`; `:1853` `CASH_PURCHASE`; `app/api/buyer/requests/route.ts:74` `paymentMethod "CASH"`; `car-request-financing.service.ts:132` `CASH_BUYER` | MISSING | none | Append `NOT_REQUIRED_CASH` (and checkpoint values) to `FinancingStatus` by additive migration; static enum test in `prisma/__tests__` | 1 | unit | `prisma/__tests__` migration-static test asserts enum value present; CI migration job green | production migration (Phase 1 wave) | Legacy `FinancingStatus` values retained, code refuses to write (§13-D18) | TO IMPLEMENT |
| T12b | MD §34 L1569 (C); HTML ACCEPT[0][2] | `NOT_REQUIRED_CASH` derived from `paymentMethod === "CASH"` | `car-request-financing.service.ts:132` derives `CASH_BUYER` only; no financing-status write | MISSING | none | Extend `lib/services/financing/__tests__` + `deal-state-machine.test.ts` with the cash branch: checkpoint resolves `NOT_REQUIRED_CASH` from the existing signal, never re-created | 7 | state-machine, unit | `financing-checkpoint.test.ts` case "CASH request → NOT_REQUIRED_CASH, no external_pre_approval required" | none | none | TO EXTEND |
| T12c | MD §34 L1569 (C); HTML ACCEPT[0][2] | Cash confirmed at funding clearance by staff | No code; funding clearance checkpoint absent (see T9) | MISSING | none | Authz + state test: only Finance/Ops confirms cash at `FUNDING_PENDING`; buyer/dealer refused | 8 | unit, state-machine | `release-checkpoints.test.ts` cash-confirmation case + 403 case | none | none | TO IMPLEMENT |
| T13a | MD §34 L1570 (D); HTML ACCEPT[0][3] | Payoff good-through date captured on the trade packet | 0 code matches `goodThrough`/`payoffQuote`/`lienholder`; `lib/services/trade-in/` has only `trade-in.service.ts`, no `__tests__` | MISSING | none | Create `lib/services/trade-in/__tests__/` + `test:trade-in` script (chained into `test:all`); case: packet with lien requires payoff amount + good-through date | 4 | unit | New `lib/services/trade-in/__tests__/trade-packet.test.ts`; `test:coverage-check` OK; `test:all` chain includes `test:trade-in` | production migration (trade FK/payoff fields, Phase 1) | none | TO IMPLEMENT |
| T13b | MD §34 L1570 (D); HTML ACCEPT[0][3] | Payoff good-through enforced at handover; trade surrendered; obligations tracked after completion | No code; surrender/obligation hits are marketing copy only | MISSING | none | Cases: expired good-through blocks handover (exception raised); surrender recorded at scan; `post_completion_obligations` timers + overdue escalation | 9 | unit, state-machine, integration | `lib/services/pickup/__tests__/handover-trade.test.ts`; `app/api/cron/__tests__/obligations-overdue-route.test.ts` | production migration (`post_completion_obligations`) | none | TO IMPLEMENT |
| T14a | MD §34 L1572; HTML ACCEPT[1][0] | Real-DB concurrency suites run in CI | `select-offer-concurrency.test.ts:23-25` skips without real DSN; `ci.yml:69-72` placeholder DSN; `test:integration` not in CI | BROKEN in CI | `migrations` job already has a postgres service (`ci.yml:95-196`) | Add CI job step with postgres service running `pnpm test:concurrency` and `pnpm test:integration` under `DATABASE_URL=…/autolenis_e2e` | 1 | concurrency, integration | `.github/workflows/ci.yml` new step; run log shows `select-offer-concurrency` executed (not skipped) | none | `test:integration` orphaned from CI | TO IMPLEMENT |
| T14b | MD §34 L1572; HTML ACCEPT[1][0] | Replay on every money path (refund, premium fee) | Replay proven for deposit only: `stripe-idempotency.test.ts:204`; duplicate charge `create-intent-duplicate-charge.test.ts:151`, `service-fee-duplicate-charge.test.ts:97`; CAS `advance-deal-status.test.ts:96,128` | PARTIAL | All four cited tests | Add refund-replay and premium-fee-replay webhook cases; PaymentIntent-creation concurrency case | 3 | integration, concurrency | `stripe-idempotency.test.ts` cases "refund.updated replay" and "premium fee replay"; concurrency suite case for create-intent | refund/money-path activation | none | TO EXTEND |
| T15 | MD §34 L1572; HTML ACCEPT[1][1] | Missing Stripe webhook recovered by reconciliation | `lib/services/monitoring/__tests__/deposit-provider-evidence.test.ts:168`; `deposit-settlement.test.ts:205`; `app/api/cron/__tests__/deposit-activation-reconcile-route.test.ts:133`; settlement OFF by default (`deposit-settlement.test.ts:131`) | ALREADY CORRECT (unit) | Provider-evidence sweep + settlement sweep tests | None at unit level; Phase 3 enables the job and folds `checkDepositProviderEvidence` into one rail (§13-D12) — update tests to the single-rail `raiseException` path | 3 | unit, integration | Existing tests green after fold; scenario harness sets `DEPOSIT_SETTLEMENT_RECONCILE_ENABLED=true` | env flag `DEPOSIT_SETTLEMENT_RECONCILE_ENABLED` (production enable) | Duplicate `checkDepositProviderEvidence` detector (folded, §13-D12); `SYSTEM_ALERT` exception store mirror | TO CONSOLIDATE |
| T16 | MD §34 L1572; HTML ACCEPT[1][2] | Each sourcing band exercised | `lib/services/auction/__tests__/coverage.test.ts:228,232` (`RADIUS_TIERS` ladder); whether tiers equal the spec's 100/150/250 bands is unproven | UNVERIFIED | Ladder "tightest-first" invariant (`coverage.test.ts:228`) | Once the sourcing-case service lands (100→150→250), one case per band asserting provider-call budget and rooftop set; band-by-band expansion history | 5 | unit, state-machine | `lib/services/sourcing/__tests__/sourcing-bands.test.ts` (3 band cases + budget assertions) | MarketCheck terms (§13-D8 cache) for call-budget assertions | none | UNVERIFIED — RADIUS_TIERS vs spec bands not provable from tests |
| T17 | MD §34 L1572; HTML ACCEPT[1][2] | Buyer authorization beyond 250 miles | `lib/services/shortlist/__tests__/shortlist-radius.test.ts:40` (100-mile provider ceiling); `:61` beyond → custom request; no authorization record | BROKEN (test encodes 100-mile ceiling) | none | Add sourcing-seam test: >250 mi requires recorded buyer authorization (`RADIUS_AUTHORIZATION_REQUIRED`, 24h/72h reminders, 14-day close); unauthorized → blocked; reconcile `:40` with owner | 5 | unit, state-machine, playwright | `sourcing-bands.test.ts` authorization cases; Playwright "zero-coverage → radius authorisation screen" (Phase 5 gate) | owner decision: shortlist 100-mile ceiling vs 250-mile authorization (map open question 2) | Client-controlled radius (HTML BUILD row 7) | TO IMPLEMENT |
| T18 | MD §34 L1572; HTML ACCEPT[1][2] | Limited auction with an Operations approval | No test; grep hits only `lib/services/ai/action-intent/__tests__/engine.test.ts` | MISSING | none | Auction-service test: limited auction cannot open without an audited Operations approval row; approval actor recorded | 5 | unit, state-machine | `lib/services/auction/__tests__/limited-auction-approval.test.ts` | none | none | TO IMPLEMENT |
| T19 | MD §34 L1572; HTML ACCEPT[1][3] | Zero offers | `lib/services/auction/auction.service.ts:169-172,178` (no auto-refund, buyer notification exists); `deposit-activation-exception.test.ts:78` ops-only alert; `dealer-award.test.ts:67`; stale comment `auction-close-idempotency.test.ts:8` "zero-offer refund" | PARTIAL | No auto-refund rule (`auction.service.ts:169-172`) | Add close test: CLOSED with 0 offers → buyer notice with recovery action, exception raised, deposit retained, one Ops-owned relaunch without second $99; fix stale header comment | 6 | unit, state-machine | `auction-close-idempotency.test.ts` zero-offer case; relaunch case; comment corrected | none | Reconciler `close`-on-zero branch retired in Phase 3/5 (§8.4 row 1) | TO EXTEND |
| T20 | MD §34 L1572; HTML ACCEPT[1][3] | Offer above budget rejected server-side | `lib/services/offer/offer.service.ts:39,78,278` `assertWithinBuyerBudget` untested; `lib/services/offer/__tests__/otd-truthfulness.test.ts` arithmetic only | MISSING (code exists) | Server-side budget check in `offer.service.ts:39` | Add `lib/services/offer/__tests__/budget-gate.test.ts` (in `test:offer`): submit and revise above `maxOtdAmountCents` rejected; approval recheck on select/confirm | 6 | unit | `budget-gate.test.ts` green; over-budget exception raised via `raiseException` | none | none | TO IMPLEMENT |
| T21 | MD §34 L1572; HTML ACCEPT[1][4] | Dealer timeout at reaffirmation | 0 code/test matches `reaffirm`/`confirmAvailability` | MISSING | none | Cron route test (pattern `app/api/cron/__tests__/*-route.test.ts`) + service test: 24h deadline breach → back to remaining offers, scorecard entry, SLA violation on repeat | 7 | unit, state-machine | `app/api/cron/__tests__/reaffirmation-timeout-route.test.ts`; `lib/services/deal/__tests__/reaffirmation.test.ts` | production migration (`dealer_reaffirmations`) | Identity-firewall lift at award (moves to reaffirmation) | TO IMPLEMENT |
| T22 | MD §34 L1572; HTML ACCEPT[1][4] | Material change accepted and rejected | 0 files match `material.?change`/`amendment` | MISSING | none | Service test at the deal seam: side-by-side proposal → accept (auto-apply lower OTD, refuse above-ceiling) / reject (recovery path) | 7 | unit, state-machine | `reaffirmation.test.ts` material-change accept/reject cases | none | none | TO IMPLEMENT |
| T23 | MD §34 L1572; HTML ACCEPT[1][4] | Vehicle hold expiry | 0 files match `holdExpir`/`VEHICLE_HOLD`/`holdUntil`; `holds` cron is a no-op (§8.4) | MISSING | none | Cron route + service tests for hold-until expiry → exception + buyer/dealer comms via dispatcher; extend/release cases | 7 | unit, integration | `app/api/cron/__tests__/holds-route.test.ts` repurposed; `reaffirmation.test.ts` hold cases | none | `holds` no-op cron (repurposed §13-D26) | TO IMPLEMENT |
| T24 | MD §34 L1572; HTML ACCEPT[1][5] | Financing failure then successful alternate path | `financing-orchestrator.test.ts:93` (fail-closed to human review); `:106` adapter fail-closed; no alternate-path case | PARTIAL | Fail-closed-to-human-review behaviour (`:93,:106`) | Add: after failure/expiry, Finance records external alternate path → `TERMS_LOCKED`, deal proceeds without auto-cancel, original failure retained in history | 7 | state-machine, unit | `financing-checkpoint.test.ts` "failure → alternate external → TERMS_LOCKED" with history assertion | none | Orchestrator/lender services removed or re-pointed in Phase 7 freeze | TO EXTEND |
| T25 | MD §34 L1572; HTML ACCEPT[1][6] | Insurance rejection | `upload-proof-gate.test.ts:148` (proof-write failure holds gate); `advance-deal-status.test.ts:181`; `schema.prisma:1493-1501` InsuranceStatus has `FAILED`, no `REJECTED`/`UNDER_REVIEW`; `EXTERNAL_UPLOADED` counts as satisfied (`deal.service.ts:41-45,134-138`) | PARTIAL | Gate-hold on write failure (`upload-proof-gate.test.ts:148`); supersede case `:174` | Reuse `FAILED` or add `UNDER_REVIEW`/`REJECTED` by migration (no parallel status); add: Ops rejects proof → deal parked, buyer notified with recovery, re-upload supersedes; `EXTERNAL_UPLOADED` no longer satisfies | 8 | unit, state-machine | `upload-proof-gate.test.ts` rejection cases; `advance-deal-status.test.ts` "EXTERNAL_UPLOADED does not satisfy" | production migration (InsuranceStatus values, Phase 1) | `EXTERNAL_UPLOADED` auto-satisfaction (§13-D31) | TO EXTEND |
| T26 | MD §34 L1572; HTML ACCEPT[1][6] | Contract Shield mismatch | `contract-auto-approve.test.ts:128` (WARNING/FAIL never advance); `approve-binds-to-reviewed-version.test.ts:172`; no offer/recap comparison exists in `contract-shield.service.ts` | PARTIAL | Never-auto-advance on WARNING/FAIL; approval-binds-to-version | Add rule-engine test against the real `scanContract` (see S20) with contract OTD/fees mismatching accepted offer + recap → FAIL + exception routing | 8 | unit | `lib/services/contract-shield/__tests__/scan-engine.test.ts` mismatch case importing the real service | none | Admin `CONTRACT_SHIELD_OVERRIDDEN` synthetic PASS; `contract-upload.service.ts` second writer (retired Phase 8) | TO EXTEND |
| T27 | MD §34 L1572; HTML ACCEPT[1][6] | Signature expiry and reissue | `lib/services/esign/__tests__/buyer-signing.test.ts:331,346,356` lazy expiry; `:405-431` EXPIRED → archived attempt 1, new attempt 2 re-bound to approved hash; `esign-gate-route.test.ts:115` | ALREADY CORRECT (unit) | CAS-guarded lazy expiry + immutable archive of prior attempt (`:405-431`) | None; optional minor case that the old EXPIRED row cannot be signed after reissue; 14-day expiry constant verified in Phase 8 | 8 | unit, state-machine | Existing `buyer-signing.test.ts:405` loop stays green; new "stale attempt refused after reissue" case | `ESIGN_EXECUTED_ARTIFACT_ENABLED` | none | ALREADY PRESENT (VERIFY IN PHASE) |
| T28 | MD §34 L1572; HTML ACCEPT[1][6] | Dealer that never executes | Only `lib/services/notifications/__tests__/contract-stage-messaging.test.ts` (messaging); no SLA/escalation test; `document_requests.dueAt` never set, `requestDocument()` uncalled | MISSING | none | Cron + service test: contract-execution SLA (24h `document_requests` due) breach → Ops exception + scorecard consequence; `DEALER_EXECUTED` required predecessor | 8 | unit, integration, state-machine | `app/api/cron/__tests__/document-request-overdue-route.test.ts`; `deal-state-machine.test.ts` `SIGNED → PICKUP_SCHEDULED` refused without `DEALER_EXECUTED` | none | `SIGNED → PICKUP_SCHEDULED` open transition with `force: true` (§13-D29) | TO IMPLEMENT |
| T29 | MD §34 L1572; HTML ACCEPT[1][7] | Blocked handover on identity mismatch | `scan-route.test.ts:146` wrong-dealer only; unused enums `schema.prisma:1946` `IDENTITY_MISMATCH`, `:1950-1954` `IdentityVerificationStatus` | MISSING | none | Reuse existing enums; scan-route case: identity check fails → 409, pickup not completed, exception raised | 9 | unit | `scan-route.test.ts` identity-mismatch case; `grep IDENTITY_MISMATCH lib app` non-empty | none | none | TO IMPLEMENT |
| T30 | MD §34 L1572; HTML ACCEPT[1][7] | Delivery-discrepancy hold | 0 files match `discrepanc` outside marketing copy | MISSING | none | Service + route tests: discrepancy at delivery → deal parked (hold), obligations created, recovery action, append-only correction | 9 | unit, state-machine | `lib/services/pickup/__tests__/delivery-discrepancy.test.ts` | none | none | TO IMPLEMENT |
| T31 | MD §34 L1572; HTML ACCEPT[1][7] | Cancellation before and after contract execution | `lib/services/deal/__tests__/cancel-deal-seam.test.ts:91,119,157`; `deal-state-machine.test.ts:52`; no pre/post-execution distinction; admin `REFUND_TRIGGERED`/`DEAL_CANCELLED` auto-refund (`action/route.ts:222-250`) | PARTIAL | `cancel-deal-seam.test.ts:119,183` single writer, never refunds | Two parameterised cases on executed vs unexecuted: `FROZEN_PENDING_RELEASE` after execution, differing obligations/refund outcomes; admin actions split onto `cancelDeal` seam | 10 | state-machine, unit | `cancel-deal-seam.test.ts` pre/post-execution cases; `admin/deals/__tests__` action-route regression | none | Three refund implementations consolidated to one (Phase 3 defect 3) | TO EXTEND |
| T32 | MD §34 L1572; HTML ACCEPT[1][7] | Refund request | `app/api/admin/payments/__tests__/deposit-refund-route.test.ts:92,114,144`; `lib/services/payment/__tests__/refund-deposit-charge.test.ts:53,69,77` | ALREADY CORRECT (unit) | Idempotency-keyed refund; no-charge never labelled refunded (`refund-deposit-charge.test.ts:61,84`) | None for admin execution; add buyer-initiated refund *request* → hold + cancel unsent outreach if spec surface required | 3 | unit | Existing suites green; new `refund-request.test.ts` if buyer surface built | refund/money-path activation (refunds stay manual) | none | ALREADY PRESENT (VERIFY IN PHASE) |
| T33a | MD §34 L1572; HTML ACCEPT[1][8] | Standard→Premium upgrade taken at payment | `upgrade.test.ts:92` no-charge only | BROKEN | none | Checkout-time upgrade test: plan snapshot Premium, $400 balance intent with $99 credit, window opens at settlement | 3 | unit, integration | Rewritten `upgrade.test.ts` + checkout math Playwright (Phase 3 gate) | refund/money-path activation | none | TO IMPLEMENT |
| T33b | MD §34 L1572; HTML ACCEPT[1][8] | Upgrade taken again at the Best Price Report | No BPR-point upgrade surface or test | MISSING | none | Second entry-point test at the BPR surface (touchpoint 2) carrying the same idempotent upgrade token; impression/conversion recorded | 6 | unit | `app/api/buyer/plan/__tests__/upgrade-entry-points.test.ts` BPR case | none | none | TO IMPLEMENT |
| T34a | MD §34 L1572; HTML ACCEPT[1][8] | Upgrade from the dashboard and from touchpoint-1 receipt email | `components/buyer/PlanUpgradeCard.tsx` on dashboard/deal-payment/billing/profile pages; no email template contains an upgrade CTA (`lib/services/email/__tests__/suppression-tier.test.ts` only hit) | MISSING (tests) | none | Route test: dashboard entry uses the same idempotent token; email template test for receipt-line touchpoint 1 (pattern `admin-cta-destinations.test.ts`) | 3 | unit | `upgrade-entry-points.test.ts` dashboard case; `lib/services/email/__tests__/premium-touchpoints.test.ts` touchpoint 1 | none | none | TO IMPLEMENT |
| T34b | MD §34 L1572; HTML ACCEPT[1][8] | Upgrade from the post-acceptance invitation and touchpoints 2–4 emails | No post-acceptance invitation surface; no email CTA | MISSING | none | Tests: interstitial shown once, 1-hour follow-up only if declined/dismissed, each email carries the same token; impressions/dismissals/conversions recorded | 6 | unit, playwright | `upgrade-entry-points.test.ts` invitation cases; `premium-touchpoints.test.ts` 2–4; Playwright "interstitial declinable and non-blocking" | none | none | TO IMPLEMENT |
| T34c | MD §34 L1572; HTML ACCEPT[1][8] | Upgrade from the touchpoint-5 email | No email CTA | MISSING | none | Email template test for touchpoint 5 (financing stage) with the same token and suppression rules | 7 | unit | `premium-touchpoints.test.ts` touchpoint 5 | none | none | TO IMPLEMENT |
| T35 | MD §34 L1572; HTML ACCEPT[1][8] | Suppressed prompt during an open exception | No tests; no code | MISSING | none | Service test: open `queue_items` exception on the lineage → upgrade prompt suppressed on dashboard and in every email | 10 | unit | `lib/services/plan/__tests__/upgrade-suppression.test.ts` | none | none | TO IMPLEMENT |
| T36 | MD §34 L1572; HTML ACCEPT[1][9] | Premium→Standard downgrade before and after the $400 settles | `app/api/buyer/plan/` holds only `upgrade/` + `__tests__/`; every `downgrade` hit unrelated (`lib/events/lifecycle-advance.ts:8`) | MISSING | `refund-deposit-charge.test.ts` idempotency pattern to reuse | Route + webhook tests: before settlement cancels intent; after settlement produces idempotency-keyed refund via the single refund primitive; re-upgrade case | 3 | unit, integration | `app/api/buyer/plan/__tests__/downgrade.test.ts` (both timings + replay) | refund/money-path activation | none | TO IMPLEMENT |
| T37 | MD §34 L1572; HTML ACCEPT[1][9] | Unpaid Premium balance reverts to Standard (spec: at acceptance; plan: at funding clearance / window close) | No tests; no code (`grep plan.*revert` → 0) | MISSING | none | Test: `$400` intent never succeeded → plan snapshot reverts to Standard, fee row $0, Premium window closed; record the acceptance-vs-funding-clearance timing divergence | 8 | state-machine, unit | `release-checkpoints.test.ts` "unpaid election reverts" case | owner decision: revert point (acceptance vs funding clearance) | none | TO IMPLEMENT |
| T38 | MD §34 L1572; HTML ACCEPT[1][9] | Concurrent completion attempts | `advance-deal-status.test.ts:106,112,128`; `scan-route.test.ts:205` (mocked Prisma only) | PARTIAL | CAS + exactly-once completion (mocked) | Add to the real-DB concurrency suite: N parallel scans / possession confirmations → exactly one `COMPLETED` event, one outbox set, in one transaction | 9 | concurrency | New `lib/services/deal/__tests__/complete-deal-concurrency.test.ts` under `test:concurrency` with `/autolenis_e2e/` guard, executed in the CI postgres job | none | Non-crypto pickup QR nonce | TO EXTEND |
| T39a | MD §34 L1574; HTML ACCEPT[2][0] | Form-walk Playwright suite seeded (homepage, refinance, guest capture, duplicate submit) | No spec submits a public form; `dealer-funnel.spec.ts:33-118` walks authenticated dealer forms only; `ci.yml:300` runs `dealer-outreach.spec.ts` only | MISSING | CI E2E runs with no vendor credentials (`ci.yml:245-246`); specs skip with reasons | Create `tests/e2e/public-forms.spec.ts` under `playwright.e2e.config.ts` with per-form fixtures and Prisma assertions on lane rows; run in the `e2e` CI job | 2 | playwright, integration | `public-forms.spec.ts` homepage/refinance cases green in CI `e2e` job; DB assertion one buyer, one request | none | Two Playwright roots (`e2e/`, `tests/e2e/`) consolidated first (§12.2 item 4) | TO IMPLEMENT |
| T39b | MD §34 L1574; HTML ACCEPT[2][0] | Every listed form walked (inventory, vehicle detail, shortlist, AMIPS, blog CTAs, social landing, affiliate links, conversational intake, callback, trade-in, prequal, dealer + affiliate application, support) | Same as T39a; `tests/visual/design-system.visual.spec.ts:20-26` screenshots 5 marketing pages only | MISSING | none | Extend `public-forms.spec.ts` to all 16 forms; each proves lane, attribution, ZIP, consent, no duplicate buyer/request | 11 | playwright | `public-forms.spec.ts` 16 cases; `ACCEPTANCE-REPORT.md` form-walk table | Supabase preview project (§12.3 preflight) for authenticated forms (shortlist, trade-in) | none | TO IMPLEMENT |
| T40 | MD §34 L1574; HTML ACCEPT[2][1] | Each form lands in its correct lane | `lib/services/acquisition/__tests__/unified-intake-emit.test.ts:47` | PARTIAL | Single intake handler emits opportunity + request | Parameterise intake tests by `source` (form) → expected lane; refinance stays Lane 2 | 2 | unit | `unified-intake-emit.test.ts` source→lane table | none | none | TO EXTEND |
| T41 | MD §34 L1574; HTML ACCEPT[2][1] | Attribution recorded per form | `lib/services/affiliate/__tests__/attribution-chain.test.ts` (affiliate chain only) | PARTIAL | Affiliate attribution chain | Add per-form UTM/affiliate attribution assertions to intake tests and `public-forms.spec.ts` | 2 | unit, playwright | Intake cases assert attribution fields persisted; Playwright DB assertion | none | none | TO EXTEND |
| T42 | MD §34 L1574; HTML ACCEPT[2][1] | ZIP recorded per form | `request-progression.test.ts:56` (no-zip stays SUBMITTED); `docs/plans/BUYER-LOCATION-GAP.md` (no journey path writes buyer location) | PARTIAL | none | Failing-first per BUYER-LOCATION-GAP "Test (failing first)" (doc line 296): every ZIP-collecting form persists buyer/request location | 2 | unit, playwright | `lib/services/buyer/__tests__/buyer-location-backfill.test.ts` extended; intake ZIP persistence cases | none | none | TO EXTEND |
| T43 | MD §34 L1574; HTML ACCEPT[2][1] | Consent recorded per form | `lib/services/sms/__tests__/consent-basis-gate.test.ts`; `lib/services/notifications/__tests__/contact-sms-consent.test.ts` | PARTIAL | Consent-basis gate + suppression tests | Add consent persistence assertions per form (SMS/email basis, timestamp, source) | 2 | unit, playwright | Intake consent cases; `public-forms.spec.ts` consent DB assertion | none | none | TO EXTEND |
| T44a | MD §34 L1574; HTML ACCEPT[2][1] | One open request per buyer enforced at the database | No guard in intake/request write paths (`lib/services/acquisition/*.ts`, `lib/services/vehicle-request/*.ts`, `app/api/buyer/requests/route.ts` → 0 hits); workflow §5 plans a partial unique index | MISSING | none | DB-backed test in CI migration job: second open `vehicle_requests` row for same buyer violates the partial unique index | 1 | integration | `prisma/__tests__` static SQL assertion + CI migration-job DB test on the predicate | production migration (partial unique index) | none | TO IMPLEMENT |
| T44b | MD §34 L1574; HTML ACCEPT[2][1] | No duplicate buyer; second submission attaches to the open request | `no-phone-keyed-buyer-mutation.test.ts`; `lib/services/__tests__/contact-dedup-fail-closed.test.ts`; `promote-opportunity.test.ts`; no "second open request" behaviour | PARTIAL | Phone-keyed mutation guard; dedup fail-closed | Add: same email/phone twice → one buyer, one open request, second submission attaches; concurrency test for duplicate submissions | 2 | unit, concurrency, playwright | Intake case "duplicate submit attaches"; concurrency suite case; Playwright "duplicate submit creates nothing" | none | none | TO EXTEND |
| T45 | MD §34 L1576; HTML ACCEPT[3][0] | Buyer portal, dealer portal, Ops queue show the same checkpoint, party, deadline, recovery action against one lineage | 0 tests match `checkpoint`/`responsible.?party`/`recovery.?action`; `lib/services/buyer/__tests__/journey.test.ts:60` buyer-only | MISSING | none | One "transaction status" DTO test asserting buyer/dealer/admin projections derive from one function; Playwright three-portal parity once auth stubs exist | 10 | unit, playwright | New `lib/services/deal/__tests__/transaction-status-parity.test.ts`; Playwright parity journey | Supabase preview project (buyer/dealer auth for Playwright) | none | TO IMPLEMENT |
| T46 | CLAUDE.md "Commands"; skills; hook comments | `test:all` documented as "26 suites" | `package.json` `test:all` chains 65 of 72 `test*` scripts; stale in CLAUDE.md, `autolenis-testing-quality-gates/SKILL.md:50`, `autolenis-code-verification/SKILL.md:95`, `autolenis-production-readiness/SKILL.md:112`, `.claude/hooks/verification/lib.mjs:40,46`, `__tests__/lib.test.mjs:48` | BROKEN (documentation) | none | Replace the count with "every `test:*` suite (65 today)" or derive from package.json in all six locations | 1 | n/a | Diff of the six files; hook self-test still 37+ green | none | none | TO IMPLEMENT |
| T47 | skill quality-gates; workflow §12.1 | Every test file reachable from a `test:*` script and from `test:all` | `scripts/check-test-coverage.ts:95-96` checks any `test*` script, never `test:all` membership; `:115` message only; `:44` ignores `*.itest.ts`/`*.spec.ts`; observed `360/360 OK` | PARTIAL | Reachability guard itself (`ci.yml:60-61`) | Parse the `test:all` chain and fail on any un-chained `test:*` script; optionally recognise itest/spec via allow-list | 1 | unit | `scripts/__tests__/check-test-coverage.test.ts` fixture with an un-chained script → exit 1 | owner decision: extend to itest/spec (map open question 4) | none | TO EXTEND |
| T48 | skill quality-gates | CI runs typecheck → lint → coverage-check → `test:all` → build | `.github/workflows/ci.yml:51-75` | ALREADY CORRECT | CI order as written | None; add the postgres concurrency step (T14a) without reordering | 1 | n/a | `ci.yml` unchanged order; green run | none | none | PRESERVED STRONGER SAFEGUARD |
| T49 | supabase-postgres skill | Migration chain replay from empty DB, drift ratchet, CRM runbook | `ci.yml:95-196` (apply twice `:158-165`, drift `:172-173`); `scripts/check-migration-drift.ts`; `prisma/drift-baseline.json:2` `structuralStatements: 345` | ALREADY CORRECT | From-zero replay twice + drift ratchet at 345 + runbook twice | None; Phase 1 wave must keep the job green and not raise the ratchet | 1 | integration | CI `migrations` job green after the Phase 1 wave; `pnpm db:check-drift` output | production migration | none | PRESERVED STRONGER SAFEGUARD |
| T50 | skill quality-gates; workflow §12.4 | Playwright E2E for buyer + dealer critical paths runs in CI | Only `dealer-outreach.spec.ts` in CI (`ci.yml:203-208`); buyer/dealer/affiliate specs self-skip (`buyer-remediation.spec.ts:20-21`); admin JWT via `scripts/e2e-admin-storage-state.ts` | PARTIAL | `/autolenis_e2e/` refusal in every `tests/e2e` spec and the storage-state script (`:25-30`) | Stand up buyer/dealer storage-state minting (as done for admin) or a Supabase preview project; widen the `e2e` CI job | 2 | playwright | `ci.yml` `e2e` job runs buyer + dealer specs (not skipped) | Supabase preview project / auth stub (§12.3 preflight) | none | TO EXTEND |
| T51 | skill quality-gates | Public UI change → visual snapshot | `playwright.visual.config.ts:12-46`; `tests/visual/design-system.visual.spec.ts:20-26`; 30 baseline files; `visual.yml:24-31` path filter; `README.md:85` CI-only authoritative | ALREADY CORRECT (marketing tier) | Triple gate (pixel + text + metadata), baseline pinned to CI image | Add dashboard-tier baseline (`VISUAL_STORAGE_STATE`) when buyer screens change (Phase 4+) | 2 | visual | `visual.yml` green on changed public forms; dashboard baseline committed when introduced | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| T52 | code-verification skill; CLAUDE.md loop | Stop hook enforces the verification loop | `.claude/hooks/verification/lib.mjs:168-169` first `# fail N` wins (probe: `test:all` with later `# fail 2` → `pass`); `track.mjs:49-51` no exit code; `lib.test.mjs:123-125` single-suite fixtures; `:216` unknown satisfies gate; `lib.mjs:50` visual trigger on any `components/**/*.tsx` | BROKEN (parser) / PARTIAL (gate) | Anchored command matching, quoted-string safe, interrupted run = fail (`lib.mjs:90-156`, `track.mjs:52-55`); over-broad visual trigger | Fail on any `# fail N>0` (`matchAll`) or non-zero exit / `ELIFECYCLE`; treat unknown `test:all` as not-run; add multi-suite fixture; update "26" comment | 1 | unit | `lib.test.mjs` multi-suite fixture (suite 1 `# fail 0`, suite 40 `# fail 2` → `fail`); `node --test` ≥38 pass | none | none | TO IMPLEMENT |
| T53 | quality-gates skill; workflow §12.2 item 3 | Real-DB concurrency proof is safe and runs somewhere | `select-offer-concurrency.test.ts:20-21` gates only on `!dsn.includes("placeholder")`, no `/autolenis_e2e/` guard, `:39-61` creates users/buyers/deposits/auctions/dealers/offers with no cleanup, `:32-33` 5 rounds × 8; chained into `test:all`; `ci.yml:70` `secrets.DATABASE_URL \|\| placeholder` | UNVERIFIED in CI + SAFETY HAZARD | `scripts/e2e-admin-storage-state.ts:25-30` refusal pattern | Add `/autolenis_e2e/` refusal (skip if unset, throw if set but not e2e); cleanup in `finally` or rollback per round; move `test:concurrency` out of `test:all` into the postgres CI job | 1 | concurrency, unit | Test file header guard + `finally` cleanup; `package.json` `test:all` no longer chains `test:concurrency`; CI step log shows execution | owner decision: whether GitHub secret `DATABASE_URL` is set and what it points at | none | TO IMPLEMENT |
| T54 | master §11 (assignment); workflow Part 0 L29-33 | Test-level policy and §11 coverage reconciliation exist in the repo | `IMPLEMENTATION-WORKFLOW.md` §12 (12.1–12.4) carries the policy; no `## §11` heading; §12.1 reachability claim over-states `check-test-coverage.ts` | PARTIAL (documentation) | none | Owner supplies master §11 text; author workflow §11 coverage reconciliation table (exception → test); correct the §12.1 reachability sentence | 11 | n/a | `IMPLEMENTATION-WORKFLOW.md` `## §11` present; `ACCEPTANCE-REPORT.md` cites it | owner decision: supply master §11 and where it lives (open question 6) | none | OWNER-GATED |
| T55 | MD §34 L1563 (second clause); MD §26 L1233 | Every §26 exception exercised — row-by-row proof | No register mapping each §26 exception to a proving test; no test references §26 ids | MISSING | none | Table-driven completeness test: every `exception_code` and `template_key` has ≥1 raise/enqueue site (workflow §8.3); `docs/transaction-flow/verification/exception-coverage.md` exception → test file:line; aggregate `test:exceptions` | 10 | unit, state-machine | `lib/services/operations/__tests__/exception-register-completeness.test.ts` (48 rows) + coverage doc | none | `SYSTEM_ALERT` Notification as exception store (kept as mirror) | TO IMPLEMENT |
| T56 | MD §34 L1569 (C, last clause); HTML ACCEPT[0][2] | Custom-request origin traverses the identical auction, deal, pickup path as A | Shared services exist (`lib/services/auction`, `deal`, `pickup`); no test parameterised on origin; `lib/services/auction/__tests__/outside-invite.test.ts` covers the parallel `outside_auction_invites` path | PARTIAL | Shared spine services | Origin-parameterised state-machine tests in `lib/services/deal/__tests__` and `lib/services/pickup/__tests__` asserting identical transitions and events for both origins | 6 | state-machine | `deal-state-machine.test.ts` + pickup suite run under `origin: SELECTED_INVENTORY` and `CUSTOM_REQUEST` fixtures | legacy removal (owner-gated `outside_auction_invites` drop) | `outside_auction_invites` parallel path (§8.4 row 2) | TO EXTEND |
| T57 | MD §34 L1570 (D, first clause); HTML ACCEPT[0][3] | Single unbroken lineage from custom request through completion with trade/lien | Deposit has no `vehicleRequestId`; trade-in has no deal/request FK (workflow §5 rows `deposits`, `trade_in_submissions`) | MISSING | none | After FKs land, one lineage test walking request → deposit → auction → deal → pickup → obligations by ids, failing on any null link | 11 | integration | `tests/scenarios/lineage.itest.ts` (real Postgres) asserting no null link on scenario D | production migration (Phase 1 FKs) | none | TO IMPLEMENT |
| T58 | MD §34 L1568 (B) vs HTML ACCEPT[0][1] | "Concierge assigned at settlement" present in Markdown, absent from HTML B text | HTML B text omits concierge; Markdown governs (row T8 kept) | PARTIAL (documentation divergence) | none | Record the divergence in `IMPLEMENTATION-WORKFLOW.md` §2 so the HTML is not used as the acceptance checklist | 3 | n/a | §2 divergence row present; T8 test exists | none | none | TO EXTEND |
| S1 | MD §29 L1442; HTML SAFE[0][0] | FCRA consent persisted before MicroBilt pull; duplicate paid pulls claimed safely | `lib/services/prequal/__tests__/prequal-decisioning.test.ts:134` consent fixture; claim path `prequal.service.ts:215-279`; no test names in-flight/duplicate claim | PARTIAL | Consent fixture + claim path | Failing-first test: concurrent pull → one MicroBilt call, second claim returns in-flight outcome | 2 | unit, concurrency | `prequal-decisioning.test.ts` "duplicate claim yields one provider call" | none | none | TO EXTEND |
| S2 | MD §29 L1443; HTML SAFE[0][1] | No SSN required; OFAC fails closed on positive or indeterminate | `prequal-decisioning.test.ts:162,170`; `prequal-provider-failure.test.ts:236,275`; `microbilt-config-and-payload.test.ts` | ALREADY CORRECT | Fail-closed OFAC on null and hit | None; keep green through Phase 2 intake changes | 2 | unit | Existing suite green in `pnpm test` | none | none | PRESERVED STRONGER SAFEGUARD |
| S3 | MD §29 L1444; HTML SAFE[0][2] | Adverse-action delivery distinguishes sent, duplicate, failed | `admin-prequal.service.ts:665-668` three codes; tests assert only SENT (`prequal-decisioning.test.ts:124,186`) | PARTIAL | Three-outcome code path | Add cases for `_SUPPRESSED_DUPLICATE` and `_SEND_FAILED`; route through the comms outbox dispatcher | 2 | unit | `prequal-decisioning.test.ts` duplicate + failed cases | none | Direct Resend send in adverse-action path (allowlist → zero) | TO EXTEND |
| S4 | MD §29 L1446; HTML SAFE[1][0-1] | Stripe is the authority; provider-side duplicate-charge checks on deposit and Premium fee | `payment-confirmation.test.ts:102`; `create-intent-duplicate-charge.test.ts:151`; `service-fee-duplicate-charge.test.ts:97,115,145` (concierge fee PI, not $400 Premium fee) | ALREADY CORRECT (current fee model) | Provider-evidence rule + duplicate-charge checks | Extend duplicate-charge check to the $400 Premium balance intent and to admin `create-intent`/`send-link` (Phase 3 defect 4) | 3 | unit, integration | `service-fee-duplicate-charge.test.ts` premium-fee cases; `app/api/admin/payments/__tests__` existing-obligation case | refund/money-path activation | Admin second-$99 intent path (Phase 3 defect 4) | ALREADY PRESENT (VERIFY IN PHASE) |
| S5 | MD §29 L1447; HTML SAFE[1][2] | Deposit transitions guarded by a state matrix; unroutable successful payments raise an Operations exception | `lib/payments/__tests__/deposit-state.test.ts:13-39`; `stripe-delivery-observability.test.ts:198,225,251`; `deposit-state.ts:12-41` FAILED terminal | ALREADY CORRECT | State matrix + unroutable-payment exception | Extend matrix for Phase 3 defect 1 (`FAILED` reserved for cancelled/expired intents; live-intent retry may settle) with failing-first regression; exception via `raiseException` | 3 | unit, state-machine | `deposit-state.test.ts` "declined then retried on live intent settles once"; observability tests green on the single rail | none | none | TO EXTEND |
| S6 | MD §29 L1449; HTML SAFE[2][0] | Auction close atomic claim; reprocess closed auctions with unfinished side effects | `lib/services/auction/__tests__/auction-close-idempotency.test.ts:14,19,25,31` | ALREADY CORRECT | Atomic claim + reprocess | None; fix stale "zero-offer refund" header comment (`:8`) | 6 | unit, concurrency | Existing cases green; comment corrected | none | none | PRESERVED STRONGER SAFEGUARD |
| S7 | MD §29 L1450; HTML SAFE[2][1] | Anti-snipe with hard cap and full audit | `lib/services/auction/__tests__/anti-snipe.test.ts:49,70,89` | ALREADY CORRECT | Hard cap + ANTI_SNIPE audit + atomic guard | None; must survive 48-hour sealed-auction change in Phase 5/6 | 6 | unit | Existing cases green | none | none | PRESERVED STRONGER SAFEGUARD |
| S8 | MD §29 L1451; HTML SAFE[2][2] | Offer selection serialized under a DB lock | `select-offer-concurrency.test.ts:23-25` skips without real DB; never executed in CI (`ci.yml:69-72`) | UNVERIFIED (in CI) | DB-lock test file itself | Execute in the CI postgres job after T53 hardening; keep under new selection → `DEALER_CONFIRMATION` Deal creation | 6 | concurrency | CI step log shows `select-offer-concurrency` ran 5 rounds and passed | owner decision: GitHub `DATABASE_URL` secret (T53) | none | UNVERIFIED — skips under CI placeholder DSN |
| S9 | MD §29 L1452; HTML SAFE[2][3] | Offer arithmetic and approved budget checked server-side | Arithmetic `otd-truthfulness.test.ts:13-66`; budget NONE (`offer.service.ts:39`) | PARTIAL | Arithmetic truthfulness tests | Budget half → T20 `budget-gate.test.ts`; approval recheck on submit/revise/select/confirm | 6 | unit | `budget-gate.test.ts`; `otd-truthfulness.test.ts` green | none | none | TO EXTEND |
| S10 | MD §29 L1453; HTML SAFE[2][4] | Best Price rankings persisted with weights and results | NONE; code `lib/services/offer/best-price.service.ts:112` `bestPriceCalculationLog.create`; only unrelated AMIPS tests match | MISSING (test) | Persistence code path exists | Add `lib/services/offer/__tests__/best-price-persistence.test.ts` (in `test:offer`): weights snapshot + results row written, deterministic ties, within-/cross-candidate ranking | 6 | unit | `best-price-persistence.test.ts` asserting `BestPriceCalculationLog` row with weights | none | none | TO IMPLEMENT |
| S11 | MD §29 L1455; HTML SAFE[3][?] (Deal transitions) | Deal transitions: legal table, CAS, history, exactly-once completion | `deal-state-machine.test.ts:13-74`; `advance-deal-status.test.ts:96,106,112,139`; `cancel-deal-seam.test.ts:119,183` single writer | ALREADY CORRECT | Legal table + CAS + single writer + exactly-once | Extend the legal table for every new status (`DEALER_CONFIRMATION`, `DEALER_EXECUTED`, `FUNDING_PENDING`, `PICKUP_READINESS`, `HANDOVER_PENDING`, `FROZEN_PENDING_RELEASE`) as each phase lands | 6 | state-machine | `deal-state-machine.test.ts` table rows per new status; illegal transitions asserted | production migration (DealStatus values, Phase 1) | `SIGNED → PICKUP_SCHEDULED` open transition | PRESERVED STRONGER SAFEGUARD |
| S12 | MD §29 L1456; HTML SAFE[3][0] | Contract uploads private, dealer-owned, versioned create-before-supersede, fail closed on extraction/scan failure | `app/api/dealer/contracts/__tests__/upload-completes-pipeline.test.ts:121,131,140`; `admin-approve-signable.test.ts:245,265`; `contract-document-ref.test.ts:66` (SSRF); extraction/scan-failure case not named | PARTIAL | Private, dealer-owned, versioned, SSRF guard | Add named cases: extraction failure → never approval; scan failure → FAIL/hold; `document_hash` recorded; retire `contract-upload.service.ts` second writer | 8 | unit, integration | `upload-completes-pipeline.test.ts` extraction-failure + scan-failure cases | none | `contract-upload.service.ts` unscanned `ContractVersion` writer (retired Phase 8) | TO EXTEND |
| S13 | MD §29 L1457; HTML SAFE[3][1] | Approval binds to the reviewed version; rejects upload-during-review races | `admin-approve-signable.test.ts:141,163,278`; `approve-binds-to-reviewed-version.test.ts:159,172` | ALREADY CORRECT | Version-bound approval + race rejection | None; `CONTRACT_SHIELD_OVERRIDDEN` and FAIL→WARNING mutation retired behind this single path | 8 | unit | Existing cases green; append-only scan assertion added | none | Admin `CONTRACT_SHIELD_OVERRIDDEN` synthetic PASS; queue resolve `FAIL → WARNING` mutation | PRESERVED STRONGER SAFEGUARD |
| S14 | MD §29 L1458; HTML SAFE[3][2] | E-sign binds envelope to document bytes by hash; refuses to sign when evidence storage unavailable | `buyer-signing.test.ts:240,295,540`; evidence-storage-unavailable refusal not found by name | PARTIAL | Hash binding + mutation-invalidates-envelope | Add named case: evidence storage unavailable → prepare/sign refused, no envelope sent | 8 | unit | `buyer-signing.test.ts` "evidence store down → refuses" case | `ESIGN_EXECUTED_ARTIFACT_ENABLED` | none | TO EXTEND |
| S15 | MD §29 L1460; HTML SAFE[4][0] | Pickup negotiation: strict turn-taking, proposal-time CAS, two-counter cap, compensating recovery | `lib/services/pickup/__tests__/pickup-coordination.test.ts:163,205,255,277,298` | ALREADY CORRECT | All four properties tested | None; preserve through `PICKUP_READINESS` + crypto token change | 9 | unit, state-machine | Existing cases green after Phase 9 refactor | none | Non-crypto QR nonce | PRESERVED STRONGER SAFEGUARD |
| S16 | MD §29 L1461; HTML SAFE[4][1] | Pickup emails dispatch durably with round-specific idempotency keys | Code `pickup-notifications.service.ts:117,151,228,262` (`pickup-proposed-${dealId}-${roundKey}`); `pickup-coordination.test.ts:220-` asserts fire only, not the key | PARTIAL | Round-keyed idempotency key in code | Assert the exact key string per round on the comms outbox enqueue; move sends onto the dispatcher | 9 | unit | `pickup-coordination.test.ts` key-string assertion; `comms-outbox` enqueue spy | none | Direct Resend send in pickup notifications (allowlist → zero) | TO EXTEND |
| S17 | MD §29 L1447-1448 (refund); HTML SAFE[1][3] | Refund execution idempotency-keyed; never labels a no-charge record as refunded | `refund-deposit-charge.test.ts:53,61,69,77,84`; `deposit-refund-route.test.ts:92,104,144` | ALREADY CORRECT | Idempotency key + NO_CHARGE never REFUNDED | None; admin `REFUND_TRIGGERED` must reuse this primitive (Phase 3 defect 3) | 3 | unit | Existing cases green; admin action-route regression asserts `NO_CHARGE` never advances to `REFUNDED` | refund/money-path activation | Admin `action/route.ts:222-250` REFUNDED-on-NO_CHARGE | PRESERVED STRONGER SAFEGUARD |
| S18a | MD §29 L1462; HTML SAFE[5][0] | Identity firewall between buyer and non-winning dealerships | `dealer-invitation-pii.test.ts:104,118`; `dealer-award.test.ts:99`; `dealer-award.dispatch.test.ts:147`; `esign-dto.test.ts:67` | ALREADY CORRECT | No buyer PII in invitations or to non-winners | None; invitation content test extended for tokenised `auction_invitations` (registered + outside) | 5 | unit | Existing cases green; new invitation-token PII case | none | `outside_auction_invites` embedded offer fields | PRESERVED STRONGER SAFEGUARD |
| S18b | MD §29 L1462; HTML SAFE[5][0] | Firewall lift only at dealer reaffirmation with secure handoff | Lift currently at award (`dealer-award.ts`, `dealer-deals.service.ts`, dealer deal page); `dealer-award.test.ts:99` encodes first name + last initial at award | PARTIAL | First-name-only rule pre-lift | Rewrite award tests: no identity at award; lift + co-buyer/trade packet handoff only after reaffirmation confirmation | 7 | unit, state-machine | `dealer-award.test.ts` "award carries no identity"; `reaffirmation.test.ts` "confirmation lifts firewall" | none | Identity-firewall lift at award (§8.4 last row) | TO IMPLEMENT |
| S19 | MD §29 L1463; HTML SAFE[5][1] | Anti-circumvention monitoring with pattern capture and Operations routing | NONE; code `lib/services/trust/anti-circumvention.service.ts`, `app/api/cron/trust-check/route.ts` | MISSING (test) | Monitoring code exists | Add `lib/services/trust/__tests__/anti-circumvention.test.ts` + `app/api/cron/__tests__/trust-check-route.test.ts`: pattern captured, exception raised through `raiseException`, §25.2 consequence | 5 | unit, integration | New tests reachable from a chained `test:*` script (`test:coverage-check` OK) | none | none | TO IMPLEMENT |
| S20 | MD §29 L1459; HTML SAFE[3][3] | Junk-fee patterns, fee caps, APR validation, payment packing, disclosure checks executed by Contract Shield | `contract-auto-approve.test.ts:78` imports real module but only `planContractAutoAdvance`/`autoAdvanceContractOnPass` (`:101-199`); `buyer-cannot-self-approve.test.ts:64-71`, `buyer-route-surface.test.ts:68,85` are source scans; `contract-shield.service.ts:30,70,145`; APR/packing/disclosure/markup never evaluated (Phase 8 defect 4) | MISSING (no executing test) | Source-scan guards keeping `scanContract` off buyer routes | Add `scan-engine.test.ts` importing real `scanContract`: one case per `ContractScanRuleType` (FEE_CAP, JUNK_FEE_KEYWORD, APR_VALIDATION, PAYMENT_PACKING, DISCLOSURE_CHECK, FINANCE_MARKUP) + offer/recap comparison | 8 | unit | `lib/services/contract-shield/__tests__/scan-engine.test.ts` six rule cases green | none | none | TO IMPLEMENT |
| S-SC-a | HTML SAFE[5][2] (Marketplace integrity, scorecard); MD §25.2 | Dealer scorecard consequences for reaffirmation timeout / rejection | NONE; `rg dealer-scorecard --glob '*.test.ts'` empty; code `lib/services/dealer/dealer-scorecard.service.ts` | MISSING (test) | Scorecard service exists | Add `lib/services/dealer/__tests__/dealer-scorecard.test.ts` (in `test:dealer`): timeout → entry; repeat → SLA violation | 7 | unit | `dealer-scorecard.test.ts` reaffirmation cases | none | none | TO IMPLEMENT |
| S-SC-b | HTML SAFE[5][2]; MD §25.2 | Scorecard consequences for no-shows, contract delays, overdue obligations | Same as S-SC-a | MISSING (test) | Scorecard service exists | Extend `dealer-scorecard.test.ts`: no-show, contract-execution delay, overdue post-completion obligation → consequence + Ops routing | 9 | unit | `dealer-scorecard.test.ts` pickup/obligation cases | none | none | TO IMPLEMENT |

## Owner-gated items

- **Production migration deploy** of the Phase 1 additive wave (`deposits.vehicle_request_id`, `FinancingStatus.NOT_REQUIRED_CASH` + checkpoint values, `InsuranceStatus` review values, new `DealStatus` values, partial unique index on open `vehicle_requests`, trade-in/payoff FKs, `post_completion_obligations`, `dealer_reaffirmations`) — rows T2, T5, T12a, T13a, T13b, T21, T25, T44a, T49, S11, T57.
- **Env flag `DEPOSIT_SETTLEMENT_RECONCILE_ENABLED`** — settlement sweep is OFF by default (`deposit-settlement.test.ts:131`); production enable is an owner action (T15).
- **Env flag `ESIGN_EXECUTED_ARTIFACT_ENABLED`** (§13-D4) — e-sign activation verified in preview only (T10, T27, S14).
- **Refund / money-path activation** — Stripe test mode with `livemode=false` asserted; $400 Premium balance, downgrade refunds, replay cases (T7, T14b, T32, T33a, T36, S4, S17).
- **MarketCheck terms** (§13-D8 criteria-hash cache) — sourcing-band provider-budget assertions (T16).
- **GitHub secret `DATABASE_URL`** — whether it is set and what it points at decides if `test:concurrency` currently writes race fixtures to a real database on every CI run (T53, S8).
- **Supabase preview project / buyer-dealer auth stub** (§12.3 preflight) — required before buyer/dealer Playwright journeys, the full form walk, and three-portal parity run in CI (T1, T39b, T45, T50).
- **Legacy removal** — owner-gated drops of `outside_auction_invites` (T56, S18a) and `credit_applications` retention sign-off (T5).
- **Owner decisions** — scenario harness form (node:test integration vs Playwright, T1); shortlist 100-mile ceiling vs 250-mile authorization (T17); unpaid-Premium revert point, acceptance vs funding clearance (T37); extend `check-test-coverage` to `*.itest.ts`/`*.spec.ts` (T47); supply master §11 text and its repo location (T54); canonical safeguard list 18 vs 20 vs 22 (S rows).

## Legacy paths

- Deposit settlement → immediate auction create + invite (webhook `stripe-idempotency.test.ts:193` and activation reconciler `deposit-activation.test.ts:57`); reconciler `close`-on-zero branch — T2, T15, T19.
- `test:integration` (`tests/integration/*.itest.ts`) run by nobody in CI — T1, T14a.
- Second Playwright root `e2e/` (`playwright.config.ts`, `test:e2e-autopilot`; header comment `:13` names the wrong script) alongside `tests/e2e/` — consolidate before the form-walk suite — T39a.
- `test` core subset vs `test:all` gate; `lib/services/dealer/__tests__/batch2` covered only by `test:dealer-onboarding` — documentation (T46).
- Pre-payment sourcing side effects in `request-progression` and acquisition outreach — T3, T11b.
- Signup `user_metadata.plan` Premium flag and `FEE_PENDING` gating (Phase 3 defect 2) — T4, T7.
- Admin second-$99 intent (`create-intent`/`send-link`) and three refund implementations (`action/route.ts:222-250`) — S4, S17, T31.
- Duplicate `checkDepositProviderEvidence` detector and `SYSTEM_ALERT` Notification as exception store (kept as read-only mirror) — T15, T55.
- Direct Resend/Twilio sends in transaction code (adverse-action, pickup notifications) — S3, S16.
- In-house credit application route/UI, orchestrator/lender services and tests (`lender-adapter.test.ts`, `credit-application.test.ts`); buyer self-advance `POST /api/buyer/financing` — T5, T24.
- Identity-firewall lift at award (`dealer-award.ts`, `dealer-deals.service.ts`, `dealer-award.test.ts:99`) — S18b, T21.
- `outside_auction_invites` parallel path (`outside-invite.test.ts`) — T56, S18a.
- `holds` no-op cron (repurposed for vehicle-hold expiry) — T23.
- `SIGNED → PICKUP_SCHEDULED` open transition with `force: true`; `EXTERNAL_UPLOADED` insurance auto-satisfaction; admin `CONTRACT_SHIELD_OVERRIDDEN` synthetic PASS; queue resolve `FAIL → WARNING` mutation; `contract-upload.service.ts` second `ContractVersion` writer — T25, T26, T28, S11, S12, S13.
- Non-crypto pickup QR nonce — T6, T38, S15.
- Client-controlled search radius (HTML BUILD row 7) — T17.
