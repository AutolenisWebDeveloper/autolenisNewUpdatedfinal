# Control planes — FINAL parity table (§24 cancellation · §26 exception register · §27 communications · §28.3 transition controls · §29 safeguards)

Source map: `parity/control.md` (HEAD 0cd399f) including its "Verification corrections (adversarial pass)" section; no `control-gaps-r*.md` files existed. Where a correction changed a status, the corrected status is used. Paths are relative to `frontend/` unless prefixed. Phase numbers follow IMPLEMENTATION-WORKFLOW §8.1/§8.2; test levels follow §12.1. Row families: C24 = §24 rows; E26 = §26 rows (numbered as in the map, `E26-00` = header rule); M27 = §27 header rules; K27-<line> = §27.1 rows keyed by spec line; T28 = §28.3 controls; G29 = §29 safeguards; D1 = map Deliverable 1 direct-send sites (each is a concrete instance of M27-02); DUP = map "Duplicates" list; X = map out-of-scope findings that land in a phase. HTML `EXC[i]` indices follow the array order at `AutoLenis-Transaction-Flow.html:866-914` (which differs from the Markdown order for rows 15–19 and 43–48). `SAFE[g][i]` = group/item at `:935-940`; `TRANSITION[i]` at `:932`.

Production note carried from IMPLEMENTATION-WORKFLOW §5.2: `comms_outbox`, `lifecycle_touch_schedule`, `idempotency_keys`, `jobs_dead_letter` are **present** in production (probed 2026-09-03), which resolves the map's "table existence UNVERIFIED" items.

Schema additions the §8.2 Phase 1 list does **not** enumerate and that this area needs in the wave: (a) `PickupStatus.CANCELLED` and `PickupStatus.MISSED`; (b) `AuctionInvitationStatus.CANCELLED` (the listed enum has no cancel value); (c) `comms_outbox.channel` CHECK extended with `'in_app'` and `status` CHECK extended with `'cancelled'`; (d) `DepositStatus.DISPUTED` if absent from the matrix; (e) `Offer.expires_at` is listed, but an `OfferStatus.EXPIRED` writer job is behaviour (Phase 6).

| Ref | Document & section | Requirement | Current route / service / model / component / email / cron / queue / admin action | Status | Stronger safeguard to preserve | Exact required change | Phase | Test level | Acceptance evidence | Owner-gated dependency | Legacy path affected | Final disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C24-01 | MD §24 L1213 | Cancellation requires an authorized actor | Route-level only: `app/api/admin/buyers/[buyerId]/workflow/cancel/route.ts:23` (`deals.cancel`), `app/api/admin/deals/[dealId]/action/route.ts:38-40` (SUPER/OPS), `workflow/pause/route.ts:20-21` (any admin); `cancelDeal` actor unchecked `lib/services/deal/deal.service.ts:377-381`; sixth path `DEAL_STAGE_ADVANCED→CANCELLED` `deals/[dealId]/action/route.ts:59-72`, `deal.service.ts:66` | BROKEN | `workflow/cancel` requires `deals.cancel`; admin actions require non-empty reason (`deals/[dealId]/action/route.ts:48-50`) | `cancelTransaction(actor:{id,role})` asserts role in service; `DEAL_STAGE_ADVANCED` refuses CANCELLED/REFUNDED targets; `workflow/pause` gets a permission check | 10 | unit, integration | New `lib/services/deal/__tests__/cancel-transaction.test.ts` (unauthorised role → refused); extend `app/api/admin/deals/__tests__` (STAGE_ADVANCED to CANCELLED → 400) | none | admin `deals/[id]/action` STAGE_ADVANCED→CANCELLED; `workflow/pause` | TO IMPLEMENT |
| C24-02 | MD §24 L1213 | Reason is mandatory on every cancel path | `cancelDeal(dealId, reason)` string `deal.service.ts:378-379`; buyer cancel writes no reason `app/api/buyer/requests/[requestId]/cancel/route.ts:27-32`; `CancellationReason` enum unused `prisma/schema.prisma:1921-1927` | PARTIAL | Admin routes reject empty reason (`deals/[dealId]/action/route.ts:48-50`) | Orchestrator signature `reason: CancellationReason, note?: string`; buyer request cancel passes reason and records it on the event | 10 | unit, integration | `cancel-transaction.test.ts`: missing reason → validation error; DB assertion `vehicle_request_events.reason` populated | none | buyer `requests/[id]/cancel` | TO EXTEND |
| C24-03 | MD §24 L1213 | Current stage recorded at cancellation | Deal: `DealStatusHistory.fromStatus` `deal.service.ts:167-176` (write swallowed `:176`); auction cancel records no prior stage `app/api/admin/auctions/[auctionId]/action/route.ts:176`; request cancel event only `requests/[requestId]/cancel/route.ts:28-31` | PARTIAL | `expectedFrom` pin on `cancelDeal` (`deal.service.ts:385-391`) | Orchestrator snapshots prior status for deal, auction, request; history write moves inside the CAS `$transaction` | 10 | integration, state-machine | Extend `lib/services/deal/__tests__/cancel-deal-seam.test.ts`: history row present for auction/request cancels; failed history write fails the cancel | none | none | TO EXTEND |
| C24-04 | MD §24 L1213 | Unsent sourcing and outreach stopped | Intake/coverage act only on `SUBMITTED/INTAKE/ACTIVE_SOURCING` (`lib/services/acquisition/intake-processor.service.ts:282`, `request-coverage-gate.service.ts:39`); R3/R4 drain guards `lifecycle-touch-drain.service.ts:136,157,174`; no cancel path calls `cancelDepositReminderTouches`/`cancelPreCheckoutTouches` (`:599-660`; callers `stripe/route.ts:252`, `deposit/create-intent/route.ts:277` only); outbox has no cancel state `prisma/manual_supabase_sql/comms_outbox.sql:28-29` | PARTIAL | Drain-time guards `depositConversionResolved`/`preCheckoutResolved` (`lifecycle-touch-drain.service.ts:41`); status-scoped intake | Orchestrator calls touch cancellers by base_key, `cancelByKey(cancel_key)` on outbox, flags request so in-flight intake stages skip | 10 | unit, integration | `cancel-transaction.test.ts`: touches cancelled, outbox rows `cancelled`, intake stage no-op after cancel | none | `lifecycle_touch_schedule` R4; QStash R3 | TO EXTEND |
| C24-05a | MD §24 L1213 | Auction closed on cancel — invitation cancel state | `AuctionInvitation` has no status column `prisma/schema.prisma:518-531` | MISSING | none | Add `CANCELLED` to the Phase 1 `AuctionInvitationStatus` enum (not in §8.2 list) | 1 | integration | `prisma/__tests__` enum assertion; migration re-apply no-op | production migration (Phase 1 wave) | none | TO IMPLEMENT |
| C24-05b | MD §24 L1213 | Auction closed on cancel — CAS close + invitations cancelled | `AUCTION_REFUND_TRIGGERED` counts SUBMITTED offers only, no status guard, raw `auction.update CANCELLED` `auctions/[auctionId]/action/route.ts:158-176`; `pauseBuyerWorkflow` writes CANCELLED, `resumeBuyerWorkflow` expects PENDING `admin-buyer-command-center.service.ts:867-876,899-908`; `cancelDeal` ignores auction `deal.service.ts:377-402` | BROKEN | none | Orchestrator closes auction via `updateMany WHERE status IN (…)`, refuses when an ACCEPTED offer/Deal exists unless cascading, marks invitations CANCELLED; replace pause misuse with a PAUSED value or a flag | 10 | integration, state-machine, concurrency | `cancel-transaction.test.ts`: auction with ACCEPTED offer → cascade or refuse; concurrent cancel vs select-offer; pause→resume round-trip | none | `pauseBuyerWorkflow` CANCELLED-as-pause | TO IMPLEMENT |
| C24-06 | MD §24 L1213 | Affected dealerships notified | In-app only on admin auction cancel, mislabelled `type:'AUCTION_STARTED'` `auctions/[auctionId]/action/route.ts:193-207`; deal cancel never tells the winning dealer | PARTIAL | none | Orchestrator enqueues dealer cancellation email + in-app through dispatcher (`template_key=dealer_cancellation`) | 10 | integration | `cancel-transaction.test.ts`: outbox rows for winning + invited dealers; register completeness test lists `dealer_cancellation` | none | none | TO IMPLEMENT |
| C24-07 | MD §24 L1213 | Unsigned envelopes voided | `voidEnvelope` `lib/services/esign/esign.service.ts:56-64`; `voidEnvelopeInternal` `buyer-signing.service.ts:546`; sole caller `app/api/admin/deals/[dealId]/esign/void/route.ts:41`; no cancel path calls either | MISSING | CAS void (`esign.service.ts:60-63`); terminal envelope records immutable | Orchestrator calls one void function (`voidEnvelopeInternal`) for every non-terminal envelope incl. co-buyer rows; `voidEnvelope` becomes a thin wrapper | 10 | integration, state-machine | `cancel-transaction.test.ts`: SIGNING_PENDING deal → envelopes VOIDED; COMPLETED envelope untouched | none | `esign.service.voidEnvelope` (duplicate) | TO CONSOLIDATE |
| C24-08a | MD §24 L1213 | Pickup cancelled — status value | `PickupStatus` has no CANCELLED `prisma/schema.prisma:1583-1593` | MISSING | none | Add `PickupStatus.CANCELLED` (and `MISSED`, see E26-37) to the Phase 1 wave (not in §8.2 list) | 1 | integration | `prisma/__tests__` enum assertion | production migration (Phase 1 wave) | none | TO IMPLEMENT |
| C24-08b | MD §24 L1213 | Release tokens revoked on cancel | Scan resolves by `qrCodeData` and enforces `qrExpiresAt` `app/api/dealer/pickup/scan/route.ts:36-37,65-67`; `regenerateQr` rewrites token `lib/services/pickup/pickup.service.ts:67-80`; no cancel path invokes it | MISSING | Token lookup by stored value + expiry check (regenerate = revoke) — keep | Orchestrator sets pickup CANCELLED and calls the Phase 9 `revokeToken()` (`token_revoked_at`) | 10 | integration | `cancel-transaction.test.ts`: revoked token scan → 422; pickup CANCELLED | none | non-crypto QR nonce (replaced Phase 9) | TO EXTEND |
| C24-09a | MD §24 L1213 | Payment treatment — admin cancel/refund actions consolidated | `DEAL_CANCELLED` refunds before transition `deals/[dealId]/action/route.ts:177-190`; `AUCTION_REFUND_TRIGGERED` `auctions/[auctionId]/action/route.ts:165-176`; `REFUND_TRIGGERED` advances to REFUNDED on `NO_CHARGE` `:222-250`; `refund.service.ts:23-44` | PARTIAL | `NO_CHARGE` never labelled refunded (`refund.service.ts:27-28`); key `refund-deposit-${id}` (`:31`); status-guarded flip | Split admin actions onto `cancelDeal` seam + one refund primitive; refund only after successful CAS; `REFUNDED` only on real refund (workflow Phase 3 defect 3) | 3 | unit, integration | Extend `app/api/admin/deals/__tests__` + `lib/services/payment/__tests__/refund.service.test.ts`: NO_CHARGE never REFUNDED; failed CAS → no refund | refund/money-path activation (refunds manual) | admin `DEAL_CANCELLED` auto-refund | TO CONSOLIDATE |
| C24-09b | MD §24 L1213 | Payment treatment recorded explicitly (retain / refund / review) | `cancelDeal` decides nothing `deal.service.ts:377-402`; buyer request cancel decides nothing `requests/[requestId]/cancel/route.ts:9-35` | PARTIAL | Deposit never auto-refunded (`deposit-activation.service.ts:33-41`) | Orchestrator persists `paymentTreatment` + raises Finance `queue_items` row when REVIEW; refund executes after CAS via the single primitive | 10 | unit, integration | `cancel-transaction.test.ts`: treatment persisted; REVIEW → queue row; DB assertion deposit status unchanged on RETAIN | refund/money-path activation | none | TO IMPLEMENT |
| C24-10 | MD §24 L1213 | Buyer notified of cancellation | In-app delegated to callers `acquisition-comms.ts:295-308`; SMS flag-gated `:341-343`, needs CRM contact `:545-556`; `cancelBuyerWorkflow` path silent `admin-buyer-command-center.service.ts:927-975`; no email anywhere | PARTIAL | consent-aware `dealStatusCommsPlan` exhaustiveness guard | Orchestrator enqueues buyer cancellation email + in-app (see K27-1375) with state recheck | 10 | integration | `cancel-transaction.test.ts`: buyer outbox row + in-app on every path; register test | owner decision Q6 (SMS flag after outbox routing) | `ACQUISITION_COMMS_SMS_ENABLED` direct SMS | TO IMPLEMENT |
| C24-11 | MD §24 L1213 | Full history preserved; terminal deals never re-cancelled | `cancelDeal` `force:true`, `expectedFrom: deal.status` — writes COMPLETED/REFUNDED → CANCELLED if called directly `deal.service.ts:129,385-391`; guards only in callers `admin-buyer-command-center.service.ts:935-938`, `deals/[dealId]/action/route.ts:168`; test covers race + already-CANCELLED only `cancel-deal-seam.test.ts:105,167` | PARTIAL | `DealStatusHistory` + `BuyerActivityEvent DEAL_CANCELLED` + `adminAuditLog` (`deal.service.ts:167-186,398-400`; `:962-974`); honest `cancelled:false` | `cancelDeal` refuses TERMINAL states in-service; history write non-swallowed | 10 | unit, state-machine | `cancel-deal-seam.test.ts` add failing-first case "already COMPLETED → refused" | none | none | TO EXTEND |
| C24-12 | MD §24 L1215 | One cancellation orchestration performs everything | Six writers: `cancelDeal` `deal.service.ts:377-402`; admin `DEAL_CANCELLED` `deals/[dealId]/action/route.ts:167-219`; `DEAL_STAGE_ADVANCED→CANCELLED` `:59-72`; `AUCTION_REFUND_TRIGGERED` `auctions/[auctionId]/action/route.ts:158-208`; `pauseBuyerWorkflow` `admin-buyer-command-center.service.ts:862-892`; buyer `requests/[requestId]/cancel/route.ts:9-35` | MISSING | `cancelDeal` is the correct seam to build on | New `lib/services/deal/cancel-transaction.service.ts` composing `cancelDeal`, `voidEnvelopeInternal`, invitation cancel, touch cancellers, `cancelByKey`, pickup revoke, comms, `paymentTreatment`; all six entries route through it | 10 | unit, integration, state-machine, concurrency, playwright | `cancel-transaction.test.ts` (all six entry points); single-writer guard extended in `workflow-stage-seam.test.ts`; Playwright: admin cancels SIGNING_PENDING deal → envelope void, dealer notice, buyer notice | none | all six paths | TO IMPLEMENT |
| C24-13a | MD §24 L1217; HTML DEALSTATES[17] | `FROZEN_PENDING_RELEASE` enum value | Absent; `DealStatus` ends at REFUNDED `prisma/schema.prisma:1513-1530`; `rg FROZEN_PENDING_RELEASE` → none | MISSING | none | Add value in Phase 1 wave (listed in §8.2) | 1 | integration | `prisma/__tests__` enum assertion; migration re-apply no-op | production migration (Phase 1 wave) | none | TO IMPLEMENT |
| C24-13b | MD §24 L1217; HTML DEALSTATES[17] | Post-execution cancellation becomes coordination state | `cancelDeal` force from any non-terminal incl. SIGNED `deal.service.ts:385-391`; `canTransition` allows CANCELLED from any non-terminal `:66` | MISSING | none | `TRANSITIONS`: `DEALER_EXECUTED`/`FUNDING_PENDING`/later → `FROZEN_PENDING_RELEASE` → COMPLETED or CANCELLED; orchestrator routes post-execution cancels there with `frozen_at/frozen_reason` | 10 | state-machine, unit | Failing-first `canTransition` cases in `lib/services/deal/__tests__/deal-transitions.test.ts`; `cancel-transaction.test.ts` post-execution → FROZEN | none | none | TO IMPLEMENT |
| C24-14 | MD §24 L1211 | Scope "before contract execution": unilateral cancel not permitted after execution | No executed boundary in code; cancel legal from SIGNED/PICKUP_SCHEDULED `deal.service.ts:66` | BROKEN | none | Boundary = `DEALER_EXECUTED` (Phase 8); `cancelDeal`/admin actions refuse beyond it (use C24-13b) | 10 | state-machine | Same suite as C24-13b: cancel at PICKUP_SCHEDULED → FROZEN, never CANCELLED | none | admin `deals/[id]/action` | TO IMPLEMENT |
| E26-00a | MD §26 L1235 | `queue_items` table with owner, buyer-visible status, required action, deadline, return point | No model; orphan enums `prisma/schema.prisma:1890-1907` (`migration.sql:86,89`); no manual SQL table | MISSING | none | Phase 1 `queue_items` per §8.2 (exception_code, owner_role, refs, buyer_visible_status, required_action, deadline_at, return_point, idempotency_key unique) + additive `QueueItemType` values | 1 | integration | `prisma/__tests__/queue-items.test.ts` (DB-backed unique idempotency_key); chain guards | production migration (Phase 1 wave) | none | TO IMPLEMENT |
| E26-00b | MD §26 L1235 | Single exception writer used by every reconciler and stage | Derived admin queues `lib/services/admin/admin-queue.service.ts:7-39`; resolve swallows DB errors and still audits `:41-107,89-94` | MISSING | Resolve requires explicit decision words (`:44-83`) — keep | New `lib/services/operations/queue-item.service.ts` `raiseException()` upsert on idempotency key, `assign/escalate/resolve/listOpen`; admin queues re-read from `queue_items`; `SYSTEM_ALERT` becomes read-only mirror | 2 | unit, integration | New `lib/services/operations/__tests__/queue-item.service.test.ts` (idempotent raise, resolve fails on DB error); admin queue route tests | none | `Notification(SYSTEM_ALERT)` as ops store | TO IMPLEMENT |
| E26-01 | MD §26 L1239; HTML EXC[0] | Buyer does not verify → remind 1h/24h/72h, abandon | No reminder cron (`app/api/cron` none); resend user-initiated `app/api/auth/resend-verification/route.ts:154` | MISSING | none | `verification_reminder_1..3` dispatcher sequence with state recheck; abandon at 72h; `raiseException(owner=System)` | 2 | unit, integration | New `lib/services/buyer/__tests__/verification-reminders.test.ts`: three rows scheduled, cancelled on verify | none | none | TO IMPLEMENT |
| E26-02 | MD §26 L1240; HTML EXC[1] | Onboarding location unusable → block stages, correction task | No production write path for city/state/zip (`docs/plans/BUYER-LOCATION-GAP.md:14-31`); matcher fails closed `dealer-invitation.service.ts:199-210` | MISSING | Matcher fail-closed on null location — keep | Phase 2 Stage 2 address capture + geocode; hard location gate predicate; `raiseException(LOCATION_UNUSABLE, owner=Buyer)` blocking sourcing | 2 | unit, integration | Defect #1 regression test (§7) + `queue-item.service.test.ts` LOCATION_UNUSABLE case | none | none | TO IMPLEMENT |
| E26-03 | MD §26 L1241; HTML EXC[2] | Prequal manual/OFAC review → hold, route to reviewer | Derived `PREQUAL_MANUAL`/`OFAC_ALERT` `admin-queue.service.ts:9,11`; `ofac.service.ts:4-11` P0 alert; `prequal-sla-escalation/route.ts:25-58` | PARTIAL | Explicit CLEAR/CONFIRM resolve (`admin-queue.service.ts:44-83`); OFAC fail-closed | Back with `queue_items` row owner=Compliance, `deadline_at` from SLA; keep derived view as mirror | 2 | unit, integration | `queue-item.service.test.ts`: OFAC hold raises once; SLA escalation updates `escalated_at` | none | derived admin queue | TO EXTEND |
| E26-04 | MD §26 L1242; HTML EXC[3] | Provider delay → retry, honest processing notice | Held MANUAL_REVIEW + `createAlertOnce` `prequal.service.ts:103-121`; under-review email `:703`; retry UNVERIFIED | PARTIAL | fail-closed hold | Retry job with bounded attempts; `provider_delay` template via dispatcher; queue row owner=System | 2 | unit, integration | Extend `lib/services/prequal/__tests__`: provider timeout → queue row + delay notice, retry succeeds → row resolved | none | none | TO EXTEND |
| E26-05 | MD §26 L1243; HTML EXC[4] | Prequal decline → decision + adverse action | Adverse-action email with persisted outcome `prequal.service.ts:640-670` | PARTIAL | Outcome discrimination SENT/DUPLICATE/FAILED/THREW (§29) — keep | Queue row owner=Compliance only on FAILED/THREW; email via dispatcher preserving outcome persistence | 2 | unit, integration | Prequal tests: FAILED outcome → queue row; SENT → none | none | R1 `sendIdempotent` | TO EXTEND |
| E26-06 | MD §26 L1244; HTML EXC[5] | Approval expires mid-transaction → pause, require renewal | `isPrequalValid` used only by buyer pages/create-intent/journey-status (`rg` → `lib/constants.ts`, `app/buyer/*`, `deposit/create-intent`, `journey-status`); `prequal-stale-cleanup/route.ts:24` counts; `prequal-ibv-reminders/route.ts:85-105` raw Resend | PARTIAL | none | Phase 2 approval recheck helper consumed at payment/selection/contract request; expiry → queue row owner=Buyer + pause transition | 2 | unit, state-machine | New `lib/services/prequal/__tests__/approval-recheck.test.ts`; deal transition test: expired prequal blocks CONTRACT_PENDING | none | none | TO EXTEND |
| E26-07 | MD §26 L1245; HTML EXC[6] | Payment failure → preserve request, safe retry | Deposit `payment_intent.payment_failed` → FAILED via matrix, request untouched, no buyer comms `app/api/webhooks/stripe/route.ts:613-623`; fee failure in-app only `:625-637`; FAILED terminal `lib/payments/deposit-state.ts:29-40` | PARTIAL | State matrix — keep | Reserve FAILED for cancelled/expired intents (workflow Phase 3 defect 1); `payment_failed` template via dispatcher; queue row owner=Buyer | 3 | unit, integration | `test:webhooks` + `test:payments`: declined then retry settles once; outbox row `payment_failed` | none | none | TO EXTEND |
| E26-08 | MD §26 L1246; HTML EXC[7] | Payment succeeded, webhook missed → reconcile, alert | `deposit-activation-reconcile` cron; `deposit-settlement.service.ts:104-125`; `deposit-activation.service.ts:70-86` deduped SYSTEM_ALERT | PARTIAL | Reconciler sweeps by state not window (`:36-38`) — keep | Reconciler enabled + `raiseException(PAYMENT_EXCEPTION, owner=Finance, stripe ref)` on one rail (fold `checkDepositProviderEvidence`, §13-D12) | 3 | integration | Extend `lib/services/payment/__tests__/deposit-settlement.test.ts`: recovered gap → queue row with PI id | none | duplicate `checkDepositProviderEvidence` detector | TO CONSOLIDATE |
| E26-09 | MD §26 L1247; HTML EXC[8] | Payment unroutable → immediate exception, never absorbed | Webhook accepts, no state change, deduped SYSTEM_ALERT `stripe/route.ts:33-54,606` | PARTIAL | Never absorbed — keep | Replace SYSTEM_ALERT with `raiseException(owner=Finance)` | 3 | integration | `test:webhooks`: unroutable PI → queue row, 200 returned | none | `SYSTEM_ALERT` store | TO EXTEND |
| E26-10 | MD §26 L1248; HTML EXC[9] | Disputed/refunded → hold fulfilment, stop outreach | `charge.dispute.created` → audit only `stripe/route.ts:769-797`; `charge.refunded` flips deposit/fee `:643-760`; no hold, no outreach stop | PARTIAL | Refund flip status-guarded | Dispute → Deposit DISPUTED (matrix), hold request/deal, cancel touches + outbox by `cancel_key`, queue row owner=Finance | 3 | integration, state-machine | `test:webhooks`: dispute event → DISPUTED, touches cancelled, queue row | production migration if `DepositStatus.DISPUTED` absent | none | TO IMPLEMENT |
| E26-11 | MD §26 L1249; HTML EXC[10] | No coverage at 250 mi → radius authorization, 24h/72h reminders, 14-day close | Coverage gate parks request `lib/services/acquisition/request-coverage-gate.service.ts:102-190,211`; `coverage-hold-reconcile` cron; no buyer notice/reminders/close | PARTIAL | Fail-closed hold | `RADIUS_AUTHORIZATION_REQUIRED` status (Phase 1), buyer notice + reminders via dispatcher, 14-day close job, queue row owner=Buyer/Ops | 5 | unit, integration, state-machine | New `lib/services/acquisition/__tests__/radius-authorization.test.ts`; Playwright zero-coverage → authorization screen | none | none | TO EXTEND |
| E26-12 | MD §26 L1250; HTML EXC[11] | Zero dealer coverage → review, then close or expand | `deposit-activation.service.ts:33-41` closes no-dealer auction; `intake-pipeline.service.ts:86-87` `zeroSupply` | PARTIAL | Never auto-refunds — keep | Readiness hold instead of auto-close; `raiseException(SOURCING_EXCEPTION, owner=Ops)` + buyer notice | 5 | unit, integration | Sourcing case tests: zero coverage → queue row, no auction close | none | deposit-activation `close`-on-zero branch (retired Phase 3/5) | TO EXTEND |
| E26-13 | MD §26 L1251; HTML EXC[12] | Invitation bounced → replace contact/rooftop in window | Resend webhook suppresses + pauses prospect sequence `app/api/webhooks/resend/route.ts:102-107,188-189`; invitation row untouched | PARTIAL | Suppression on bounce — keep | Map bounce → `auction_invitations.status=BOUNCED`, `bounced_at`; queue row owner=Ops; replacement flow | 5 | integration | Extend `app/api/webhooks/__tests__/resend.test.ts`: bounce → invitation BOUNCED + queue row | none | none | TO EXTEND |
| E26-14 | MD §26 L1252; HTML EXC[13] | Zero offers at close → owned case, one relaunch without second $99 | `processAuctionClose` notices `auction.service.ts:161-192`; `AUCTION_REOPENED` `auctions/[auctionId]/action/route.ts:208-225` | PARTIAL | Deposit retained; atomic close claim | `raiseException(AUCTION_EXCEPTION, owner=Ops)` on zero-offer close; relaunch counter (one) | 6 | unit, integration | Extend `lib/services/auction/__tests__/auction-close.test.ts`: zero offers → queue row; second reopen refused | none | none | TO EXTEND |
| E26-15 | MD §26 L1253; HTML EXC[43] | Shortlisted candidate stale/sold mid-auction → drop, tell buyer | `AuctionVehicle` has no status/dropped field (`prisma/schema.prisma` model); no drop path in `lib/services/auction/*` | MISSING | none | Phase 1 `candidate_status`/`dropped_reason`; Phase 4 revalidation drops candidate + buyer notice + queue row owner=Ops | 4 | unit, integration | New `lib/services/auction/__tests__/candidate-revalidation.test.ts` | none | none | TO IMPLEMENT |
| E26-16 | MD §26 L1254; HTML EXC[44] | No in-radius inventory → lead with custom request | Not traced in this area (inventory area) | UNVERIFIED | none | Owned by inventory table; Phase 4 qualified-results empty state | 4 | playwright | Inventory area Playwright journey (thin/zero results offers request path) | none | none | UNVERIFIED — inventory area owns the evidence |
| E26-17 | MD §26 L1255; HTML EXC[45] | Provider budget ceiling → alert before ceiling | `lib/services/inventory/inventory-budget-alert.service.ts:95-120` deduped SYSTEM_ALERT | PARTIAL | Alerts before ceiling — keep | Route through `raiseException(INVENTORY_EXCEPTION, owner=Ops)` | 4 | unit | Extend `inventory-budget-alert` tests: WARNING → queue row once | none | `SYSTEM_ALERT` store | TO EXTEND |
| E26-18 | MD §26 L1256; HTML EXC[46] | Sweep returns fewer listings → failed run | Not traced (inventory area) | UNVERIFIED | none | Inventory area: run-size anomaly → `raiseException` | 4 | unit | Inventory area suite | none | none | UNVERIFIED — inventory area owns the evidence |
| E26-19 | MD §26 L1257; HTML EXC[47] | No stored location on inventory page → ask for ZIP | Not traced (inventory area) | UNVERIFIED | none | Inventory area ZIP prompt | 4 | playwright, visual | Inventory area journey | none | none | UNVERIFIED — inventory area owns the evidence |
| E26-20 | MD §26 L1258; HTML EXC[14] | All offers exceed budget → never qualified, route to recovery | Over-budget rejected at submission `lib/services/offer/offer.service.ts:47-52,77`; no recovery routing | PARTIAL | Server-side budget rejection — keep | Queue row owner=Ops when zero in-budget offers at close; buyer recovery notice | 6 | unit | `auction-close.test.ts`: zero in-budget → queue row | none | none | TO EXTEND |
| E26-21 | MD §26 L1259; HTML EXC[15] | Buyer does not select → remind before expiry; revalidate or close | Nudge engine has no selection stage `prisma/schema.prisma:1781-1787`, `nudge.service.ts:49-100`; only QStash `offer-follow-up` `app/api/jobs/offer-follow-up/route.ts:6,25` / R4 `auction_closing`; no `OfferStatus.EXPIRED` writer | PARTIAL | `hasSelectedOffer` send-time guard | Offer `expires_at` (Phase 1) + expiry job writing EXPIRED, reminder via dispatcher, queue row owner=Buyer | 6 | unit, integration | New `lib/services/offer/__tests__/offer-expiry.test.ts`; reminder outbox row with recheck | none | QStash `offer-follow-up` | TO IMPLEMENT |
| E26-22 | MD §26 L1260; HTML EXC[16] | Winning dealer rejects/times out → return to offers, scorecard | No confirmation state `prisma/schema.prisma:1513-1530` | MISSING | none | `dealer_reaffirmations` + `DEALER_CONFIRMATION` (Phase 1); 24h timeout job → return-to-offers, scorecard entry, queue row | 7 | unit, state-machine, integration | New `lib/services/deal/__tests__/reaffirmation.test.ts` (timeout → offers reopened, scorecard row) | none | award-time identity lift (`dealer-award.ts`) | TO IMPLEMENT |
| E26-23 | MD §26 L1261; HTML EXC[17] | Dealer changes material terms → side-by-side accept/reject | Pre-close revisions only `lib/services/offer/offer-revision.service.ts` | PARTIAL | Above-budget refused at submission | Material-change proposal on reaffirmation (jsonb + `buyer_decision`), refuse above ceiling, auto-apply lower OTD | 7 | unit, integration, playwright | `reaffirmation.test.ts` material-change cases; Playwright dealer reaffirms with change → buyer accepts | none | none | TO IMPLEMENT |
| E26-24 | MD §26 L1262; HTML EXC[18] | Vehicle hold expires → extend or release | `holds` cron is a deposit-hold no-op `app/api/cron/holds/route.ts:1-21`; no vehicle-hold model | MISSING | none | `vehicle_hold_until`/`hold_until` (Phase 1); repurpose `holds` cron for hold expiry (§13-D26); notices + queue row | 7 | unit, integration | New `app/api/cron/__tests__/holds-route.test.ts`: expiring hold → notice, expired → release + queue row | none | `holds` no-op cron (repurposed) | TO IMPLEMENT |
| E26-25 | MD §26 L1263; HTML EXC[19] | Vehicle sold before contract/pickup → return to offers or replacement | No path found | MISSING | none | Reaffirmation/candidate `availability_confirmed=false` path → return-to-offers + queue row owner=Ops | 7 | unit, state-machine | `reaffirmation.test.ts` sold case | none | none | TO IMPLEMENT |
| E26-26 | MD §26 L1264; HTML EXC[20] | Outside winner fails verification → block advancement | `lib/services/auction/outside-invite.service.ts`, `lib/services/dealer/dealer-verification.service.ts` exist; gating not traced | UNVERIFIED | none | Phase 7 claim/verification/agreement sequencing; `advanceDealStatus` validator requires verified dealer | 7 | unit, state-machine | Trace in Phase 7 opening; test: unverified outside dealer cannot leave DEALER_CONFIRMATION | none | `outside_auction_invites` | UNVERIFIED — gating wiring not traced |
| E26-27 | MD §26 L1265; HTML EXC[21] | Recap disputed → new recap version | No recap entity | MISSING | none | `deal_recaps` (Phase 1) versions; dispute → new version; repeated failure → queue row | 7 | unit, integration | New `lib/services/deal/__tests__/deal-recap.test.ts` | none | none | TO IMPLEMENT |
| E26-28 | MD §26 L1266; HTML EXC[22] | Financing fails/expires → alternate path, no auto-cancel | `routeToReview` → `FinancingReviewTask` `financing-orchestrator.service.ts:69,156,167,176`; `review-queue.service.ts:34-45`; `FinancingStatus` has no EXPIRED `prisma/schema.prisma:1711-1716` | PARTIAL | Idempotent routing (`openWhere`); no auto-cancel | `FinancingStatus.FAILED/EXPIRED` (Phase 1); checkpoint service raises `queue_items` FINANCING_EXCEPTION; fold `FinancingReviewTask` into `queue_items` follow-ups; buyer alternate-path notice | 7 | unit, state-machine | Financing checkpoint suite: FAILED → queue row + notice, deal not cancelled | none | `FinancingReviewTask`; dormant orchestrator/review-queue | TO CONSOLIDATE |
| E26-29 | MD §26 L1267; HTML EXC[23] | Funding not cleared → block release | No FUNDING state `prisma/schema.prisma:1513-1530` | MISSING | none | `FUNDING_PENDING` (Phase 1) + six-item clearance; release gate in `advanceDealStatus` | 8 | state-machine, unit | Deal transitions test: PICKUP_READINESS unreachable without `funding_cleared_at` | none | none | TO IMPLEMENT |
| E26-30 | MD §26 L1268; HTML EXC[24] | Trade payoff stale → refresh before clearance | `TradeInSubmission` has no payoff/lien fields; `rg -i payoff lib/services/trade-in` → none | MISSING | none | `payoff_good_through_date`/`verified_payoff_cents` (Phase 1, captured Phase 4); clearance checks date → queue row owner=Ops | 8 | unit | Funding clearance test: stale payoff blocks clearance | none | none | TO IMPLEMENT |
| E26-31 | MD §26 L1269; HTML EXC[25] | Insurance rejected/expired → name defect, block release | `InsuranceStatus` lacks REJECTED/EXPIRED `prisma/schema.prisma:1493-1502`; derived `INSURANCE_EXCEPTION` at 72h `admin-queue.service.ts:13-18` | MISSING | Release gate `INSURANCE_SATISFIED` (`deal.service.ts:41-45,134-138`) — keep the gate, tighten to VERIFIED/POLICY_BOUND | `UNDER_REVIEW/REJECTED/EXPIRED` + `rejection_reason` (Phase 1); Ops decision writes reason; buyer notice; queue row | 8 | unit, state-machine | Insurance suite: REJECTED blocks release, notice carries reason | none | `EXTERNAL_UPLOADED` counted as satisfied (§13-D31) | TO IMPLEMENT |
| E26-32 | MD §26 L1270; HTML EXC[26] | Contract overdue from dealer → remind at deadline, escalate | No deadline logic (`grep overdue\|deadline lib/services/dealer/dealer-contract.service.ts` → none) | MISSING | none | `document_requests.dueAt` 24h set from CONTRACT_PENDING transition; overdue job → reminder + `raiseException(owner=Ops)` | 8 | unit, integration | New `lib/services/contract/__tests__/contract-request-deadline.test.ts` | none | `requestDocument()` (0 callers, reused) | TO IMPLEMENT |
| E26-33 | MD §26 L1271; HTML EXC[27] | Contract mismatch → correction + rescan, name discrepancies | `contract-comparison.service.ts`; dealer issues email + buyer alert `contract-shield.service.ts:369-372`; derived `CONTRACT_FAIL` `admin-queue.service.ts:10` | PARTIAL | Approval binds to reviewed version (`dealer-contract.service.ts:78-112`) | Comparison against offer + reaffirmation + recap; `raiseException(owner=Ops)` with named discrepancies; both-party notices via dispatcher | 8 | unit, integration | Contract-shield suite: mismatch → queue row listing discrepancies | none | admin queue CONTRACT_FAIL resolve mutating scans | TO EXTEND |
| E26-34 | MD §26 L1272; HTML EXC[28] | Contract extraction failure → retry, never approval | Empty extraction throws `lib/services/contract-shield/extract-text.ts:41-48`; retry UNVERIFIED | PARTIAL | Fail-closed — keep | Bounded retry + `raiseException(owner=Ops)` after final failure | 8 | unit | Extract-text tests: failure never yields PASS; retry counted | none | none | TO EXTEND |
| E26-35 | MD §26 L1273; HTML EXC[29] | Buyer/co-buyer does not sign → remind, expire 14d, reissue | 14-day TTL, lazy + cron CAS expiry `buyer-signing.service.ts:41,385-394,670-680`, `app/api/cron/esign-envelope-expiry/route.ts:17`; no reminders; derived `ESIGN_EXCEPTION` `admin-queue.service.ts:19-24`; co-buyer absent | PARTIAL | CAS expiry; hash-bound signing | Reminder sequence via dispatcher; co-buyer envelopes (`signer_kind`, Phase 1); queue row owner=Buyer | 8 | unit, integration | Esign suite: reminders scheduled with recheck; co-buyer envelope expiry | `ESIGN_EXECUTED_ARTIFACT_ENABLED` (preview only) | none | TO EXTEND |
| E26-36 | MD §26 L1274; HTML EXC[30] | Dealer does not execute → escalate, release blocked | No DEALER_EXECUTED state | MISSING | none | `DEALER_EXECUTED` (Phase 1) required predecessor (§13-D29); overdue → queue row owner=Ops | 8 | state-machine, unit | Deal transitions test: SIGNED → PICKUP_READINESS illegal without DEALER_EXECUTED | none | `SIGNED → PICKUP_SCHEDULED` with `force` | TO IMPLEMENT |
| E26-37 | MD §26 L1275; HTML EXC[31] | Pickup missed → reschedule, revoke + reissue token | No MISSED status `prisma/schema.prisma:1583-1593`; `regenerateQr` `pickup.service.ts:67`; derived `PICKUP_EXCEPTION` at 7d `admin-queue.service.ts:25-30` | PARTIAL | QR bound to deal (`qr.service`) | `PickupStatus.MISSED` (Phase 1); missed job → MISSED, revoke token, reschedule notice, queue row owner=Dealership | 9 | unit, integration | Pickup suite: past-due unconfirmed → MISSED + token revoked | none | none | TO IMPLEMENT |
| E26-38 | MD §26 L1276; HTML EXC[32] | Identity mismatch at handover → block release, urgent exception | No identity check; check-in writes CHECKED_IN only `pickup.service.ts:87-94`; scan validates token/owner/expiry `scan/route.ts:36-70`; `qr.service.ts:10-13` | MISSING | Token/ownership/expiry checks — keep | `identity_verified_at` (Phase 1); handover step requires it; mismatch → `raiseException(urgent, owner=Ops)` | 9 | unit, integration | Pickup suite: release without `identity_verified_at` refused | none | none | TO IMPLEMENT |
| E26-39 | MD §26 L1277; HTML EXC[33] | Trade appraisal changed at handover → accept or contract revision | Not traced | UNVERIFIED | none | Phase 9 changed-appraisal path → contract revision (Phase 8 flow) | 9 | unit, state-machine | Trace at Phase 9 opening; test: changed appraisal → CONTRACT_REVIEW | none | none | UNVERIFIED — trade-in handover path not traced |
| E26-40 | MD §26 L1278; HTML EXC[34] | Delivery discrepancy → hold completion, open case | No path | MISSING | none | Delivery variant evidence + `raiseException(owner=Ops)` blocking `COMPLETED` | 9 | unit, state-machine | Pickup suite: open discrepancy case blocks completion | none | none | TO IMPLEMENT |
| E26-41 | MD §26 L1279; HTML EXC[35] | Dealer released, buyer not confirmed → remind, never auto-complete | Dealer scan advances to COMPLETED `app/api/dealer/pickup/scan/route.ts:90`; `PICKUP_SCHEDULED→COMPLETED` legal `deal.service.ts:29` | BROKEN | none | `HANDOVER_PENDING` (Phase 1); scan → `dealer_released_at`; buyer possession confirmation → atomic completion; reminder job | 9 | state-machine, concurrency, playwright | Failing-first: scan no longer yields COMPLETED (`app/api/dealer/pickup/__tests__/scan.test.ts`); concurrent completion attempts complete once; Playwright dealer scan → buyer confirms → completed | owner decision Q5 (accept spec) | dealer-scan auto-completion | TO IMPLEMENT |
| E26-42a | MD §26 L1280; HTML EXC[36] | Circumvention detected → capture + Ops routing | Messaging flags/redacts + undeduped SYSTEM_ALERT `lib/services/messaging/messaging.service.ts:13-45,38-44`; `recordCircumventionAttempt` zero callers `lib/services/trust/anti-circumvention.service.ts:5-11` | PARTIAL | Pattern capture + redaction — keep | Messaging calls `recordCircumventionAttempt`; `raiseException(owner=Ops)` deduped per thread | 5 | unit | Extend messaging tests: flagged message → CircumventionAttempt row + one queue row | none | `SYSTEM_ALERT` per message | TO EXTEND |
| E26-42b | MD §26 L1280; HTML EXC[36] | Circumvention consequences: scorecard, suspension, termination | No scorecard/suspension link | MISSING | none | §25.2 consequences wired from queue resolution to scorecard/suspension | 10 | unit | Scorecard test: confirmed circumvention → consequence entry | owner decision (suspension policy) | none | TO IMPLEMENT |
| E26-43a | MD §26 L1281; HTML EXC[38] | Premium unpaid — plan model so Standard continues | Fee mandatory in ladder `deal.service.ts:17-20`; `service-fee.service.ts:26-28,104,141,182`; `lib/constants.ts:7-8` ($499/$400); no plan branch | BROKEN | Provider-side duplicate-charge check (`service-fee.service.ts:89,114,141`) | `plan_snapshots` (Phase 1) replaces flag; FEE_PENDING only for Premium election; Standard skips | 3 | unit, state-machine | Plan suite: Standard deal never enters FEE_PENDING; Premium $400 with $99 credit | none | `Buyer.plan` from signup metadata | TO IMPLEMENT |
| E26-43b | MD §26 L1281; HTML EXC[38] | Premium unpaid when funding clears → revert to Standard | No funding state | MISSING | none | Funding clearance closes upgrade window; unpaid election reverts + reversion notice; queue row owner=Buyer | 8 | unit, state-machine | Funding clearance test: unpaid Premium → plan snapshot STANDARD, notice queued | none | none | TO IMPLEMENT |
| E26-44 | MD §26 L1282; HTML EXC[39] | Premium payment fails → retry + notify, never stalls | Fee PI failure → in-app only `stripe/route.ts:625-637`; deal parked at FEE_PENDING | PARTIAL | none | Failure notice with retry instruction + reversion date via dispatcher; Finance queue row; transaction continues on Standard | 3 | integration | `test:webhooks`: fee failure → outbox row + queue row; deal not stalled | none | none | TO EXTEND |
| E26-45 | MD §26 L1283; HTML EXC[40] | Upgrade prompt suppressed during open exception | No suppression logic | MISSING | none | Prompt gate reads `listOpen(buyerId)`; impressions record suppression | 10 | unit, playwright | Plan-prompt test: open queue row → no prompt; Playwright exception open → no interstitial | none | none | TO IMPLEMENT |
| E26-46 | MD §26 L1284; HTML EXC[41] | Downgrade after Premium settled → manual refund review | None | MISSING | none | Downgrade request → plan snapshot + `raiseException(PLAN_EXCEPTION, owner=Finance)`; refund manual | 3 | unit | Plan suite: downgrade after settlement → queue row, no auto refund | refund/money-path activation | none | TO IMPLEMENT |
| E26-47 | MD §26 L1285; HTML EXC[42] | $99 charged back after Premium → Finance exception, entitlement hold | Dispute handler audit-only `stripe/route.ts:769-800` | MISSING | none | Dispute on deposit charge with Premium snapshot → queue row owner=Finance + entitlement hold flag | 3 | integration | `test:webhooks`: chargeback with Premium → queue row + hold | none | none | TO IMPLEMENT |
| E26-48 | MD §26 L1286; HTML EXC[37] | Post-completion obligation overdue → notify, escalate, scorecard | No obligation model | MISSING | none | `post_completion_obligations` (Phase 1); overdue job → both-party notice, queue row, scorecard consequence | 9 | unit, integration | New `lib/services/deal/__tests__/post-completion-obligations.test.ts` | none | none | TO IMPLEMENT |
| E26-49 | HTML EXC[48] | Communication terminal failure → alert Operations | Terminal = `status='failed'` + `logger.error` `lib/services/comms/comms-outbox.service.ts:444-451`; RECLAIM_UNCERTAIN `:402-414` | PARTIAL | Columns-only terminal, no DLQ re-emit, no re-send after `dispatched_at` — keep | Dispatcher raises `raiseException(COMMS_EXCEPTION, owner=System)` on terminal failure and RECLAIM_UNCERTAIN; `terminal_failed_at` stamped | 2 | unit | Extend `lib/services/comms/__tests__/comms-outbox-queue.test.ts`: 4th failure → queue row; RECLAIM_UNCERTAIN → queue row | none | none | TO EXTEND |
| E26-50 | MD §26 L1235 | Buyer-visible status for every exception | No buyer exception surface; admin derived reads only `admin-queue.service.ts:7-39` | MISSING | none | Buyer/dealer portal status surfaces read `buyer_visible_status`/deadline from `queue_items` (cross-portal parity) | 10 | playwright, visual | Playwright: open exception renders same checkpoint/owner/deadline in buyer, dealer, Ops views; visual baselines | none | none | TO IMPLEMENT |
| M27-01a | MD §27 L1290 | Outbox carries trigger event, refs, cancellation rule, terminal-failure marker | Columns `comms_outbox.sql:22-47`: no `trigger_event`, entity refs, cancel key, recheck, terminal marker | PARTIAL | UNIQUE `dedup_key` (`:50`), partial drain index (`:53-55`) | Phase 1: bring table under Prisma chain (`CREATE TABLE IF NOT EXISTS` matching production) + additive columns per §8.2 | 1 | integration | Migration job from-zero + re-apply no-op; `prisma/__tests__/comms-outbox-columns.test.ts` | production migration (Phase 1 wave) | manual SQL file | TO EXTEND |
| M27-01b | MD §27 L1290 | Dispatcher contract: send-time state recheck, cancellation, terminal alert | No recheck, no cancel API, `logger.error` only `comms-outbox.service.ts:354-468`; `enqueueTransactionalEmail` `lib/services/email/transactional-dispatch.ts:28-38` | PARTIAL | CAS claim `:369-374`, stale reclaim `:379-390`, RECLAIM_UNCERTAIN `:402-414`, provider idempotency key `comms-providers.ts:38-52`, hard-suppression + TCPA gates `:181-185,302-304` | `enqueueTransactional()` with recheck registry keyed by `template_key`, `cancelByKey()`, `next_attempt_at`/`max_attempts`, terminal → `raiseException`; `COMMS_TRANSPORT=capture` | 2 | unit, integration | Extend `comms-outbox-queue.test.ts` + `comms-delivery.test.ts`: recheck false → `skipped`; cancelByKey → `cancelled`; capture mode never calls provider | none | none | TO EXTEND |
| M27-02a | MD §27 L1292 | No page request determines survival — allowlist + build-failing rule | ~90 request/after()/webhook-bound sites (D1 rows) | BROKEN | EmailSendLog dedup on R1 keys — keep as parity keys | `lib/services/comms/__tests__/no-direct-transactional-send.test.ts` with allowlist tagged by removal phase; `LEGACY_PATH_WRITE` adapters | 2 | unit | The rule test itself (fails on new direct send; fails on stale allowlist entry) | none | R1/R2/R3/R5 rails | TO IMPLEMENT |
| M27-02b | MD §27 L1292 | Allowlist reaches zero; every send durable | Same | BROKEN | none | Each D1 row migrated in its phase; Phase 10 asserts allowlist empty and removes wrappers | 10 | unit, integration | `no-direct-transactional-send.test.ts` allowlist length 0; `LEGACY_PATH_WRITE` counter review | legacy removal (owner-gated per §8.4) | R1/R2/R3/R5 rails | TO CONSOLIDATE |
| M27-03 | MD §27 L1294 | Zero production outbox rows reconciled with code | Only CRM/nurture + 10 lifecycle senders enqueue (map Deliverable 3); table present in production per workflow §5.2 | ALREADY CORRECT | none | No code change; Phase 1 `CREATE TABLE IF NOT EXISTS` is a no-op in production | 1 | n/a | Workflow §5.2 probe record; Phase 1 migration job re-apply no-op | production migration (Phase 1 wave) | none | ALREADY PRESENT (VERIFY IN PHASE) |
| M27-04a | MD §27 L1290 | In-app notices dispatch through the outbox — channel | `channel CHECK IN ('email','sms')` `comms_outbox.sql:24` | MISSING | none | Extend CHECK with `'in_app'` in the Phase 1 wave (not in §8.2 list) | 1 | integration | Migration job; DB assertion insert with `channel='in_app'` succeeds | production migration (Phase 1 wave) | none | TO IMPLEMENT |
| M27-04b | MD §27 L1290 | In-app notices dispatch through the outbox — delivery | Inline `prisma.notification.create` at 10+ sites, e.g. `deals/[dealId]/action/route.ts:107,121,193,207`, `acquisition-comms.ts:434-452` | MISSING | Notification dedup-by-title where present | `deliverInApp()` in dispatcher writing `Notification` idempotently on `dedup_key`; sites migrate with their phase | 2 | unit, integration | `comms-delivery.test.ts` in_app case; register completeness test counts in-app templates | none | inline `notification.create` | TO IMPLEMENT |
| M27-05 | MD §27 L1290 | Cancellation rule per row (cancel on entity terminal) | No `cancelled` status `comms_outbox.sql:28-29`; no entity refs to key on | MISSING | none | Phase 1 `cancel_key`, `cancelled_at`, `cancel_reason`, status `'cancelled'`; Phase 2 drain checks entity terminal via recheck before send | 2 | unit, integration | `comms-outbox-queue.test.ts`: cancelled row never sent; recheck on terminal deal → skipped | production migration (Phase 1 wave) | none | TO IMPLEMENT |
| M27-06 | MD §27 L1290 | Outbox SMS honours quiet hours | `isRecipientInQuietHours` only in `lib/crm/recipient-timezone.ts`, `lib/crm/sms-gate.ts`, `lib/services/sms/crm-sms.ts`, `dealer-sms-wiring.ts`; outbox SMS path `comms-outbox.service.ts:291-331` ignores it | PARTIAL | `sendCrmSms` quiet-hours + TCPA + suppression gates — port, do not drop | `deliverSms` applies quiet hours by deferring `run_at` (not skipping) | 2 | unit | `comms-delivery.test.ts`: in-quiet-hours SMS re-queued with later `run_at` | none | `sendCrmSms` R5 | TO EXTEND |
| M27-07 | MD §27 L1290 | Transactional payload with `contactId` still deduped as transactional | `isTransactional` requires no contactId `comms-outbox.service.ts:169-170` → skips EmailSendLog precheck | PARTIAL | `transactionalEmailAlreadySent` precheck (`email-send-log.ts:23-29`) | Decide transactional by `payload.type`, not contactId presence | 2 | unit | `comms-delivery.test.ts`: transactional + contactId → DUPLICATE when SENT log exists | none | none | TO EXTEND |
| K27-1300 | MD §27.1 L1300 | Registration submitted → verification link + expiry | `sendWelcomeEmail` R1 from `lib/auth/actions.ts:398` and `app/api/auth/resend-verification/route.ts:154`; `resend.service.ts:252-262` | PARTIAL | EmailSendLog key parity | Enqueue via dispatcher with expiry in body | 2 | unit, integration | `sender-migration.test.ts` parity case; outbox row asserted in auth tests | none | R1 | TO CONSOLIDATE |
| K27-1301 | MD §27.1 L1301 | Verification completed → welcome + onboarding link | `sendEmailVerifiedEmail` R1 from `app/auth/callback/route.ts:8,184`; `resend.service.ts:911-920` | PARTIAL | key parity | Enqueue via dispatcher | 2 | unit, integration | `sender-migration.test.ts` parity case | none | R1 | TO CONSOLIDATE |
| K27-1302 | MD §27.1 L1302 | Onboarding incomplete → exact unfinished requirement | `check_form_completion_1..3` R3/R4 `app/api/jobs/check-form-completion/route.ts:37-73`; `lifecycle-scheduler.ts` | PARTIAL | send-time guards on R4 | Dispatcher rows with recheck; body names the missing requirement | 2 | unit, integration | Touch-drain tests re-pointed to outbox rows | none | R3 QStash, R4 | TO CONSOLIDATE |
| K27-1303 | MD §27.1 L1303 | Guest capture → claim link | Pre-checkout touches issue single-use hashed resume token `lifecycle-touch-drain.service.ts:46-52`; `request-resume-token.service.ts`; R4 (flag) / R3 | PARTIAL | Single-use hashed resume token — keep | Claim link via dispatcher on guest capture (Phase 2 intake) | 2 | unit, integration | Intake tests: guest capture → outbox row with resume link | none | R3/R4 | TO CONSOLIDATE |
| K27-1304 | MD §27.1 L1304 | Draft abandoned → four-touch resume | `form_submitted, check_form_completion_1..3` `lifecycle-touch-drain.service.ts:632-634,640-660`; `lifecycle-scheduler.ts:9` flag OFF → QStash; cancelled by `cancelPreCheckoutTouches` | PARTIAL | `cancelPreCheckoutTouches` on checkout — keep | Four rows via dispatcher with `cancel_key=vr:<id>`; 14-day abandonment | 2 | unit, integration | Intake tests: four rows scheduled; checkout cancels all | none | R3/R4 | TO CONSOLIDATE |
| K27-1305 | MD §27.1 L1305 | Application submitted → AutoLenis receipt | `sendAdminPrequalAlertEmail` R1 twice: `prequal.service.ts:749`, `admin-prequal.service.ts:738` | PARTIAL | none | One producer via dispatcher; dedupe implementations | 2 | unit | Prequal tests assert single outbox row | none | R1; duplicate prequal notice set | TO CONSOLIDATE |
| K27-1306 | MD §27.1 L1306 | Prequal approved → amount, expiration, next step | R1 `prequal.service.ts:599` (+ `admin-prequal.service.ts:596`) | PARTIAL | none | Dispatcher | 2 | unit | `sender-migration.test.ts` parity | none | R1 | TO CONSOLIDATE |
| K27-1307 | MD §27.1 L1307 | Prequal under review → honest status | R1 `prequal.service.ts:703` | PARTIAL | none | Dispatcher | 2 | unit | parity case | none | R1 | TO CONSOLIDATE |
| K27-1308 | MD §27.1 L1308 | Provider delay → processing-delay notice | Under-review email reused `prequal.service.ts:103-121` | PARTIAL | fail-closed hold | Dedicated `provider_delay` template via dispatcher | 2 | unit | Prequal tests: provider timeout → `provider_delay` row | none | none | TO EXTEND |
| K27-1309 | MD §27.1 L1309 | Prequal declined → adverse action | R1 `prequal.service.ts:651,640-670` with outcome persistence | PARTIAL | Outcome discrimination — keep (compliance event) | Dispatcher path that reports outcome back to the compliance event on send | 2 | unit, integration | Prequal tests: outcome SENT recorded from outbox result | none | R1 | TO CONSOLIDATE |
| K27-1310 | MD §27.1 L1310 | Approval expiring/expired → warning/renewal | Raw Resend in cron `app/api/cron/prequal-ibv-reminders/route.ts:85-105` + Notification dedup | PARTIAL | none | Dispatcher rows keyed `prequal-expiry-<id>-<day>` with recheck | 2 | unit | Cron route test: rows enqueued, no SDK call | none | R2 in cron | TO CONSOLIDATE |
| K27-1311 | MD §27.1 L1311 | Vehicle Request submitted → summary, $99 explanation, checkout link | R1 `request-received-*` + R2 `sendVehicleRequestConfirmation` both fire `app/api/public/request-vehicle/route.ts:656,663`; `resend.service.ts:978` | DUPLICATED | none | One dispatcher row from the Lane 1 handler with checkout link | 2 | unit, integration | Intake tests: exactly one outbox row per request | none | R2 `vehicle-offers.email.ts`; R1 | TO CONSOLIDATE |
| K27-1312 | MD §27.1 L1312 | $99 unpaid → six-touch sequence | `deposit_reminder_1..6` R4 `lifecycle-touch-drain.service.ts:612-615,599-628`; R3 `jobs/deposit-reminder`; cancelled on paid `stripe/route.ts:249-252` | PARTIAL | `cancelDepositReminderTouches` on settlement — keep | Six rows via dispatcher keyed to request, `cancel_key`; runs on minute drain (§13-D24) | 3 | unit, integration | Deposit suite: six rows; settlement cancels remaining | none | Deposit-reminder direct producer; R3 | TO CONSOLIDATE |
| K27-1313 | MD §27.1 L1313 | Payment processing → do-not-pay-again notice | None | MISSING | none | Enqueue on `payment_intent.processing` | 3 | integration | `test:webhooks`: processing event → row | none | none | TO IMPLEMENT |
| K27-1314 | MD §27.1 L1314 | Payment succeeded → receipt (buyer + AutoLenis) | R1 `stripe/route.ts:287,439` buyer only | PARTIAL | Webhook idempotency | Dispatcher rows for buyer + AutoLenis written in the settlement transaction | 3 | integration | `test:webhooks`: two rows in same transaction | none | R1 | TO CONSOLIDATE |
| K27-1315 | MD §27.1 L1315 | Payment failed → truthful failure + retry path | Deposit failure: no comms; fee failure: in-app only `stripe/route.ts:613-637` | BROKEN | none | `payment_failed` template via dispatcher for deposit and fee | 3 | integration | `test:webhooks`: failed PI → row | none | none | TO IMPLEMENT |
| K27-1316 | MD §27.1 L1316 | Reconciliation gap → Finance exception with Stripe reference | SYSTEM_ALERT `deposit-settlement.service.ts:104-125` | PARTIAL | Sweep-by-state | `raiseException` + Finance email via dispatcher (see E26-08) | 3 | integration | Same as E26-08 | none | `SYSTEM_ALERT` | TO EXTEND |
| K27-1317 | MD §27.1 L1317 | Refund or dispute → hold status + review instructions | Refund R1 `stripe/route.ts:685,754`; dispute audit only `:769` | PARTIAL | Refund key `refund-confirmed-${refundId}` | Dispute notice + refund notice via dispatcher | 3 | integration | `test:webhooks`: dispute → buyer + Finance rows | none | R1 | TO EXTEND |
| K27-1318 | MD §27.1 L1318 | Radius authorization needed → shortfall + max distance | Hold without notice `request-coverage-gate.service.ts:114-190` | MISSING | none | Dispatcher row on `RADIUS_AUTHORIZATION_REQUIRED` | 5 | unit | `radius-authorization.test.ts` row asserted | none | none | TO IMPLEMENT |
| K27-1319 | MD §27.1 L1319 | Sourcing completed → auction preparation status | R2 `sendDealersContactedEmail` `intake-pipeline.service.ts:513` via `buyer-notifications.service.ts` (no key) | PARTIAL | none | Dispatcher row keyed `sourcing-complete-<vrId>` | 5 | unit | Sourcing case tests | none | R2 `buyer-notifications.service.ts` | TO CONSOLIDATE |
| K27-1320 | MD §27.1 L1320 | Auction launched → 48-hour timeline | R1 `sendAuctionActivatedEmail` `stripe/route.ts:293`, `launch-auction/route.ts:315` + R3 `jobs/auction-active` | DUPLICATED | key `auction-activated-${auctionId}` | One dispatcher row from launch-readiness service | 5 | unit, integration | Launch tests: one row | none | R1; R3 | TO CONSOLIDATE |
| K27-1321 | MD §27.1 L1321 | Dealer invited → secure invitation, criteria, deadline | R1 + IA + R3/R4 `dealer-invitation.service.ts:379-395` | PARTIAL | Invitation content carries no buyer identity | Dispatcher row per `auction_invitations` row with tokenised link | 5 | unit, integration | Invitation tests: N rows for N rooftops, no PII in payload | none | R1; R3/R4 `dealer_invited` | TO CONSOLIDATE |
| K27-1322 | MD §27.1 L1322 | Dealer invitation reminder → remaining time | Two crons, same key `dealer-auction-reminder-${auctionId}-${to}`: `dealer-invitation-reminder/route.ts:100`, `auction-close/route.ts:76`; `resend.service.ts:1618` | DUPLICATED | Shared key prevents double send while EmailSendLog holds — keep key | One producer (50%/90% via dispatcher, `reminder_50_sent_at/90`), retire the second cron path | 5 | unit | Cron tests: one row per invitation per milestone | none | `auction-close` reminder branch | TO CONSOLIDATE |
| K27-1323 | MD §27.1 L1323 | Invitation bounced → Operations replacement exception | Suppression only `webhooks/resend/route.ts:188-189` | PARTIAL | suppression on bounce | `raiseException` (E26-13) + Ops notice | 5 | integration | E26-13 test | none | none | TO EXTEND |
| K27-1324 | MD §27.1 L1324 | Offer received → count only, never sealed pricing | R2 first-offer `offer.service.ts:156-180` via `buyer-notifications.service.ts:60` (no key) + R3 `jobs/offer-received` | PARTIAL | none | Dispatcher row keyed `offer-received-<auctionId>-<n>` with count-only content | 6 | unit | Offer tests: row content has no price | none | R2; R3 | TO CONSOLIDATE |
| K27-1325 | MD §27.1 L1325 | Auction nearing zero offers → Ops alert | `checkSLAs` SYSTEM_ALERT without dedup `lib/services/monitoring/health.service.ts:509-524`; `vercel.json:116-117` | PARTIAL | none | `raiseException` idempotent per auction; no per-tick re-alert | 6 | unit | Health tests: same auction → one row | none | `SYSTEM_ALERT` | TO EXTEND |
| K27-1326 | MD §27.1 L1326 | Offers ready → report + Premium second mention | OB `resend.service.ts:428-438`; no `premium` in template (`rg -i premium lib/services/email/templates`) | PARTIAL | Durable OB key `offers-ready-${auctionId}` | Add Premium copy (touchpoint 2) | 6 | unit, visual | Template test asserts Premium block; visual baseline | none | none | TO EXTEND |
| K27-1327 | MD §27.1 L1327 | Zero offers → outcome + recovery path (buyer + Ops) | Dealer OB + buyer IA `auction.service.ts:161-192` | PARTIAL | atomic close claim | Buyer email via dispatcher + `raiseException` | 6 | unit | `auction-close.test.ts` | none | none | TO EXTEND |
| K27-1328 | MD §27.1 L1328 | Buyer selects offer → confirmation + reaffirmation request | Buyer OB `select-offer/route.ts:114`; dealer OB via award drain `dealer-award.ts:272` | PARTIAL | `deal-selected-${dealId}` key; award marker | Dealer row becomes reaffirmation request with 24h deadline | 6 | unit | Selection tests: dealer row template `reaffirmation_request` | none | none | TO EXTEND |
| K27-1329 | MD §27.1 L1329 | Premium invitation shown at acceptance | Bare POST `app/api/buyer/plan/upgrade/route.ts:21-89` | MISSING | none | Full-screen interstitial once + impression record (touchpoint 3) | 6 | unit, playwright, visual | Playwright: interstitial declinable, non-blocking | none | none | TO IMPLEMENT |
| K27-1330 | MD §27.1 L1330 | Premium follow-up first (1h, only if declined) | None | MISSING | none | Dispatcher row `run_at=+1h`, recheck skips if upgraded | 6 | unit | Plan tests: declined → row; upgraded → skipped | none | none | TO IMPLEMENT |
| K27-1331 | MD §27.1 L1331 | Premium follow-up final (reaffirmation/recap) | None | MISSING | none | Row at reaffirmation confirm; no further prompts | 7 | unit | Reaffirmation tests: one final row, no more | none | none | TO IMPLEMENT |
| K27-1332 | MD §27.1 L1332 | Losing offers → non-award notice | OB via award drain `dealer-award.ts:289`; dispatcher scans 7-day window, abandons after 4 attempts with log only `dealer-award-dispatch.service.ts:31,38,50,106-118` | PARTIAL | Durable `dealerAwardDispatchedAt` marker, bounded attempts — keep | Scan by null marker not window; abandonment → `raiseException` | 6 | unit | Extend award-dispatch tests: old deal dispatched; 4th failure → queue row | none | none | TO EXTEND |
| K27-1333 | MD §27.1 L1333 | Reaffirmation reminder 12h | None | MISSING | none | Row `run_at=+12h` with recheck on `dealer_reaffirmations.status` | 7 | unit | `reaffirmation.test.ts` | none | none | TO IMPLEMENT |
| K27-1334 | MD §27.1 L1334 | Dealer confirms → buyer + AutoLenis | None | MISSING | none | Rows on CONFIRMED | 7 | unit | `reaffirmation.test.ts` | none | none | TO IMPLEMENT |
| K27-1335 | MD §27.1 L1335 | Dealer rejects/times out → return-to-offers notice | None | MISSING | none | Rows on REJECTED/TIMED_OUT | 7 | unit | `reaffirmation.test.ts` | none | none | TO IMPLEMENT |
| K27-1336 | MD §27.1 L1336 | Material change proposed → side-by-side | None | MISSING | none | Row on MATERIAL_CHANGE_PENDING | 7 | unit | `reaffirmation.test.ts` | none | none | TO IMPLEMENT |
| K27-1337 | MD §27.1 L1337 | Vehicle hold expiring → extend or release | `holds` no-op `cron/holds/route.ts` | MISSING | none | Rows from repurposed holds cron | 7 | unit | `holds-route.test.ts` | none | `holds` cron | TO IMPLEMENT |
| K27-1338 | MD §27.1 L1338 | Outside dealer verification needed | `outside-invite.service.ts` exists; comms not traced | UNVERIFIED | none | Row on outside-winner claim | 7 | unit | Trace at Phase 7; test row asserted | none | `outside_auction_invites` | UNVERIFIED — comms not traced |
| K27-1339 | MD §27.1 L1339 | Recap ready → confirm numbers | None | MISSING | none | Rows on recap version created | 7 | unit | `deal-recap.test.ts` | none | none | TO IMPLEMENT |
| K27-1340 | MD §27.1 L1340 | Financing path selected → external handoff | IA (+SMS flag) `acquisition-comms.ts:113-121` | PARTIAL | plan table exhaustiveness | Email row via dispatcher (touchpoint 5) | 7 | unit | Financing checkpoint tests | none | seam direct comms | TO EXTEND |
| K27-1341 | MD §27.1 L1341 | Financing in progress → reminders | None | MISSING | none | Rows on IN_PROGRESS with missing-evidence recheck | 7 | unit | Financing checkpoint tests | none | none | TO IMPLEMENT |
| K27-1342 | MD §27.1 L1342 | Financing terms locked | None | MISSING | none | Rows on TERMS_LOCKED | 7 | unit | Financing checkpoint tests | none | none | TO IMPLEMENT |
| K27-1343 | MD §27.1 L1343 | Financing completed → verified checkpoint | Advance to FEE_PENDING → IA/SMS `financing-orchestrator.service.ts:142` | PARTIAL | buyer can never set completion | Rows on COMPLETED recorded by Finance/Ops | 7 | unit | Financing checkpoint tests | none | buyer self-advance routes | TO EXTEND |
| K27-1344 | MD §27.1 L1344 | Financing failed/expired → alternative path | Review task only `financing-orchestrator.service.ts:69,167` | PARTIAL | idempotent routing | Buyer + AutoLenis rows | 7 | unit | Financing checkpoint tests | none | `FinancingReviewTask` | TO EXTEND |
| K27-1345 | MD §27.1 L1345 | Funding cleared/blocked → release status | None | MISSING | none | Rows on `funding_cleared_at` / block | 8 | unit | Funding clearance tests | none | none | TO IMPLEMENT |
| K27-1346 | MD §27.1 L1346 | Standard plan confirmed → nothing further due | No Standard path; fee mandatory `deal.service.ts:17-20`, `service-fee.service.ts:104` | BROKEN | none | Row at settlement when snapshot=STANDARD (after E26-43a) | 3 | unit | Plan tests | none | mandatory FEE_PENDING | TO IMPLEMENT |
| K27-1347 | MD §27.1 L1347 | Premium balance required → $400 + secure link | Seam IA/SMS on FEE_PENDING `acquisition-comms.ts:123-131`; admin manual send-link R1 `admin/payments/concierge-fee/send-link/route.ts:100` | PARTIAL | provider-side duplicate check | Automatic email row with link on Premium election | 3 | unit | Plan tests | none | R1 admin send-link | TO EXTEND |
| K27-1348 | MD §27.1 L1348 | Premium balance succeeded → buyer + concierge + Finance | R1 buyer only `stripe/route.ts:537` | PARTIAL | key `concierge-fee-confirmed-${pi}` | Three rows in settlement transaction | 3 | integration | `test:webhooks` | none | R1 | TO CONSOLIDATE |
| K27-1349 | MD §27.1 L1349 | Premium balance failed → retry + reversion date | In-app only `stripe/route.ts:625-637` | PARTIAL | none | Buyer + Finance rows | 3 | integration | `test:webhooks` | none | none | TO EXTEND |
| K27-1350 | MD §27.1 L1350 | Premium reverted to Standard | None | MISSING | none | Row at funding clearance reversion (E26-43b) | 8 | unit | Funding clearance tests | none | none | TO IMPLEMENT |
| K27-1351 | MD §27.1 L1351 | Downgrade requested | None | MISSING | none | Buyer + Finance rows | 3 | unit | Plan tests | none | none | TO IMPLEMENT |
| K27-1352 | MD §27.1 L1352 | Downgrade refund decision | None | MISSING | none | Rows on Finance decision | 3 | unit | Plan tests | refund/money-path activation | none | TO IMPLEMENT |
| K27-1353 | MD §27.1 L1353 | Insurance required → requirements + link | IA/SMS `acquisition-comms.ts:143-151` | PARTIAL | none | Email row with secure link at contract request moment | 8 | unit | Insurance tests | none | seam direct comms | TO EXTEND |
| K27-1354 | MD §27.1 L1354 | Insurance uploaded → receipt + Ops task | `upload-proof/route.ts:138` writes status; Ops task not traced | UNVERIFIED | none | Row + `raiseException(UNDER_REVIEW)` | 8 | unit | Trace at Phase 8; test row + queue row | none | none | UNVERIFIED — Ops task path not traced |
| K27-1355 | MD §27.1 L1355 | Insurance verified → clearance | `BuyerTriggers.insuranceBound` dead `notification.service.ts:41-43` | MISSING | none | Rows on VERIFIED/POLICY_BOUND; delete dead trigger | 8 | unit | Insurance tests | none | dead `BuyerTriggers` | TO IMPLEMENT |
| K27-1356 | MD §27.1 L1356 | Insurance rejected/expired → correction | No status `prisma/schema.prisma:1493-1502` | MISSING | none | Row with `rejection_reason` | 8 | unit | Insurance tests | none | none | TO IMPLEMENT |
| K27-1357 | MD §27.1 L1357 | Contract requested → upload link + 24h deadline | R1 `deals/[dealId]/action/route.ts:91`; no deadline | PARTIAL | none | Row from CONTRACT_PENDING transition + `document_requests.dueAt` | 8 | unit, integration | `contract-request-deadline.test.ts` | none | R1 | TO CONSOLIDATE |
| K27-1358 | MD §27.1 L1358 | Contract overdue → reminder + escalation | None | MISSING | none | Rows from overdue job | 8 | unit | `contract-request-deadline.test.ts` | none | none | TO IMPLEMENT |
| K27-1359 | MD §27.1 L1359 | Contract revision required → mismatches | R1 dealer issues with `Date.now()` key `contract-shield/[reviewId]/route.ts:182,221` + buyer alert | PARTIAL | approval binds to version | Rows keyed `contract-issues-<dealId>-<versionId>` | 8 | unit | Contract-shield route tests: stable key | none | R1 non-idempotent key | TO CONSOLIDATE |
| K27-1360 | MD §27.1 L1360 | Contract approved → signing readiness | `contract-shield.service.ts:368` + `contract-shield/[reviewId]/route.ts:131` both send `contract-approved-${dealId}` | DUPLICATED | EmailSendLog key dedup | One producer (service) via dispatcher | 8 | unit | Tests: one row | none | R1 route duplicate | TO CONSOLIDATE |
| K27-1361 | MD §27.1 L1361 | Signature required → buyer + co-buyer link | IA/SMS `acquisition-comms.ts:188-196`; `sendEnvelope` IA `esign.service.ts:39-51`; no email; no co-buyer | PARTIAL | hash-bound signing | Rows per signer (`signer_kind`) | 8 | unit | Esign tests | none | seam direct comms | TO EXTEND |
| K27-1362 | MD §27.1 L1362 | Signature reminder/expiration | Silent CAS expiry `buyer-signing.service.ts:670-680` | PARTIAL | CAS expiry | Reminder rows + expiry notice | 8 | unit | Esign tests | none | none | TO EXTEND |
| K27-1363 | MD §27.1 L1363 | Buyer signatures completed → dealer execution request | Buyer `contract-signed` R1 in after() `buyer-signing.service.ts:881`; dealer in-app "executed" (misleading) `:888-910`; no dealer email | PARTIAL | deduped key `esign-executed:${dealId}:${envelopeId}` | Dealer execution-request row; rename in-app copy | 8 | unit | Esign tests | none | R1 in after() | TO EXTEND |
| K27-1364 | MD §27.1 L1364 | Fully executed stored → executed-document notice | `esign-artifact-reconcile` re-drives `finalizeSignedContract` behind `ESIGN_EXECUTED_ARTIFACT_ENABLED` (OFF) `app/api/cron/esign-artifact-reconcile/route.ts:1-12`, `esign-schema-gate.ts:37`, `buyer-signing.service.ts:876-884` | PARTIAL | schema gate refuses without evidence tables | Rows on DEALER_EXECUTED via dispatcher; verified with flag on in preview | 8 | integration | Esign reconcile tests with flag on | `ESIGN_EXECUTED_ARTIFACT_ENABLED` (§13-D4) | none | OWNER-GATED |
| K27-1365 | MD §27.1 L1365 | Pickup readiness blocked → missing prerequisite | None | MISSING | none | Row per blocked checklist item owner | 9 | unit | Readiness tests | none | none | TO IMPLEMENT |
| K27-1366 | MD §27.1 L1366 | Pickup proposal/counter | OB round-keyed `pickup-notifications.service.ts:105-151` | ALREADY CORRECT | round-specific keys | none (re-verify with recheck hook) | 9 | unit | Existing pickup-notifications tests stay green | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| K27-1367 | MD §27.1 L1367 | Pickup confirmed → time, location, preparation | OB `notifyDealerConfirmed` `pickup-notifications.service.ts:177` + admin R1 ×2 `pickup/schedule/route.ts:100,108` + buyer IA | DUPLICATED | OB producer | One producer per recipient via dispatcher; retire admin route sends | 9 | unit | Pickup tests: one row per recipient | none | R1 admin schedule | TO CONSOLIDATE |
| K27-1368 | MD §27.1 L1368 | Pickup approaching 24h/2h | None | MISSING | none | Rows with `reminder_24h_sent_at/2h` markers and recheck | 9 | unit | Pickup tests | none | none | TO IMPLEMENT |
| K27-1369 | MD §27.1 L1369 | Pickup rescheduled → new schedule + revoked token | `scheduling.service.ts` sends nothing, no QR touch | MISSING | none | Reschedule revokes + reissues token and enqueues rows | 9 | unit | Pickup tests: reschedule → token revoked + rows | none | none | TO IMPLEMENT |
| K27-1370 | MD §27.1 L1370 | Handover blocked | None | MISSING | none | Rows on block with `raiseException` | 9 | unit | Pickup tests | none | none | TO IMPLEMENT |
| K27-1371 | MD §27.1 L1371 | Dealer releases → possession-confirmation request | None (scan completes deal) | MISSING | none | Row on `dealer_released_at` | 9 | unit | Pickup tests | none | dealer-scan auto-completion | TO IMPLEMENT |
| K27-1372 | MD §27.1 L1372 | Buyer confirms possession → completion confirmation | None | MISSING | none | Row in atomic completion transaction | 9 | unit, concurrency | Completion tests | none | none | TO IMPLEMENT |
| K27-1373 | MD §27.1 L1373 | Deal completed → buyer, dealership, AutoLenis | R1 ×2 `deals/[dealId]/action/route.ts:116`, `pickup/complete/route.ts:99`; raw R2 `dealer/pickup/scan/route.ts:125-134`; SMS seam `acquisition-comms.ts:228-236`; R3 `deal-complete` | DUPLICATED | exactly-once completion event | One producer keyed on the completion seam, rows written in the completion transaction | 9 | unit, concurrency | Completion tests: three rows, once | none | R1 ×2; R2 scan; R3 | TO CONSOLIDATE |
| K27-1374 | MD §27.1 L1374 | Title/payoff/due-bill follow-up | None | MISSING | none | Rows from `post_completion_obligations` | 9 | unit | `post-completion-obligations.test.ts` | none | none | TO IMPLEMENT |
| K27-1375 | MD §27.1 L1375 | Cancellation → buyer + affected dealership | IA route-owned + SMS flag `acquisition-comms.ts:238-246`; dealer IA on auction cancel only `auctions/[auctionId]/action/route.ts:193-207`; no email | PARTIAL | none | Rows from `cancelTransaction` (C24-06, C24-10) | 10 | unit | `cancel-transaction.test.ts` | none | seam direct comms | TO IMPLEMENT |
| K27-1376 | MD §27.1 L1376 | Refund decision → buyer + Finance | R1 refund confirmation `stripe/route.ts:685,754` | PARTIAL | key `refund-confirmed-${refundId}` | Rows on refund decision (buyer + Finance) | 3 | integration | `test:webhooks` | refund/money-path activation | R1 | TO CONSOLIDATE |
| T28-01 | MD §28.3 L1425; HTML TRANSITION[0] | Authorization per transition | Seam accepts free-form `actorRole` `deal.service.ts:73-74,104-108`; DEALER completes via scan `scan/route.ts:90` | PARTIAL | route-level guards | Per-transition allowed-role matrix inside `advanceDealStatus` | 6 | unit, state-machine | New `lib/services/deal/__tests__/transition-authz.test.ts` | none | none | TO EXTEND |
| T28-02 | MD §28.3 L1426; HTML TRANSITION[1] | Validation: facts current, not cached | Seam re-reads deal `:109`, insurance gate `:134-138`; prequal/financing/contract-version/envelope not re-validated; `isPrequalValid` never in deal/offer services | PARTIAL | insurance hard-gate | Per-transition validators (prequal valid, approved contract version, envelope COMPLETED, funding cleared, pickup confirmed) | 6 | unit, state-machine | `deal-transitions.test.ts` validator cases (extended in 7/8/9) | none | none | TO EXTEND |
| T28-03 | MD §28.3 L1427; HTML TRANSITION[2] | Conditional write on observed state | CAS for `Deal.status` only `deal.service.ts:127,148-165`; see T28-10a-d for bypasses | PARTIAL | Lost race re-resolves once, never rewinds — keep | Every material writer uses `updateMany WHERE status=observed` | 10 | concurrency | `test:concurrency` cases per entity | none | none | TO EXTEND |
| T28-04 | MD §28.3 L1428; HTML TRANSITION[3] | Atomicity: state + records commit together | History/activity/comms/completion after CAS, swallowed `deal.service.ts:148-204,176,186` | PARTIAL | `opts.data` in same `updateMany` | Wrap CAS + history + outbox rows + queue rows in one `$transaction`; completion event row in same transaction | 6 | integration | `workflow-stage-seam.test.ts`: failed history → transition rolled back | none | none | TO EXTEND |
| T28-05 | MD §28.3 L1429; HTML TRANSITION[4] | Idempotency of replay | SMS guard claim-then-lose `acquisition-comms.ts:410-417,468-478`; fail-open without service-role env and on insert throw `:407-419,497-500`, `lib/jobs/idempotency.ts:102-106`; `Date.now()` keys `resend.service.ts:1702` | PARTIAL | `acq-comms:deal:*` dedup; CAS | Outbox dedup_key replaces the SMS ledger; fail-closed when guard unavailable; stable keys | 6 | unit | `acquisition-comms` tests: guard unavailable → refuse; failed send → not `completed` | none | `lib/jobs/idempotency.ts` ledger for comms | TO CONSOLIDATE |
| T28-06 | MD §28.3 L1430; HTML TRANSITION[5] | Audit: actor, prior, next, reason, time | `DealStatusHistory` `deal.service.ts:167-176` swallowed; `schema.prisma:2810-2822`; admin `adminAuditLog` `deals/[dealId]/action/route.ts:233`; buyer cancel no reason | PARTIAL | history + activity + admin audit | Non-swallowed history in transaction (T28-04); reason on every path | 6 | integration | `workflow-stage-seam.test.ts` | none | none | TO EXTEND |
| T28-07 | MD §28.3 L1431; HTML TRANSITION[6] | Communication scheduled durably | Seam emits in-app + direct SMS in request `acquisition-comms.ts:353-486` | BROKEN | consent-aware plan table + exhaustiveness guard | Seam writes outbox rows (email/sms/in_app) in the transition transaction | 6 | unit, integration | `acquisition-comms` tests: rows not sends; register completeness | owner decision Q6 (SMS flag) | seam direct SMS | TO CONSOLIDATE |
| T28-08 | MD §28.3 L1432; HTML TRANSITION[7] | Recovery: owner + return path | Arrival hooks `deal.service.ts:225-338`; reconcile crons; no owner model | PARTIAL | self-healing arrival hooks — keep | Every failure/timeout → `raiseException` with `return_point` | 10 | unit | Register completeness test: every `exception_code` has a raise site | none | none | TO EXTEND |
| T28-09 | MD §28.3 L1435 | [BUILT] legal-transition table, CAS, history, idempotent comms, exactly-once completion | `TRANSITIONS` `deal.service.ts:15-34`, `canTransition` `:63-70`, CAS `:148`, history `:167`, comms `:194`, completion `:202-204` → `deal-completion-event.service.ts:23-55`, `lib/events/emit.ts:198-212` after(); admin `DEAL_STAGE_ADVANCED` with `force` skips table + insurance gate `deals/[dealId]/action/route.ts:59-72` | ALREADY CORRECT | Single-writer guard test (commit 4e52391: `cancel-deal-seam.test.ts`, `workflow-stage-seam.test.ts`) | Extend table with Phase 1 statuses; completion event forwarded from outbox/queue with retry; `force` only for SUPER_ADMIN with audited reason | 6 | state-machine, unit | Existing seam tests stay green; new transition cases | none | admin `force` override | ALREADY PRESENT (VERIFY IN PHASE) |
| T28-10a | MD §28.3 L1427 | Bypass: `VehicleRequest.status` raw update | `app/api/buyer/requests/[requestId]/cancel/route.ts:28` (no CAS) | PARTIAL | transaction with event | Request status writer with CAS in the Lane 1 service | 2 | unit, concurrency | Intake tests: concurrent cancel/submit resolves once | none | buyer request cancel route | TO EXTEND |
| T28-10b | MD §28.3 L1427 | Bypass: `Auction.status` raw updates | `admin-buyer-command-center.service.ts:873-876,905-908`; `auctions/[auctionId]/action/route.ts:176`; `auction.service.ts` close (CAS only on post-close claim) | PARTIAL | `postCloseClaimWon` CAS | Auction status writer with CAS in `auction.service`; pause/resume/cancel go through it | 5 | unit, concurrency | Auction suite: CAS on close/cancel; pause→resume | none | `pauseBuyerWorkflow` | TO EXTEND |
| T28-10c | MD §28.3 L1427 | Bypass: `Deal.insuranceStatus` direct writers | `insurance/upload-proof:138`, `admin/insurance-requests/respond:59`, `journey/{complete,complete-all,reopen}` | PARTIAL | release gate | Insurance status writer in `lib/services/insurance` with CAS + audit | 8 | unit | Insurance tests: single writer guard | none | journey routes | TO CONSOLIDATE |
| T28-10d | MD §28.3 L1427 | Bypass: `Pickup.status` raw write in scan | `dealer/pickup/scan/route.ts:108-112` after deal advance | PARTIAL | Pickup own CAS elsewhere | Scan goes through pickup service CAS (`dealer_released_at`) | 9 | unit | `scan.test.ts` | none | scan route | TO EXTEND |
| G29-01 | MD §29 L1442; HTML SAFE[0][0] | FCRA consent persisted before pull; duplicate pulls claimed safely | `prequal.service.ts:215-273,313,342` | ALREADY CORRECT | atomic claim + `FCRA_CONSENT_TEXT`; stale-claim reclaim | none — must stay green through Phase 2 | 2 | unit | Existing prequal tests | none | none | PRESERVED STRONGER SAFEGUARD |
| G29-02 | MD §29 L1443; HTML SAFE[0][1] | No SSN; OFAC fails closed | `prequal.service.ts:4,381-383`; `ofac.service.ts:4-11` | ALREADY CORRECT | OFAC_REVIEW hold, P0 alert | none; Phase 1 410 on credit-apply route removes the only SSN write | 2 | unit | Prequal tests + `credit-applications-frozen.test.ts` | none | `app/api/buyer/financing/apply` | PRESERVED STRONGER SAFEGUARD |
| G29-03 | MD §29 L1444; HTML SAFE[0][2] | Adverse-action outcomes distinguished | `prequal.service.ts:640-670`; `resend.service.ts:120-127` | ALREADY CORRECT | `EmailSendOutcome` | Dispatcher must return an equivalent outcome (K27-1309) | 2 | unit | Prequal tests after migration | none | R1 | PRESERVED STRONGER SAFEGUARD |
| G29-04 | MD §29 L1445; HTML SAFE[1][0..1] | Stripe authority; duplicate-charge checks on deposit + Premium fee | Deposit `create-intent/route.ts:88-93,147-160`; fee `service-fee.service.ts:89,114,141` (`concierge-fee-${dealId}`); `lib/constants.ts:7-8` | ALREADY CORRECT | provider search + idempotency key | Extend check to admin create-intent/send-link (Phase 3 defect 4) | 3 | unit, integration | `test:payments`: admin second intent refused | none | admin send-link | PRESERVED STRONGER SAFEGUARD |
| G29-05 | MD §29 L1446; HTML SAFE[1][2] | Deposit state matrix; unroutable → Ops exception | `lib/payments/deposit-state.ts:29-40`; `stripe/route.ts:33-54,606` | ALREADY CORRECT | never absorbed | Exception via `raiseException` (E26-09); matrix FAILED fix (E26-07) | 3 | unit, integration | `test:webhooks` | none | none | PRESERVED STRONGER SAFEGUARD |
| G29-06 | MD §29 L1447; HTML SAFE[2][0] | Auction close atomic claim + reprocess | `processAuctionClose` `auction.service.ts:109-135,208`; `cron/auction-close/route.ts:26-41` | ALREADY CORRECT | `updateMany WHERE postCloseProcessedAt IS NULL` | none | 6 | unit, concurrency | Existing auction-close tests | none | none | PRESERVED STRONGER SAFEGUARD |
| G29-07 | MD §29 L1448; HTML SAFE[2][1] | Anti-snipe hard cap + audit | `anti-snipe.service.ts:16,53-67` | ALREADY CORRECT | `MAX_AUTO_EXTENSIONS=6`, CAS, `auctionExtensionLog` | none | 5 | unit | Existing anti-snipe tests | none | none | PRESERVED STRONGER SAFEGUARD |
| G29-08 | MD §29 L1449; HTML SAFE[2][2] | Single winner under DB lock | `select-offer.service.ts:41-43` | ALREADY CORRECT | `SELECT … FOR UPDATE` | none (Deal created at DEALER_CONFIRMATION inside same lock) | 6 | concurrency | `select-offer-concurrency.test.ts` (after §12.2 DSN guard fix) | none | none | PRESERVED STRONGER SAFEGUARD |
| G29-09 | MD §29 L1450; HTML SAFE[2][3] | Offer arithmetic + budget server-side | `offer.service.ts:17-19,39-55,68-74` (`assertOtdComponentsMatch`, `assertWithinBuyerBudget`); `otd.ts:9,18`; `offer-validation.service.ts:21-24`; passes silently with no prequal row | ALREADY CORRECT | throws at submission | Close the no-prequal bypass: refuse submission when buyer has no valid prequal | 6 | unit | Offer tests: no prequal → refused | none | none | PRESERVED STRONGER SAFEGUARD |
| G29-10 | MD §29 L1451; HTML SAFE[2][4] | Best Price persisted with weights | `best-price.service.ts:112-120` | ALREADY CORRECT | persisted calc log | none | 6 | unit | Existing best-price tests | none | none | PRESERVED STRONGER SAFEGUARD |
| G29-11 | MD §29 L1452; HTML SAFE[4][2] | Deal transitions: table, CAS, history, exactly-once completion | `deal.service.ts:15-34,148,167,202` | ALREADY CORRECT | see T28-09 | see T28-04/T28-09 | 6 | state-machine | Seam tests | none | none | PRESERVED STRONGER SAFEGUARD |
| G29-12 | MD §29 L1453; HTML SAFE[3][0] | Contract uploads private, versioned, fail-closed | `contract-upload.service.ts:9-12`; `dealer-contract.service.ts:21`; `extract-text.ts:39-48` | ALREADY CORRECT | version = max+1; `assertDealerOwnsDeal` | Retire second unscanned writer (`contract-upload.service.ts`) behind the single path | 8 | unit | Contract tests | none | `contract-upload.service.ts` second writer | PRESERVED STRONGER SAFEGUARD |
| G29-13 | MD §29 L1454; HTML SAFE[3][1] | Shield approval binds to reviewed version | `dealer-contract.service.ts:59-112`; `contract-shield/[reviewId]/route.ts:79-88` | ALREADY CORRECT | `SUPERSEDED_BY_NEWER_UPLOAD` | none | 8 | unit | Existing tests | none | none | PRESERVED STRONGER SAFEGUARD |
| G29-14 | MD §29 L1455; HTML SAFE[3][2] | E-sign hash-bound; refuses without evidence storage | `buyer-signing.service.ts:91-94,212,409,58-69` | ALREADY CORRECT | `ESignSchemaUnavailableError` gate | none | 8 | unit | Existing esign tests | `ESIGN_EXECUTED_ARTIFACT_ENABLED` | none | PRESERVED STRONGER SAFEGUARD |
| G29-15 | MD §29 L1456; HTML SAFE[4][0] | Pickup strict turns, CAS, two-counter cap, compensating recovery | `pickup-coordination.service.ts:4-17,50,88` | ALREADY CORRECT | proposal-time CAS | none | 9 | unit, concurrency | Existing pickup tests | none | none | PRESERVED STRONGER SAFEGUARD |
| G29-16 | MD §29 L1457; HTML SAFE[4][1] | Pickup emails durable with round-specific keys | `pickup-notifications.service.ts:105,142,177,215,253`; `pickup-sla.service.ts:12,37` | ALREADY CORRECT | round keys | none | 9 | unit | Existing tests | none | none | PRESERVED STRONGER SAFEGUARD |
| G29-17 | MD §29 L1458; HTML SAFE[1][3] | Refund idempotency-keyed; NO_CHARGE never refunded | `refund.service.ts:23-44` | ALREADY CORRECT | `refund-deposit-${id}`; NO_CHARGE | Single primitive (C24-09a) | 3 | unit | Refund tests | refund/money-path activation | none | PRESERVED STRONGER SAFEGUARD |
| G29-18 | MD §29 L1459; HTML SAFE[5][0] | Identity firewall buyer ↔ non-winning dealers | `offer/dealer-display.ts:26` buyer→dealer masking; dealer→buyer isolation not traced here; lift at award `dealer-award.ts` | PARTIAL | `buyerFacingDealerName` | Phase 5 invitation payload no PII; Phase 7 lift moves to reaffirmation | 5 | unit | Dealer-isolation tests: invitation/offer payloads contain no buyer PII | none | award-time lift | TO EXTEND |
| G29-19 | MD §29 L1460; HTML SAFE[5][1] | Anti-circumvention capture + Ops routing | `messaging.service.ts:13-45`; `anti-circumvention.service.ts:5-11` zero callers | PARTIAL | capture + redaction | See E26-42a | 5 | unit | E26-42a test | none | none | TO EXTEND |
| G29-20 | MD §29 L1461; HTML SAFE[3][3] | Junk-fee, fee caps, APR, packing, disclosure in Shield | Rule engine not read in this area; contract area found APR/PACKING/DISCLOSURE/MARKUP never evaluated | UNVERIFIED | keyword + caps rules exist | Contract area (Phase 8 defect 4) | 8 | unit | Contract-shield rule tests | none | none | UNVERIFIED — contract area owns the evidence |
| G29-21 | HTML SAFE[5][2] | Scorecard consequences for reaffirmation failures, no-shows, contract delay, overdue obligations | `dealer-scorecard.service.ts:18-37` rates only | MISSING | none | Reaffirmation consequence (7); no-show/contract delay/overdue (9); §25.2 wiring (10) | 7 | unit | Scorecard tests per consequence type | none | none | TO IMPLEMENT |
| D1-01 | MD §27 L1292; map D1 #1-3 | Deposit confirmed email durable | R1 `app/api/webhooks/stripe/route.ts:287,439` (`deposit-confirmed-${depositId}`), webhook-bound | PARTIAL | idempotency key | Outbox row in settlement transaction (K27-1314) | 3 | integration | `test:webhooks` | none | R1 | TO CONSOLIDATE |
| D1-02 | MD §27 L1292; map D1 #2 | Auction activated email durable | R1 `stripe/route.ts:293` | PARTIAL | key | Move to launch service (K27-1320); settlement no longer launches | 5 | integration | Launch tests | none | R1; settlement→auction legacy | TO CONSOLIDATE |
| D1-03 | MD §27 L1292; map D1 #4 | Concierge-fee confirmed durable | R1 `stripe/route.ts:537` | PARTIAL | key | K27-1348 | 3 | integration | `test:webhooks` | none | R1 | TO CONSOLIDATE |
| D1-04 | MD §27 L1292; map D1 #5 | Refund confirmed durable | R1 `stripe/route.ts:685,754` | PARTIAL | key | K27-1376 | 3 | integration | `test:webhooks` | none | R1 | TO CONSOLIDATE |
| D1-05 | MD §27 L1292; map D1 #6-7 | Deal selected (buyer) durable | Outbox `select-offer/route.ts:114`; `requests/[requestId]/offer/respond/route.ts:138` | ALREADY CORRECT | `deal-selected-${dealId}` | Add recheck + refs | 6 | unit | Existing tests | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| D1-06 | MD §27 L1292; map D1 #8 | First-offer-received durable | R2 `offer.service.ts:156-180` via `buyer-notifications.service.ts:60`, after(), no key | BROKEN | none | K27-1324 | 6 | unit | Offer tests | none | R2 `buyer-notifications.service.ts` | TO CONSOLIDATE |
| D1-07 | MD §27 L1292; map D1 #9-10 | Intake emails durable (dealers-contacted, opp confirmation, founder hot lead) | R2 `intake-pipeline.service.ts:513`; R1 `:303,314` | PARTIAL | keys on R1 | Dispatcher rows from intake handler | 2 | unit | Intake tests | none | R1; R2 | TO CONSOLIDATE |
| D1-08 | MD §27 L1292; map D1 #11-14 | Dealer invitation emails durable (service + 3 admin routes) | R1 `dealer-invitation.service.ts:390`; `launch-auction/route.ts:281,298,315`; `auctions/[auctionId]/action/route.ts:140`; `invite-outside-dealers/route.ts:70` | PARTIAL | key `dealer-auction-invitation-${auctionId}-${to}` | Single invitation producer (K27-1321); admin routes call it | 5 | unit | Invitation tests | none | R1 ×4; `outside_auction_invites` | TO CONSOLIDATE |
| D1-09 | MD §27 L1292; map D1 #15-16 | Invitation reminder + offer-revision-closing durable | R1 `dealer-invitation-reminder/route.ts:100`; `auction-close/route.ts:76,93` | DUPLICATED | shared key | K27-1322; closing notice via dispatcher | 5 | unit | Cron tests | none | R1 crons | TO CONSOLIDATE |
| D1-10 | MD §27 L1292; map D1 #17-19 | Offers-ready, no-winner, won/lost durable | Outbox `auction.service.ts:161,192`; `dealer-award.ts:272,289,312` | ALREADY CORRECT | durable marker | K27-1332 window fix | 6 | unit | Existing tests | none | none | ALREADY PRESENT (VERIFY IN PHASE) |
| D1-11 | MD §27 L1292; map D1 #20 | Dealer offer submitted/revised confirmation durable | R1 `app/api/dealer/offers/route.ts:59`, `[offerId]/revise/route.ts:64` | PARTIAL | key `dealer-offer-submitted-${offerId}` | Row from offer service | 6 | unit | Offer tests | none | R1 | TO CONSOLIDATE |
| D1-12 | MD §27 L1292; map D1 #21-23 | Prequal notice set durable (service, admin service, 3 admin routes) | R1 `prequal.service.ts:599,651,703,749`; `admin-prequal.service.ts:596,647,707,738`; `manual-override/route.ts:152,201`; `resend-email/route.ts:70,82`; `compliance/ofac/[prequalId]/route.ts:107` | DUPLICATED | outcome persistence | One prequal notice module via dispatcher used by both services and routes | 2 | unit | Prequal tests | none | R1; duplicate implementations | TO CONSOLIDATE |
| D1-13 | MD §27 L1292; map D1 #24 | Prequal expiry warning durable | R2 in cron `prequal-ibv-reminders/route.ts:95` | BROKEN | Notification dedup | K27-1310 | 2 | unit | Cron test | none | R2 in cron | TO CONSOLIDATE |
| D1-14 | MD §27 L1292; map D1 #25-26 | Contract approved / shield alert / dealer issues durable | R1 `contract-shield.service.ts:368,370`; `contract-shield/[reviewId]/route.ts:131,178,182,221` | DUPLICATED | key dedup | K27-1359/1360 | 8 | unit | Contract-shield tests | none | R1 route duplicate; `Date.now()` key | TO CONSOLIDATE |
| D1-15 | MD §27 L1292; map D1 #27 | Admin deal action emails durable | R1 `deals/[dealId]/action/route.ts:91,116,153` | PARTIAL | keys | Rows from seam transitions (K27-1357, 1373) | 8 | unit | Admin route tests | none | R1 | TO CONSOLIDATE |
| D1-16 | MD §27 L1292; map D1 #28 | Dealer esign initiated durable | R1 `deals/[dealId]/esign/route.ts:70` | PARTIAL | key | Row from esign service | 8 | unit | Esign tests | none | R1 | TO CONSOLIDATE |
| D1-17 | MD §27 L1292; map D1 #29 | Contract signed durable | R1 in after() `buyer-signing.service.ts:881`; reconcile behind flag | PARTIAL | idempotent key + reconcile | K27-1363 | 8 | unit | Esign tests | `ESIGN_EXECUTED_ARTIFACT_ENABLED` | R1 after() | TO CONSOLIDATE |
| D1-18 | MD §27 L1292; map D1 #30 | Admin pickup schedule emails durable | R1 `pickup/schedule/route.ts:100,108` | DUPLICATED | OB producer exists | K27-1367 | 9 | unit | Pickup tests | none | R1 | TO CONSOLIDATE |
| D1-19 | MD §27 L1292; map D1 #31 | Admin pickup complete emails durable | R1 `pickup/complete/route.ts:99,125,134` | DUPLICATED | keys | K27-1373; payout-initiated row in completion transaction | 9 | unit | Completion tests | none | R1 | TO CONSOLIDATE |
| D1-20 | MD §27 L1292; map D1 #32 | Dealer scan completion email durable | R2 in route, no key, `.catch(()=>{})` `dealer/pickup/scan/route.ts:126` | BROKEN | none | Removed with auto-completion (E26-41); row from atomic completion | 9 | unit | `scan.test.ts`: no SDK call | none | R2 in route | TO CONSOLIDATE |
| D1-21 | MD §27 L1292; map D1 #33 | Admin payment-link emails durable | R1 `admin/payments/deposit/send-link/route.ts:99`, `concierge-fee/send-link/route.ts:100` | PARTIAL | keys | Rows via dispatcher; existing-obligation check first (G29-04) | 3 | unit | Admin payments tests | none | R1 | TO CONSOLIDATE |
| D1-22 | MD §27 L1292; map D1 #34 | Deposit override confirmation durable | R1 `deposit/override/route.ts:108` | PARTIAL | key | Row via dispatcher | 3 | unit | Admin tests | none | R1 | TO CONSOLIDATE |
| D1-23 | MD §27 L1292; map D1 #35 | Admin-created buyer invite durable | R1 + R2 `admin/buyers/[buyerId]/invite/route.ts:40,52` | PARTIAL | key on R1 | One row via dispatcher | 2 | unit | Admin tests | none | R1; R2 | TO CONSOLIDATE |
| D1-24 | MD §27 L1292; map D1 #36 | Public vehicle-request emails durable | R2 `vehicle-offers.email.ts` + R1 `public/request-vehicle/route.ts:623,656,663`; `complete/route.ts:220-221` | DUPLICATED | key on R1 | K27-1311 + admin notification row | 2 | unit | Intake tests | none | R2 `vehicle-offers.email.ts`; R1 | TO CONSOLIDATE |
| D1-25 | MD §27 L1292; map D1 #37 | Concierge-track invitation/offer emails durable | R2 `vehicle-offers.email.ts:21-32` from `send-to-dealers/route.ts:79`, `vehicle-offers/[id]/*`, `public/dealer-offer/[token]/route.ts:248,260`, `outside-dealer-offer/[token]/route.ts:167`, `admin/offers/route.ts:238`, `buyer-offer-review/[reviewToken]/question/route.ts:57` — no key, no suppression | BROKEN | none | Staff-intake surfaces write canonical `offers` (Phase 6) and reuse canonical notices | 6 | unit | Concierge-track tests | none | R2 concierge track | TO CONSOLIDATE |
| D1-26 | MD §27 L1292; map D1 #38 | Dealer agreement confirmation durable | R2 `dealer-agreement.service.ts:159` → `dealer-agreement-confirmation.service.ts:103` after() | PARTIAL | none | Row via dispatcher (outside-winner agreement sequencing) | 7 | unit | Agreement tests | none | R2 | TO CONSOLIDATE |
| D1-27 | MD §27 L1292; map D1 #39-40 | Dealer opportunity / post-intake outreach durable | R1 `dealer-opportunity-notification.service.ts:67`; R2 via `dealer-email-send.service.ts:241` from `post-intake-outreach.service.ts:274` | PARTIAL | key on R1 | Sourcing case outreach via dispatcher; consent governance preserved | 5 | unit | Sourcing tests | none | R1; R2 | TO CONSOLIDATE |
| D1-28 | MD §27 L1292; map D1 #41 | QStash `/api/jobs/*` R3 workloads | 16 consumers scheduled by `lib/services/crm/lifecycle-scheduler.ts` (flag OFF → QStash, vendor dead) | BROKEN | none | Phase 2: workloads internal-by-default via dispatcher; Phase 10: delete dependency/routes/env (§13-D23) | 2 | unit | Scheduler tests: no QStash call; `no-direct-transactional-send` allowlist | legacy removal (§13-D23, Phase 10) | QStash R3 | TO CONSOLIDATE |
| D1-29 | MD §27 L1292; map D1 #42 | `lifecycle_touch_schedule` R4 second durable outbox | `lifecycle-touch-drain.service.ts` cron; UNIQUE(base_key,sequence) | PARTIAL | send-time guards; UNIQUE | Touch sends route through dispatcher (`run_at`), table brought under chain (Phase 1) | 2 | unit, integration | Touch-drain tests assert outbox rows | owner decision Q2 (fold R4 into outbox) | R4 | TO CONSOLIDATE |
| D1-30 | MD §27 L1292; map D1 #43 | Seam SMS/in-app per DealStatus durable | R5 + IA `acquisition-comms.ts:560`; SMS flag `:342` | BROKEN | consent-aware plan | T28-07 | 6 | unit | `acquisition-comms` tests | owner decision Q6 | seam direct SMS | TO CONSOLIDATE |
| D1-31 | MD §27 L1292; map D1 #44-45 | Admin cancel/refund in-app notices durable | IA only `auctions/[auctionId]/action/route.ts:176-207`; `deals/[dealId]/action/route.ts:193-215,245-254` | PARTIAL | none | Rows from `cancelTransaction` (in_app channel) | 10 | unit | `cancel-transaction.test.ts` | none | inline `notification.create` | TO CONSOLIDATE |
| D1-32 | MD §27 L1292; map D1 #46 | Ops/voice/public/MFA raw Resend (out of transaction scope) | R2 `cron/morning-briefing/route.ts:39`, `twilio/voice/status/route.ts:68,181`, `public/{feedback,contact}`, `admin/auth/setup-mfa/send-email/route.ts:167`, `admin/dealer-outreach/compose/route.ts:187` | PARTIAL | none | Keep outside the transaction trees with documented reason, or adapter-wrap; not on allowlist scope | 10 | n/a | Allowlist scope review at Phase 10 | owner decision (retire vs keep) | R2 non-transaction | OWNER-GATED |
| D1-33 | MD §27 L1292; map D1 #47 | Raw Twilio senders without consent gate | `lib/services/sms/twilio.service.ts:25-39` (no gate), callers `lib/voice/transactional-sms.ts:33`, `lib/social/sms-distribution.service.ts:56,84`, `lib/voice/dispatch-request.ts:376`, `dealer-sms-wiring.ts:115`, `crm/conversations/[id]/reply/route.ts:103`; `acquisition/twilio.service.ts:45` | BROKEN | `sendCrmSms` gates (R5) | Transaction-tree callers route via dispatcher SMS (TCPA + suppression + quiet hours); voice/social callers wrapped with gate or documented | 10 | unit | `no-direct-transactional-send` allowlist zero; gate test on `twilio.service.sendSms` | owner decision (voice/social scope) | ungated `twilio.service.sendSms` | TO CONSOLIDATE |
| DUP-01 | map Duplicates #1 | Two deal-cancel writers | `cancelDeal` `deal.service.ts:385` vs admin `advanceDealStatus(force)` `deals/[dealId]/action/route.ts:190` | DUPLICATED | `cancelDeal` seam | See C24-09a (Phase 3) and C24-12 (Phase 10) | 3 | unit | C24-09a evidence | none | admin direct cancel | TO CONSOLIDATE |
| DUP-02 | map Duplicates #2 | Two envelope void functions | `esign.service.voidEnvelope` vs `buyer-signing.voidEnvelopeInternal` | DUPLICATED | CAS void | See C24-07 | 10 | unit | C24-07 evidence | none | `voidEnvelope` | TO CONSOLIDATE |
| DUP-03 | map Duplicates #3 | Three durable comms planes | `comms_outbox` vs `lifecycle_touch_schedule` (R4) vs QStash `/api/jobs/*` (R3) + `lifecycle-scheduler.ts` | DUPLICATED | outbox CAS/dedup | One dispatcher with `run_at`; R3 deleted Phase 10; R4 folded per owner decision | 2 | unit, integration | D1-28/D1-29 evidence | owner decision Q2; legacy removal §13-D23 | R3, R4 | TO CONSOLIDATE |
| DUP-04 | map Duplicates #4 | Four operations-exception planes | `Notification(SYSTEM_ALERT)` (≥6 writers), `PlatformAlert` (`createAlertOnce`), `FinancingReviewTask`, Sentry `pageOnCall` | DUPLICATED | dedup-by-title; `createAlertOnce` | `queue_items` is the store; SYSTEM_ALERT/PlatformAlert become mirrors; FinancingReviewTask folded (E26-28) | 2 | unit | `queue-item.service.test.ts`; register completeness | none | `SYSTEM_ALERT` store; `PlatformAlert` | TO CONSOLIDATE |
| DUP-05 | map Duplicates #5 | Two prequal notice-set implementations | `prequal.service.ts:599-749`; `admin-prequal.service.ts:596-738` | DUPLICATED | none | See D1-12 | 2 | unit | D1-12 evidence | none | duplicate implementation | TO CONSOLIDATE |
| DUP-06 | map Duplicates #6 | Contract-approved email twice | `contract-shield.service.ts:368`; `contract-shield/[reviewId]/route.ts:131` | DUPLICATED | key dedup | See K27-1360 | 8 | unit | K27-1360 evidence | none | route duplicate | TO CONSOLIDATE |
| DUP-07 | map Duplicates #7 | Deal-complete email four ways | `deals/[dealId]/action:116`, `pickup/complete:99`, `dealer/pickup/scan:126`, QStash `deal-complete` | DUPLICATED | completion seam | See K27-1373 | 9 | unit | K27-1373 evidence | none | R1 ×2, R2, R3 | TO CONSOLIDATE |
| DUP-08 | map Duplicates #8 | Vehicle-request confirmation twice | `public/request-vehicle/route.ts:656,663` | DUPLICATED | none | See K27-1311 | 2 | unit | K27-1311 evidence | none | R2 | TO CONSOLIDATE |
| DUP-09 | map Duplicates #9 | Dealer invitation reminder twice | `dealer-invitation-reminder` + `auction-close` crons | DUPLICATED | shared key | See K27-1322 | 5 | unit | K27-1322 evidence | none | `auction-close` reminder branch | TO CONSOLIDATE |
| DUP-10 | map Duplicates #10 | Auction activated three producers | R1 webhook + admin, R3 `auction-active` | DUPLICATED | key | See K27-1320 | 5 | unit | K27-1320 evidence | none | R1; R3 | TO CONSOLIDATE |
| DUP-11 | map Duplicates #11 | Two `createAlert` helpers | `health-alert.service.createAlert` (PlatformAlert) vs `inventory-budget-alert.service` local `createAlert` (Notification) | DUPLICATED | none | Both call `raiseException`; local helper deleted | 4 | unit | E26-17 evidence | none | local `createAlert` | TO CONSOLIDATE |
| DUP-12 | map Duplicates #12 | In-app trigger catalogs | `notification.service.BuyerTriggers/DealerTriggers` (0 callers) vs `acquisition-comms.dealStatusCommsPlan` | DUPLICATED | plan exhaustiveness guard | Delete dead catalog; plan table becomes template registry input | 6 | unit | Typecheck + dead-code removal test in seam suite | none | dead `BuyerTriggers/DealerTriggers` | TO CONSOLIDATE |
| DUP-13 | map Duplicates #13 | Two history writers | `advanceDealStatus` vs `deal-timeline.recordStatusTransition` (0 callers) | DUPLICATED | seam history | Delete dead second plane | 6 | unit | Seam tests; grep-based dead-code assertion | none | `deal-timeline.recordStatusTransition` | TO CONSOLIDATE |
| X-01 | map out-of-scope #2 | `resolveQueueItem` swallows DB errors and still audits "resolved" | `admin-queue.service.ts:89-94` (TODO at `:92`) | BROKEN | explicit decision words | Resolve via `queue-item.service.resolve()`; DB error → 500, no audit row | 2 | unit | `queue-item.service.test.ts` | none | derived queue resolve | TO EXTEND |
| X-02 | map out-of-scope #3 | `sendIdempotent` fail-open when EmailSendLog lookup throws; no suppression check | `resend.service.ts:145-149`; no `SuppressionService` import | BROKEN | EmailSendLog dedup | Adapter wrapper fails closed until sender migrated; wrapper removed at allowlist zero | 2 | unit | `sender-migration.test.ts`: lookup throw → refuse | none | R1 | TO EXTEND |
| X-03 | map out-of-scope #4 | Non-idempotent `Date.now()` keys | `dealer-contract-issues`, `affiliate-*`, `dealer-account-suspended/reinstated`, `dealer-stale-listing` (`resend.service.ts`) | BROKEN | none | Stable keys on migration; `dealer-contract-issues` in Phase 8 (K27-1359); others with their owning area | 8 | unit | Key-parity tests | none | R1 | TO EXTEND |
| X-04 | map out-of-scope #5 | Dealer cancel notice mislabelled `AUCTION_STARTED` | `auctions/[auctionId]/action/route.ts:197` | BROKEN | none | `cancelTransaction` in-app rows use `AUCTION_CANCELLED` type | 10 | unit | `cancel-transaction.test.ts` | none | none | TO EXTEND |
| X-05 | map out-of-scope #6 | `INAPP_OWNED_BY_CALLERS` includes CANCELLED but `cancelBuyerWorkflow` creates none | `acquisition-comms.ts:295-308`; `admin-buyer-command-center.service.ts:927-975` | BROKEN | none | Seam owns CANCELLED in-app via outbox (C24-10) | 10 | unit | `cancel-transaction.test.ts` | none | none | TO EXTEND |
| X-06 | map out-of-scope #8 | Skill `autolenis-deal-lifecycle` still describes DocuSign | Code is in-house signing `esign.service.ts:1-9` | PARTIAL | none | Update skill text during Phase 8 close | 8 | n/a | Skill diff in phase report | none | none | TO EXTEND |

## Owner-gated items

- Production deploy of the Phase 1 migration wave (§13-D1 ledger resolve → `prisma migrate deploy`): `queue_items`, `comms_outbox` under the chain + additive columns (`trigger_event`, refs, `cancel_key`, `cancelled_at`, `state_recheck`, `terminal_failed_at`, channel `in_app`, status `cancelled`), `FROZEN_PENDING_RELEASE`/`DEALER_EXECUTED`/`FUNDING_PENDING`/`HANDOVER_PENDING`, `PickupStatus.CANCELLED/MISSED`, `AuctionInvitationStatus.CANCELLED`, `DepositStatus.DISPUTED` (if absent) — rows C24-05a, C24-08a, C24-13a, E26-00a, E26-10, M27-01a, M27-03, M27-04a, M27-05.
- Refund / money-path activation (refunds remain manual; single refund primitive) — C24-09a, C24-09b, E26-46, K27-1352, K27-1376, G29-17.
- `ESIGN_EXECUTED_ARTIFACT_ENABLED` (§13-D4; preview-only verification) — E26-35, K27-1364, D1-17, G29-14.
- `ACQUISITION_COMMS_SMS_ENABLED` — owner decision Q6 whether seam SMS stays flag-gated after outbox routing — C24-10, T28-07, D1-30.
- Owner decision Q2 — fold `lifecycle_touch_schedule` (R4) into `comms_outbox` or keep as durable plane — D1-29, DUP-03.
- Legacy removal §13-D23 — delete `@upstash/qstash`, `lib/qstash/*`, 16 `app/api/jobs/*` routes, `QSTASH_*` env in Phase 10 — D1-28, DUP-03, M27-02b.
- Owner decision Q5 — accept spec: dealer scan never completes the deal — E26-41.
- Owner decision (scope) — voice/social/ops raw Resend and ungated Twilio senders: retire or keep with documented reason — D1-32, D1-33.
- Owner decision (policy) — suspension/termination consequences for confirmed circumvention — E26-42b.
- Owner assignment table (map Q7) — which admin role resolves each `QueueItemType` — E26-00b (blocks admin surface labels only, not the writer).

## Legacy paths

- `resend.service.sendIdempotent` direct rail (R1, ~75 senders) — allowlisted Phase 2, migrated per owning phase, allowlist zero Phase 10 (D1-01..D1-27, X-02, X-03).
- Raw `new Resend()` senders (R2): `vehicle-offers.email.ts`, `buyer-notifications.service.ts`, `dealer/pickup/scan/route.ts:126`, `prequal-ibv-reminders/route.ts:95`, `dealer-agreement-confirmation.service.ts`, `dealer-email-send.service.ts` (D1-06, D1-07, D1-13, D1-20, D1-24, D1-25, D1-26, D1-27).
- QStash `/api/jobs/*` + `lifecycle-scheduler.ts` flags (R3) — neutralised Phase 2, deleted Phase 10 (D1-28, DUP-03, K27-1302/1304/1312/1320/1321/1324/1373).
- `lifecycle_touch_schedule` drain (R4) — second durable outbox; brought under chain Phase 1, sends via dispatcher Phase 2 (D1-29, K27-1303/1304/1312).
- Seam direct SMS `sendCrmSms` (R5) and inline `prisma.notification.create` in-app writes (T28-07, M27-04b, D1-30, D1-31).
- `lib/services/sms/twilio.service.sendSms` ungated sender (voice/social) (D1-33).
- `Notification(type=SYSTEM_ALERT)` dedup-by-title as the ops queue; `PlatformAlert`; `FinancingReviewTask` — become mirrors / folded into `queue_items` (E26-00b, E26-03, E26-08, E26-09, E26-17, E26-28, K27-1316, K27-1325, DUP-04, DUP-11).
- Admin `DEAL_CANCELLED` auto-refund and `REFUND_TRIGGERED → REFUNDED` on `NO_CHARGE`; `DEAL_STAGE_ADVANCED→CANCELLED`; `force` override (C24-01, C24-09a, C24-14, T28-09, DUP-01).
- `pauseBuyerWorkflow` using `AuctionStatus.CANCELLED` as pause (C24-05b, T28-10b).
- Buyer `requests/[id]/cancel` raw status write without reason (C24-02, T28-10a).
- Dealer-scan auto-completion (`PICKUP_SCHEDULED → COMPLETED`) and non-crypto QR nonce (E26-41, K27-1371, T28-10d, C24-08b).
- `esign.service.voidEnvelope` duplicate of `voidEnvelopeInternal` (C24-07, DUP-02).
- Dead code: `notification.service.BuyerTriggers/DealerTriggers`, `deal-timeline.recordStatusTransition`, `anti-circumvention.recordCircumventionAttempt` (0 callers), `requestDocument()` (0 callers — reused) (DUP-12, DUP-13, E26-42a, E26-32, K27-1355).
- `holds` no-op cron — repurposed for vehicle-hold expiry Phase 7 (E26-24, K27-1337).
- Concierge (vehicle-request) track offer/invite/email set on R2 — folded into canonical `offers` Phase 6 (D1-25).
- `outside_auction_invites` — reads kept, writes stopped Phase 5 (D1-08, E26-26, K27-1338).
- `contract-upload.service.ts` second unscanned `ContractVersion` writer; admin CONTRACT_FAIL resolve mutating scans; `EXTERNAL_UPLOADED` counted as insurance-satisfied; `Deal.insuranceStatus` direct writers (G29-12, E26-33, E26-31, T28-10c).
- `Buyer.plan` from signup metadata + mandatory `FEE_PENDING` (E26-43a, K27-1346).
- Award-time identity-firewall lift (`dealer-award.ts`) — moves to reaffirmation Phase 7 (E26-22, G29-18).
- `lib/jobs/idempotency.ts` comms ledger (fail-open) — replaced by outbox `dedup_key` for comms (T28-05).
- Duplicate `checkDepositProviderEvidence` reconciliation detector (E26-08).

## Gap-fill rows (round 1)

Nineteen requirements the completeness critic found uncovered. Same 13 columns and rules as the table above; paths relative to `frontend/` unless prefixed. New ref families: `B2` = §2 "AutoLenis never" role boundaries; `S12D` = HTML Stage 12 dealership bullets; `W30` = §30 "Who is responsible for what" rows; `G35` = §35 governing statement; `P23` = §23.2 upgrade-window divergence. HTML line numbers are the true file lines (`AutoLenis-Transaction-Flow.html`), with the critic's cited line kept in parentheses where it differed.

| Ref | Document & section | Requirement | Current route / service / model / component / email / cron / queue / admin action | Status | Stronger safeguard to preserve | Exact required change | Phase | Test level | Acceptance evidence | Owner-gated dependency | Legacy path affected | Final disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| B2-01 | MD §2 "AutoLenis never" bullet 1 (L82) | AutoLenis never sells or takes title to a vehicle — the dealership is always the seller of record | Only a marketing-copy guard exists: rule `buyer_misrepresentation` `lib/content/compliance.ts:104-116` with `lib/content/__tests__/compliance.test.ts:81,88`, reached only from `lib/content/generator.ts`, `lib/content/quality.ts`, `lib/services/content/content-validation.service.ts` (AMIPS content) — never from a transaction surface. No seller-of-record or title field anywhere (`rg seller\|sellerOfRecord\|purchaseFrom\|takeTitle` over `app/**` + `lib/services/**` + `prisma/schema.prisma` → only buyer-education copy and MarketCheck `seller_email` `lib/services/inventory/adapters/marketcheck.adapter.ts:84,411`). Money model is already one-sided in code: buyer→AutoLenis only (`lib/services/deal/service-fee.service.ts:19-43`; `lib/constants.ts:6-9`), no buyer↔dealer rail | PARTIAL | The `buyer_misrepresentation` pattern list and its two tests; the absence of any buyer↔dealer money rail (deposit + fee are the only Stripe intents) — keep both untouched | Phase 1 enforcement object (4th, alongside the `credit_applications` freeze S14a): `lib/__tests__/role-boundary-frozen.test.ts` scanning `app/**`, `lib/services/**`, `prisma/schema.prisma` for seller-of-record / title-taking constructs and for AutoLenis-as-payee-to-a-dealer, with an explicit allowlist that is empty on HEAD; it imports the existing `buyer_misrepresentation` patterns rather than restating them and applies them to transaction-facing copy (recap, receipt, contract-request and completion emails), not just AMIPS content. Zero runtime behaviour change | 1 | unit | New test fails on an introduced `sellerOfRecord`/`titleHolder` field or an "AutoLenis sells/finances" string in a transaction template; passes on HEAD with an empty allowlist; `pnpm test:all` green | none | `backend/server.py` proxy surface is out of the scan root (see G35-01) | TO IMPLEMENT |
| B2-02 | MD §2 "AutoLenis never" bullet 7 (L88) | AutoLenis never issues titles, registrations, or temporary tags | No issuance path exists and no assertion that none may be added: `rg` for title/registration/temp-tag issuance across `app/**` + `lib/**` returns buyer-education copy only (`lib/tools/dealer-fees.ts:81-88`, `lib/leads/honest-guide-content.ts:44`, `app/(public)/buying-guide/dealer-fees-complete-guide/page.tsx:32,86`, `lib/services/email/resend.service.ts:294`). The Stage 21 record that would carry the dealership's title/registration/temp-tag duty does not exist — `post_completion_obligations` absent (`rg post_completion` → 0; pickup map R21.1a, Phase 1 wave) | MISSING | Existing education copy already names title and registration as state-set fees paid to the DMV and not to AutoLenis — keep that wording as the buyer-facing statement of the boundary | On the Phase 1 `post_completion_obligations` table, constrain `owner_role` for the `TITLE_REGISTRATION_DELIVERY` and `TEMP_TAG_EXPIRY` types to `DEALERSHIP` at the single writer (never `AUTOLENIS`/`SYSTEM`), render "the dealership delivers, AutoLenis tracks" in the buyer and dealer surfaces, and extend `role-boundary-frozen.test.ts` (B2-01) with the negative case that no route creates, renders or emits a title, registration, plate or temporary-tag artifact | 9 | unit, integration | New `lib/services/deal/__tests__/post-completion-obligations.test.ts` case "title/registration obligation is dealership-owned; `owner_role=AUTOLENIS` is rejected"; role-boundary test extended and green | none | none | TO IMPLEMENT |
| B2-03 | MD §2 "AutoLenis never" bullet 8 (L89); MD Stage 19 L975 | AutoLenis never releases a vehicle or substitutes for the dealership's delivery obligations | Two AutoLenis-actored release paths exist. `app/api/admin/deals/[dealId]/pickup/complete/route.ts:53-74` upserts `Pickup.status=COMPLETED` and calls `advanceDealStatus(dealId,"COMPLETED",{force:true})`, explicitly bypassing the insurance gate on an admin reason string; `app/api/admin/buyers/[buyerId]/journey/complete-all/route.ts:17-20,26-28` walks `ALL_STAGES` including `pickup`. The dealer path is correctly dealer-actored (`app/api/dealer/pickup/scan/route.ts:36-62,87`) but completes the Deal on the dealer's word alone — no buyer possession confirmation exists, contradicting "the Deal never completes automatically on the dealer's word alone" | BROKEN | Insurance hard gate re-checked inside the seam and mapped to a truthful 409 (`app/api/dealer/pickup/scan/route.ts:73-81,95-101`); SUPER/OPERATIONS-only plus mandatory non-empty reason on the override (`pickup/complete/route.ts:31-33,48-50`); token ownership + expiry checks (`scan/route.ts:53-67`) — keep all | Split release from completion: dealer release evidence writable only by a dealer-authenticated action, buyer possession confirmation only by the buyer; the admin route stops writing release/possession and becomes a recorded discrepancy resolution that names the releasing dealership and never `force`-bypasses insurance; drop `pickup` from `ALL_STAGES` in `complete-all`; extend `role-boundary-frozen.test.ts` so no AutoLenis-actored path may set `dealer_released_at` or `possession_confirmed_at` | 9 | unit, integration, state-machine, playwright | New `lib/services/pickup/__tests__/handover.test.ts`: admin cannot set release or possession; dealer release alone does not complete the Deal; Playwright dealer scan → buyer confirms → completed exactly once | none | admin pickup force-complete override; `journey/complete-all` pickup stage; dealer-scan auto-completion (E26-41, K27-1371, T28-10d) | TO IMPLEMENT |
| S12D-01 | HTML `S[11].dealer[0]` `AutoLenis-Transaction-Flow.html:629` (cited L637); MD §12 L735 | Dealership arranges financing where the path is dealer-arranged | Surface exists but is read-only: `app/dealer/financing/page.tsx:20-45` lists `Financing` rows with `path:"DEALER"` scoped to this dealer's deals and renders APR / term / monthly / approved / status with no action. No dealer write route (`rg financing app/api/dealer` → only `offers/[offerId]/revise/route.ts`). `Financing` is written only by the buyer (`app/api/buyer/financing/route.ts:64`) and by the frozen in-house orchestrator (`lib/services/financing/financing-orchestrator.service.ts:107`). `app/dealer/deals/[dealId]/page.tsx:45-63` returns no next action for `FINANCING_PENDING`, so the dealership is never told it owns the stage | PARTIAL | Dealer isolation on the page — rows scoped to `offer.dealerId === dealer.id` and CANCELLED/REFUNDED excluded (`app/dealer/financing/page.tsx:25-30`); the no-in-house-financing spine test (`lib/services/auction/__tests__/no-inhouse-financing-on-auction-spine.test.ts`) | Add a dealer-actored terms attestation on the existing page — `POST /api/dealer/deals/[dealId]/financing/terms` writing lender name, approved amount, down payment, APR, term, payment and expiry onto `Financing`, guarded by the deal's financing path and left unverified until staff verification (§30 row 12 "verify and record"); add the `FINANCING_PENDING` next-action to the dealer switch. Never a credit application, never an SSN field | 7 | unit, integration | New `app/api/dealer/deals/__tests__/financing-terms-route.test.ts`: another dealer → 403, non-DEALER path → 422, submitted terms land unverified and cannot reach terms-locked without a staff action | `FinancingStatus` needs `TERMS_LOCKED`/`VERIFIED` (Phase 1 wave; `prisma/schema.prisma:1711-1716` has PENDING, SELECTED, APPROVED, DECLINED only) | frozen in-house `credit_applications` orchestrator and lender services (§8.4) | TO IMPLEMENT |
| S12D-02 | HTML `S[11].dealer[1]` `AutoLenis-Transaction-Flow.html:629` (cited L637) | Dealership submits to its lenders outside AutoLenis — the out-of-platform hand-off the dealer is instructed to perform | Nothing represents the hand-off. `FinancingPath` (`prisma/schema.prisma:1844-1848`) offers DEALER, EXTERNAL, CASH with no submitted-outside marker; `Financing` (`:2059-2073`) has no `submitted_at`, lender reference or evidence field; the dealer portal carries no copy telling the dealership that submission happens with its own lenders outside AutoLenis (`app/dealer/financing/page.tsx:46-56` subtitle and empty state say only "buyers select dealer financing"). The only lender-submission code is the frozen in-house adapter `lib/services/financing/lender/lender-service.ts` | MISSING | `lib/services/auction/__tests__/no-inhouse-financing-on-auction-spine.test.ts` already forbids in-house financing on the transaction spine — extend it rather than writing a second guard | On the same dealer surface, state plainly that submission happens with the dealership's own lenders outside AutoLenis and that AutoLenis records only the outcome; record `submitted_outside_at` and the lender name on the S12D-01 attestation as evidence, not as a submission; extend the no-in-house test so any import of the lender adapter from a dealer or buyer route fails the build | 7 | unit | `no-inhouse-financing-on-auction-spine.test.ts` extended (dealer route may not import `lender-service`); dealer-page copy assertion for the outside-lender statement | none | `lib/services/financing/lender/lender-service.ts` in-house adapter (frozen) | TO EXTEND |
| W30-01 | MD §30 row 1 (L1467); HTML `S[0..1]` | Stages 1–2 responsibility split stored and displayed: buyer registers, verifies, provides address; dealership none; staff correct geocoding failures; system verifies, geocodes, dedupes | No responsible-party storage exists anywhere (`rg responsibleParty\|responsible_party\|ownerRole\|owner_role\|stageOwner` over `frontend/` → 0 hits). Three unrelated derivations stand in for it: buyer `lib/services/buyer/journey.ts:14-18` (15 portal stages, not the 21 spec stages) with a buyer-only `nextAction` string and no owner or deadline (`:52-71,143`, served by `app/api/buyer/journey-status/route.ts`); dealer `app/dealer/deals/[dealId]/page.tsx:45-63` (hardcoded switch); admin `lib/services/admin/admin-queue.service.ts:7-39` (8 status-derived queues). Stage 1–2 specifics: consent recorded at registration `app/auth/callback/route.ts:136,153`; verification resend user-initiated only `app/api/auth/resend-verification/route.ts:154`; no address or geocode write path at all (`docs/plans/BUYER-LOCATION-GAP.md:14-31`, row E26-02), so the staff geocoding-correction duty has no surface | MISSING | The fact-derived, never-manually-advanced design of `journey.ts` (a pure function over facts, `:1-13`) — the registry must stay derived, not hand-set; matcher fail-closed on null location (E26-02) | Introduce the canonical registry `lib/services/transaction/stage-responsibility.ts` in Phase 2 alongside the `queue_items` writer (C2): `STAGE_RESPONSIBILITY` keyed by the 21 spec stages, each carrying buyer / dealership / staff / system duty text, the `owner_role` that owns the current checkpoint, the deadline source and the recovery action; persisted as `owner_role` on the checkpoint and on every `queue_items` row. Seed stages 1–2 in this phase, including a staff geocode-correction action, and have the Stage 1–2 buyer surface read its duty text from the registry rather than `NEXT_ACTION` | 2 | unit, integration | New `lib/services/transaction/__tests__/stage-responsibility.test.ts`: every spec stage resolves exactly one `owner_role`; stages 1–2 duties match MD §30 row 1 verbatim; buyer surface renders the registry text | none | `journey.ts` `NEXT_ACTION` map as the only next-action source | TO IMPLEMENT |
| W30-02 | MD §30 row 2 (L1468); HTML `S[2]` | Stage 3 responsibility split: buyer applies and consents; staff perform manual and OFAC review and adverse action; system pulls, screens, decides, notifies | Duties exist as code but are never attributed. Buyer apply/consent and system pull/screen/decide/notify: `lib/services/prequal/prequal.service.ts:103-121` (held MANUAL_REVIEW plus `createAlertOnce`), `:640-670` (adverse-action email with persisted outcome); OFAC screen `lib/services/identity/ofac.service.ts:4-11`. Staff review is a derived list with no owner or deadline: `lib/services/admin/admin-queue.service.ts:9,11` (`OFAC_ALERT`, `PREQUAL_MANUAL`) plus the escalation cron `app/api/cron/prequal-sla-escalation/route.ts:25-58`. Nothing tells the buyer who holds their file or by when | PARTIAL | Explicit CLEAR/CONFIRM resolve wording on the derived queue (`admin-queue.service.ts:44-83`); OFAC fail-closed; adverse-action outcome discrimination SENT/DUPLICATE/FAILED/THREW (`prequal.service.ts:640-670`) — all preserved | Extend the W30-01 registry with Stage 3: `owner_role=Compliance` while a manual or OFAC hold is open, `owner_role=Buyer` while an application is incomplete, `System` otherwise; the `queue_items` rows raised by E26-03/E26-04/E26-05 carry that `owner_role` and the SLA deadline from `prequal-sla-escalation`, and the buyer-visible status names the responsible party in plain language | 2 | unit, integration | `stage-responsibility.test.ts` Stage 3 cases; `lib/services/operations/__tests__/queue-item.service.test.ts` asserts OFAC/manual rows carry `owner_role=Compliance` and a deadline; buyer surface shows "AutoLenis Compliance is reviewing" not a bare status | none | derived admin queue as the only review surface | TO EXTEND |
| W30-03 | MD §30 row 3 (L1469); HTML `S[3]` | Stage 4 responsibility split: buyer defines and elects; dealership none; staff none; system validates criteria and revalidates inventory | System half partially exists — criteria matching and revalidation in `lib/services/inventory/request-inventory-match.service.ts:64-73` and `lib/services/inventory/inventory-match-score.ts:35,70-77,87-96`, refreshed by the `inventory-match-refresh` cron into `VehicleRequestMatchResult`; the buyer half is the Vehicle Request itself (`prisma/schema.prisma:1022-1070`, no `entry_type` — row R4a). Nothing records that Stage 4 has **no** staff and **no** dealership owner, so an unattended Stage 4 currently reads the same as an unattended Stage 3 in every portal | MISSING | Required-criteria handling and the fail-closed match path being extended in Phase 4 (R10a); `VehicleRequest` as the single record both entry methods write | Extend the registry with Stage 4 and make the empty owner explicit: `dealership=none`, `staff=none`, `owner_role=Buyer` until criteria are complete, then `System` during validation and revalidation. An empty owner is an assignment, not a gap: the portals render "waiting on you" or "AutoLenis is checking availability" and never an unattributed spinner, and no `queue_items` row may be raised with `owner_role=Ops` for Stage 4 | 4 | unit | `stage-responsibility.test.ts` Stage 4 case asserts a null staff and dealership owner and that raising an Ops-owned Stage 4 exception is rejected | none | none | TO IMPLEMENT |
| W30-04 | MD §30 row 4 (L1470); HTML `S[4]` | Stage 5 responsibility split: buyer pays the $99 Standard plan or adds Premium; staff work reconciliation exceptions; system charges once, unlocks, opens the upgrade window | System half is the strongest area in the codebase but unattributed: single-charge and unlock via `app/api/webhooks/stripe/route.ts:613-623` and the deposit state matrix `lib/payments/deposit-state.ts:29-40`; reconciliation by the `deposit-activation-reconcile` cron and `lib/services/payment/deposit-settlement.service.ts:104-125`, which raises a deduped `SYSTEM_ALERT` (`deposit-activation.service.ts:70-86`) with no owner, deadline or recovery action. Staff "reconciliation exceptions" therefore have no owned queue (E26-08, E26-09). The "opens the upgrade window" duty is unimplemented — `app/api/buyer/plan/upgrade/route.ts:31-38` flips the plan with no window at all (P23-01) | PARTIAL | Reconciler sweeps by state rather than by time window (`deposit-settlement.service.ts:36-38`); deposit never auto-refunded (`deposit-activation.service.ts:33-41`); idempotent single charge — keep all three | Extend the registry with Stage 5: `owner_role=Buyer` before settlement, `System` during settlement, `Finance` the moment a reconciliation exception is raised; the E26-08/E26-09 `queue_items` rows carry `owner_role=Finance` and the Stripe reference; and the registry records "opens the upgrade window" as the system duty that P23-01 implements | 3 | unit, integration | `stage-responsibility.test.ts` Stage 5 cases; extend `lib/services/payment/__tests__/deposit-settlement.test.ts` — a recovered settlement gap raises a Finance-owned row with a deadline, and the buyer surface names Finance as the responsible party | refund and money-path activation (refunds are manual) | `Notification(SYSTEM_ALERT)` dedup-by-title as the reconciliation queue | TO EXTEND |
| W30-05 | MD §30 row 5 (L1471); HTML `S[5]` | Stage 6 responsibility split: buyer authorizes radius if asked; staff approve limited auctions and manual sourcing; system searches bands, validates, enriches | System half exists: the radius-escalation ladder and coverage primitive `lib/services/auction/coverage.service.ts:30,35,45-48` escalate radius tiers tightest-first **automatically**, so the buyer is never asked and no authorization is recorded (`rg` for a buyer radius-authorization surface → none). Staff "approve limited auctions and manual sourcing" has no approval object — the zero-invitation case only writes an `adminAuditLog` sentinel row (`lib/services/auction/dealer-invitation.service.ts:195-210`) with no owner or action. Nothing in any portal says who is holding sourcing open | BROKEN | The coverage primitive's tightest-first ladder and its distinct-contactable-dealership counting (`coverage.service.ts:30,45-48`); the zero-invitation audit sentinel — keep and attach the owner to them rather than replacing them | Extend the registry with Stage 6: `owner_role=Buyer` when a radius beyond the default requires authorization, `Ops` when a limited auction or manual sourcing needs approval, `System` while bands are being searched. The ladder consults the recorded buyer authorization before escalating past the default band and raises a Buyer-owned `queue_items` row instead of silently widening; the limited-auction approval becomes an Ops-owned row with a deadline | 5 | unit, integration | `stage-responsibility.test.ts` Stage 6 cases; extend `lib/services/auction/__tests__/dealer-invitation.test.ts` — escalation past the default band without a recorded buyer authorization raises a Buyer-owned row and does not widen | none | silent automatic radius escalation | TO IMPLEMENT |
| W30-06 | MD §30 row 6 (L1472); HTML `S[6]` | Stage 7 responsibility split: dealership receives the invitation; staff replace bounced contacts; system launches, invites, reminds, closes | System half exists (`lib/services/auction/dealer-invitation.service.ts` invite path, auction close/extend crons). The dealership half cannot be tracked: `AuctionInvitation` has no status column at all (`prisma/schema.prisma:518-531`; row C24-05a adds `CANCELLED` in the Phase 1 wave), so "received", "bounced" and "replaced" are unrepresentable, and the staff duty to replace a bounced contact has no queue, no owner and no deadline. Dealer-side display is the hardcoded switch `app/dealer/deals/[dealId]/page.tsx:45-63`, which is silent for every pre-contract stage | MISSING | Dealer PII isolation on the invitation path (`lib/services/auction/__tests__/dealer-invitation-pii.test.ts`); the zero-invitation audit sentinel — keep | Extend the registry with Stage 7: `owner_role=Dealership` from invitation to response, `Ops` on a bounced or undeliverable contact, `System` for launch, reminders and close. Bounce handling writes an invitation status and raises an Ops-owned `queue_items` row naming the dealership and the replacement deadline; the dealer portal renders the registry duty for an open invitation | 5 | unit, integration | `stage-responsibility.test.ts` Stage 7 cases; invitation test asserts a bounced contact produces exactly one Ops-owned row with a deadline and the dealer surface shows the invitation duty | Phase 1 wave must add the invitation status values (not in the §8.2 list — see C24-05a) | outbound invitation sends outside the dispatcher (D1 rows) | TO IMPLEMENT |
| W30-07 | MD §30 row 7 (L1473); HTML `S[7]` | Stage 8 responsibility split: dealership submits and revises offers; staff review flagged offers; system validates, ranks, seals | Dealership and system halves exist: `app/api/dealer/offers/**` submit and `offers/[offerId]/revise/route.ts`, ranking in `lib/services/offer/best-price.service.ts` with junk-fee detection. The staff "review flagged offers" duty has no owned queue — `lib/services/admin/admin-queue.service.ts:7-39` has no offer queue type at all, so a flagged offer is reviewed only if someone opens the offers page. No portal states who owns an offer under review or by when | PARTIAL | Offer validation, junk-fee patterns, fee caps and APR validation (§29 safeguards, rows G29-*); the sealed-ranking guarantee — keep unchanged | Extend the registry with Stage 8: `owner_role=Dealership` while the auction is open, `Ops` while an offer is flagged, `System` for validation, ranking and sealing. A flagged offer raises an Ops-owned `queue_items` row with a deadline and a recovery action, and the dealer portal shows "your offer is under review by AutoLenis" rather than an unattributed status | 6 | unit, integration | `stage-responsibility.test.ts` Stage 8 cases; offer-validation test asserts a flagged offer raises exactly one Ops-owned row and that the dealer-visible status names the reviewer | none | offer review performed off-queue on the admin offers page | TO EXTEND |
| W30-08 | MD §30 row 8 (L1474); HTML `S[8]` | Stage 9 responsibility split: buyer selects one offer and accepts or declines Premium; staff assign the concierge on upgrade; system serializes the single winner | Buyer and system halves exist: `app/api/buyer/auctions/[auctionId]/select-offer/route.ts` serializes selection. The staff duty is unimplemented — `vehicle_requests.assigned_admin_id` exists (`prisma/schema.prisma:1032`) but no upgrade path assigns it: `app/api/buyer/plan/upgrade/route.ts:31-79` flips the plan and writes audit, activity and an in-app notice, and never assigns or introduces a concierge (§23.2 "assign the named concierge and introduce them by name the same day"). No portal shows who owns the request after an upgrade | BROKEN | Race-safe `updateMany` plan flip with a `plan != PREMIUM` guard and the audit and activity rows (`upgrade/route.ts:31-38,45-79`); single-winner serialization on selection — keep both | Extend the registry with Stage 9: `owner_role=Buyer` until selection, `Ops` for the concierge assignment when Premium settles, `System` for winner serialization; the settlement path sets `vehicle_requests.assigned_admin_id`, appends the plan snapshot with the converting touchpoint, and notifies buyer and concierge. Standard requests record the Operations pool as the owner so every paid request has a named owner | 6 | unit, integration, concurrency | `stage-responsibility.test.ts` Stage 9 cases; extend `app/api/buyer/plan/__tests__/upgrade.test.ts` — a settled upgrade assigns `assigned_admin_id` and notifies both parties; concurrent selection still yields one winner | Premium $400 charge (money-path activation, see P23-01) | free no-charge upgrade with no concierge assignment | TO IMPLEMENT |
| W30-09 | MD §30 rows 9–11 (L1475-1477); HTML `S[9..11]` | Stages 10–12 responsibility split: staff chase timeouts, verify outside winners, resolve recap disputes, and verify and record financing | Only the financing-verification half has a surface: `app/api/admin/financing-reviews/route.ts` and `[taskId]` with the operational-role check asserted at `app/api/admin/financing-reviews/__tests__/resolve-route.test.ts:51,62`, backed by `FinancingReviewTask`. Reaffirmation, the vehicle hold, outside-winner verification and the deal recap do not exist at all (rows in the deal-early map; `rg reaffirm\|deal_recaps` → none), so three of the four staff duties in these rows have no object to own. `FinancingStatus` has no verified value (`prisma/schema.prisma:1711-1716`) | MISSING | Staff-only authorization on financing review (`resolve-route.test.ts:51`); the no-in-house-financing spine test — keep both, and keep `FinancingReviewTask` as the mirror while `queue_items` becomes the store | Extend the registry with Stages 10–12: `owner_role=Dealership` while reaffirmation is outstanding, `Ops` on a reaffirmation timeout, an outside winner needing verification, or a recap dispute, `Buyer` while the recap awaits buyer confirmation, and `Ops` for the financing verify-and-record duty (bolded in §30 row 12). Each raises one `queue_items` row carrying that owner, the deadline and the recovery action; `FinancingReviewTask` becomes a read-only mirror | 7 | unit, integration, state-machine | `stage-responsibility.test.ts` Stages 10–12 cases; new `lib/services/financing/__tests__/financing-checkpoint.test.ts` — only an Ops actor can record verification, and the recorded actor is stored; reaffirmation timeout raises exactly one Ops-owned row | `FinancingStatus` `TERMS_LOCKED`/`VERIFIED` values (Phase 1 wave) | `FinancingReviewTask` as an independent ops store (E26-17, DUP-11) | TO IMPLEMENT |
| W30-10 | MD §30 rows 12–13 (L1478-1479); HTML `S[12..13]` | Stages 13–14 responsibility split: staff review contract holds, escalate overdue uploads, and record financing completion and funding clearance | Contract-hold review exists as a derived list only: `lib/services/admin/admin-queue.service.ts:11` (`CONTRACT_FAIL` from `ContractScan.status=FAIL`) and `:20-25` (`ESIGN_EXCEPTION` derived from a deal sitting 48h in `SIGNING_PENDING`) — no owner, no deadline, no recovery action, and resolve swallows DB errors while still auditing (`:41-107,89-94`). Overdue-upload escalation has no timer (`document_requests.dueAt` unenforced — row K27 contract set). Funding clearance does not exist in any form (`rg funding_cleared\|fundingCleared` → 0; `DealStatus` `prisma/schema.prisma:1513-1530` has no funding state), so the bolded staff duty "record completion and clearance" has no object | MISSING | Explicit decision-word resolve on the derived queue (`admin-queue.service.ts:44-83`); Contract Shield fail-closed and bind-to-reviewed-version guarantees (§29) — keep | Extend the registry with Stages 13–14: `owner_role=Dealership` for package upload and execution, `Buyer` for signing, `Ops` for a held contract, an overdue upload and the funding-clearance record. Each becomes a `queue_items` row with owner, deadline and recovery; the funding-clearance record names the Ops actor and blocks release until present, and the derived contract queues become mirrors | 8 | unit, integration, state-machine | `stage-responsibility.test.ts` Stages 13–14 cases; funding-clearance test asserts only an Ops actor may record clearance, the actor is stored, and release stays blocked without it | none | derived `CONTRACT_FAIL`/`ESIGN_EXCEPTION` queues; admin CONTRACT_FAIL resolve mutating scans (E26-33) | TO IMPLEMENT |
| W30-11 | MD §30 row 14 (L1480); HTML `S[14]` | Stage 15 responsibility split: buyer provides proof; staff verify; system tracks status and expiry | Partly present and mis-attributed. Staff verification exists (`app/api/admin/insurance-requests/respond/route.ts`), and the release gate reads `INSURANCE_SATISFIED` (`app/api/dealer/pickup/scan/route.ts:73-81`), but `Deal.insuranceStatus` has direct writers outside the verification path and `EXTERNAL_UPLOADED` currently counts as satisfied without a staff verification step (rows E26-31, T28-10c). The staff duty is bolded in §30 row 14 precisely because it is the gate, yet a buyer upload can satisfy it unverified. Expiry tracking does not exist (`rg insuranceExpiresAt` → none). The derived `INSURANCE_EXCEPTION` queue fires on a 72h-old `INSURANCE_PENDING` deal with no owner (`admin-queue.service.ts:13-18`) | BROKEN | The insurance hard gate at release, re-checked inside the seam (`scan/route.ts:73-81,95-101`) — keep and strengthen; never widen `INSURANCE_SATISFIED` to reach green | Extend the registry with Stage 15: `owner_role=Buyer` until proof is uploaded, `Ops` from upload to verification, `System` for expiry tracking. `EXTERNAL_UPLOADED` stops satisfying the gate on its own; verification is the single writer of the satisfied state and records the verifying actor; expiry before release raises a Buyer-owned row | 8 | unit, integration, state-machine | `stage-responsibility.test.ts` Stage 15 case; insurance test asserts an unverified external upload does not satisfy release and that verification stores the Ops actor | none | `Deal.insuranceStatus` direct writers; `EXTERNAL_UPLOADED` counted as satisfied | TO IMPLEMENT |
| W30-12 | MD §30 rows 15–18 (L1481-1484); HTML `S[15..20]` | Stages 16–21 responsibility split: staff schedule after two counters, resolve handover blocks and delivery discrepancies, and escalate overdue post-completion obligations | Only the two-counter rule exists: `lib/services/pickup/pickup-coordination.service.ts:70,88,204,212` tracks `counterCount` and the `DEALER_COUNTERED` state, and `app/dealer/pickups/page.tsx:71,109-115` groups "waiting on the buyer" — the one place in the platform that names a responsible party, and it is a hardcoded page-local filter. Nothing schedules after the second counter, no handover-block object exists, no delivery-discrepancy object exists, and no post-completion obligation record exists (`rg post_completion` → 0), so the last three staff duties have nothing to own. The derived `PICKUP_EXCEPTION` queue fires after 7 days with no owner (`admin-queue.service.ts:26-31`) | PARTIAL | Strict turn-taking, proposal-time compare-and-swap and the two-counter cap in pickup negotiation (§29; `pickup-coordination.service.ts`); round-specific idempotency keys on pickup emails — keep intact | Extend the registry with Stages 16–21: `owner_role` alternates Buyer and Dealership through scheduling per the turn-taking state, becomes `Ops` on the second counter (Ops schedules), on any handover block, on a delivery discrepancy, and on an overdue post-completion obligation. Each raises one `queue_items` row with owner, deadline and recovery; the `dealer/pickups` page-local "waiting on" filter is replaced by the registry so all three portals agree | 9 | unit, integration, state-machine, playwright | `stage-responsibility.test.ts` Stages 16–21 cases; pickup test asserts the second counter transfers ownership to Ops and raises one row; Playwright shows the same owner and deadline in buyer, dealer and Ops surfaces | none | page-local "waiting on the buyer" derivation in `app/dealer/pickups/page.tsx`; derived `PICKUP_EXCEPTION` queue | TO IMPLEMENT |
| G35-01 | MD §35 L1586 | "This document authorizes no parallel website, no replacement architecture, and no unrelated code changes" — the governing scope constraint needs a row, a gate and an acceptance check | No gate exists. Every CI job is scoped to the app root (`.github/workflows/ci.yml:16,100,214,317` all set `working-directory: frontend`), so a second HTTP surface is invisible to it — and one exists: `backend/server.py:1-97` is a FastAPI proxy that forwards `/api/*` from port 8001 to Next.js, plus unrun root Python suites `tests/test_autolenis.py`, `tests/test_autolenis_v2.py`, `tests/test_autolenis_v3.py` and an `automation/` tree. The verification hook counts changed `frontend/` files only (`.claude/hooks/verification/`, CLAUDE.md L220-226), and `pnpm test:coverage-check` guards unreachable tests, not out-of-scope code. Nothing in IMPLEMENTATION-WORKFLOW §8 or §12 fails a phase for a change outside its scope | MISSING | The `test:coverage-check` reachability guard and the per-phase "no future-phase absorption" close step — reuse their shape rather than inventing a new mechanism | Phase 1 enforcement object: `lib/__tests__/scope-guard.test.ts` plus a CI step run **outside** `working-directory: frontend`, asserting exactly one Next.js app root and one API surface, listing `backend/`, `automation/` and root `tests/` as frozen legacy that may gain no new files, and failing when a phase diff touches a path outside that phase's §8.2 scope list; add the matching gate line to §12 and an `ACCEPTANCE-REPORT.md` check line "no parallel website, no replacement architecture, no unrelated change" | 1 | unit, integration | Guard fails on an added second `next.config.*`, a new `backend/` route or a new root test file; CI step runs at the repository root; the acceptance-report line exists and is checked in Phase 11 | retirement of `backend/server.py` (an Emergent preview-ingress dependency) — freeze now, remove only on an owner decision | `backend/server.py` FastAPI proxy; root `tests/test_autolenis*.py`; `automation/` | TO IMPLEMENT |
| P23-01 | HTML `S[4].buyer[2]` `AutoLenis-Transaction-Flow.html:539` (cited L644); MD §23.2 L1139-1148; IMPLEMENTATION-WORKFLOW §2 D6 (L87) | The HTML Stage 5 statement "Optionally add the $400 Premium balance now, or any time before accepting an offer" must be reconciled with §23.2 (the window closes at funding clearance) and implemented as a window with a real open and close | The divergence is already adjudicated in the workflow (`IMPLEMENTATION-WORKFLOW.md:87` D6 — Markdown governs, the HTML line is presentational shorthand and the Stage 5 screen must state the Markdown rule), but no window exists in code at all. `app/api/buyer/plan/upgrade/route.ts:11-17,31-38` flips `Buyer.plan` to PREMIUM free of charge, with no $99-settled precondition, no close condition, no per-request scoping (plan lives on `Buyer`, `prisma/schema.prisma:47`) and no $400 charge; `components/buyer/PlanUpgradeCard.tsx:19-42` offers the upgrade unconditionally; the $400 is modelled only at fee collection (`lib/services/deal/service-fee.service.ts:19-43`, `lib/constants.ts:7-8`); no plan-snapshot table exists; funding clearance does not exist (row E26-43b) so the close condition has nothing to fire on | BROKEN | Race-safe `updateMany` plan flip with the `plan != PREMIUM` guard, the rate limit, and the audit plus activity rows (`upgrade/route.ts:31-38,45-79`); the recorded owner decision comment at `:11-17` — keep the CAS, the audit trail and the decision record | Make the window explicit and Markdown-governed: it opens when the $99 settles (Phase 3) and CLOSES at funding clearance, which does not exist until Phase 8 — the close predicate lands there, never at offer acceptance; the Stage 5 buyer copy states the funding-clearance rule (§2 D6, §11.6 ruling 11). | 8 | unit, integration, state-machine | Extend `app/api/buyer/plan/__tests__/upgrade.test.ts`: upgrade before the $99 settles → refused; after funding clearance → refused except on an audited admin approval; Stage 5 copy assertion reads "before funding clears"; one plan snapshot appended per change | charging the $400 balance — the free upgrade is a recorded owner decision (`upgrade/route.ts:11-17`); money-path activation | free no-charge self-service upgrade; `Buyer.plan` as the single per-buyer plan flag | TO IMPLEMENT |

## Gap-fill rows (round 2)

Two requirements the round-2 completeness critic found uncovered. Same 13 columns and rules as the tables above; paths relative to `frontend/` unless prefixed. New ref families: `L3` = MD §3 "One lineage, never broken" (L139); `S11D` = HTML Stage 11 dealership bullet. HTML line numbers are the true file lines of `AutoLenis-Transaction-Flow.html`, with the critic's cited line kept in parentheses where it differed.

| Ref | Document & section | Requirement | Current route / service / model / component / email / cron / queue / admin action | Status | Stronger safeguard to preserve | Exact required change | Phase | Test level | Acceptance evidence | Owner-gated dependency | Legacy path affected | Final disposition |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| L3-01 | MD §3 L139 ("One lineage, never broken"); no §26 register row covers it (checked L1235-1260) | The orphan rule for the five non-payment record classes: an **auction, offer, deal, contract or pickup** that cannot resolve its parent by stored reference is an orphan — it raises an Operations exception and is **never silently re-parented or duplicated into a parallel transaction**. (The payment half is already covered by E26-09 / jobs C6.) | **No exception half exists at all**: `rg raiseException\|prisma.queueItem\|queueItem.create` over `lib/**` + `app/**` → 0 hits; no `queue_items` model (E26-00a); `QueueItemType` `prisma/schema.prisma:1898-1907` holds 8 values, none for a lineage class, and the additive list at `IMPLEMENTATION-WORKFLOW.md:538-545` adds per-domain codes but no orphan type. **Per class:** *auction* — `Auction.vehicleRequestId` is nullable with `onDelete: SetNull` (`schema.prisma:443,451,456`), so deleting the parent request silently orphans the auction; `deposit-activation.service.ts:207` creates an auction with no `vehicleRequestId` at all; `resolveOwnedVehicleRequestId` (`lib/services/auction/auction.service.ts:34-43`) returns `null` for a mistyped or hostile id and `app/api/admin/buyers/[buyerId]/launch-auction/route.ts:150` then creates the auction anyway, raising nothing. *offer* — `Offer.auctionId` FK `onDelete: Cascade` (`schema.prisma:565`): DB-protected, no runtime check. *deal* — `Deal.offerId` and `Deal.vehicleRequestOfferId` are both nullable (`schema.prisma:577-578,605-606`) with no constraint requiring exactly one, and no `vehicle_request_id`/`auction_id`/`deposit_id` columns exist (WORKFLOW R31 MISSING); a deal that resolves neither is **silently absorbed** at `lib/services/deal/dealer-award-dispatch.service.ts:73-84`, which stamps `dealerAwardDispatchedAt` terminal and counts it `skipped`; read surfaces degrade quietly (`app/buyer/insurance/page.tsx:68`, `app/buyer/pickup/page.tsx:37,60`). *contract* — `ContractScan.dealId` Cascade (`schema.prisma:633,640`), `ContractVersion.dealId` FK with default Restrict (`schema.prisma:2626-2627`): DB-protected, no runtime check. *pickup* — `Pickup.dealId` unique FK Cascade (`schema.prisma:828,850`): DB-protected, no runtime check | PARTIAL | The DB-level parent FKs that already make three of the five classes unorphanable (`Offer.auctionId` Cascade `schema.prisma:565`; `ContractScan.dealId` Cascade `:640`; `Pickup.dealId` unique Cascade `:828,850`); the uniqueness constraints that already forbid a parallel duplicate (`Deal.offerId` and `Deal.vehicleRequestOfferId` `@unique` `schema.prisma:577-578`, `Auction.depositId` `@unique` `:420`, `Offer.originalOfferId` `@unique` `:552`, `Pickup.dealId` `@unique` `:828`); the ownership-scoped resolve in `resolveOwnedVehicleRequestId` (`auction.service.ts:34-43`) which refuses to cross-link another buyer's request; the fail-loud no-deal-after-commit branch at `app/api/buyer/requests/[requestId]/offer/respond/route.ts:112-117` and its idempotent single-deal lookup at `:103-110`; `approveContractVersionByAdmin` hard-refusing a null scan→version link (`schema.prisma:645-652`) | Phase 2 behaviour, with the writer: `assertParentResolvable()` in `lib/services/operations/queue-item.service.ts` is called by every creation path for the five non-payment classes; a reconciler sweeps existing null-parent rows; a build-failing rule forbids writing a parent id onto an existing row of these classes outside the audited re-parent admin action. §8.1 Phase 2, §8.2 Phase 2 Part A and §11.4 all place it here — an earlier draft of this row said Phase 10, the only dissenting voice. | 2 | unit, integration, state-machine | New `lib/services/operations/__tests__/lineage-orphan.test.ts`: for each of auction, offer, deal, contract, pickup an unresolvable parent yields exactly one `queue_items` row typed `LINEAGE_ORPHAN` owned by Operations, the stored parent reference is unchanged afterwards (never re-parented) and the lineage row count stays 1 (never duplicated); extend `lib/services/deal/__tests__/dealer-award-dispatch.test.ts` (reuse the existing `offer: null` fixture at `:103`) — failing first: the null-lineage deal raises and leaves `dealerAwardDispatchedAt` NULL rather than stamping it; extend `app/api/admin/buyers/[buyerId]/launch-auction/__tests__` — unresolvable `vehicleRequestId` → 4xx + exception, no auction row; CI migration job asserts the `LINEAGE_ORPHAN` enum value and the Deal one-parent CHECK | production migration deploy of the Phase 1 wave (§13-D1) for the enum value, the Deal lineage columns and the one-parent CHECK; **owner decision on historical rows** — the deposit-activation reconciler creates auctions with `vehicle_request_id` NULL by design (`deposit-activation.service.ts:207`, `auction.service.ts:15-19`), so whether a back-sweep raises one exception per pre-existing parentless auction (or they are back-filled/grandfathered) must be approved before the sweep runs | `dealer-award-dispatch.service.ts:73-84` silent terminal stamp; `Auction.vehicleRequestId onDelete: SetNull` (`schema.prisma:451`); the null-coalescing parent reads `app/buyer/insurance/page.tsx:68` and `app/buyer/pickup/page.tsx:37,60`; `SYSTEM_ALERT` Notification as the exception store (shared with E26-09) | TO IMPLEMENT |
| S11D-01 | MD §11 L703; HTML `S[10].dealer[0]` `AutoLenis-Transaction-Flow.html:616` (cited L117); supporting: MD §12 L792 (Contract Shield compares "trade allowance and payoff figures") | The **dealer-provided preliminary trade allowance, when available**, is supplied by the dealership as an explicit Stage 11 action and carried as a named line on the final deal recap — distinct from the *final* allowance confirmed at handover (pickup R18.10, `final_allowance_cents`, Phase 9) | No dealer-supplied allowance exists anywhere. The dealer-facing offer schemas carry no allowance field: auction path `app/api/dealer/offers/route.ts:9-15` (otd/vehicle/tax/fees/apr/term/junk items only); concierge path `app/api/public/dealer-offer/[token]/route.ts:71-101` has only `tradeInAccepted: z.boolean()` at `:84`. No reaffirmation surface exists at all (`rg reaffirm` over `lib/**` + `app/**` → 0 hits), so the Stage 10→11 dealership seam that would collect it is absent. On the trade record, `TradeInSubmission.valuationCents`/`valuedAt` (`prisma/schema.prisma:2051-2052`) are **never written** by any service — `lib/services/trade-in/trade-in.service.ts:7-25` writes only the buyer-supplied packet — and are read only for display at `app/buyer/trade-in/page.tsx:126-130`; there is no dealer-attributed allowance column, no `deal_id` on the trade row, and no `preliminary_allowance_cents` in the schema. Buyer trade facts flow one way to the dealer as free text (`VehicleOffer.buyerTrade*` `schema.prisma:3767-3775`, rendered at `app/(public)/dealer-offer/[token]/page.tsx:70-77`), with payoff as an unparsed String (`buyerTradePayoff` `:3772`). Recap side: `deal_recaps` does not exist (deal-early C2a MISSING) and the recap payload enumerated in deal-early C2b omits the allowance | MISSING | The trade record has **no dealer-writable surface today** — no dealer route or service mutates `TradeInSubmission` (`rg tradeInSubmission` over `app/api/dealer/**` → 0 hits) — so a dealer can never alter the buyer's own trade packet; keep that boundary by giving the dealer a separate, dealer-attributed allowance field rather than write access to the buyer's row. Also keep the identity-firewall ordering that the trade packet reaches a dealership only at reaffirmation (§8.2 Phase 7 "identity firewall lift + secure handoff (co-buyer + trade packet) at confirmation") | Phase 1 wave already lists `trade_in_submissions.preliminary_allowance_cents` and `verified_payoff_cents` (`IMPLEMENTATION-WORKFLOW.md:549-550`, R63/R12a) — write them here: the Phase 7 reaffirmation/recap flow gains a dealership action "supply the preliminary trade allowance and verified payoff where available" that stamps `preliminary_allowance_cents` (integer minor units) with the supplying dealer id and timestamp, marked optional so a recap can still build with the allowance absent and the line rendered as "not provided by the dealership"; `lib/services/deal/deal-recap.service.ts` (deal-early C2b) adds the allowance as its own named recap line, feeding the net-equity line (MD §11 L705) and the Contract Shield comparison set (§12 L792); the recap version records the allowance value, so a later change at handover (`final_allowance_cents`, R18.10 Phase 9) is detected as an appraisal change (R18.17) against a stored recap figure rather than against nothing; buyer-supplied `buyerTradePayoff` free text stays read-only evidence and is never treated as the verified payoff | 7 | unit, integration | Extend `lib/services/deal/__tests__/deal-recap.test.ts` (new in C2b): recap version 1 carries a named `preliminary_allowance_cents` line when the dealership supplied one and an explicit "not provided" line when it did not; net-equity equals allowance minus verified payoff and the itemised sum still reconciles to `otd_cents_confirmed`; a dealer submitting an allowance leaves the buyer's own trade packet fields byte-identical (dealer-write boundary); new reaffirmation-route test — allowance rejected as non-integer or negative, and rejected from a dealer that is not the reaffirming winner; Phase 9 `handover.test.ts` (R18.10) compares `final_allowance_cents` against the recap's `preliminary_allowance_cents` and routes a difference to the R18.17 appraisal-change path | none for the behaviour — the columns are already inside the Phase 1 wave, whose production migration deploy (§13-D1) is the standing owner-gated dependency | `TradeInSubmission.valuationCents`/`valuedAt` — written by nothing, displayed to buyers at `app/buyer/trade-in/page.tsx:126-130`; `VehicleOffer.buyerTradePayoff` free-text String duplicating payoff (`schema.prisma:3772`); the `tradeInAccepted` boolean as the only trade signal a dealer returns (`app/api/public/dealer-offer/[token]/route.ts:84`) | TO IMPLEMENT |
