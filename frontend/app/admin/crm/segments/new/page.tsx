import { SegmentBuilder } from '@/components/admin/crm/SegmentBuilder';

export const dynamic = 'force-dynamic';

export default function NewSegmentPage() {
  return <SegmentBuilder mode="create" />;
}
