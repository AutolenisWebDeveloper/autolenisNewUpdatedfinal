// Test-only stub for Next.js's virtual `server-only` module so unit tests can
// import server-scoped helpers under `tsx --test` (where the bundler-provided
// module is unavailable). Intentionally empty — importing it is a no-op.
export {};
